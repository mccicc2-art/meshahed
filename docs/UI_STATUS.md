# UI_STATUS — حالةُ إعادة تنظيم الواجهات

> ملفٌّ يتتبّع خطّة أحمد ذاتَ الثماني مراحل (١٨ أغسطس ٢٠٢٦).
> **القاعدة**: لا يُكتب هنا «مكتمل» إلا لما شُحن وقِيس حيّاً.

آخر تحديث: **١٨ أغسطس ٢٠٢٦** — بعد المرحلتين ١ و٢.

---

## Completed

### المرحلة ١ — الرئيسية / العرض البصريّ (D-434)
- **ترويسةٌ جديدةٌ للرئيسية** (`HomeHeader`): وردمارك Loopz · الجرس ·
  الإعدادات · الأفتار والتحيّة (`Good evening, Ahmed`) · مبدّلُ العرض ·
  بطاقةُ الأرقام.
- **ترويسةُ الحساب الكاملةُ غادرت الرئيسية** (الغلافُ والنبذةُ والمتابعون
  والمستوى) — **والرئيسيةُ صارت صفحةَ مكتبةٍ لا صفحةَ حساب.**
- **`Continue Watching`**: بطاقاتُ مشهدٍ عريضة، الحلقةُ والباقي، شريطُ
  تقدّمٍ ونسبة — **بلا تغييرٍ في وظيفة «شاهدت الحلقة».**
- **`To Watch`**: ملصقاتٌ عمودية **والاسمُ تحت الملصق لا عليه**
  (`PosterCard titleBelow`) · **بلا شارة «ما بدأته»** · **بلا الزرَّين
  العائمين** (الفعلُ في قائمة الضغط المطوّل).
- **`Upcoming`**: التاريخُ أوّلاً واسمُ العمل تحته.
- **شريطُ التنقّل السفليّ بالأسماء** تحت الرموز (D-436، **نقضٌ لـD-258**).

### المرحلة ٢ — الرئيسية / العرض المضغوط (D-434)
- **وضعٌ لا صفحة**: نفسُ الجلب ونفسُ الترتيب ونفسُ المكوّنات —
  **والفرقُ في آخر خطوةِ رسمٍ وحدَها.**
- `Continue Watching` → صفوفٌ أفقيّةٌ مضغوطة (`ContinueCard variant="row"`).
- `To Watch` و`Upcoming` → `CompactMediaRow` (ملصقٌ صغير أو رقاقةُ تاريخ).
- **الاختيارُ محفوظٌ في الحساب** (`profiles.home_prefs.view`) — **بلا هجرة**،
  العمودُ JSON قائم. **ويعود المستخدمُ فيجد آخرَ وضعٍ اختاره.**
- المبدّلُ في الرئيسية **وفي صفحة التخصيص** — قيمةٌ واحدة، بابان.

### مكوّناتٌ مشتركة (جديدة أو موسَّعة)
| المكوّن | الحال |
|---|---|
| `HomeHeader` | 🆕 |
| `HomeGreeting` | 🆕 (`useSyncExternalStore` — ساعةُ القارئ لا الخادم) |
| `HomeViewSwitch` | 🆕 |
| `CompactMediaRow` | 🆕 — **صفٌّ واحدٌ يخدم «للمشاهدة» و«القادم»** |
| `ContinueCard` | + `variant="card" \| "row"` |
| `PosterCard` | + `titleBelow` |
| `Section` (في `page.tsx`) | + `view` — **رأسٌ واحدٌ وجسدان** |
| `BottomNav` | الأسماءُ عادت |
| `ToWatchCard` | 🗑️ **حُذف** — بلا قارئ بعد سقوط الزرّين (D-214) |

---

## Remaining
- المرحلة ٣ — `Profile` صفحةً اجتماعيّةً كاملة (`/u/[username]`).
- المرحلة ٤ — `Customize` موحَّدة بتبويبَي Home/Profile ومعاينةٍ حيّة وسحبٍ
  وإفلات.
- المرحلة ٥ — `Library`.
- المرحلة ٦ — `Discover`.
- المرحلة ٧ — `Community`.
- المرحلة ٨ — `Tools` و`Filters`.

---

## Known issues / دَينٌ مكتوبٌ لا سهو
1. **`ProfileHeader` بلا قارئٍ الآن** — لم يُحذف لأنه **مادّةُ المرحلة ٣**
   (الغلافُ والأفتارُ المتداخل والنبذةُ والمتابعون). **إن لم تُستعمل في
   المرحلة ٣ فتُحذف** (D-214).
2. **«البطاقة الحمراء» غابت عن ملصقات «للمشاهدة» في الرئيسية** — بابُها
   اليومَ صفحةُ العمل وشبكةُ المكتبة. **تُضاف إلى قائمة الضغط المطوَّل**
   في دفعةٍ لاحقة (تحتاج `holdExtra` وهو دالّةٌ لا تعبر حدَّ الخادم —
   فالحلُّ فعلُ خادمٍ مباشرٌ في `PosterHold` لا تمريرُ دالّة).
3. **`Upcoming` بلا رقم الحلقة** («Tomorrow · Episode 5» في التصميم).
   **لا نملك الرقم**: صفُّ المتابعة يحمل `next_air_date` وحدَه، ورقمُ
   الحلقة في `next_episode_to_air` من TMDB — **نداءٌ لكلِّ عمل.**
   المقترح: تخزينُه مع `stats_updated_at` في نفس مزامنة الإحصاءات
   (هجرةٌ صغيرة + `ShowStatsSync`). **ولا يُخمَّن رقمٌ** — رقمٌ خاطئ أسوأ
   من لا رقم (D-432).
4. **مفاتيحُ «المستوى/المتابعون/التعليقات» سقطت من تخصيص الرئيسية** —
   قيمُها باقيةٌ في العمود، **ومكانُها تبويبُ «الملفّ» في المرحلة ٤.**
5. **`titleBelow` معامِلٌ مؤقّت** — يسقط ويصير السلوكَ الوحيد يومَ تنتقل
   المكتبةُ والاستكشاف (المرحلتان ٥ و٦).

---

## Files changed (المرحلتان ١ و٢)
```
src/lib/homePrefs.ts          + HomeView و view في التفضيلات
src/lib/actions.ts            + setHomeView (يقرأ الحيَّ ثم يبني فوقه)
src/lib/data.ts               getUnreadSignals → cache()
src/lib/i18n.ts               + تحيّاتٌ وأسماءُ الوضعين وعدّادات
src/app/page.tsx              الترويسةُ الجديدة + الأقسامُ بوضعين + ٣ استعلاماتٍ ساقطة
src/app/profile/page.tsx      يحوّل إلى /u/<username> لا إلى الجذر
src/components/HomeHeader.tsx        🆕
src/components/HomeGreeting.tsx      🆕
src/components/HomeViewSwitch.tsx    🆕
src/components/CompactMediaRow.tsx   🆕
src/components/ContinueCard.tsx      + variant
src/components/PosterCard.tsx        + titleBelow
src/components/BottomNav.tsx         الأسماءُ عادت
src/components/HomeCustomize.tsx     + مبدّلُ الوضع − ٣ مفاتيحَ ميتة
src/components/ToWatchCard.tsx       🗑️ محذوف
```

## الفحوص
- `npx tsc --noEmit` ✅ · `npx eslint src` = 0 ✅ · `npx next build` ✅
- عرضان جوّالان · RTL/LTR · لا فيضٌ أفقيّ · لقطاتٌ مقارنةٌ بالتصميم: **بعد النشر.**
