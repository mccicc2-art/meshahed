# Phase 7 — جودةُ الشيفرة والتنظيف

> **الحالة:** `READY_FOR_REVIEW` · **Audited SHA:** `f8a2b33c` · **التاريخ:** يُثبَّت من `git log` بعد الالتزام — انظر `SHARED_PROGRESS.md`
>
> **حدودُ التنفيذ كما أمرت — تدقيقٌ ثابتٌ وقراءةٌ فقط:** **صفر حذف · صفر تعديلٍ على شيفرة التطبيق · صفر تثبيتٍ دائم · صفر تغييرٍ على `dependencies` أو `config` أو `lockfile` · صفر تغييرٍ على Production.**
>
> ⚠️ **وكلُّ فحصٍ هنا وقع على الـAudited SHA نفسِه** (`f8a2b33c`) في شجرةِ عملٍ منفصلة، **لا على رأس فرع التوثيق** — **لأنّ رأسَ الفرع يختلف عن الـbaseline في عشرين ملفّاً، وفحصُه كان سيقيس شيئاً آخر.** **والاستثناءُ الوحيدُ فحصُ الأسرار: أُجري على الاثنين معاً كما أمرت، والفرقُ مذكورٌ في §9.**

## 0. الخلاصة قبل التفصيل

**المشروعُ في هذه المرحلة أنظفُ ممّا توقّعت، وأخطرُ ما فيه ليس ما وجدتُه بل ما لم أجد: لا اختبارَ واحد.**

| البند | القيمة | الحكم |
|---|---:|---|
| ملفّاتُ `src` | ٤٥١ · **١٠٨٬٠٢٦ سطراً** | — |
| **بوّاباتٌ اجتازت** | `tsc` **صفر خطأ** · `eslint` **صفر خطأ** · `next build` **نجح** | ✅ |
| تحذيراتُ `eslint` | **١٦** — وهو **عددُ الأساس المعتمَد** في Phase 5 | ✅ لا انحدار |
| **`TODO`/`FIXME`/`HACK`** | **٠** | ✅ |
| **`any` غيرُ مبرَّرة** | **٠** | ✅ |
| **`@ts-ignore` / `@ts-expect-error`** | **٠** | ✅ |
| **استيرادٌ دائريّ** | **٠** | ✅ |
| ثغراتُ `npm audit` | **٠** | ✅ |
| **أسرارٌ في رأس الفرع** | **٠ قيمةَ سرّ** — و١٤ إشارةً كلُّها اسمُ الدور `service_role` في أوامر `grant` | ✅ |
| ملفّاتٌ لا يصل إليها الرسمُ البيانيّ | **٩** — **منها واحدٌ حيٌّ عبر `next.config.ts`** | ⚠️ **٨ مرشّحة · ٦٥٨ سطراً** |
| تبعيّاتٌ غيرُ مستعمَلة | **١** (`@fontsource/cairo`) — **وقد عولجت على `main` بعد الـbaseline** | ⚠️ |
| تبعيّاتٌ مهجورة | **١** (`eslint@9.39.5` — «no longer supported») | ⚠️ |
| **اختباراتٌ آليّة** | **صفر ملفّ · صفر مشغّل · صفر mock** | 🔴 **`LOOPZ-AUD-0001` — قائمةٌ لا جديدة** |

> 🔑 **والسطرُ الذي يجمع المرحلة:** **الشيفرةُ منضبطةٌ ومبنيّةٌ ومكتوبةٌ بعنايةٍ ظاهرة — وليس تحتها شبكةُ أمانٍ واحدة.** **كلُّ ما في §١…§٨ دَينٌ صغيرٌ يُسدَّد في ساعات؛ و`0001` وحدَه هو ما يجعل كلَّ إصلاحٍ قادمٍ مخاطرةً بدل أن يكون إجراءً.**

## 1. المنهج ومفرداتُ الحكم

**ولا أُسوّي إشارةً آليّةً بعيب.** كلُّ صفٍّ أدناه يحمل: **المسارَ · والدليلَ · وطريقةَ الفحص.**

| الحكم | معناه |
|---|---|
| `USED` | ثبت استعمالُه بدليلٍ مباشر |
| `UNUSED_CANDIDATE` | **لا مستدعيَ مباشراً ولا ديناميكيّاً ولا في config ولا بالاسم نصّاً** — ويبقى مرشَّحاً لا حكماً |
| `DUPLICATE_CANDIDATE` | ثبت تشابهٌ بدليل (تجزئةٌ متطابقة أو خلَفٌ يؤدّي الدور) |
| `UNKNOWN` | إشارةٌ قائمةٌ وسببٌ محتمَلٌ لم أستطع حسمه — **ولا يُحذف ما حالُه `UNKNOWN`** |

**وطريقةُ الفحص للملفّات:** **رسمٌ بيانيٌّ للاستيراد** من مداخل Next (`page` · `layout` · `route` · `error` · `sitemap` · `proxy` · `instrumentation` · `globals.css`) مع **حلِّ اللقب `@/`** و**التقاطِ `import()` الديناميكيّ و`require` و`@import` في CSS**، ثمّ **بحثٌ نصّيٌّ بالاسم في `src` و`next.config.ts` و`tsconfig.json` و`package.json` و`postcss.config.mjs` و`eslint.config.mjs` و`vercel.json`** — **فما لم يظهر في الرسم ولا في النصّ ولا في config فهو مرشَّح.**

## 2. بوّاباتُ القراءة والبناء — قابلةٌ لإعادة الإنتاج

**التثبيتُ من القفل بلا تعديله:** `npm ci` — **٣٩١ حزمةً في ٢٤ ثانية · خروجٌ صفر.**

| البوّابة | الأمر | النسخة | النتيجة |
|---|---|---|---|
| الأنواع | `npx tsc --noEmit` | TypeScript **5.9.3** | ✅ **صفر خطأ** (خروج 0) |
| اللِّنت | `npx eslint .` | ESLint **9.39.5** | ✅ **صفر خطأ · ١٦ تحذيراً** (خروج 0) |
| البناء | `npx next build` | Next **16.3.0** (Turbopack) | ✅ **نجح** — «Compiled successfully in 25.5s» · **٨١ مساراً** |
| الثغرات | `npm audit` | — | ✅ **صفر** بكلِّ الدرجات |

> ⚠️ **والبناءُ احتاج ثلاثةَ متغيّراتٍ بيئيّةٍ نائبة** (`NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` · `NEXT_PUBLIC_SITE_URL`) **مرَّرتُها في سطر الأمر ولم أكتب ملفَّ بيئةٍ في المستودع** — **فلا أثرَ على شجرة العمل.** **وقيمُها نائبةٌ ظاهرةُ الزيف لا أسرار.**

**والتحذيراتُ الستّةَ عشرَ مصنَّفة:**

| التحذير | العدد | التصنيف |
|---|---:|---|
| `@next/next/no-img-element` + `jsx-a11y/alt-text` في `src/lib/shareCard.tsx` | ١٠ | 🟢 **ليست عيباً** — الملفُّ يرسم صورةَ OG بـ`ImageResponse`، **و`next/image` لا يعمل داخلها أصلاً** |
| `@typescript-eslint/no-unused-vars` | ٥ | ⚠️ **دَينٌ حقيقيّ** — §7 |
| متفرّق | ١ | — |

**⇢ وهذا يفصل العددَ عن معناه: ١٠ من ١٦ تحذيراً حدُّ أداةٍ لا خطأُ كاتب.**

## 3. الملفّاتُ غيرُ المستعمَلة — تسعةٌ، وواحدٌ منها فخّ

🔴 **والفخُّ أوّلاً، لأنّه لو حُذف لسقط تحسينُ الصور كلُّه:**

| الملفّ | الحكم | الدليل |
|---|---|---|
| `src/lib/imageLoader.ts` | ✅ **`USED`** | **لا يستورده أحد — و`next.config.ts:93` يقول `loaderFile: "./src/lib/imageLoader.ts"`.** **مرجعٌ من config بسلسلةِ نصّ، لا يراه أيُّ رسمٍ بيانيّ.** |

**وهذا بالضبط ما حذّرت منه: «افحص ملفّات التسجيل/config قبل الحكم».** **ولولا ذلك لكان أوّلَ مرشَّحٍ في القائمة.**

**والثمانيةُ الباقية — `UNUSED_CANDIDATE`:**

| الملفّ | الأسطر | آخرُ لمسة | الدليل على عدم الاستعمال | احتمالُ نداءٍ ديناميكيّ |
|---|---:|---|---|---|
| `src/components/HeaderTools.tsx` | ٥٧ | التزامُ الاستيراد الأوّل وحدَه | **صفر ظهورٍ للاسم في المستودع كلِّه** خارج ملفّه | **لا** — لا اسمَ في أيِّ سلسلة |
| `src/components/LanguageSwitch.tsx` | ٦٠ | نفسُه | صفر ظهور | لا |
| `src/components/RailWindow.tsx` | ١٠٤ | نفسُه | صفر ظهور | لا |
| `src/components/settings/SettingsDoneAction.tsx` | ٦٣ | نفسُه | صفر ظهور | لا |
| `src/components/settings/SettingsSoon.tsx` | ٤٤ | نفسُه | صفر ظهور | لا |
| `src/components/MediaTitle.tsx` | ٨٧ | نفسُه | **ظهورٌ واحدٌ في تعليقٍ فقط** (`u/[username]/page.tsx:1242` يذكر «عُرفُ `MediaTitle`») | لا |
| `src/lib/nationality.ts` | ١٣١ | نفسُه | **ظهورٌ واحدٌ في تعليقٍ فقط** (`lib/intent.ts:5`) | لا |
| `src/lib/reports.ts` | ٢٥٥ | ٢٦ أغسطس ← ٣٠ أغسطس | **`DUPLICATE_CANDIDATE`** — انظر أدناه | لا |
| **المجموع** | **٦٥٨ سطراً** | | | |

**🔑 ودليلٌ تاريخيٌّ يحسم السبعةَ الأولى:** **كلُّها ملموسةٌ بالتزامٍ واحدٍ فقط — التزامِ استيراد المستودع الأوّل** (٦٢٠ ملفّاً · إضافاتٌ كلُّها). **أي أنّها لم تُستورد قطُّ في ٢٦٨ التزاماً.** **فليست «ماتت مؤخّراً»، بل دخلت المستودعَ ميّتة** — على الأرجح بقايا من قبل بدء التاريخ.

> 🔴 **`src/lib/reports.ts` — والحالةُ الوحيدةُ التي فيها خلَفٌ حيّ:**
>
> **`src/lib/periodStats.ts` يؤدّي الدورَ نفسَه ويزيد:** `asStatsPeriod` ⇠ `asReportPeriod` · `STATS_PERIODS` ⇠ `REPORT_PERIODS` · `StatTitle`/`StatBucket`/`PeriodStats` ⇠ `ReportTitle`/`ReportBucket`/`PeriodReport` · `buildPeriodStats` ⇠ `buildPeriodReport`.
> **وصفحةُ `/reports` نفسُها تستورد من `periodStats` لا من `reports`** (`src/app/reports/page.tsx:8`)، **و`StatsPeriod` يضيف `"all"` الذي لا يعرفه `ReportPeriod`.**
> **⇢ خلَفٌ أوسعُ حلَّ محلَّ سلَفِه ولم يُحذف السلَف.**

**والأثرُ الحقيقيُّ لهذه الثمانية:** **٦٥٨ سطراً لا تدخل الحزمةَ أصلاً** — فالمحزِّمُ لا يضمّ ما لا يُستورد. **فالكلفةُ ليست وزناً بل قراءةً:** **مطوّرٌ يفتح `reports.ts` ويظنّه المصدرَ فيعدّله، ثمّ لا يتغيّر شيءٌ في المنتَج.** **وهذا نوعُ الوقت الذي لا يظهر في أيّ قياس.**

## 4. التبعيّات — على النسخ المثبَّتة لا على الأحدث

**والدرسُ من Phase 5 مطبَّقٌ هنا:** `npm view <pkg> deprecated` **يفحص الأحدثَ لا المثبَّت** — **فكلُّ سطرٍ أدناه فُحص بـ`<pkg>@<installed>`.**

| الحزمة | المثبَّتة | ملفّاتٌ تشير إليها | الحكم |
|---|---|---:|---|
| **`@fontsource/cairo`** | 5.3.0 | **٠** | 🔴 **`UNUSED_CANDIDATE`** — **و`globals.css` يستورد `tajawal` و`poppins` فقط.** ⚠️ **وقد عولجت على `main` في `37d2a337` بعد الـbaseline** — تُسجَّل هنا لأنّها قائمةٌ في الـAudited SHA، **ولا تُطلب مرّتين** |
| **`eslint`** | **9.39.5** | ٢٣ | 🔴 **مهجورة**: «This version is no longer supported» · **والترقيةُ إلى 10 كُسرت مقيسةً** (`eslint-config-next` يقصر إضافاتِه على `^9`) — **مطابقٌ لتوصيف `S-14`، ولا مرشَّحَ جديد** |
| `@fontsource/poppins` · `@fontsource/tajawal` | 5.3.0 | ١ لكلٍّ | ✅ `USED` عبر `@import` في `globals.css` |
| `@supabase/ssr` · `@supabase/supabase-js` | 0.12.4 · 2.111.0 | ٣ · ٢ | ✅ `USED` |
| `@vercel/speed-insights` | 2.0.0 | ١ | ✅ `USED` (`layout.tsx`) |
| `fast-xml-parser` | 5.10.1 | ١ | ✅ `USED` |
| `next` · `react` · `react-dom` | 16.3.0 · 19.2.4 | ٢٦٦ · ١٨٢ · ٢ | ✅ |
| `@types/node` · `@types/react` · `@types/react-dom` | — | **٠** | ✅ **`USED` ضمناً** — أنواعٌ يستهلكها المصرِّفُ لا الشيفرة، **وصفرُ الإشارات هنا ليس دليلَ موت** |
| `@tailwindcss/postcss` · `tailwindcss` · `typescript` · `eslint-config-next` | — | ١–٢ | ✅ `USED` في config |

**⇢ حزمةٌ واحدةٌ ميتةٌ فعلاً، وواحدةٌ مهجورةٌ محبوسةٌ بقيدٍ مثبت. ولا شيءَ غيرَهما.**

## 5. التكرار — والأغلبيّةُ ليست تكراراً

**فحصتُ ثلاثةَ أنواعٍ من الشبَه، ولم أعتمد على الاسم وحدَه كما أمرت.**

**١) تجزئةٌ متطابقةٌ بايتاً ببايت — ثلاثُ مجموعات (١٠ ملفّات):**

| المجموعة | الملفّات |
|---|---|
| أ (٢) | `app/movie/[id]/loading.tsx` · `app/show/[id]/loading.tsx` |
| ب (٣) | `app/post/[key]/loading.tsx` · `app/review/[type]/[id]/loading.tsx` · `app/talk/[type]/[id]/loading.tsx` |
| ج (٥) | `app/lists/loading.tsx` · `app/lists/[id]/loading.tsx` · `app/profile/settings/loading.tsx` · `app/ratings/loading.tsx` · `app/stats/loading.tsx` |

> ⚠️ **وهذه `DUPLICATE_CANDIDATE` لا `UNUSED_CANDIDATE`، والفرقُ يغيّر العلاج:** **Next يوجب ملفَّ `loading.tsx` في كلِّ قطعةِ مسار** — **فالحذفُ يُسقط الهيكلَ عن خمسِ صفحات.** **العلاجُ ثلاثةُ مكوّناتِ هيكلٍ مشتركة تستوردها العشرةُ**، فيبقى الملفُّ ويصير سطرَين. **ولو كتبتُ «احذف عشرةَ ملفّاتٍ مكرّرة» لكان الحكمُ صحيحاً والعلاجُ كارثة.**

**٢) اسمٌ مصدَّرٌ من أكثر من وحدة — ١٣ اسماً، أحدَ عشرَ منها عُرفُ إطارٍ لا عيب:**
`GET` · `POST` · `metadata` · `generateMetadata` · `dynamic` · `runtime` · `maxDuration` · `size` · `alt` · `contentType` — **كلُّها أسماءٌ يفرضها Next على كلِّ مسار.** **عدُّها تكراراً خطأُ قراءةٍ للأداة.**

**والاثنان الباقيان حقيقيّان، وكلاهما `USED`:**

| الاسم | الموضعان | الحكم |
|---|---|---|
| `ShareCard` | `components/ShareCard.tsx` (١٦٢ سطراً · مكوّنُ واجهة) · `lib/shareCard.tsx` (١٠٦٩ سطراً · راسمُ صورة OG) | ⚠️ **تصادمُ تسميةٍ لا تكرار** — **شيئان مختلفان باسمٍ واحد**، وكلاهما مستعمَل (`ReportView` و`/stats` و`/reports` للأوّل · `/api/share` للثاني) |
| `searchPeople` | `lib/data.ts` (بحثُ الأعضاء في قاعدتنا) · `lib/tmdb.ts` (بحثُ الفنّانين في TMDB) | ⚠️ **تصادمُ تسمية** — **ويظهر ثمنُه في `api/search/route.ts` الذي يستوردهما معاً فيضطرّ إلى `searchPeople as searchMembers`** |

**⇢ والحكمُ: لا تكرارَ منطقٍ في الاثنين — بل اسمان يحملان معنيَين. الكلفةُ قراءةٌ لا حجم، والدرجةُ P3.**

**٣) `hooks` و`utilities`:** خمسةُ `hooks` (`useScrollMemory` · `useUiLocale` · `useBeforePaint` · `usePoll` · `useKeyboard`) — **صفرُ تكرارٍ بينها**، كلٌّ بمسؤوليّةٍ منفصلة.

## 6. الأصول والمسارات والجداول — وما يبدو ميّتاً وليس بميّت

### 6.1 الأصول (`public`) — ٢٥ ملفّاً · 576KB

🔴 **وفخٌّ ثانٍ هنا:** **ثلاثةَ عشرَ ملفَّ `splash/launch-*.png` (≈٣٥٠KB) لا يظهر اسمُ أيٍّ منها في الشيفرة نصّاً** — **ومع ذلك كلُّها حيّة:** `layout.tsx:86` يبنيها بقالبٍ نصّيّ `` `/splash/launch-${w}x${h}.png` ``، **و`sw.js:266` يخبّئ البادئة `/splash/`.** **بحثٌ بالاسم كان سيقتل صورَ الإقلاع كلَّها.**

| الأصل | الحجم | الحكم | الدليل |
|---|---:|---|---|
| `public/loopz-cover.jpg` | 39KB | ⚠️ **`UNUSED_CANDIDATE`** | **صفر ظهورٍ للاسم في `src` أو `public`** — ولا قالبَ يبنيه |
| `public/loopz-mark-192.png` | 6KB | ⚠️ **`UNUSED_CANDIDATE`** | صفر ظهور · **و`icon-192.png` هو المستعمَل في البيان** |
| `public/google3d0e328e26c2ca31.html` | 53B | ✅ **`USED` — ولا يُحذف** | **ملفُّ تحقّقِ Search Console**: **مستهلكُه جوجل لا شيفرتُنا**، **وحذفُه يُسقط ملكيّةَ النطاق** |
| `public/splash/*` (١٣) | ≈350KB | ✅ **`USED`** | قالبٌ نصّيٌّ في `layout.tsx` + بادئةٌ في `sw.js` |
| `public/fonts/*.woff` (٤) | 69KB | ✅ **`USED`** | `readFile` في `opengraph-image.tsx` و`api/share` و`api/list-og` — **خطوطُ صورِ OG لا خطوطُ الصفحة** |
| الباقي (أيقونات · `sw.js` · شعار) | — | ✅ `USED` | مشارٌ إليها ١–٤ مرّات |

**⇢ 45KB مرشَّحةٌ للحذف من 576KB. مكسبٌ ضئيلٌ — وأذكره لأنّ الجرد طُلب كاملاً، لا لأنّه يستحقّ التنفيذ وحدَه.**

### 6.2 المسارات — ٢١ مساراً، كلُّها حيّة

`api/`: `build` · `curated` · `franchise` · `genres` · `imdb-chart` · `lang-ping` · `list-og/[id]` · `news-gen` · `search` · `season` · `share` · `suggest` · `title-meta` · `trakt/callback` · `trakt/start` — **و`auth/callback` · `auth/signout` · `i/[size]/[file]` · `join/[code]` · `p/[code]` · `robots.txt`.**
**صفر مسارٍ يتيم:** كلٌّ منها إمّا مستدعىً من الشيفرة، أو مدخلٌ خارجيٌّ بطبيعته (OAuth · مشاركة · تحويلُ دعوة).

### 6.3 الجداول — والفخُّ الثالث والأكبر

**٥٢ جدولاً تظهر في نداءات `.from()` من ٧٢ في القاعدة.** **والفارقُ ٢٢ جدولاً — ولو أعلنتُها ميّتةً لكان ذلك أسوأَ خطأٍ في المرحلة.**

**فحصتُ كلَّ واحدٍ منها في القاعدة نفسها** (قراءةٌ محضة: `pg_proc.prosrc` · `pg_trigger` · `pg_policies` · `cron.job`):

| الحال | العدد | الجداول |
|---|---:|---|
| ✅ **حيٌّ عبر دالّةٍ أو مشغِّلٍ أو `cron`** | **١٩** | `episode_ratings` · `featured_lists` · `imdb_pool` · `news_posts` · `partner_clicks` · `partners` · `plus_rewards` · `profile_views` · `provider_content_links` · `provider_events` · `referral_codes` · `referral_events` · `referrals` · `runtime_errors` · `title_room_global_pins` · `user_active_days` · `verification_requests` · `visit_langs` · `weekly_top` |
| ⚠️ **`UNKNOWN` — بُني ولم يُشغَّل بعد** | ١ | **`subscriptions`** — **صفر دالّة وصفر شيفرة، ومشغِّلان وسياسة.** **ونصُّ المنتَج نفسُه يقول «الاشتراكات لم تُفتح بعد»** — **فهو استعدادٌ لا موت، ولا يُحذف** |
| 🔴 **`UNKNOWN` — يستحقّ سؤالاً** | ١ | **`partner_details`** — **صفر شيفرة · صفر دالّة · صفر مشغِّل · صفر `cron`** — **وسياستان.** **وهو الجدولُ الذي يحمل أحسَّ ما في القاعدة (IBAN والهويّة).** **⇢ جدولٌ حسّاسٌ مفتوحٌ بسياسةٍ ولا كاتبَ له ولا قارئ.** **يُحال إلى `LOOPZ-AUD-0040` ولا يُنشأ له مرشَّحٌ منفصل** |
| 🔴 **يتيمٌ تماماً** | ١ | **`watched_episodes_backup_133`** — **صفر في كلِّ المقاييس الخمسة.** **وهو `LOOPZ-AUD-0055` (Phase 6) و`LOOPZ-AUD-0005` (Phase 0) — ولا ثالثَ لهما** |

**🔑 والدرسُ العامّ:** **التطبيقُ يكلّم قاعدتَه بالدوالِّ لا بالجداول.** **فغيابُ `.from("x")` يقيس أسلوبَ الوصول لا حياةَ الجدول** — **و«١٩ من ٢٢» هو حجمُ الخطأ الذي كان سيقع لو اكتفيتُ ببحثٍ نصّيٍّ في `src`.**

### 6.4 الأعلام والبيانات التجريبيّة

**١٤ متغيّرَ بيئةٍ تُقرأ في الشيفرة**، وستّةٌ منها **بواباتُ ميزةٍ حقيقيّة** (غيابُ المفتاح يُخفي الميزةَ ولا يكسرها): `TRAKT_CLIENT_ID`/`SECRET` · `GEMINI_API_KEY` · `GIPHY_API_KEY` · `DEEPL_API_KEY` · `OMDB_API_KEY`.
**⚠️ وأربعةٌ منها ما زالت غائبةً عن `.env.example`** — **وهو `LOOPZ-AUD-0002` القائم، لا مرشَّحٌ جديد.**
**والبياناتُ التجريبيّة: صفر `mock` وصفر `fixture` وصفر `.test.` وصفر `.spec.` وصفر `__tests__` في المستودع كلِّه** — §8.

## 7. شيفرةُ التنقيح والدَّين الصغير

| البند | العدد | القراءة |
|---|---:|---|
| `TODO`/`FIXME`/`HACK`/`XXX` | **٠** | ✅ **في ١٠٨ آلاف سطر** |
| `any` | **٠** | ✅ |
| `@ts-ignore`/`@ts-expect-error` | **٠** | ✅ |
| `console.*` | **٧** | ⚠️ مفصَّلةٌ أدناه |
| `eslint-disable` | **٢٤** | ⚠️ مفصَّلةٌ أدناه |
| متغيّراتٌ غيرُ مستعمَلة | **٥** | ⚠️ دَينٌ حقيقيّ |

**والسبعةُ `console` مصنَّفةٌ لا معدودة:**

| الموضع | النوع | الحكم |
|---|---|---|
| `lib/actions.ts` · `app/error.tsx` · `thread/ThreadReplies.tsx` (×٢) · `trailers/TrailerPlaybackController.tsx` | `error`/`warn` | ✅ **مقصودة** — تشخيصٌ في مسار فشلٍ، **ولا قيمةَ حسّاسةً في أيٍّ منها** (فُحص في Phase 6 §4.2) |
| **`api/title-meta/route.ts` · `api/genres/route.ts`** | **`console.log(r)`** | 🔴 **`P7-0x`** — **طباعةُ ناتجِ عمليّةٍ إداريّةٍ كاملاً في سجلّ الخادم** · **ومسارانِ إداريّانِ لا يمرّان بمصفاة** |

**والخمسةُ المتغيّراتُ غيرُ المستعمَلة:** `FOUNDER_PLUS_UNTIL` (`app/features/page.tsx:6`) · `rankByImdb` (`app/news/page.tsx:69`) · `epMinutes` (`components/LibraryAnalysis.tsx:1380`) · `ar` (`components/ListCoverSheet.tsx:55`) · `TOUR_META` (`components/TourGuide.tsx:6`).
**⚠️ ولا أُعلن أيّاً منها «احذفه» بلا قراءة:** **`TOUR_META` و`FOUNDER_PLUS_UNTIL` ثابتانِ يُحتمَل أن يكونا بقيّةَ ميزةٍ نصفَ مسحوبة** — **والفرقُ بين «متغيّرٌ منسيّ» و«ميزةٌ سُحبت نصفَ سحبة» لا تحسمه أداةُ لِنت.**

**والأربعةُ والعشرون `eslint-disable`:** ثمانيةٌ `no-img-element` (صورُ OG — مبرَّرة) · سبعةٌ `react-hooks/exhaustive-deps` · ثلاثةٌ `no-unused-vars` · اثنان `react-hooks/purity` · واحدةٌ `prefer-const`. **⚠️ والسبعةُ `exhaustive-deps` وحدَها تستحقّ مراجعةً بندَ بند** — **فكلُّ واحدةٍ منها احتمالُ حالةٍ قديمةٍ لا يراها المصرِّف.** **ولم أراجعها في هذه المرحلة** (سلوكٌ لا جودةُ نصّ) — **تُسجَّل ولا تُدَّعى.**

## 8. البنية — والفراغُ الأخطر

| المقياس | القيمة | الحكم |
|---|---:|---|
| **استيرادٌ دائريّ** | **٠** | ✅ **في ٤٥١ ملفّاً — نتيجةٌ غيرُ شائعة** |
| أكبرُ الملفّات | `lib/actions.ts` 262KB · `lib/data.ts` 256KB · `lib/i18n.ts` 211KB | ⚠️ **`P7-0x`** — ثلاثةُ ملفّاتٍ تحمل **٧٠٠KB** أي نحوَ **٤٠٪ من `src`** |
| مكوّنات · مكتبات · صفحات | ٢٢٩ · ١١٨ · ٤٩ | — |
| **ملفّاتُ اختبار** | **٠** | 🔴 |

> 🔴 **وأخطرُ ما في Phase 7 ليس عيباً وجدتُه، بل غيابٌ:**
>
> **صفر ملفِّ اختبار · صفر مشغّل · صفر `script` باسم `test` · صفر `mock`.** **وهو `LOOPZ-AUD-0001` المسجَّلُ منذ Phase 0 — ولا أُنشئ له مرشَّحاً ثانياً.**
>
> **ولماذا يظهر هنا أثقلَ ممّا ظهر هناك؟** **لأنّ Phase 7 هي المرحلةُ التي تُنتج قوائمَ حذف.** **وكلُّ صفٍّ في §3 و§4 و§5 يقول «احذف» — ولا اختبارَ واحدٌ يقول إن كان الحذفُ سليماً.** **فالجملةُ الصادقةُ: هذه القائمةُ لا تُنفَّذ اليومَ بأمان.**
>
> ⚖️ **ويجتمع مع `LOOPZ-AUD-0006` (لا بيئةَ معزولة) و`LOOPZ-AUD-0054` (لا مسارَ استعادة) في مثلثٍ واحد:** **لا اختبارٌ يمنع الخطأ · ولا بيئةٌ تُجرَّب فيها · ولا نسخةٌ تُرجع منها.** **وأيُّ خطّةِ حذفٍ تُكتب قبل كسر ضلعٍ من هذا المثلّث خطّةُ رجاءٍ لا خطّةُ هندسة.**

**والملفّاتُ الثلاثةُ الكبيرة** — لا أوصي بتقسيمها الآن: **تقسيمُ ملفٍّ بـ٢٦٢KB بلا اختبارٍ واحد هو أخطرُ عمليّةٍ في هذه القائمة كلِّها.** **ترتيبُها بعد `0001`.**

## 9. فحصُ الأسرار — على الرأس وحدَه، وبأداةٍ لا تطبع القيم

**الأداةُ مكتوبةٌ لهذا الغرض: تطبع المسارَ ورقمَ السطر واسمَ القاعدة وطولَ السطر — ولا تطبع القيمةَ المطابقةَ أبداً.** **إحدى عشرةَ قاعدة:** `JWT` · `service_role` · مفتاحٌ خاصّ · `AKIA` · `AIza` · `gh*_` · `xox*-` · `sk_live/test` · `sk-` · مفتاحُ DeepL · وإسنادُ `secret/token/password/api_key` بقيمةٍ حرفيّة.

| الشجرة | ملفّاتٌ فُحصت | إشارات | **قيمُ أسرار** |
|---|---:|---:|---:|
| **الـAudited SHA `f8a2b33c`** | ٦٤٦ | ١٤ | 🟢 **٠** |
| **رأسُ فرع التوثيق `f5e06d5`** | ٦٥٤ | ٢١ | 🟢 **٠** |

**والأربعَ عشرةَ إشارةً في الـbaseline كلُّها الشيءُ نفسُه:** **اسمُ دور Postgres `service_role` داخل أوامر `grant execute … to …` في ملفّات الهجرات** — **اسمُ دورٍ لا قيمةَ سرّ.** **والسبعُ الزائدةُ في رأس الفرع هي تقاريرُ التدقيق نفسُها إذ تتحدّث عن الدور.**

✅ **صفر `JWT` · صفر مفتاحٍ خاصّ · صفر مفتاحِ مزوّدٍ حرفيّ · صفر إسنادٍ لقيمةِ سرّ.** **وكلُّ مفتاحٍ في المشروع يُقرأ من `process.env` — أربعةَ عشرَ موضعاً، فُحصت واحداً واحداً.**
⚠️ **ولم أفحص التاريخَ:** أمرتَ بالرأس وحدَه، **والمستودعُ عامّ فنشرُ نتيجةِ فحصٍ تاريخيٍّ فيه ضررٌ لا نفع.** **`NOT_IN_SCOPE` مُعلَنٌ لا مسكوتٌ عنه.**

## 10. المصالحة مع المشكلات القائمة — ولا مرشَّحَ مكرَّر

| القائم | ما وجدتُه في Phase 7 | القرار |
|---|---|---|
| **`LOOPZ-AUD-0001`** (لا اختبارات) | **صفر ملفّ · صفر مشغّل · صفر mock** | ✅ **يؤكَّد ويُرفَع أثرُه** — §8. **لا رقمَ جديد** |
| **`LOOPZ-AUD-0002`** (متغيّراتٌ غائبةٌ عن `.env.example`) | أربعةٌ منها مؤكَّدةٌ في الاستعمال | ✅ **يؤكَّد. لا رقمَ جديد** |
| **`LOOPZ-AUD-0003`** (`eslint-config-next` لا يطابق `next`) | قائمٌ في الـbaseline · **وعولج على `main` في `37d2a337`** | ✅ **لا رقمَ جديد** |
| **`LOOPZ-AUD-0004`** (لا `engines`/`packageManager`) | مؤكَّد: `package.json` بلا الحقلَين · **والبناءُ هنا وقع على Node 22.22.2** بلا تثبيت | ✅ **يؤكَّد. لا رقمَ جديد** |
| **`LOOPZ-AUD-0005` / `0055`** (`watched_episodes_backup_133`) | **يتيمٌ في المقاييس الخمسة** — دليلٌ سادسٌ يسندهما | ✅ **لا ثالثَ لهما** |
| **`LOOPZ-AUD-0040`** (`SECURITY DEFINER`) | **`partner_details` بلا كاتبٍ ولا قارئ** — §6.3 | ✅ **يُحال إليه. لا رقمَ جديد** |
| **`S-14`** (Phase 5 — `eslint` مثبَّتٌ على `^9`) | **`eslint@9.39.5` مهجورةٌ بنصِّ المسجّل** | ✅ **يؤكَّد. لا رقمَ جديد** |

> ⚠️ **وتنبيهٌ على المهمّة نفسِها:** **أمرتَ بالمصالحة مع `LOOPZ-AUD-0010` و`0011` — وهما غيرُ موجودَين.** الأرقامُ الصادرةُ حتى الآن: `0001`…`0007` ثمّ `0009` ثمّ `0014`…`0060`. **`0008` و`0010`…`0013` لم تُصدَر قطُّ.** **فلم أُصالح رقمَين لا وجودَ لهما، ولم أخترع لهما مقابلاً** — **وصالحتُ ما وجدتُه فعلاً أعلاه.** فإن كنتَ تقصد رقمَين آخرَين فسمِّهما وأُصالحهما.

## 11. المرشّحات — خطّةٌ بلا تنفيذ

| المعرّف | الدرجة | العنوان | مجموعةُ الحذف المقترحة | الأثرُ المتوقَّع | اختباراتُ ما بعد الحذف |
|---|---|---|---|---|---|
| **`P7-01`** | 🟠 **P2** | **ملفّان إداريّان يطبعان ناتجَ العمليّة كاملاً في السجلّ** (`api/title-meta` · `api/genres`) | مستقلّة — سطران | صفرُ أثرٍ على المستخدم | نداءُ المسارَين وتأكّدُ بقاءِ الجواب |
| **`P7-02`** | 🟢 **P3** | **ثمانيةُ ملفّاتٍ لم تُستورد قطُّ — ٦٥٨ سطراً** | **أ:** السبعةُ التي لم تُلمس منذ الاستيراد · **ب:** `lib/reports.ts` وحدَه (خلَفُه `periodStats`) | **صفرُ أثرٍ على الحزمة** (لا تدخلها أصلاً) · وأثرٌ على القراءة | `tsc` + `eslint` + `next build` + جردُ ٨١ مساراً |
| **`P7-03`** | 🟢 **P3** | **`@fontsource/cairo` غيرُ مستعمَلة** | مستقلّة | حزمةٌ أقلّ · **وعولجت على `main`** | `npm ci` نظيف + بناءٌ + **فحصُ الخطوط المحمَّلة حيّاً** |
| **`P7-04`** | 🟢 **P3** | **١٠ ملفّاتِ `loading.tsx` متطابقةٌ في ٣ مجموعات** | **لا حذف** — ثلاثةُ مكوّناتِ هيكلٍ مشتركة | ⚠️ **الحذفُ يُسقط الهيكل** — العلاجُ استخراجٌ لا إزالة | فحصٌ بصريٌّ للهيكل في العشرة |
| **`P7-05`** | 🟢 **P3** | **`loopz-cover.jpg` + `loopz-mark-192.png` — 45KB** | مستقلّة | ضئيل | **فحصُ بطاقات المشاركة والبيان بعد الحذف** |
| **`P7-06`** | 🟢 **P3** | **خمسةُ متغيّراتٍ غيرِ مستعمَلة** | **لا تُحذف بلا قراءة** — `TOUR_META` و`FOUNDER_PLUS_UNTIL` قد يكونان بقيّةَ ميزة | — | `eslint` يعود إلى ١١ تحذيراً |
| **`P7-07`** | 🟢 **P3** | **تصادمُ تسمية: `ShareCard` ×٢ · `searchPeople` ×٢** | إعادةُ تسميةٍ لا حذف | قراءةٌ أوضح · **ويزول `as searchMembers`** | `tsc` |
| **`P7-08`** | 🟢 **P3** | **ثلاثةُ ملفّاتٍ تحمل ٤٠٪ من `src`** | **مؤجَّلٌ خلف `0001`** | — | **لا تُنفَّذ بلا اختبارات** |
| **`P7-09`** | 🟢 **P3** | **سبعُ `exhaustive-deps` معطَّلةٌ بلا مراجعةٍ بندَ بند** | مراجعةٌ لا حذف | احتمالُ حالةٍ قديمة | مراجعةٌ يدويّةٌ لكلِّ موضع |

🔴 **وشرطٌ واحدٌ يعلو القائمةَ كلَّها:** **`P7-02`…`P7-08` عمليّاتُ حذفٍ في مشروعٍ بصفر اختبار** (`LOOPZ-AUD-0001`) **وصفر بيئةٍ معزولة** (`LOOPZ-AUD-0006`) **وصفر مسارِ استعادة** (`LOOPZ-AUD-0054`). **فالترتيبُ الصحيح: `P7-01` وحدَه يجوز الآن — والباقي بعد أوّلِ حزمةِ اختبارات.** **وهذا رأيي مكتوباً لا مضمَراً.**

## 12. إقرار

- ✅ **صفر حذف · صفر تعديلٍ على شيفرة التطبيق · صفر تغييرٍ على `dependencies` أو `config` أو `lockfile` · صفر تثبيتٍ دائم · صفر Merge · صفر Deploy · صفر Migration · صفر تغييرٍ على Production.**
- ✅ **والبوّاباتُ الثلاثُ شُغّلت على الـAudited SHA `f8a2b33c` في شجرةِ عملٍ منفصلة** — **والأمرُ والنسخةُ والنتيجةُ في §2 فيُعاد تشغيلُها ولا تُصدَّق.**
- ✅ **وكلُّ استعلامٍ لقاعدة البيانات في §6.3 قراءةٌ للبيانات الوصفيّة** (`pg_proc` · `pg_trigger` · `pg_policies` · `cron.job`) — **صفر صفٍّ من بيانات الأعضاء.**
- ✅ **وأسماءُ دوالِّ القاعدة محجوبةٌ كما في Phase 5 و6**؛ **وأسماءُ الجداول والملفّات صريحةٌ لأنّها في مستودعٍ عامٍّ أصلاً.**
- ✅ **وفحصُ الأسرار لم يطبع قيمةً واحدة** — بالبناء لا بالحظّ: الأداةُ تطبع الطولَ ولا تطبع المطابَقة.
- ⚠️ **ولم أفحص:** **تاريخَ المستودع** (خارج النطاق بأمرك) · **وسلوكَ الـ`exhaustive-deps` السبع** (مراجعةُ سلوكٍ لا نصّ) · **ولم أشغّل أدواتِ تحليلِ استيرادٍ خارجيّة** — **الرسمُ البيانيُّ مكتوبٌ هنا ومنهجُه في §1، ولم أثبّت حزمةً واحدة.**
- 🔑 **ولم أُسوِّ إشارةً آليّةً بعيب:** **ثلاثةُ فِخاخٍ كانت ستمرّ لو فعلت** — `imageLoader.ts` الحيُّ عبر config · و١٣ صورةَ إقلاعٍ حيّةً عبر قالبٍ نصّيّ · و**١٩ جدولاً من ٢٢ حيّةً عبر الدوالّ لا عبر `.from()`**.

**متوقّفٌ عند البوابة. لن أبدأ Phase 8 قبل ردّك.**
