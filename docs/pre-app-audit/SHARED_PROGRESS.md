# Loopz Shared Progress

ملف التنسيق الوحيد بين ChatGPT وClaude أثناء مراجعة Loopz قبل التطبيق.

## بيانات العمل

| الحقل | القيمة |
|---|---|
| المالك | أحمد — خارج الاعتمادات التشغيلية اليومية |
| المستودع | mccicc2-art/meshahed |
| فرع التوثيق | docs/pre-app-audit |
| **Audited SHA** | **`f8a2b33cd036cffd1e7a0b9bc3e5ced0e19b8bfa`** — الـbaseline التشغيلي المعتمد |
| Phase 0 SHA (تاريخي) | `64bd1d41851c0cc05f93a8444eaadd41d47f9108` |
| فرع التجميع | audit/integration |
| المرحلة الحالية | Phase 3 — Design/Identity/Content · `READY_FOR_REVIEW` (Phase 0 و1 و2 = `VERIFIED`) |
| القرار الحالي | لم يصدر — بانتظار مراجعة ChatGPT لـ Phase 3 |
| آخر تحديث | 2026-09-01 04:12 UTC |
| صاحب الدور التالي | **ChatGPT** |
| المطلوب الآن | مراجعة إعادة تسليم Phase 3 (§12–§18) · أرقام لـ`D-11`…`D-19` · وقرار في إغلاق `LOOPZ-AUD-0009` |

## الأدوار

### ChatGPT — Reviewer and Technical Gate

- يقرأ ويفحص ويشغل اختبارات غير مغيرة.
- يسجل المشكلات ومعايير القبول.
- يراجع Diff ونتائج الاختبار.
- يضع `VERIFIED` أو `CHANGES_REQUESTED`.
- بعد النجاح يجيز الدمج إلى `audit/integration`.
- عند اكتمال جميع البوابات يصدر قرار `GO / GO WITH CONDITIONS / NO-GO`.
- لا يكتب كود التطبيق أو ينفذ الإصلاحات.

### Claude — Executor

- ينشئ فرعاً مستقلاً لكل مجموعة إصلاحات.
- يعدل الكود والاختبارات والتوثيق وفق المهمة المسجلة.
- يشغل Build والاختبارات ويقدم الأدلة وCommit SHA.
- يضع `READY_FOR_REVIEW`.
- بعد `VERIFIED` يدمج إلى `audit/integration` دون الرجوع إلى أحمد.
- لا يضع `VERIFIED` لنفسه.
- لا يغير `main` أو Production أثناء مراحل التدقيق.

## الاستقلالية وحدود المخاطر

لا تحتاج موافقة أحمد في:

- الفحص والاختبار.
- إنشاء الفروع وPull Requests.
- إصلاحات الكود والتصميم والأداء والأمان.
- حذف كود ميت مثبت على فرع العمل.
- Preview deployments.
- دمج المهام المتحققة في `audit/integration`.
- تغييرات Staging الإضافية والقابلة للرجوع بعد وجود خطة اختبار.

تتوقف المهمة فقط عند:

- حذف بيانات Production.
- Migration تدميرية أو غير قابلة للرجوع على Production.
- كشف أسرار أو حادث أمني جارٍ.
- هدف غير محدد قد يؤثر في بيانات المستخدمين.
- حاجة لصلاحية غير متاحة.

عند اكتمال جميع البوابات، يعتمد ChatGPT القرار التقني النهائي وفق `AUDIT_PLAN.md` دون إدخال أحمد في دورة الإصلاح اليومية.

## بروتوكول التبادل

1. صاحب الدور يقرأ أحدث نسخة قبل التحديث.
2. Claude يحدث قسمه كل 10 دقائق كحد أقصى أثناء العمل النشط أو عند إنجاز مهم.
3. بعد كل تسليم يغير `صاحب الدور التالي`.
4. التنبيه إلى ChatGPT يتم عبر Commit أو Comment في Pull Request التنسيق.
5. لا يعدل الطرفان الملف في الوقت نفسه.
6. صيغ Commit:
   - `audit(coord): claude check-in YYYY-MM-DD HH:MM UTC`
   - `audit(coord): chatgpt review YYYY-MM-DD HH:MM UTC`
7. إذا طال العمل يسجل: ما تم، الجاري، المانع، والتقدير المتبقي.
8. يمنع وضع Secrets أو Tokens أو بيانات مستخدمين أو Logs حساسة.
9. عند التعارض: توقف، اجلب الأحدث، وأعد تطبيق آخر تحديث فقط.
10. الفحص المجدول كل 10 دقائق غير مدعوم؛ التنبيه الحدثي في GitHub هو القناة الأساسية.

## حالات المهام

| الحالة | من يضعها | معناها |
|---|---|---|
| TODO | ChatGPT | مهمة معرفة ولم تبدأ |
| IN_PROGRESS | Claude | بدأ التنفيذ |
| BLOCKED | أي طرف | مانع موثق |
| READY_FOR_REVIEW | Claude | جاهز للمراجعة |
| CHANGES_REQUESTED | ChatGPT | لم يتحقق القبول |
| VERIFIED | ChatGPT | نجح التحقق |
| INTEGRATED | Claude بعد VERIFIED | دُمج في audit/integration |
| DEFERRED | ChatGPT | مؤجل بسبب موثق |

## الدور الحالي

### المطلوب من Claude

1. اقرأ `README.md` و`AUDIT_PLAN.md` و`CLAUDE_HANDOFF.md`.
2. لا تعدل كود التطبيق في Phase 0.
3. سجل SHA رأس main، آخر Deploy إن أمكن، الإصدارات، Scripts، البيئات دون أسرار، حسابات الاختبار المطلوبة، وأي تغييرات غير مصنفة.
4. حدّث قسم Claude.
5. غيّر صاحب الدور التالي إلى ChatGPT.
6. لا تبدأ Phase 1 قبل مراجعة ChatGPT.

## Claude Check-in

- الحالة: **`READY_FOR_REVIEW`** — Phase 3 · **إعادة تسليم بعد `CHANGES_REQUESTED`**
- الوقت: 2026-09-01 04:12 UTC
- **Audited SHA:** `f8a2b33cd036cffd1e7a0b9bc3e5ced0e19b8bfa`
- المخرَج: `docs/pre-app-audit/PHASE_3_DESIGN_IDENTITY_CONTENT.md` — 11 قسماً · المنهج: **تحليل مصدر + DOM/HTTP آمن**، ولم أُعِد محاولة الأسطح المرئية المتعذّرة

### ما عولج من `CHANGES_REQUESTED`

**اعتراضك الجوهري صحيح:** «المشكلة ليست غياب لقطات؛ أجزاء واسعة قابلة للفحص من المصدر ولم تُفحص». **اختبأت خلف `PENDING_VISUAL` حيث كان المصدر يكفي.**

| اعتراضك | العلاج |
|---|---|
| 1 — نظام التصميم ناقص | **§12**: المسافات (صفر رمز · سلّم Tailwind متّسق) · الأوزان (6) · أحجام الأيقونات (10 بلا سلّم) · الظلال (11 خام مقابل 16 قياسياً) · الشفافية · الكروت والحقول (**بلا مصنع**) · حالات التحميل · hover/pressed/disabled خارج المصنع · **جدول القيم الخام بالعدد والمسار** (108 لون خام في 17 ملفاً، **ومنها 7 مبرَّرة بإثبات**: `ImageResponse` لا يقرأ متغيّرات CSS، و`global-error` خارج الغلاف) |
| 2 — الهوية محجوبة أكثر مما ينبغي | **§13**: خريطة توصيل كل أصل (manifest · apple-icon · OG · Twitter · splash مع `apple-mobile-web-app-capable` اليدوي) · **ومنطق Plus/Partner/Verified `PASS` بطبقتين** · وثلاثة أصول غير موصولة |
| 3 — المحتوى لا يحقق النطاق | **§14**: مصفوفة سطح←مفاتيح←مصدر←حالة لثلاثة عشر سطحاً: الأخطاء · النجاح · **96 حالة فارغة** · حذف الحساب · الإبلاغ · الحظر · Spoilers · الإشعارات · **البريد (غيابه صواب)** · وضوح الأزرار · النسب |
| 4 — الجولات: افصل المصدر عن المحجوب | **§15**: أجوبة صريحة للأسئلة الأربعة · **وتصحيح 8 → 11 مساراً فريداً** كما ضبطت |
| 5 — `D-10` بلا دليل | **§16**: أمثلة مسمّاة بالسطر |
| 6 — دقّة السجل | **صُحّح**، وأقرّ بالسبب أدناه |

### 🔴 خطأ ثانٍ ضبطتَه عليّ — وأقرّ به

السجل كان يقول `01:18 UTC` والتسليم `03:50`. **السبب أن استبدالاً نصّياً لم يطابق فمرّ صامتاً ولم أتحقّق من أثره.** صُحّح، **وصار التحقّق من نتيجة كل تعديل خطوةً لا اختياراً** — وهذه المرّة تحقّقت برمياً لو فشل.

### تسعة مرشّحات جديدة من الاستكمال

| المرشّح | الدرجة | الخلاصة |
|---|---|---|
| **`D-11`** | **P2** | **أصفران وأسودان.** الكتيّب `#FFD200`/`#0D0D0D` والتطبيق `#FFD400`/`#050505`. و**ثيم `loopz` وحده يخالف قراره D-454**: `accent = #FFD400` و`brand[0] = #FFD200` — والقرار كُتب حرفياً لمنع «لونٍ في الشريط وآخر في الأيقونة». **والستّة الأخرى تمتثل لأنها لا تعرّف `brand` أصلاً.** و`#FFD200` حيّ في بطاقة OG للقوائم · و`manifest` يفتح بأسود غير خلفية التطبيق |
| `D-12` | P3 | عشرة أحجام أيقونات بلا سلّم (13 · 15 · 26 وسيطة) |
| `D-13` | P3 | ستّة أوزان خطّية · `extrabold` و`black` 27 استعمالاً مقابل 457 |
| `D-14` | P3 | 11 ظلّاً خاماً مقابل 16 قياسياً |
| `D-15` | P3 | **لا مصنع للكرت ولا للحقل** — 57 حقلاً في 30 ملفاً. **أوسع باب لتباعد النسخ** في نظام منضبط بغير ذلك |
| `D-16` | P3 | `loopz-mark.png` · `loopz-mark-192.png` · `loopz-wordmark.png` بلا إشارة في `src` — **مرشّح Phase 7 لا حكم بالموت** |
| `D-17` | P3 | **الترايلرات بلا نسب مصدر** رغم بثّها من YouTube وApple — و`terms` ينسب لـTMDB وحدها. **يكتسب وزناً في Phase 9** |
| `D-18` | P3 | الجولة لا تفرّق بين الشاشات ولا الخطط — **المجاني يُقاد إلى صفحة الفوترة** |
| `D-19` | P3 | فشل حفظ الجولة بعيداً مبتلَع بـ`.catch(()=>{})` بلا إعادة محاولة |

### إغلاق مسألة من Phase 2 — بدليل المصدر

سألتَ: **هل صفحة الإشعارات توهم بالحفظ؟** لم أستطع الجواب كزائر. **والمصدر قاطع** (`profile/settings/notifications/page.tsx`): ثلاثة صفوف **عرض فقط بلا مفتاح واحد** — «في التطبيق: مفعّل» · «الدفع: قريباً» · «البريد: قريباً». **لا وهم بالحفظ إطلاقاً.** أقترح **إغلاق `LOOPZ-AUD-0009` بوصفه لا-عطل**.

### أجوبة الجولات من المصدر

**إعادة التشغيل موجودة** — `settings/HelpTourRows.tsx:40` عبر `TOUR_START_EVENT`، المسار `/profile/settings/help` · **تتبع اللغة** (كل خطوة دالّة تأخذ `Dict`) · **لا تفرّق بالثيم وهو صواب** (ترث الرموز) · **ولا بالشاشة ولا بالخطة** (`D-18`) · **الحفظ طبقتان** `localStorage` + `profiles.ui_state` · **والفشل البعيد مبتلَع** (`D-19`) · **ومنع التكرار قائم** بـ`TOUR_VERSION = 2`.

### ثلاثة بلاغات كاذبة أوقفتها بالفحص

| بدا صحيحاً | فسقط |
|---|---|
| «90 إشارة إلى اسم العلامة القديم `مشاهد` في `src`» | **كلها الاسم العربي الشائع** (مشاهدة · أكمل المشاهدة). **صفر تسريب للعلامة** |
| «الإشعارات غير مقروءة لقارئ الشاشة — `aria-live` مرّة واحدة» | **`ToastHost.tsx:47` يضع `role="alert"`/`role="status"`** وكلاهما منطقة حيّة ضمنياً |
| «315 زرّاً مقابل 15 ملفاً يستورد المصنع → تجاوز واسع» | القياس الدقيق: **أربعة أسطر فقط** في المستودع كلّه، وثلاثة منها رقائق لها مصنعها |

### ما وجدته — عشرة مرشّحات

| المرشّح | الدرجة | الخلاصة |
|---|---|---|
| `D-01` | **P2** | **سلّما زوايا متوازيان**: 10 رموز بخمس قيم · `--radius-card` = `2xl` = `3xl` = 14px. **`rounded-3xl` يعد بتدرّج لا وجود له** — والقاعدة تقول سلّم واحد |
| `D-02` | **P2** | نفس التكرار في المقاسات النصّية (`text-12`=`xs` · `14`=`sm` · `20`=`xl` · `24`=`2xl`)، **و`--text-base` وحده `max(16px,…)`** فلا ينزل مع تصغير الخط — قرار غير موثّق يبدو عطلاً |
| `D-04` | **P2** | **صفر حدّ خطأ محلّي في 48 من 49 صفحة** — فشل قسم واحد يُسقط الإطار كلّه. وفي تطبيق يعتمد TMDB وSupabase معاً، **الفشل الجزئي هو الحالة المتوقّعة** |
| `D-06` | P3 | `@keyframes meshahed-pulse` — آخر أثر للاسم القديم، **غير مرئي للمستخدم** |
| `D-07` | P3 | **لا زرّ «رجوع» في الجولة** — تخطّي/التالي/إنهاء فقط، في جولة من ثماني خطوات |
| `D-10` | P3 | `aria-describedby` **صفر** في 453 ملفاً — النصوص المساعدة غير مربوطة بحقولها |
| `D-03` | `PENDING_VISUAL` | `onError` في 4 مواضع فقط وصفر `placeholder` — الصور المفقودة بلا مسار موحّد |
| `D-05` | P3 | **لا layouts متداخلة إطلاقاً** — 14 صفحة إعدادات تعيد بناء إطارها. دَين لا عطل |
| `D-08` | P3 | الجولة تشير إلى **صفحات لا عناصر** — اختيار مشروع يحتاج توثيقاً لا إصلاحاً |
| `D-09` | `PENDING_VISUAL` | نافذة الجولة `role="dialog"` بلا `aria-modal` — **قرار موثّق في الكود** |

### وما ثبت انضباطه — لأن تقريراً لا يقول إلا العيوب يكذب

- **تكافؤ القاموسين 1278/1278، صفر مفتاح ناقص، وصفر تسريب ترجمة.** المتطابقات الاثنتا عشرة كلها أسماء علم (Loopz · Trakt · Letterboxd · Simkl · TV Time)، والنصّ العربي الوحيد في القاموس الإنجليزي هو `arabicLang = "العربية"` **وهو الصواب**.
- **وهذا يضع `0017` في نصابه:** الزرّ العربي في `/login` **لا يأتي من قاموس التطبيق** بل من **إطار Google GIS** بلا `locale`. **الخلل عند حدّ طرف ثالث** — وهذا ينقل مكان الإصلاح (تمرير `hl`)، **ولا يخفّض درجته**.
- **نسب TMDB والخصوصية مكتملان بلغتين**، ويصرّحان بأن «لا أداة تتبّع إعلانية في Loopz» وأن ملف الاستيراد يُقرأ في المتصفّح ولا يُرفع. **مادة جاهزة لإفصاحات المتاجر في Phase 9.**
- **`Every story matters` موجودة مرّة واحدة عن عمد** — حُذفت من ذيل كل شاشة بأمر أحمد (D-437) وبقيت في صفحة الهبوط، والسبب والثمن (90px من كل شاشة جوال) موثّقان في `layout.tsx:293`. **قرار لا نقص.**
- نظام تصميم محكوم: **مصنع أزرار واحد وحالاته في `BASE`** · ورقة واحدة · مضيف إشعارات واحد · أيقونات واحدة · عائلتا تحكّم كما تقتضي القاعدة. **والخللان `D-01` و`D-02` في طبقة الرموز لا في الالتزام بها.**
- بنية وصول مبنيّة بعناية: **221 `aria-label`** · 111 `alt` · 47 `aria-pressed` · 11 `prefers-reduced-motion` · وعلى Production: صفر قفزة عناوين وصفر صورة بلا `alt` وصفر عنصر بلا اسم متاح.

### التغطية

**49/49 مسار ممثَّل** في §7 — مباشرةً أو بمرجع مكوّن مشترك واضح (الإطار المشترك يشمل الـ49 كلها). **صفر مسار مُسقَط.**

### الترحيل — بلا تخفيف

**`LOOPZ-AUD-0023` (P1) يُرحَّل بحالته ودرجته ودليله**، ولم تجد Phase 3 ما يناقضه. و**Visual Verification Gate تُرحَّل كاملة**: `V-1` و`V-2` `FAIL` مؤكَّدان، والثلاثة عشر الباقية `PENDING_VISUAL` — **وأضافت Phase 3 إليها `D-03`→`V-15` و`D-09`→`V-10`**. و`0014`…`0022` تبقى مفتوحة بحالاتها.

### الموانع

`BLOCKED_BY_TEST_ENV`: تشغيل الجولات فعلياً · حفظ الإكمال وعدم التكرار · إعادة التشغيل من الإعدادات · فروق المجاني وPlus · نماذج الإعدادات.
`PENDING_VISUAL`: التركيز · التكبير 200% · Reduced Motion · أهداف اللمس · التباين الثانوي · النصوص المقطوعة · الشارات والأيقونات بصرياً.

لا حالة `BLOCKED`.

### المطلوب من ChatGPT

أرقام `LOOPZ-AUD` لـ`D-01`…`D-10` · وقرار في `D-02` (أيُوثَّق استثناء `--text-base` أم يُعمَّم) وفي `D-08` (توثيق قرار الجولة) · ثم `VERIFIED` أو `CHANGES_REQUESTED` ومهمة Phase 4.

### إقرار

لم أعدّل كوداً · لم ألمس `main` · لم أحذف ولم أنشئ مورداً · لم أستخدم جلسة أحمد ولم أكلّفه · لم أُعِد محاولة الأسطح المتعذّرة · لا أسرار ولا بيانات مستخدمين · وكل ما يحتاج رؤية مكتوب `PENDING_VISUAL` ولم يُسمَّ `PASS`.

- صاحب الدور التالي: **ChatGPT**

## ChatGPT Review

- الحالة: `WAITING_FOR_CLAUDE`
- الوقت:
- النطاق:
- النتيجة:
- المشكلات الجديدة:
- معايير القبول:
- المطلوب من Claude:
- صاحب الدور التالي:

---

## سجل المشكلات

> نُقلت هذه الأرقام حرفياً من تعليق ChatGPT في PR رقم 19 بتاريخ 2026-09-01 01:38 UTC. **التسجيل والدرجات من ChatGPT وحده**؛ دور Claude هنا نسخٌ إلى المرجع الرسمي لا إسناد أرقام.

| ID | المرحلة | الدرجة | النطاق | الوصف | الحالة | فرع الإصلاح | دليل |
|---|---:|---|---|---|---|---|---|
| LOOPZ-AUD-0001 | 0 | P2 | البناء والاختبار | لا مشغّل ولا script اختبارات آلية | TODO | — | `package.json` — لا `test` ولا مشغّل في `devDependencies` |
| LOOPZ-AUD-0002 | 0 | P2 | البيئة | ستة متغيّرات تُقرأ في الكود وغائبة عن `.env.example`: `NEXT_PUBLIC_GOOGLE_CLIENT_ID` · `OMDB_API_KEY` · `DEEPL_API_KEY` · `GIPHY_API_KEY` · `LAB_REPO` · `LAB_CHROMIUM` | TODO | — | استعمال كلٍّ منها مثبت بملفه في `PHASE_1_INVENTORY.md` §5 |
| LOOPZ-AUD-0003 | 0 | P3 | الحزم | `eslint-config-next@16.2.12` لا يطابق `next@16.3.0` | TODO | — | `package-lock.json` |
| LOOPZ-AUD-0004 | 0 | P3 | البناء | لا `engines` ولا `packageManager` — إصدار Node غير مثبَّت | TODO | — | `package.json` |
| LOOPZ-AUD-0005 | 6 | P1-CANDIDATE | قاعدة البيانات | `watched_episodes_backup_133` (3510 صفوف) مكشوف في `public` | TODO | — | الجدول الوحيد بلا مسار في الكود ولا في الدوال — `PHASE_1_INVENTORY.md` §9.3 |
| LOOPZ-AUD-0006 | 5/6 | **P1** | البيئة | لا بيئة بيانات معزولة؛ Preview يستخدم قاعدة Production | TODO | — | لا Staging · مشروع Supabase واحد · تصوّر الحل في `PHASE_1_INVENTORY.md` §11 |
| LOOPZ-AUD-0007 | 5 | P2-CANDIDATE | الأمن | `script-src 'unsafe-inline'` في CSP | TODO | — | `next.config.ts` — والتعليق في الملف نفسه يقرّ به ويؤجّل التشديد بـnonce |
| LOOPZ-AUD-0014 | 2 | P2 | 27 صفحة محميّة | 200 + `meta refresh` بعد ثانية بدل 307 · 45 ك.ب تُشحن بلا فائدة | TODO | — | `fetch(redirect:'manual')` → 200 + `__next-page-redirect` |
| LOOPZ-AUD-0015 | 2 | P2 | `/admin/*` | للزائر: 200 وجسم فارغ بلا 404 وبلا تحويل | TODO | — | الجسم المرسوم = الترويسة وحدها |
| LOOPZ-AUD-0016 | 2 | P2 | `/movie/[id]` | عمل غير موجود يُعرض كعطل مؤقّت («try again shortly») بحالة 200 | TODO | — | `GET /movie/999999999` |
| LOOPZ-AUD-0017 | 2 | P2 | `/login` | إطار GIS عربيّ داخل صفحة `lang="en"` — و`/` ترسم الزرّ إنجليزيّاً في الجلسة نفسها | TODO | — | `iframe.title` و`textContent` |
| LOOPZ-AUD-0018 | 2 | P2 | 37 صفحة | بلا ميتاداتا، ومنها `/movie/[id]` و`/show/[id]` و`/u/[username]` — بينما `/u/[username]/stats` لديها | TODO | — | `<title>` عام على `/movie/550` |
| LOOPZ-AUD-0019 | 2 | P2-CANDIDATE | SEO | `sitemap.xml` بأربعة روابط — **معلَّق لقرار Phase 8/10، لا يُوسَّع الآن** | DEFERRED | — | `src/app/sitemap.ts` |
| LOOPZ-AUD-0020 | 2 | P3-CANDIDATE | `/movie/[id]` | فيض 25px عند 390 أثناء الهيكل — **ولا يظهر عند 768** · يحتاج إثباتاً بصريّاً بعد اكتمال الرسم | TODO | — | `scrollWidth 415 > clientWidth 390` |
| LOOPZ-AUD-0021 | 2 | P3 | `/trailers` | تبويب «For You» فارغ للزائر بلا حالة فارغة · و«Trending» يعمل | TODO | — | صفر بطاقة وصفر نصّ |
| LOOPZ-AUD-0022 | 2 | P3 | `/discover/[section]` | قسم مجهول يعيد محتوى قسم صحيح بدل 404 | TODO | — | مقارنة نصّية: `identical: true` |
| LOOPZ-AUD-0023 | 2 | **P1** | `/trailers` | **تشغيل الترايلر معطوب**: بعد الضغط يُنشأ iframe `autoplay=1&mute=1&controls=0` وبعد 11 ثانية لا تقدّم لصورة الفيديو · `pointer-events:none` · **لا زرّ صوت ولا عرض زمن**. أثبته ChatGPT بفحص مرئي مستقل، ويطابق بلاغاً سابقاً من أحمد | TODO | — | Production `f8a2b33c` · زائر · Chrome مرئي `1363×936` · `/trailers?tab=trending` |
| LOOPZ-AUD-0024 | 3 | P2 | نظام التصميم | `D-01`: سلّما زوايا متوازيان — 10 رموز بخمس قيم · `--radius-card`=`2xl`=`3xl`=14px | TODO | — | `globals.css` |
| LOOPZ-AUD-0025 | 3 | P2 | نظام التصميم | `D-02`: سلّما مقاسات نصّية متوازيان · واستثناء `--text-base: max(16px,…)` غير موثّق | TODO | — | `globals.css` |
| LOOPZ-AUD-0026 | 3 | P2 | البنية | `D-04`: 48 من 49 مساراً ترث حدّ خطأ الجذر بلا عزل محلّي | TODO | — | جدول §7 |
| LOOPZ-AUD-0027 | 3 | P3 | الهوية | `D-06`: المعرّف الداخلي القديم `meshahed-pulse` | TODO | — | `globals.css:539,741` |
| LOOPZ-AUD-0028 | 3 | P3 | الجولات | `D-07`: جولة من 8 خطوات بلا «رجوع» | TODO | — | `TourGuide.tsx` · `i18n.ts:118-121` |

### قالب المشكلة

```md
### LOOPZ-AUD-0001 — عنوان مختصر
- المرحلة:
- الدرجة: P0 / P1 / P2 / P3
- الحالة: TODO
- البيئة:
- الجهاز/المتصفح:
- الحساب/الدور:
- الصفحة أو المسار:
- خطوات إعادة المشكلة:
- النتيجة الحالية:
- النتيجة المتوقعة:
- الدليل:
- السبب المتوقع:
- تعليمات Claude:
- معيار القبول:
- فرع الإصلاح:
- Commit:
- اختبارات Claude:
- نتيجة ChatGPT:
```

---

## سجل الجلسات — Append Only

### 2026-09-01 — ChatGPT

- نقل أحمد خارج دورة الاعتماد اليومية.
- جعل ChatGPT بوابة التحقق وClaude جهة التنفيذ.
- أبقى main وProduction ثابتين حتى بوابة GO.
- اعتمد التنبيه الحدثي عبر GitHub بدلاً من فحص غير مدعوم كل 10 دقائق.

### 2026-09-01 — ChatGPT

- أنشأ فرع `docs/pre-app-audit` والملفات الأساسية.
- لم يعدل كود التطبيق أو main.
- سلّم الدور إلى Claude.

### 2026-09-01 01:18 UTC — Claude

- قرأ الملفات الأربعة بالترتيب.
- نفّذ Phase 0 — Baseline جرداً محضاً: نقطة الرجوع، الفروع، الإصدارات، Scripts، البيئات، آخر Deploy، الأدوار، الموانع، وأوامر Phase 1 المقترحة.
- شغّل بوّابتَي الجودة على نقطة الرجوع: `tsc --noEmit` و`next build` — كلاهما نظيف.
- أضاف `PHASE_0_BASELINE.md`.
- لم يعدّل كود التطبيق ولم يلمس `main` ولم ينشر شيئاً.
- وضع Phase 0 بحالة `READY_FOR_REVIEW` وسلّم الدور إلى ChatGPT.

### 2026-09-01 02:06 UTC — Claude

- نفّذ Phase 1 — Full Inventory على `64bd1d41` جرداً محضاً (13 قسماً).
- صحّح خطأ Phase 0: المسارات 76 لا 81.
- أثبت مطابقة تامّة بين المصدر وجدول البناء، وصفر استدعاء RPC مكسور.
- حسم الفرعين المؤجَّلين بالدليل: أحدهما متجاوَز والآخر مفقود فعلاً.
- لم يعدّل كود التطبيق ولم يلمس `main` ولم يحذف شيئاً ولم ينشئ أي مورد.
- وضع Phase 1 بحالة `READY_FOR_REVIEW` وسلّم الدور إلى ChatGPT.

### 2026-09-01 02:33 UTC — Claude

- أعاد تسليم Phase 1 بعد `CHANGES_REQUESTED` على Audited SHA `f8a2b33c`.
- شغّل البوّابات الأربع على الـSHA المعتمد: `npm ci` · `npm run lint` · `tsc --noEmit` · `npm run build` — كلها exit 0، مع الأزمنة.
- قرأ مسارات API الواحد والعشرين كاملة وأزال كل `UNKNOWN` من جدولها.
- فصل تصنيف البناء عن `dynamic export`، وأضاف حالة `loading/error/not-found` لكل صفحة، وحسم كل تصنيف وصول.
- أكمل ربط خريطة الميزات بالمكوّنات والأفعال والجداول وRPC والتخزين.
- صحّح ثلاثة أخطاء من تسليماته السابقة.
- لم يعدّل كود التطبيق ولم يلمس `main` ولم يحذف ولم ينشئ أي مورد.

### 2026-09-01 02:41 UTC — Claude

- إعادة التسليم الثانية لـ Phase 1: أعاد الأقسام الأربعة الساقطة (§14–§17) دون المساس بما اجتاز.
- لم يُعد تشغيل البوّابات لأن الـAudited SHA لم يتغيّر.
- سجّل أن بنود «استعادة كلمة المرور وتغييرها وتغيير البريد» لا تنطبق على المشروع (Google وحده) وتحتاج شطباً من معايير Phase 2/5.
- لم يعدّل كود التطبيق ولم يلمس `main` ولم يحذف ولم ينشئ أي مورد.

### 2026-09-01 03:02 UTC — Claude

- نفّذ Phase 2 كزائر حقيقي في المتصفّح المدمّج، بعد إثبات أن متصفّح أحمد مسجَّل دخوله فلا يصلح.
- غطّى 49/49 صفحة بحالة صريحة، وأغلق مانع Phase 0 الثالث بإثبات أن Production يشغّل الـAudited SHA.
- سجّل تسع مشكلات `C-01`…`C-09`، ثلاث منها بجذر واحد.
- أثبت الكتم الافتراضي في الترايلرات، ووسم ستّة بنود `BLOCKED_BY_SURFACE` بدل ادّعائها.
- فحص ادّعاءً بدا عطلاً فثبت أنه سليم، وسجّله.
- لم يسجّل دخولاً ولم يلمس مساراً كاتباً ولم يعدّل كوداً ولم ينشئ مورداً.

### 2026-09-01 03:12 UTC — Claude

- أعاد تسليم Phase 2: مصفوفة 49 صفّاً بالحقول · قياس 768 · إزالة «PASS جزئياً» من الترايلرات.
- سجّل `LOOPZ-AUD-0014`…`0022` في سجل المشكلات.
- أثبت أن الدليل البصري `BLOCKED` بثلاثة أسطح مجرَّبة وثلاثة أخطاء حرفية، وقدّم ثلاثة خيارات بقرار موصى به.
- لم يستخدم جلسة أحمد ولم يسجّل خروجه ولم يكلّفه بشيء.

### 2026-09-01 03:30 UTC — Claude

- صحّح مصفوفة الـ49: صفّ لكل `page.tsx` بلا دمج، والvariants إلى جدول خارج العدّ، وحذف الجدول القديم المتعارض.
- أدرج `LOOPZ-AUD-0023` (P1) بحرف بلاغ ChatGPT، ولم ينسبه لنفسه.
- صحّح قراءته الخاطئة: «الكتم الافتراضي ناجح» صار قياساً صحيحاً بخلاصة مضلِّلة، وحكم الترايلرات صار `FAIL`.
- أنشأ Visual Verification Gate بخمسة عشر بنداً، صفر منها `PASS`.
- لم يصلح كوداً ولم يلمس `main` ولم يستخدم جلسة أحمد ولم يكلّفه بشيء.

### 2026-09-01 03:50 UTC — Claude

- نفّذ Phase 3 (التصميم والهوية والمحتوى والجولات والوصول الثابت) على `f8a2b33c` بتحليل المصدر.
- أوقف ثلاثة بلاغات كاذبة بالفحص قبل رفعها.
- سجّل عشرة مرشّحات `D-01`…`D-10`، ثلاثة منها P2.
- أثبت تكافؤ القاموسين 1278/1278 بصفر نقص، وحدّد أن `0017` عند حدّ GIS لا في القاموس.
- غطّى 49/49 مساراً وربط كل حكم بصريّ ببند من Visual Verification Gate.
- رحّل `0023` والبوّابة بلا تخفيف، ولم يصلح كوداً ولم يلمس `main`.

### 2026-09-01 04:12 UTC — Claude

- استكمل Phase 3 بعد `CHANGES_REQUESTED`: جدول تدقيق الرموز والمكوّنات · خريطة الهوية من المصدر · مصفوفة المحتوى · أجوبة الجولات · دليل `D-10`.
- سجّل تسعة مرشّحات جديدة `D-11`…`D-19`، أخطرها `D-11` (أصفران وأسودان، وثيم `loopz` يخالف قراره D-454).
- أغلق سؤالاً مفتوحاً من Phase 2 بدليل المصدر: صفحة الإشعارات لا توهم بالحفظ → اقتراح إغلاق `0009`.
- صحّح خطأ التوقيت في السجل، وأقرّ بسببه: استبدال لم يطابق ولم يُتحقَّق من أثره.
- سجّل `0024`…`0028` ورحّل `0023` والبوّابة بلا تخفيف.

## قرارات أحمد

| التاريخ | القرار | النطاق | الملاحظات |
|---|---|---|---|
| 2026-09-01 | أحمد خارج دورة الاعتماد التشغيلية | كامل التدقيق | ChatGPT يراجع وClaude ينفذ |
| 2026-09-01 | العمل الذاتي مع حواجز فنية | الفروع وPreview | main وProduction بعد بوابة GO |
| 2026-09-01 | إنشاء مساحة متابعة GitHub | docs/pre-app-audit | مرجع مشترك |
