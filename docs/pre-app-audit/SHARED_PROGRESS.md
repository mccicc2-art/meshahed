# Loopz Shared Progress

ملف التنسيق الوحيد بين ChatGPT وClaude أثناء مراجعة Loopz قبل التطبيق.

## بيانات العمل

| الحقل | القيمة |
|---|---|
| المالك | أحمد — خارج الاعتمادات التشغيلية اليومية |
| المستودع | mccicc2-art/meshahed |
| فرع التوثيق | docs/pre-app-audit |
| **Audited SHA** | **`f8a2b33cd036cffd1e7a0b9bc3e5ced0e19b8bfa`** — الـbaseline التشغيلي المعتمد |
| Phase 0 SHA (تاريخي) | `64bd1d41851c0cc05f93a8444eaadd41d47f9108` |
| فرع التجميع | audit/integration |
| المرحلة الحالية | **Phase 9 — جاهزية Expo: `READY_FOR_REVIEW` (الدليل `7f0609d` 19:07:59 UTC؛ التقريرُ `PHASE_9_EXPO_READINESS.md` في الالتزام الذي يليه)** — بعد Phase 10D (D-889…D-896 `VERIFIED`، آخرها `e254b2f` 18:24:06) · ⚠️ الأدوار كما هي: Claude مراجعٌ ومنفّذ بتفويض المالك؛ **القراراتُ السبعة (0076 `WONT_FIX` · 0073 `ACCEPTED_TRADEOFF` · لا إطالةَ `revalidate` · لا `append_to_response` · جذرُ العناوين العربية مؤجَّل · D-889…D-896 تبقى · «الثقل» ⇢ `0035`/`0036`) اتُّخذت بتفويضه «اتخذ الأفضل» وقابلةٌ للعكس بسطرٍ منه (§٠ من تقرير Phase 9) |
| القرار الحالي | لم يصدر — البوّابات النهائية المفتوحة: البوّابة البصرية · Web Vitals صالحة · **`LOOPZ-AUD-0040` P1 مفتوحة** — **(`0023` أُغلقت `VERIFIED/RESOLVED` 09:05:14 UTC بفحص iPhone ولم تعد بوّابة)** |
| آخر تحديث | **2026-09-02 · 19:07:59 UTC** — `7f0609d` على فرع التدقيق (`evidence/P9-classify-e254b2f.py` + `evidence/P9-lib-classification-e254b2f.txt`)؛ **التزامُ هذا السطر (`PHASE_9_EXPO_READINESS.md` + السجلّ + صفّ `0008`) يليه ووقتُه في `git log`** · قبله: `a353427` 18:30:55 (D-896) · على `main`: `e254b2f` 18:24:06 (D-896 `[deploy]`) — **لا التزامَ جديداً على `main` في هذه المرحلة (توثيقٌ فقط)** |
| صاحب الدور التالي | **المالك** — اعتمادُ تصميم طبقة الـAPI (§٤ من Phase 9: Route Handlers `/api/v1` فوق `core` مشترك؛ الخياران B وC مرفوضان بأسبابهما) وقراراتُ §٧/§٨-ب (مستودعٌ واحد بـnpm workspaces · نطاقُ v1 · المعرّفات · مزوّدُ الأعطال) · **وقرارُ `0006` (مشروعُ Supabase ثانٍ للاختبار) الذي يعلّق `0040` والـGO** · ثمّ **Claude**: الخطوةُ 1 من §٩ (`src/core/` + قاعدةُ ESLint، بلا تغيير سلوك) لا تحتاج الـGO ولا تمسّ الإنتاج — تُنفَّذ بكلمته |
| 🆕 Delta Queue | **أُنشئ أدناه** — **32 التزاماً على `main` بعد `f8a2b33c`** · 30 ملفاً · **صفر ملفٍّ في `supabase/`** · 16 موسوماً `[deploy]` |
| المطلوب الآن | **قرارُ المالك على §٤ و§٧ و§٨-ب من `PHASE_9_EXPO_READINESS.md`** · **قرارُ `0006`** · قرارُه على D-889…D-896 وعلى القرارات السبعة (اعتُمدت بالتفويض؛ يعكس ما شاء) · **تحديثُ ملفّات المشروع مؤجَّلٌ بأمره** · قياسُ «الثقل» في المقدّمة على جهازه (DevTools ⇢ Performance، 10 ثوانٍ) |
| 🔴 تنبيه للمراجع | **تحذيرك من كفاية فحص الـGET تحقّق ميدانيّاً** — سحبُ الصلاحيات كسر مساراً حيّاً: ≈15,420 خطأ/24h (§9.7). عولج في `main` بحارس تطبيقٍ لا بمنحةٍ ثانية؛ **Audited SHA وفرعُ التدقيق لم يُمسّا** |

## الأدوار

### ChatGPT — Reviewer and Technical Gate

- يقرأ ويفحص ويشغل اختبارات غير مغيرة.
- يسجل المشكلات ومعايير القبول.
- يراجع Diff ونتائج الاختبار.
- يضع `VERIFIED` أو `CHANGES_REQUESTED`.
- بعد النجاح يجيز الدمج إلى `audit/integration`.
- عند اكتمال جميع البوابات يصدر قرار `GO / GO WITH CONDITIONS / NO-GO`.
- لا يكتب كود التطبيق أو ينفذ الإصلاحات.

### Claude — Executor

- ينشئ فرعاً مستقلاً لكل مجموعة إصلاحات.
- يعدل الكود والاختبارات والتوثيق وفق المهمة المسجلة.
- يشغل Build والاختبارات ويقدم الأدلة وCommit SHA.
- يضع `READY_FOR_REVIEW`.
- بعد `VERIFIED` يدمج إلى `audit/integration` دون الرجوع إلى أحمد.
- لا يضع `VERIFIED` لنفسه.
- لا يغير `main` أو Production أثناء مراحل التدقيق.

## الاستقلالية وحدود المخاطر

لا تحتاج موافقة أحمد في:

- الفحص والاختبار.
- إنشاء الفروع وPull Requests.
- إصلاحات الكود والتصميم والأداء والأمان.
- حذف كود ميت مثبت على فرع العمل.
- Preview deployments.
- دمج المهام المتحققة في `audit/integration`.
- تغييرات Staging الإضافية والقابلة للرجوع بعد وجود خطة اختبار.

تتوقف المهمة فقط عند:

- حذف بيانات Production.
- Migration تدميرية أو غير قابلة للرجوع على Production.
- كشف أسرار أو حادث أمني جارٍ.
- هدف غير محدد قد يؤثر في بيانات المستخدمين.
- حاجة لصلاحية غير متاحة.

عند اكتمال جميع البوابات، يعتمد ChatGPT القرار التقني النهائي وفق `AUDIT_PLAN.md` دون إدخال أحمد في دورة الإصلاح اليومية.

## بروتوكول التبادل

1. صاحب الدور يقرأ أحدث نسخة قبل التحديث.
2. Claude يحدث قسمه كل 10 دقائق كحد أقصى أثناء العمل النشط أو عند إنجاز مهم.
3. بعد كل تسليم يغير `صاحب الدور التالي`.
4. التنبيه إلى ChatGPT يتم عبر Commit أو Comment في Pull Request التنسيق.
5. لا يعدل الطرفان الملف في الوقت نفسه.
6. صيغ Commit:
   - `audit(coord): claude check-in YYYY-MM-DD HH:MM UTC`
   - `audit(coord): chatgpt review YYYY-MM-DD HH:MM UTC`
7. إذا طال العمل يسجل: ما تم، الجاري، المانع، والتقدير المتبقي.
8. يمنع وضع Secrets أو Tokens أو بيانات مستخدمين أو Logs حساسة.
9. عند التعارض: توقف، اجلب الأحدث، وأعد تطبيق آخر تحديث فقط.
10. الفحص المجدول كل 10 دقائق غير مدعوم؛ التنبيه الحدثي في GitHub هو القناة الأساسية.

## حالات المهام

| الحالة | من يضعها | معناها |
|---|---|---|
| TODO | ChatGPT | مهمة معرفة ولم تبدأ |
| IN_PROGRESS | Claude | بدأ التنفيذ |
| BLOCKED | أي طرف | مانع موثق |
| READY_FOR_REVIEW | Claude | جاهز للمراجعة |
| CHANGES_REQUESTED | ChatGPT | لم يتحقق القبول |
| VERIFIED | ChatGPT | نجح التحقق |
| INTEGRATED | Claude بعد VERIFIED | دُمج في audit/integration |
| DEFERRED | ChatGPT | مؤجل بسبب موثق |

## الدور الحالي

> هذا القسم كان مكتوباً بأوامر Phase 0 حتى 04:42 UTC. **حُدِّث ليطابق المرحلة الجارية** — والملف مرجع حالة لا أرشيف.

### المطلوب من Claude الآن

1. **توجيهُ المالك المعتمَد (00:55 UTC، عبر المراجع): Phase 10 مباشرةً على `main`** — **Phase 10A `CANCELLED_BY_OWNER`** (لا مشروعَ ثانياً ولا Branch ولا Preview خاصّ) · **البدءُ من رأس `main` الحاليّ لا من الـAudited SHA** · **وموافقةُ أحمد تشمل تعديلَ كود التطبيق على `main` والنشرَ المعتاد بعد البوّابات.**
2. **بوّاباتُ العمل على `main`** (نصُّ التوجيه): نقطةُ رجوعٍ موثَّقةٌ قبل كلِّ حزمة · Commit صغيرٌ مستقلٌّ لكلِّ إصلاح · `tsc` + `eslint` + `build` قبل النشر · Smoke test بعد النشر وBefore/After · **Regression ⇢ rollback فوراً** · **لا حذفَ بيانات · لا Migration تدميريّة · لا تغييرَ RLS/Auth/صلاحيّات · لا أسرار** · **أيُّ إصلاحٍ يحتاج SQL/Migration أو كتابةً جماعيّة يُرفع قراراً مستقلّاً** · الاختباراتُ الكاتبةُ بحسابِ اختبارٍ وبياناتٍ اصطناعيّة فقط · لا اختباراتِ تحميل.
3. **التسلسل:** **10B — `PHASE_10_PERFORMANCE_BASELINE.md` على النسخة الحيّة (قياسٌ بلا تعديل؛ iPhone PWA أوّلاً؛ والمتعذّرُ `BLOCKED_BY_DEVICE`)** → **10C — الترايلرات `0023`** (مشغّلٌ واحدٌ حيّ · السابقُ يقف والتالي يبدأ · Before/After) → **ثمّ حزمةٌ واحدةٌ في كلِّ مرّة**: API/الشبكة → React → Supabase (آمنٌ غيرُ تخريبيّ) → الصور/الفيديو → Cache/Prefetch → iOS PWA → Guardrails.
4. ⚠️ **انحرافُ ترتيبٍ مُعلَن**: **شُحنت أوّلُ شريحةٍ من 10C (`D-879` · `921abd7` · غلافُ الترايلر) قبل 10B** — **لأنّ توجيهَ 00:55 وصل بين قرارِ المالك في المحادثة وقراءتي للـPR.** **القياسُ الذي يخصّها (الغلاف: قبل/بعد) مسجَّلٌ في `Claude Check-in`، أمّا Baseline الترايلرات الشامل (زمنُ البدء · أوّلُ إطار · عددُ المشغّلات) فسيُقاس في 10B على `921abd7` ويُعلَن أنّه بعد هذه الشريحة لا قبلها.** **ولا يُعاد لفُّ الالتزام**: العطلُ مقيسٌ والعلاجُ محدودٌ بملفٍّ واحد.
5. **التالي فوراً: 10B.** **حسابُ اختبارٍ مخصَّصٌ للاختبارات الكاتبة يقرّره المالك** (الدخولُ Google وحدَه).
6. Audited SHA `f8a2b33c` يبقى مرجعَ Phase 0–8 التاريخيَّ؛ **والعملُ من رأس `main`.**

## Claude Check-in

> ⚠️ **هذا القسمُ يصف الحالةَ النافذةَ وحدَها ويُحدَّث معها؛ والتاريخُ في «سجل الجلسات — Append Only» أسفل الملفّ ولا يُمسّ.**

- الحالة: **Phase 10D — Baseline `VERIFIED` (10:08:02) · D-884 (`0069`) `VERIFIED/RESOLVED` (14:05:24) · تشخيصُ `0074` §٨ `VERIFIED` (مراجعةٌ ذاتيّة بقرار المالك) · D-889 (عمل `8cf2d80`) + D-890 (فيلم `48cc29e`) + D-891 (رفُّ المرتبطات `4349bbe`) = إصلاحُ `0074` داخل حدود 10D `VERIFIED` (§٩–§١١) — يُقترح `0074 = RESOLVED (10D scope)` · **D-892 (`94c9a71`) = `0078` `VERIFIED/RESOLVED` (§١٢) + عيبٌ مرئيٌّ مكتشَف يُقترح `0080`** · **D-893 (`254ce48`) = `0077` `VERIFIED/RESOLVED` (§١٣)** · **D-894 (`f428584`) = `0081` جديد `VERIFIED` بحدود (§١٤)** · **مرحلةُ الصور: `0075`/`0079` `NOT_REPRODUCED/LIKELY_TOOLING` (§١٥) · D-895 (`54bb5fd`) = `0082` `VERIFIED/RESOLVED` (§١٦)** · **D-896 (`e254b2f`) = ترايلرُ العربيّة برحلة `VERIFIED` (§١٧) · `0076` مقيس ≈ 2 KB ⇢ يُقترح `WONT_FIX`** · `0070` `BLOCKED_BY_TOOLING_OR_DEVICE`** — **الأرقامُ المعتمَدة:** `0073` P2 SW +120 ms · `0074` P1 أوّلُ فتحةِ عمل · `0075` P3 شعارُ الرأس · `0076` P2 SVG مكرّر · `0077` P2 prefetch «الكل»/التذييل · `0078` P2 `/api/season → 401` للزائر · `0079` P3 خلفيّةُ العمل بمقاسَين.
- ⚠️ **تغييرُ الأدوار (بقرار المالك، بعد 14:37:52 UTC — وقتُه غيرُ مسجَّل):** «المراجع ما يقدر يوصل، أحتاجك تقوم بعمله: تراجع وتوجّه وتستمرّ على الخطة.» ⇢ Claude يراجع تسليماتِه بالمعايير نفسِها (أدلّةٌ على SHA ملتزَم بشجرةٍ نظيفة · أوقاتٌ من `git log` · إصلاحٌ واحدٌ في كلِّ مرّة · حدودُ 10D) **ويكتب الحكمَ قبل التنفيذ ويسجّل ما يرفضه من عمله**. الحكمُ على §٨: `VERIFIED` + ٣ تصحيحاتٍ توثيقيّة (نُفِّذت في التزام §٩) — النصُّ الكامل في سجلّ هذه الجولة.
- ✅ **D-896 — ترايلرُ العربيّة برحلةٍ واحدة** (`e254b2f` 18:24:06 [deploy] · نقطةُ الرجوع `54bb5fd` · `/api/build == e254b2f` 18:25:59 · البوّاباتُ بشجرةٍ نظيفة): `readVideosOnce` — بالعربيّة نداءٌ واحد بـ`include_video_language=ar,en` وفرزٌ بـ`iso_639_1` (العربيّ أوّلاً)؛ بالإنجليزيّة كما كان؛ `getTrailer` و`getTrailerKeys` عليه · **trailer−hero بالعربيّة +250…+360 ⇢ 0 (6/6) ومفتاحُ الفيديو موجود 6/6** · مفتاحُ كاشٍ جديد للنداء العربيّ (مُعلَن) · **`0076` قِيس ولم يُلمس: التكرارُ ≈ 2 KB على السلك (gzip 40 ⇢ 38) ⇢ يُقترح `WONT_FIX`** · الدليل `evidence/D-896-gates-e254b2f.txt` · التقرير §١٧.
- ✅ **D-895 — `0082` (جديد، صور)** (`54bb5fd` 18:03:47 [deploy] · نقطةُ الرجوع `f428584` · `/api/build == 54bb5fd` 18:05:33 · البوّاباتُ بشجرةٍ نظيفة): `POSTER_INTRINSIC.w342 = 176×264` + في `imageLoader` `w342 && width ≤ 256 ⇢ /i/w185/` (نزولاً فقط) — **srcset صار `w185 1x, w342 2x`؛ متصفّحُ المالك (DPR 1): `/i/w185/` فقط 13–20 KB بدل 40–58؛ اللوحُ (DPR 1.5): `w342` 56/56 بلا تغيير** · الثمن: مدخلا خبيئةٍ لكلِّ ملصق؛ صندوقٌ > 200 px على DPR 1 يتكبّر ≤ 1.3× (ReportView 240) · **تصحيحان لـ§١٥:** الوثيقةُ تصل `br` (استنتاجُ «بلا ضغط» خاطئ — مدخلُ التنقّل عبر SW) · أزمنةُ متصفّح المالك عبر الأتمتة = تبويبٌ خلفيّ مخنوق (8,855 ms مقابل 541 للوح على الجهاز نفسِه) ⇢ للجرد لا للزمن؛ **ثقلُه في المقدّمة ما زال غيرَ مقيس**. الدليل `evidence/D-895-gates-54bb5fd.txt` · التقرير §١٦.
- ✅ **D-894 — `0081` (جديد)** (`7122b21` 17:19:17 · `a909426` 17:20:11 · `f428584` 17:21:03 [deploy] · نقطةُ الرجوع `254ce48` · `/api/build == f428584` 17:22:57 · البوّاباتُ بشجرةٍ نظيفة): **الاكتشاف من بلاغ المالك «ثقل وبطء» وقياسٍ من متصفّحه** (4g · rtt 200): في الواجهة العربيّة يُسأل ويكي‑بيانات عن العنوان **بعد** TMDB وقبل الترويسة بمهلة 1.5 s والفشلُ لا يُخبَّأ ⇢ `/show/60059` ترويسة 2.0 ثمّ 1.7 ث · **قرارُ المالك: الخيار أ** — `prefetchWorkTitle` مع الموجة + `failedUntil` عشر دقائق (كـ`lastGood`)؛ `displayWorkTitle(..., pending?)` اختياريّ · **السقفُ: أسوأ فتحة 1,929 ⇢ 1,518 (max بدل المجموع)؛ الوسيطُ بلا فارقٍ مقيس (TMDB البارد يسيطر)؛ ذاكرةُ الفشل بالبناء لا بالقياس** · العناوينُ العربيّة ما زالت تُحلّ ✅ · **النقطةُ لا تُقاس من اللوح (CSP) ولا الحاوية** · الدليل `evidence/D-894-gates-f428584.txt` · التقرير §١٤. **لا تغييرَ في المهلة/الخبيئة/الاستعلام.**
- ✅ **D-893 — `0077`** (`f9fa8b4` 16:45:01 · `254ce48` 16:49:24 [deploy] · نقطةُ الرجوع `94c9a71` · `/api/build == 254ce48` 16:50:57 · البوّاباتُ بشجرةٍ نظيفة على كلٍّ): بابا كلِّ رفّ (العنوان و«الكلّ») في `PosterRail`/`RankedRail`/`CountdownRail` ورابطا التذييل في `LandingHero` بـ`prefetch={false}` كالبطاقات · **قياسٌ بعد 1/2 أثبت أنّ مصدرَ طلبات Discover الستّ `RankedRail` فجاء 2/2** · **`/news`: 8 ⇢ 2 طلبات RSC مسبقة (المتبقّي `/search` في الرأس — خارج البند) · `/`: 4 ⇢ 0** · smoke: «الكلّ» يتنقّل ناعماً (60 بطاقة) ✅ · قرارُ التنفيذ اتُّخذ هنا بعد «نفّذ الصحّ» من المالك. الدليل `evidence/D-893-gates-254ce48.txt` · التقرير §١٣.
- ✅ **D-892 — `0078`** (`b1c2431` 16:07:29 · `6226e91` 16:08:24 · `94c9a71` 16:09:16 [deploy] · نقطةُ الرجوع `4349bbe` · `/api/build == 94c9a71` 16:11:29 · البوّاباتُ بشجرةٍ نظيفة): **التشخيص كشف عيباً مرئيّاً**: الزائرُ يطلب `/api/season` عند الإقلاع ⇢ 401 ⇢ **يرى «Couldn't load this show right now» على كلِّ صفحة مسلسل** (يُقترح `LOOPZ-AUD-0080` P1) · **قرارُ المالك: الخيار ٢** (لا طلبَ للزائر + سطر «سجّل الدخول لتتبّع الحلقات»؛ فتحُ الحلقات للزائر مرفوضٌ الآن) · `EpisodeTracker.guest?` اختياريّة (D-152) و401 يُعامَل زائراً في كلِّ حال · `getUser()` في موجة الصفحة (مخبّأ) · **Before/After (زائر، اللوحُ ظاهر): طلبات 1×401 ⇢ 0 (حتى بعد نقر موسم) · الرسالةُ الكاذبة ⇢ غائبة · السطرُ الصادق ظاهر en/ar** · **غيرُ مقيس: مسارُ العضو (شيفرتُه لم تتغيّر؛ يؤكّده المالك بجلسته)** · الدليل `evidence/D-892-gates-94c9a71.txt` · التقرير §١٢. **تطبيقٌ خالص؛ `/api/season` لم يتغيّر.**
- ✅ **D-891 — `0074` ذيلُ الاكتمال** (`4349bbe` 15:48:21 [deploy] · نقطةُ الرجوع `48cc29e` · `/api/build == 4349bbe` 15:50:11 · البوّاباتُ بشجرةٍ نظيفة): `relatedTitles` تسأل `/recommendations` و`/similar` **معاً** (كانت حلقةً متسلسلة؛ الرفُّ آخرُ قطعةٍ في البثّ في 6/6). **related−hero +615…+699 ⇢ +195…+442 (وسيط 296) · end−hero ⇢ +297** · الترتيبُ والفشلُ الفرديُّ محفوظان · smoke 9/9 + 2/2 ar ✅ · **الأثرُ المجمَّع لـ0074 (وسيط، نسبةً إلى الترويسة): cast +313⇢0 · ratings +548⇢+249 · end +670⇢+297؛ الاكتمالُ المطلق 1,214 ⇢ ≈ 1,040 (887–1,090 بلا صفوف الإقلاع البارد)** · ملاحظةٌ ثابتة: أوّلُ 3–4 طلباتٍ بعد كلِّ نشرٍ بأوّل بايتٍ مرتفع (إقلاعُ الدالّة) — تُقارَن الأرقامُ نسبةً إلى hero. الدليل `evidence/D-891-gates-4349bbe.txt` · التقرير §١١.
- ✅ **D-890 — `0074` صفحةُ الفيلم** (`48cc29e` 15:35:10 [deploy] · نقطةُ الرجوع `8cf2d80` · `/api/build == 48cc29e` 15:36:55 · البوّاباتُ بشجرةٍ نظيفة): وعدا `getCredits`/`getTrailer` قبل الموجة وتمريرُهما إلى `CastRail`/`MovieTrailerSection`؛ لا وعدَ لـIMDb (في التفاصيل أصلاً). **Before على `8cf2d80` قبل التعديل مباشرة:** cast−hero +390…+553 ⇢ **0…+32** (7/7) · trailer(en)−hero +265…+374 ⇢ 0…+32 · ratings−hero كما هو (+234…+326) · end−hero بلا تغييرٍ مقيس (+554…+715 ⇢ +569…+837) · ar trailer−hero +500…+640 ⇢ 0…+363 · warm ثابت · smoke ✅ · **وحُسم تحفّظُ D-889: hero−fb 333–498 مقابل 361 قبلُ ⇢ لا تباطؤ.** الدليل `evidence/D-890-gates-48cc29e.txt` · التقرير §١٠.
- ✅ **D-889 — `0074`** (`5e13806` 15:07:02 · `8cf2d80` 15:09:57 [deploy] · نقطةُ الرجوع `ac78296` · `/api/build == 8cf2d80` 15:13:16 · البوّاباتُ على `8cf2d80` بشجرةٍ نظيفة): **وعودُ `getCredits`/`getTrailer`/`tvImdbId` تُنشأ قبل `await Promise.all` في صفحة العمل (بـ`.catch`) وتُمرَّر إلى `CastRail`/`TrailerSection`/`HeroRatings` بخصائصَ اختياريّة (D-152)؛ `imdbOverride` يبقى أوّلاً؛ صفحةُ الفيلم لم تُمسّ.** Before/After (النسبةُ إلى hero، وسيط): **cast−hero +313 ⇢ 0 (12/12) · trailer(en)−hero +313 ⇢ 0 · ratings−hero +548 ⇢ +249 · end−hero +670 ⇢ +513 (جزئيّ؛ الذيلُ رفُّ المرتبطات) · ar trailer−hero +630…+720 ⇢ +250…+360 · warm ثابت** · ⚠️ hero المطلق 506 ⇢ 650 **غيرُ محسوم** (داخل مدى الساعة + صفّان بإقلاعٍ بارد بعد النشر؛ قد يكون تزاحمَ ٣ رحلاتٍ متوازية — يُعاد قياسُه) · معيارُ الرجوع (cast−hero < 100) مستوفى · smoke ✅ (عاديّ · بلا ترايلر/IMDb · أنمي · معرّفٌ باطل · عربيّة) · الدليل `evidence/D-889-gates-8cf2d80.txt` · التقرير §٩. **لا كاش/مفاتيح/SQL/صور/Supabase؛ عددُ الرحلات كما هو.**
- 🔎 **`0074` — تشخيصٌ (§٨، `main @ ac78296`، 14:17–14:24 UTC، زائر، en ثمّ ar):** RSC لعملٍ بارد: أوّلُ بايت (هيكل `loading.tsx`) 126–171 ms · **الترويسة/المواسم 485–884 (وسيط 506)** = موجةُ `getTv` (`/tv/{id}` MISS ∥ providers ∥ Supabase) · **cast/trailer(en) 754–1,383 (وسيط 814 = hero + 313)** = `/credits` و`/videos` **تبدآن بعد الموجة** · **ratings 1,069–1,432 (= cast + 235)** = `/external_ids` ثمّ OMDb متسلسلَين · **اكتمال 1,070–2,088 (وسيط 1,214)** بذيلِ `/recommendations` ثمّ `/similar` (`LIKELY`) · **warm ×3 على 4 أعمال: 143 / 200 / 225 / 232 / 260** · **بالعربيّة: الترايلر رحلتان (`ar-SA` ثمّ `en-US`) ⇢ 1,105–1,511 = آخرُ ما يصل** · بروكسي TMDB من `bom1`: `/api/search` جديدة 469–1,009 ms، مكرّرة 152–312، `/api/build` 135 · **الكاش بلا تغيير:** المفتاح = العنوان الكامل بلغته (مدخلان لكلِّ عمل) · 3600 s TMDB · 86400 OMDb · 604,800 ويكي‑بيانات · `inFlight` يمنع التكرارَ المتزامن · `withPoster` قد يضيف `/tv/{id}?en` · 8–11 رحلة خارجيّة على 4 مستوياتٍ متسلسلة · **تصحيحُ الصياغة:** «1.1–1.7 s» = اكتمالُ البثّ؛ المحتوى الأوّل 0.5–0.9 s · **الخيارُ الواحد المقترَح:** إطلاقُ نداءات الحواجز مع الموجة (ملفٌّ واحد) — Before/After ومخاطرُ ورجوعٌ في §٨-هـ · **غيرُ مقيس:** الطلاء · صفحةُ الفيلم · ويكي‑بيانات معزولاً · الشبكةُ من الرياض.
- ℹ️ **إفصاح — رأسُ `main` = `ac78296`:** بعد D-884 شُحنت أربعُ حزمٍ **يوجّهها المالكُ خارجَ نطاق التدقيق** (git log UTC): **D-885** `16092e2`·`0e8a51d`·`fd02ead` [deploy] 12:19:37–12:21:18 (تنبيهُ الحساب المكرّر في `/welcome`: بريدُ الدخول + «بدّل الحساب» = نموذجُ `/auth/signout`) · **D-886** `3202004`·`055d9ae`·`b8e012d` [deploy] 12:29:25–12:31:19 («تصفَّح أوّلاً» إلى `/news` تحت زرّ Google، يظهر في وضع standalone فقط) · **D-887** `f0c1351`·`78a0603`·`7574b70` [deploy] 12:37:40–12:39:45 (سلّمُ التقدّم `--progress-1/2/3` من لون الثيم؛ خمسةُ أشرطةٍ تقرأ `--gradient-progress-x`) · **D-888** `ac78296` [deploy] 12:44:56 (`TrailerPlaybackController.unlockSound()` + `TrailerCardMedia`: أوّلُ لمسةٍ تفكّ الصوتَ حين النيّةُ مفتوحة والمشغّلُ صامتٌ بقرار WebKit — **يمسّ ملفَّي D-882؛ أثرُه على iPhone غيرُ مقيس، والمالكُ يختبره على iPhone 17 Plus**). كلُّها مرّت بالبوّابات الثلاث بشجرةٍ نظيفة و`/api/build == SHA`، **لا SQL · لا كاش · لا صور · لا Supabase**، ولا تمسّ صفحةَ العمل/`tmdb.ts`. ليست حزمَ تدقيقٍ ولا تُراجَع هنا إلا بطلبك.
- ✅ **D-884 — `0069`** — **`VERIFIED/RESOLVED` بتعليق المراجع 14:05:24 UTC** (قبِل رابطَ `?feed=all` بدل Server Action) — (`49a26e3` · `bf15af9` · `6f248f0` [deploy] · نقطةُ الرجوع `d9767dd` · `/api/build == 6f248f0` 11:12:41): **خطُّ النشاط 20 صفّاً بعد الترشيح والفرز + «عرض N منشوراً آخر» رابطاً إلى `?feed=all`** (`scroll={false}` · `prefetch={false}` · صفُّ D-535) — **`/people` 1,067 ⇢ 492 KB مفكوكةً (−54%، الهدف < 500 محقَّق) · السلك 49 ⇢ 31 KB · markup 590 ⇢ 265 · RSC 458 ⇢ 212 · SVG 346 ⇢ 122 · `<article>` 65 ⇢ 20** · TTFB 161–232 ⇢ 116–122 · DCL `PARTIAL_COLD` (مخفيّ) 896–1,967 ⇢ 681–943 · **smoke ✅** (المزيد ناعمٌ والتمريرُ ثابت · الرجوعُ 20 صفّاً · D-522 من الصفّ ١٣ و٤١ · نقاش · `/login` يُرسم) · **انحرافٌ مُعلَن:** رابطٌ لا إجراءُ خادم — الحالةُ في العنوان (D-051/D-054) فلا جزيرةَ ولا صفَّ نداءاتٍ ثانياً · الأدلّة `evidence/D-884-gates-6f248f0.txt` · التقرير §٧. **لا SQL · لا كاش · لا صور · لا بياناتِ أعضاء.**
- (سابقاً) الحالة: تشخيصٌ بلا إصلاح · `READY_FOR_REVIEW` على تقرير الجولة ٣ `09b31dd` (مقبولٌ فنيّاً بتعليق المراجع 10:01:24 UTC؛ المتبقّي كان مزامنةَ هذا السجلّ فقط — وهي هذا الالتزام) · الجولتان ١ و٢ `CHANGES_REQUESTED` مقفلتان · المرشَّحات (أ)–(و) بلا أرقامٍ حتى الاعتماد النهائيّ · لا تعديلَ `0069` قبل الاعتماد** (`PHASE_10D_API_NETWORK.md` منقّحٌ في `09b31dd` 10:00:17 UTC؛ الجولة ٢ `5a662d6` 09:56:03) **بعد `CHANGES_REQUESTED` 09:50:53 و09:51:52 و09:58:10** — **نقاطُ 09:58:10 الأربع:** (١) السجلُّ كان قد نُسِّق في `61e625f` 09:57:52 (تقاطعٌ زمنيّ) ويُنسَّق هنا ثانيةً · (٢) §5: «معزول» ⇢ «مرجَّحٌ غيرُ محسومٍ على جهاز» · (٣) §3: «DCL البارد» ⇢ `DCL PARTIAL_COLD` وبوّابةُ cold الحقيقيّة `BLOCKED_BY_TOOLING` · (٤) §2-ب: «مستمرّة» ⇢ «قائمةٌ وقتَ المغادرة/ملغاةٌ بها»، والاستمرارُ بعد اكتمال المغادرة غيرُ مقيس. — **نقاطُ المراجع الستّ:** (١) «cold» صارت `PARTIAL_COLD / HTTP_CACHE_WARM` في المنهج والجدول، وبوّابةُ cold الكاملة `BLOCKED_BY_TOOLING`، وبايتاتُ السلك لا تُربط بـDCL/Load · (٢) «ما يستمرّ بعد المغادرة» قيس بانتقالٍ فعليّ لكلِّ مسار (§٢-ب) · (٣) tap→visual: الطلاءُ `BLOCKED_BY_TOOLING` صراحةً (rAF معلَّق حتى وهو ظاهر)، والبديلُ tap→DOM 162 ms و tap→URL 160 ms و tap→RSC 1,342 ms، وتبويبٌ 2 ms/558 ms (§٢-ج) · (٤) `0070` = `LIKELY_CAUSE / BLOCKED_BY_TOOLING_OR_DEVICE` والدليلُ المؤكَّد وحدَه مُبقى · (٥) الحالةُ النافذة هنا · (٦) `/news` = Discover مكتوبةٌ في رأس التقرير. **لا تعديلَ `0069` بدأ ولا مرشَّحَ رُقِّم.** — **وقبله Phase 10C مكتملة**: D-881/D-883 `VERIFIED` (08:47:59) · D-882 `VERIFIED` (09:05:14 · بفحص iPhone) · `LOOPZ-AUD-0023` `VERIFIED/RESOLVED` · البندان ٢ و٣ `CANCELLED_BY_OWNER` · **10C = مكتملة بتعليق المراجع 09:09:53 UTC.**
- 📏 **10D — المنهجُ بحدوده المكتوبة:** لوحُ Claude المضمَّن (Chromium 148 · Windows · زائر · 1280×720) · **`PARTIAL_COLD / HTTP_CACHE_WARM`** (لا «cold») = `unregister` + مسحُ CacheStorage قبل التنقّل — **كاشُ HTTP لا يُمسح من الصفحة (21–26 من 25–29 مورداً منه)، وSW يسجّل نفسَه ويستولي أثناءَ التحميل؛ الوثيقةُ وحدَها من الشبكة دائماً، وبوّابةُ cold الكاملة `BLOCKED_BY_TOOLING`** · «warm» = التنقّلُ الثاني بـSW مسيطر · بايتاتُ السلك للزيارة الأولى بإعادةِ جلبٍ قسريّة (`cache:'reload'`) من صفحةٍ غيرِ مسيطَرٍ عليها (`zero: 0`) — **حجمٌ لا رحلةُ تنقّل، ولا يُربط بـDCL/Load** · **لا LCP/FCP/أوّلَ إطار (`BLOCKED_BY_TOOLING`)** · **قياسٌ شاذٌّ واحد مُعلَن** (`/` `PARTIAL_COLD` ٣: `requestStart` 17 s في اللوح، TTFB نفسُه 111 ms).
- 📊 **الأرقام (وسيط ٣ قياسات):** TTFB الوثيقة `PARTIAL_COLD` (من الشبكة دائماً) `/` 163 · `/news` 166 · `/people` 167 · `/show` 176 ms — **warm (SW) 331 · 298 · 284 · 295 ms (أبطأ بـ ≈ +120 ms وسيطاً، حتى +550)** · الطلبات 25–29 كلُّها `loopztv.com` · **لا طلبَ يبدأ بعد `load+1s`، وصفر URL متطابقٍ حرفيّاً (المكرّراتُ بمعاملاتٍ مختلفة في §٢-ب)** · سلكُ الزيارة الأولى 333–440 KB (JS ≈ 300–330 KB منها) · DCL/Load المسجَّلة كلُّها بكاش HTTP دافئ (تصلح للمقارنة بين المسارات لا كرقمٍ مطلق) · **`/people` وحدَها 1,067 KB مفكوكةً (49 KB سلكاً)** · أطولُ طلبين في كلِّ صفحةٍ `PARTIAL_COLD` = شعارُ الرأس عبر `/_next/image` + `loopz-wordmark.png` (108–278 ms).
- 🔎 **`0069` معزول بالعدّ لا بالنسخ:** الوثيقة 1,060 KB = markup 590 (منها **477 KB قبل أوّل `<h2>` = خطُّ النشاط: 65 `<article>` × ≈ 7 KB**) + **RSC flight 458** (الشجرةُ مرّةً ثانية) · `class` 133 KB · **346 `<svg>` / 20 فريدة ≈ 109 KB** · 1,072 عنوانَ صورةٍ ≈ 200 حرفاً (srcSet ×3) · وبقرار D-276 تُرسم اللوحاتُ الثلاث في فتحةٍ واحدة. **الإصلاحُ المرشَّح:** سقفٌ للرسم الخادميّ (≈ 20 منشوراً) + «المزيد» بإجراءِ خادم — الحدُّ في التطبيق لا في SQL · المقياس `decodedBodySize` (< 500 KB) وDCL `PARTIAL_COLD` (cold الحقيقيّ `BLOCKED_BY_TOOLING`) · الخطرُ المعلَن: استعادةُ التمرير (D-522).
- 🔎 **`0070` — `LIKELY_CAUSE / BLOCKED_BY_TOOLING_OR_DEVICE`:** **ليس SW** (RSC ليست `navigate` — سطرٌ صريحٌ في `sw.js`) **ولا build-id**: بعد الترطيب نقرةٌ حقيقيّة على ملصقٍ في `/news` ⇢ **تنقّلٌ ناعم مُثبَت** (علامةُ `window` باقية · `navigation.name` ما زال `/news` · طلبٌ واحد `?_rsc=` `200 text/x-component` 8 KB). **المرجَّح: النقرُ قبل ترطيب حاجز Suspense المبثوث** (المؤكَّد وحدَه: بعد الترطيب = ناعم؛ نقرةُ 10B = وثيقة) — في اللوح المخفيّ بعد 19 s: 99 رابطَ `/show/` صفرٌ مرطَّب و10 حواجزَ معلّقة والجذرُ مرطَّب؛ بعد الإظهار 99/99. طولُ النافذة على جهازٍ حقيقيّ **غيرُ مقيس** (`BLOCKED_BY_TOOLING`). **والأثقلُ على المستخدم: `GET /show/<id>` RSC أوّلَ مرّة 1,093–1,692 ms (٤ أعمال · `x-vercel-cache: MISS`) والثانية 242–557 ms** — كاشُ بيانات TMDB يعمل لكنّ الموجةَ الأولى طويلة؛ الصفحةُ متوازيةٌ أصلاً (`Promise.all` × 11).
- ✅ **الاختيار: `0069` أوّلاً** — أثرٌ مقيسٌ 3/3، إصلاحٌ تطبيقيٌّ خالص داخل حدود 10D، رجوعٌ بالتزامٍ واحد؛ و`0070` كلفتُه الحقيقيّة بيانات/كاش خارجَ الحزمة.
- 🆕 **مرشَّحاتٌ بلا أرقام (حتى تُقبل حدودُ الدليل):** (أ) SW +120 ms على TTFB الدافئ (مصالحتان متتاليتان بعد الشبكة) · (ب) أوّلُ فتحةِ عملٍ 1.1–1.7 s · (ج) شعارُ الرأس · (د) SVG مكرّر · (هـ) prefetch افتراضيّ لروابط «الكل» (6 RSC لمسارَين، 3 تُلغى) والتذييل (×2) · (و) `GET /api/season → 401` للزائر + خلفيّةُ العمل بمقاسَين + شعارُ الرأس بمقاسَين.
- 📡 **بعد المغادرة (انتقالٌ فعليّ إلى `/api/build` بعد كلِّ مسار، سجلُّ شبكة اللوح يعلّم `ERR_ABORTED`):** `/`: vitals + `/terms?_rsc` + شعار w=128 · `/news`: 3 prefetch «الكل» · `/people`: vitals + شعار · `/show`: vitals + شعار — **لا استعلامَ بياناتٍ كان قائماً وقتَ المغادرة؛ الاستمرارُ بعد اكتمالها غيرُ مقيس (`BLOCKED_BY_TOOLING`).**
- **صفر تعديلِ تطبيق · صفر Supabase · صفر بياناتِ أعضاء (عيّنةُ HTML التي حوت معرّفَ عضوٍ لم تُنسخ) · صفر سرّ.**
- ✅ **D-883 `VERIFIED` (`deec4ae` المسار · `d9767dd` المتحكّم [deploy] · `/api/build == d9767dd` · نقطةُ الرجوع `062d083`) — النقاطُ الخمس على D-881:** **(١)** الإشارةُ تحمل الحالةَ الجديدةَ صراحةً (`signal(next, -1, next)`) لا `phase` السابقة ✅ · **(٢)** «مرّةٌ لكلِّ نسخةِ محرّك» لا «لكلِّ جلسة» — التعليقُ صُحِّح والدلالةُ كما هي ✅ · **(٣)** **دليلُ E2E منقّح `evidence/D-883-e2e.txt`**: SHA المنشور · طلبٌ صالح (كُتب) · نوعٌ غيرُ صالح (أُسقط) · حمولةٌ فوق السقف (أُسقطت) · رمزٌ خارج المدى (كُتب مقصوصاً `-1`) · **العدُّ ١ → ٣ (+٢)** — **أعدادٌ وdigests فقط** ✅ · **(٤)** موثَّقٌ في رأس المسار **best-effort telemetry لا حماية** (الحاجزُ ذاكرةُ نسخةٍ واحدة في Serverless) · **سقفُ الحمولة ٢٥٦ بايتاً** · **الرمزُ مقصوصٌ ٠..٩٩٩ وكلُّ حقلٍ يصل الدالّةَ من قائمةٍ ثابتة** ✅ · **(٥)** وقتُ الرأس صار وقتَ الالتزام لا تقديراً ✅. **الأدلّة:** `evidence/D-883-gates.txt` (شجرةٌ نظيفة على `d9767dd`).
- 🔑 **قرارُ أحمد بنصّه (02:20 UTC):** **«الصوت صامتاً افتراضياً (لا)»** — **D-771 باقيةٌ: الصوتُ مفتوحٌ افتراضاً، والكاتمُ يُحفظ كتمُه** · **«مشغّل الاحتياط: نعم أريده لتحسين التجربة، وسرعة التشغيل مهمّة»** — **الاحتياطيُّ الكاملُ باقٍ بقرار المالك الصريح** (وهو ردٌّ مباشرٌ على طلب المراجع «مشغّلٌ واحد فقط» في 01:56 UTC — **المالكُ يعلو**). ⇢ **`LOOPZ-AUD-0072` `WONT_FIX_BY_OWNER` (قرارُ منتَجٍ مكتوب، لا عيبٌ مُنكَر)**، **و`P10-04` (الصوتُ مفتوحٌ آليّاً) يُغلق `BY_DESIGN`** — **يبقى ما قاله المراجع صحيحاً: السماعُ لم يُتحقَّق سمعيّاً**، **لكنّ الافتراضَ المطلوبَ هو المفتوح.**
- ✅ **D-882 `062d083` [deploy] `VERIFIED` (09:05:14 UTC) — التزامٌ 08:26 UTC، والبلاغُ قبله في المحادثة** (لقطةٌ من iPhone: «⏸ ما تختفي وهو شغال»): **⏸ لها مؤقّتُها المحلّيُّ (١٫٥ ثانية) لا تعليقٌ على `controlsVisible`** — **لم يُعد إنتاجُ البقاء على Chromium** فنُزع الاعتمادُ بدل تخمين السبب · **الأدلّة `evidence/D-882-gates-062d083.txt`**: بوّاباتٌ على SHA الملتزَم بشجرةٍ نظيفة · `/api/build == 062d083` · نقطةُ الرجوع `1f39f60` · Smoke بعد النشر (بيئةٌ مضمَّنة: ⏸ حاضرةٌ حتّى 1.2 s وغائبةٌ من 1.6 s والعدّادُ يتقدّم) · **iPhone — فحصُ المالك بعد الإصلاح (بلاغٌ في المحادثة، وقتُه غيرُ مسجَّل وسبق التزامَ الدليل `41354f2` 09:03:26 UTC؛ نصّاً في ملفّ الأدلّة):** الخطواتُ ١–٣ **نعم** (يظهر ويتقدّم · ⏸ تغيب بعد ثانيتين والفيديو مستمرّ · اللمستان توقفان ويستأنف) · **الخطوة ٤: الصوتُ مكتومٌ افتراضاً على iPhone، وإن فتحه بقي مفتوحاً في البطاقة التالية** — **الكتمُ الافتراضيُّ هنا سياسةُ WebKit للتشغيل الآليّ (السقوطُ الصامتُ المكتوبُ في D-771)، وحملُ الاختيار يعمل** · **الجهازُ/إصدارُ iOS/PWA-أم-Safari `UNKNOWN / NOT_RECORDED`** (لم يذكرها المالك؛ تُلحق إن ذُكرت). ⇢ **D-882 `VERIFIED`، و`LOOPZ-AUD-0023` `VERIFIED/RESOLVED` — نطاقُ الإثبات: زوالُ صورة العطل الأصلية على جهاز الإبلاغ في الخطوات الأربع، لا أوّلَ إطارٍ ولا سمعاً.**
- **D-881 — الوقت من `git log`: 2026-09-02 · `01:46` UTC (`b8bf6cc` المسار `/api/trailer-signal`) · `01:47` UTC (`1f39f60` المتحكّم) **[deploy]** · `/api/build == 1f39f60` · **نقطةُ الرجوع `c1b85dc`** · **الأدلّة:** `evidence/D-881-gates.txt` (على الرأس الملتزَم، شجرةٌ نظيفة: `tsc 0` · `eslint 0/15` · `build ✓`)
- **ما بُني:** **رمزُ خطأ يوتيوب (`onError.data`) · `autoplay-blocked` · `blocked` · `stalled`** ⇢ `POST /api/trailer-signal` بـ`sendBeacon` ⇢ **`log_runtime_error` نفسُها التي يكتب بها الخادم** (D-668) ⇢ `runtime_errors` بـ`kind = TrailerSignal`. **صفرُ معرّف** (لا فيديو ولا عمل ولا مستخدم) · **حاجزٌ بالعنوان ٢٠/١٠ دقائق** · **مرّةٌ لكلِّ (نوع، رمز) لكلِّ نسخةِ محرّك (engine instance)** · **قائمةُ سماحٍ صارمة والمجهولُ يُسقَط (`204`)**.
- ✅ **مُثبَتٌ من طرفٍ إلى طرف على Production:** إشارةُ اختبارٍ واحدةٌ (`stalled:-1`) وصلت الجدولَ (`at 01:51:42Z`)، **وإشارةٌ مشوَّهة (`kind=evil`) رُدّت `204` ولم تُكتب** — **عدُّ صفوفٍ لا قراءةُ بيانات.**
- **10B Baseline + D-880 `VERIFIED`** (01:39 UTC): **`P10-01 → 0069` · `P10-02 → 0070` · `P10-03 → 0071` · `P10-05 → 0072`** · `P10-04` `UNKNOWN` بلا رقم · `P10-06` مدمجٌ في `0023`.
- ⚠️ **بندا 10C الآخران يتوقّفان عند المالك لا عندي — ولا أنقض قراراً مكتوباً من تلقاء نفسي (`19` §0):**
  - **(٢) default-muted لأوّل استعمال** — **ينقض D-771 بنصّ أحمد («دائماً الصوت شغّال والي مزعجه يحط صامت»)** الذي نقض بدوره D-726. **القرارُ له.**
  - **(٣) الاحتياطيُّ thumbnail/metadata بدل iframe كامل** — **ينقض تصميمَ «الإقلاع الفوريّ للبطاقة التالية»** (احتياطٌ واحدٌ للمفتاح التالي، موقوف). **الكلفةُ المتوقَّعة: تأخّرُ بدء البطاقة التالية بزمن إنشاء المشغّل (~1–2 s)** مقابل ذاكرةٍ أقلّ ومشغّلٍ واحد. **القرارُ له.**
- **`0023` = `VERIFIED/RESOLVED`** (09:05:14 UTC — فحصُ iPhone في `evidence/D-882-gates-062d083.txt`).
- **صفر Migration · صفر تغييرِ RLS/Auth · صفر سرّ · صفر بياناتِ أعضاء.**

### حدود Phase 8 كما سجّلها المراجع — مقروءةً ومقبولة قبل أي فحص

**تدقيقُ قراءةٍ وPassive.** **ممنوع:** تغييرُ إعدادٍ أو متغيّرِ بيئة · النشرُ أو إعادةُ النشر أو rollback · تشغيلُ أيِّ `cron` أو دالّةِ Edge · الكتابةُ في Supabase أو Production · أيُّ اختبارٍ كاتب · **ونشرُ قيمةِ متغيّرِ بيئةٍ أو سرٍّ أو بياناتِ مستخدمٍ في التقرير أو التعليق.**
**ومعيارُ القبول:** مصفوفاتٌ صفّيّةٌ لا مجاميعُ وصفيّة · كلُّ ادّعاءٍ مربوطٌ بكودٍ أو إعدادٍ أو فحصٍ Passive أو مصدرٍ رسميٍّ مؤرَّخ · **و`UNKNOWN` صريحٌ عند غياب الدليل** · ومرشَّحٌ واحدٌ لكلِّ عيبٍ مستقلٍّ بلا تكرارِ مشكلةٍ قائمة.

### حدود Phase 7 كما سجّلها المراجع — **تاريخيّةٌ، تُحفظ ولا تُحذف**

**تدقيقٌ ثابتٌ وقراءةٌ فقط.** **ممنوع في هذه المرحلة:** حذفُ أيِّ ملفٍّ أو أصلٍ أو تبعيّة · تعديلُ كود التطبيق · تغييرُ `dependencies` أو `config` أو `lockfile` · تثبيتٌ دائم · Merge أو Deploy أو Migration · أيُّ تغييرٍ على Production · **ونشرُ أسرارٍ من تاريخ المستودع في تقريرٍ عامّ** (الفحصُ على رأس الفرع وحدَه، بأداةٍ لا تطبع القيم).
**ومعيارُ الحكم:** كلُّ `USED / UNUSED_CANDIDATE / DUPLICATE_CANDIDATE / UNKNOWN` **مرتبطٌ بمسارٍ ودليلٍ وطريقةِ فحص** · **ولا تُسوَّى إشارةٌ آليّةٌ بعيبٍ دون تحقّقٍ يدويّ** · **والأعدادُ في الملخّص تطابق الصفوف.**

### حدود Phase 5 كما سجّلها المراجع — **تاريخيّةٌ، تُحفظ ولا تُحذف**

**المراجعة `Static / Read-only / Passive` حصراً**، لأن لا Staging ولا حسابات اختبار معزولة (`LOOPZ-AUD-0006`).

**ممنوع عليّ في هذه المرحلة:** brute force · payloads هجومية نشطة · أي POST كاتب · رفع ملفات · تغيير بيانات · mass actions ضدّ Production · تغيير كود أو حزم أو إعدادات GitHub/Vercel/Supabase · `npm audit fix` · استخدام جلسة أحمد أو طلب بيانات اعتماد منه.

**والأسرار:** لا تظهر قيمة أي Secret أو Token في التقرير ولا في أي تعليق — **الاسم والموقع والحالة وبصمة مختصرة غير قابلة للاسترجاع فقط**. وإن اشتُبه في سرٍّ فعّال مكشوف: **توقّف وتنبيهٌ مصغَّر بلا القيمة.**

**ولا أدّعي نجاح عزل مستخدم A/B** — يبقى `BLOCKED_BY_TEST_ENV` مع ما أمكن إثباته من المصدر والسياسات، **ولا يخفّض ذلك خطورة `LOOPZ-AUD-0006`.**

### مفردات الحالة في هذه المرحلة

`SOURCE_VERIFIED` (من المصدر) · `PASSIVE_VERIFIED` (من فحص حيّ آمن غير كاتب) · `BLOCKED_BY_TEST_ENV` · `NOT_APPLICABLE` — **ولا `PASS` غير مثبت.**

### 🔴 `LOOPZ-AUD-0040` — **P1 · مفتوحة** · ومعالجةٌ لم تُعتمد

> ⚠️ **نسخة مُنقَّحة.** المستودع عامّ والصنف مفتوح، **فالتفاصيل التشغيلية ليست هنا** — لا أسماء ولا تواقيع ولا أوامر. وهي في قناة أحمد الخاصّة.

**ما جرى بالترتيب:** رُفع البلاغ مصغَّراً بلا تفصيل · ثمّ عُرضت أوامر السحب على أحمد **مع بيانٍ صريحٍ أن تشغيلها يخالف ثلاث قواعد قائمة** (تعليماته أن SQL يُشغَّل في لوحته · وقيد Phase 5 «لا تغيّر إعدادات Supabase» · و«Production دون تغيير حتى بوابة GO») · **فأذن صراحةً** · فنُفِّذ سحبُ صلاحيات تنفيذ لا غير (**صفر بيانات · صفر مخطَّط · صفر كود · صفر نشر**).

**وحكم المراجع — وأسجّله كما ورد لا كما أحبّ:**

> «لا يمكن اعتماد هذا التحديث ضمن Phase 5… سحب `EXECUTE` يغيّر حالة صلاحيات Production فعلياً، حتى لو كان قابلاً للرجوع… والإذن الخاصّ **غير قابل للتحقّق من أدلة الـPR**… وأيّ إصلاح صلاحيات لاحق يجب أن يكون **مهمّة أمنية منفصلة، خاصّة، بخطّة رجوع واختبار معزول واعتماد موثَّق**؛ وليس جزءاً من PR التدقيق العامّ.»

**مقبول، ومنفَّذ:**

- ⛔ **أوقفتُ أي تغيير إضافي على Production.** **ولم أُنفّذ ولن أُنفّذ rollback ولا `GRANT` ولا `REVOKE` ولا Migration** — تثبيت الحالة أقلّ خطراً من تغييرٍ ثانٍ بلا بوابة مستقلّة.
- 🧹 **نُقِّح رأس الفرع العامّ**: حُذف ملفّ SQL الأمنيّ، واستُبدل تفصيل §4.3 بتجميعٍ فئويّ آمن، وحُجبت أسماء مجموعة `search_path` والعرض المميَّز. **بلا إعادة كتابة تاريخ Git** كما أمر المراجع.
- 🔴 **`LOOPZ-AUD-0040` تبقى مفتوحة P1** حتى: جردٌ آمن لكل الاستثناءات العامّة · وإثبات الحراسة والحدود ومقاومة الإساءة لكل استثناء · **ومراجعة وراثة `PUBLIC` و`authenticated`** لا الدور المجهول وحده · **وتحقّق مستقلّ في بيئة معزولة لا على Production**.
- 📉 **التحقّق الوظيفي `BLOCKED_BY_TEST_ENV`**: خمسة `GET` عامّة ردّت 200 بأحجام مطابقة لخطّ أساس Phase 4، **وهذا لا يثبت سلامة الـRPCs ولا العمليات الموثَّقة ولا صلاحيات المستخدم المسجَّل** — ولا يُوسَّع الاختبار على Production.

### 🔴 `S-05` — **P2 (مقترَحة)** · وراثة الصلاحيات تُبطل السحب الموجَّه

**تجميع آمن:** بعض الدوالّ المكشوفة تحمل منحاً على مستوى `PUBLIC` لا على مستوى دور، **فالدور المجهول يرثها وسحبٌ موجَّه إليه وحده لا يُبطل الإرث ويفشل صامتاً**. **اكتُشف بالتحقّق بعد التنفيذ لا قبله** — ولولاه لسُجِّل نجاحٌ لم يقع. **والعدد والأسماء في القناة الخاصّة.**

> **وهو الدرس نفسه المتكرّر معي في هذا التدقيق: فحصٌ يثبت الإرسال لا يثبت الأثر** — كما أن وجود iframe لا يثبت التشغيل، ووجود القسم لا يثبت صحّة الإحالة إليه.

### 🔴 وإقرارٌ بخطأ في الحكم — لا في القياس

نشرتُ في Commit `6977868` تفصيلاً تشغيليّاً في **مستودعٍ عامّ**، بحجّة أن «المسار عولج فالإفصاح بعد الإغلاق آمن». **والحجّة خاطئة، واعتراض المراجع في محلّه:** عولج **مسارٌ واحد** والصنف مفتوح — **فالتفصيل يبقى خريطةً لما لم يُعالَج**. قاعدةُ «الإفصاح بعد الإصلاح» تخصّ ثغرةً مغلقةً بذاتها، **لا حالةً واحدةً من صنفٍ مفتوح**. نُقِّح، **ولا يُعاد**.

### ما أُنجز — النطاقات الثمانية


**النطاقات الثمانية — الحالة بعد جولة 17:20 UTC** (الفقرةُ أدناه كانت تصف حالةَ 10:02 UTC، وهي الآن مصحَّحة؛ **والسجلُّ التاريخيُّ أسفل الملفّ لا يُحذف**):

- ✅ **اكتمل:** الأسرارُ والترويسات · طبقتا RLS والتخزين **بمصفوفةٍ صفّيّةٍ من 128 سياسة** · فئاتُ الهجوم الثابتة · **تدقيقُ الدوالّ صفّاً بصفّ** (21 كاتبة · 32 قارئةً تلمس جداولَ مستخدم) · **سلسلةُ التوريد بتواريخ الإهجار والصيانة للثمانيةَ عشرَ كلِّها** · دورةُ الجلسة والحذف · GitHub/Vercel/Cron من المصدر · و**`S-04` بأدلّته الأربعة**.
- ⏳ **لم يكتمل — و`ANALYSIS_IN_PROGRESS` هي الحالة الصحيحة:** التحقّقُ الحيُّ من `LOOPZ-AUD-0040` و`S-03` و`S-12` (يحتاج حسابَين في بيئةٍ معزولة — `LOOPZ-AUD-0006`) · ومدّةُ رمز الوصول · وإعداداتُ المشروعَين الإداريّة.

**وأهمّ ما ثبت انضباطه:** **صفر مفتاح `service_role` في المشروع** — **فلا تجاوز لـRLS بمفتاح service-role في كود التطبيق** (ولا يُقرأ ذلك «لا تجاوز إطلاقاً»: مسار `SECURITY DEFINER` قائم وهو `LOOPZ-AUD-0040`) · **RLS على 72 جدولاً من 72** و**128 سياسة** · **119 سياسة تستعمل `auth.uid()`** (صُحِّحت الأرقام على مراحل: 71/126/84 ← ثمّ 73 خطأً ← **والصحيح 72**، لأن `public_profiles` عرضٌ لا جدول) · **عزل التخزين مفروضٌ بالسياسة** وSVG مرفوض على الخادم وحدّ 2MB · **صفر ثغرة في 459 حزمة وصفر سكربت تثبيت** · **صفر `eval` وصفر `innerHTML` وصفر SQL نصّيّ** في 453 ملفاً · **ودوالّ الإدارة التسع كلّها تحمل حارسها**.

**وثلاثة مرشّحات صغيرة إضافية:** `S-02` (حدّ المعدّل محلّيٌّ لكل نسخة لا موزّع — **مُقَرٌّ به في الملفّ نفسه**) · `S-03` (عرضٌ واحد بامتياز المُعرِّف) · `S-04` (`search_path`). **وكلاهما حُسم لاحقاً في §9.6 — والحسمُ هناك هو النافذ:** `S-03` **عيبٌ P2 مفتوح**، و`S-04` **يسقط كعيبٍ ويبقى ثابتاً واجبَ الحفظ**.

**وتصحيح توصيف لا تخفيف واجب:** `LOOPZ-AUD-0005` (`watched_episodes_backup_133`) **غير مقروء عبر REST** — RLS مفعّلة بلا سياسة. **فهو دَينُ احتفاظٍ لا انكشافٌ مباشر**، ويبقى واجبَ الحذف، ويبقى ضمن نطاق `SEC-ALERT-01` لأن دوالّ `SECURITY DEFINER` تتجاوز RLS.

**وما لم يكتمل مُعلَنٌ في §8.5 من التقرير** لا مسكوتاً عنه: مصفوفة صفٍّ لكل سطح حسّاس · وتدقيق الـ69 دالّة القارئة · وصلاحيات GitHub/Vercel.

**وقد رُفع البلاغ فوراً كما أمرت — بملخّص آمن، بلا محاولة إصلاح ولا استغلال.**

### ما أُنجز حتى الآن من النطاقين 2 و3 — **`SOURCE_VERIFIED`**

**الأسرار (نطاق 2):**

| الفحص | النتيجة |
|---|---|
| مفاتيح تُقرأ في الكود | **14 اسماً** — عشرة خادميّة وأربعة `NEXT_PUBLIC_*` (معروضة للعميل بحكم التصميم) |
| **مفتاح `service_role`** | **غير موجود إطلاقاً** — صفر إشارة في `src` و`next.config.ts` و`.env.example`. **فلا مسار في التطبيق يتجاوز RLS** |
| ملفّات `.env` في المستودع | **`.env.example` وحده** — ولم يُضَف غيرُه في تاريخ Git كلّه |
| **قيم `.env.example`** | **أربع قيم غير فارغة، وكلّها نائبة بالإثبات البنيويّ**: عنوان Supabase لا يحوي مُعرّف المشروع الحقيقي · ومفتاح anon ليس بشكل JWT وأقصر من الحقيقيّ بكثير · وTMDB ليس 32 خانة ستّ عشرية · وعنوان الموقع `http://localhost`. **صفر سرّ حقيقي.** (فُحصت ببصمات مختصرة وأطوال وأنماط — **ولم تُطبع قيمةٌ واحدة**) |
| **مسح تاريخ Git كاملاً** | **14,893 كائناً (blob) فُحصت** بأنماط JWT وGoogle `AIza` وGitHub/Slack tokens وAWS `AKIA` ومفاتيح خاصّة — **صفر إصابة** |
| نقص `.env.example` | يؤكّد `LOOPZ-AUD-0002`: أربعة من المفاتيح المقروءة غائبة عن القالب |

**الدخول والجلسات (نطاق 3):**

| البند | النتيجة |
|---|---|
| كوكيز الجلسة | **`sameSite: "lax"` و`secure: true` و`path: "/"` صريحةً لا افتراضاتِ مكتبة** — في `supabase/server.ts` و`proxy.ts` معاً |
| `safeNext()` | يردّ كلّ ما ليس مساراً نسبياً، ويحجب `//` و`\\` و`^/<scheme>:` — **`SOURCE_VERIFIED`** |
| `resolveAuthBase()` | **قائمة أصول مغلقة بأسمائها** (نطاقان)، والغريب يُرَدّ إلى النطاق القانونيّ |
| استعادة/تغيير كلمة المرور والبريد | **`NOT_APPLICABLE`** — الدخول بـGoogle وحده، فلا كلمة مرور أصلاً. **ويشمل ذلك تنبيه Supabase `auth_leaked_password_protection`** |
| ترويسات الأمن | CSP بـ`default-src 'self'` و`base-uri 'self'` و`form-action 'self'` و`frame-ancestors 'none'` و`object-src 'none'` · HSTS سنتان + preload · `DENY` · nosniff · Referrer · Permissions · COOP. **والثغرة الوحيدة `script-src 'unsafe-inline'` وهي `LOOPZ-AUD-0007` المسجَّلة، ومُقرٌّ بها في الملف نفسه** |

### 🟡 مرشّح `S-01` — **P3** · حارس CSRF في `/auth/signout` يسقط عند غياب الترويسة

`route.ts` يقبل الطلب إذا كان `origin` **غائباً**: `if (origin && origin !== self)`. **والأثر خروجٌ قسريّ لا أكثر** — لا كتابة بيانات — **ومخفَّف فعلياً** بأن كوكي الجلسة `sameSite=lax` فلا تُرسَل مع POST من موقع آخر. معيار القبول: رفض الطلب عند غياب `origin` أيضاً (أو الاكتفاء بفحص `sec-fetch-site`).

### الترحيل

`LOOPZ-AUD-0023` (P1) · البوّابة البصرية 15 بنداً صفر `PASS` · **وقياسات Web Vitals الصالحة** — ثلاثتها **موانع إلزامية قبل الـGO**. و`0006` و`0007` يُرحَّلان بلا تخفيف، و`0005` لمرحلة البيانات.

- صاحب الدور التالي: **Claude** (تنفيذ)

## ChatGPT Review

- الحالة: **`VERIFIED`** — Phase 4
- الوقت: 2026-09-01 07:50 UTC
- النطاق: Commit `6a38d75a84c940729308244abc9f8b326b56cd3a` · الـDiff · `PHASE_4_PERFORMANCE.md` · `SHARED_PROGRESS.md` · حالة الـPR وWorkflow runs
- النتيجة: صُحّحت إحالة `P-06` من §14 إلى §16 · والفحص الدلاليّ كشف وصحّح ثماني إحالات إضافية · **42 إحالة = 37 داخلية صحيحة + 5 خارجية موسومة + 0 معلَّقة**. التصحيح توثيقيّ فقط. **ومصفوفة API ذات 21 صفّاً وتصنيف العائلات الخارجية وفحص الصور والقوائم والذاكرة وحماية النتائج القديمة باقية مجتازة.**
- الأرقام الجديدة: `LOOPZ-AUD-0035`…`0039` (مسجَّلة أدناه).
- **ما لم يُرقَّم:** `P-03` مرتبط بـ`LOOPZ-AUD-0023` وبند `V-1` · `P-05` مقايضة موثّقة لا عطل مثبت، وقرارها مع بيانات Web Vitals · `P-06` مسحوب بعد إثبات الحماية.
- **الموانع النهائية:** «تبقى `LOOPZ-AUD-0023` بدرجة P1، والبوابة البصرية ذات 15 بنداً، وقياسات Web Vitals الصالحة **موانع إلزامية قبل قرار GO النهائي**».
- المطلوب من Claude: **Phase 5 — Security & Protection** على `f8a2b33c` · **Static / Read-only / Passive حصراً** · ثمانية نطاقات (نموذج التهديد · الأسرار · الدخول والجلسات · التحكّم بالوصول · فئات الهجوم · سلسلة التوريد · فحص Passive حيّ آمن · النتائج) · **ولا Phase 6 قبل المراجعة**.
- صاحب الدور التالي: **Claude**

---

## سجل المشكلات

> نُقلت هذه الأرقام حرفياً من تعليق ChatGPT في PR رقم 19 بتاريخ 2026-09-01 01:38 UTC. **التسجيل والدرجات من ChatGPT وحده**؛ دور Claude هنا نسخٌ إلى المرجع الرسمي لا إسناد أرقام.

| ID | المرحلة | الدرجة | النطاق | الوصف | الحالة | فرع الإصلاح | دليل |
|---|---:|---|---|---|---|---|---|
| LOOPZ-AUD-0001 | 0 | P2 | البناء والاختبار | لا مشغّل ولا script اختبارات آلية | TODO | — | `package.json` — لا `test` ولا مشغّل في `devDependencies` |
| LOOPZ-AUD-0002 | 0 | P2 | البيئة | ستة متغيّرات تُقرأ في الكود وغائبة عن `.env.example`: `NEXT_PUBLIC_GOOGLE_CLIENT_ID` · `OMDB_API_KEY` · `DEEPL_API_KEY` · `GIPHY_API_KEY` · `LAB_REPO` · `LAB_CHROMIUM` | TODO | — | استعمال كلٍّ منها مثبت بملفه في `PHASE_1_INVENTORY.md` §5 |
| LOOPZ-AUD-0003 | 0 | P3 | الحزم | `eslint-config-next@16.2.12` لا يطابق `next@16.3.0` | TODO | — | `package-lock.json` |
| LOOPZ-AUD-0004 | 0 | P3 | البناء | لا `engines` ولا `packageManager` — إصدار Node غير مثبَّت | TODO | — | `package.json` |
| LOOPZ-AUD-0005 | 6 | **P3 — صُحِّحت من P1-CANDIDATE** | قاعدة البيانات | `watched_episodes_backup_133` (3510 صفوف) — **RLS مفعّلة بلا سياسة فصفر قراءة عبر Data API**؛ **دَينُ احتفاظٍ بنسخةٍ زائدة لا انكشافٌ مباشر** | TODO | — | التوصيف المصحَّح في `PHASE_5_SECURITY.md` §4.1 و§8.3 و§9.1 · **ويبقى واجبَ الحذف وداخلَ نطاق 0040** |
| LOOPZ-AUD-0006 | 5/6 | **P1** | البيئة | لا بيئة بيانات معزولة؛ Preview يستخدم قاعدة Production | TODO | — | لا Staging · مشروع Supabase واحد · تصوّر الحل في `PHASE_1_INVENTORY.md` §11 |
| LOOPZ-AUD-0007 | 5 | P2-CANDIDATE | الأمن | `script-src 'unsafe-inline'` في CSP | TODO | — | `next.config.ts` — والتعليق في الملف نفسه يقرّ به ويؤجّل التشديد بـnonce |
| LOOPZ-AUD-0008 | 1 | **P1 / App-readiness** | البنية | لا طبقة API/BFF عامة ومؤمّنة للتطبيق؛ القراءة والكتابة عبر Server Components/Actions ومفاتيح خادمية | **DESIGN_PROPOSED — بانتظار اعتماد المالك** | — | **التصميم في `PHASE_9_EXPO_READINESS.md` §٤** (Route Handlers `/api/v1` بـBearer فوق `src/core` مشترك؛ وسومُ إبطالٍ بدل `revalidatePath`؛ لا `service_role`؛ لا يغلق `0040`) · يُغلق مع الخطوة 3 من §٩ |
| LOOPZ-AUD-0009 | 1 | P2-CANDIDATE | الإشعارات | صفحة إعدادات إشعارات بلا تخزين تفضيلات ولا Push/Email | **مُغلقة بلا عيب** | — | Phase 2: ثلاثة صفوف عرض فقط بلا مفتاح — `review question resolved / NOT_APPLICABLE` |
| LOOPZ-AUD-0010 | 1 | P3 | اللِّنت | lint ينجح مع **16 warning** | TODO | — | **Phase 7 §2 و§10.1: العدد نفسه اليوم — و10 منها حدُّ أداةٍ داخل راسم OG، و5 متغيّرات غير مستعمَلة، وواحد متفرّق** |
| LOOPZ-AUD-0011 | 1 | P3-CANDIDATE | كود ميت | خمسة مكوّنات بلا مراجع و`/diary/loading.tsx` غير قابل للظهور | TODO | — | **Phase 7 §10.2: الخمسة مؤكَّدة وتوسَّعت إلى ثمانية (`P7-02`) · و`/diary/loading.tsx` `USED_BUT_UNREACHABLE` بالتصميم فلا يُحذف (`P7-10`)** |
| LOOPZ-AUD-0012 | 1 | P2-CANDIDATE | الأداء وسلوك الفشل | كل الصفحات ديناميكية ولا error boundaries محلية | TODO | — | يُقاس في Phase 2/4 |
| LOOPZ-AUD-0013 | 1 | **P2 / App-readiness** | التطبيق | لا Universal Links/App Links artifacts | TODO | — | يُعالج في Phase 9 |
| LOOPZ-AUD-0014 | 2 | P2 | 27 صفحة محميّة | 200 + `meta refresh` بعد ثانية بدل 307 · 45 ك.ب تُشحن بلا فائدة | TODO | — | `fetch(redirect:'manual')` → 200 + `__next-page-redirect` |
| LOOPZ-AUD-0015 | 2 | P2 | `/admin/*` | للزائر: 200 وجسم فارغ بلا 404 وبلا تحويل | TODO | — | الجسم المرسوم = الترويسة وحدها |
| LOOPZ-AUD-0016 | 2 | P2 | `/movie/[id]` | عمل غير موجود يُعرض كعطل مؤقّت («try again shortly») بحالة 200 | TODO | — | `GET /movie/999999999` |
| LOOPZ-AUD-0017 | 2 | P2 | `/login` | إطار GIS عربيّ داخل صفحة `lang="en"` — و`/` ترسم الزرّ إنجليزيّاً في الجلسة نفسها | TODO | — | `iframe.title` و`textContent` |
| LOOPZ-AUD-0018 | 2 | P2 | 37 صفحة | بلا ميتاداتا، ومنها `/movie/[id]` و`/show/[id]` و`/u/[username]` — بينما `/u/[username]/stats` لديها | TODO | — | `<title>` عام على `/movie/550` |
| LOOPZ-AUD-0019 | 2 | P2-CANDIDATE | SEO | `sitemap.xml` بأربعة روابط — **معلَّق لقرار Phase 8/10، لا يُوسَّع الآن** | DEFERRED | — | `src/app/sitemap.ts` |
| LOOPZ-AUD-0020 | 2 | P3-CANDIDATE | `/movie/[id]` | فيض 25px عند 390 أثناء الهيكل — **ولا يظهر عند 768** · يحتاج إثباتاً بصريّاً بعد اكتمال الرسم | TODO | — | `scrollWidth 415 > clientWidth 390` |
| LOOPZ-AUD-0021 | 2 | P3 | `/trailers` | تبويب «For You» فارغ للزائر بلا حالة فارغة · و«Trending» يعمل | TODO | — | صفر بطاقة وصفر نصّ |
| LOOPZ-AUD-0022 | 2 | P3 | `/discover/[section]` | قسم مجهول يعيد محتوى قسم صحيح بدل 404 | TODO | — | مقارنة نصّية: `identical: true` |
| LOOPZ-AUD-0023 | 2 | **P1** | `/trailers` | **تشغيل الترايلر معطوب**: بعد الضغط يُنشأ iframe `autoplay=1&mute=1&controls=0` وبعد 11 ثانية لا تقدّم لصورة الفيديو · `pointer-events:none` · **لا زرّ صوت ولا عرض زمن**. أثبته ChatGPT بفحص مرئي مستقل، ويطابق بلاغاً سابقاً من أحمد | TODO | — | Production `f8a2b33c` · زائر · Chrome مرئي `1363×936` · `/trailers?tab=trending` |
| LOOPZ-AUD-0024 | 3 | P2 | نظام التصميم | `D-01`: سلّما زوايا متوازيان — 10 رموز بخمس قيم · `--radius-card`=`2xl`=`3xl`=14px | TODO | — | `globals.css` |
| LOOPZ-AUD-0025 | 3 | P2 | نظام التصميم | `D-02`: سلّما مقاسات نصّية متوازيان · واستثناء `--text-base: max(16px,…)` غير موثّق | TODO | — | `globals.css` |
| LOOPZ-AUD-0026 | 3 | P2 | البنية | `D-04`: 48 من 49 مساراً ترث حدّ خطأ الجذر بلا عزل محلّي | TODO | — | جدول §7 |
| LOOPZ-AUD-0027 | 3 | P3 | الهوية | `D-06`: المعرّف الداخلي القديم `meshahed-pulse` | TODO | — | `globals.css:539,741` |
| LOOPZ-AUD-0028 | 3 | P3 | الجولات | `D-07`: جولة من 8 خطوات بلا «رجوع» | TODO | — | `TourGuide.tsx` · `i18n.ts:118-121` |
| LOOPZ-AUD-0029 | 3 | P3 | الوصول | `D-10`: تلميحات وأخطاء حقول غير مربوطة برمجياً · `aria-invalid` = 0 و`aria-busy` شبه غائب | TODO | — | `EditProfileForm.tsx:461` و`:315-317` |
| LOOPZ-AUD-0030 | 3 | **P2** | الهوية | `D-11`: أصفران وأسودان بين الثيم وmanifest وOG · وثيم `loopz` يخالف القرار D-454 | TODO | — | `themes.ts:99,114,289` · `manifest` · بطاقة OG |
| LOOPZ-AUD-0031 | 3 | P3 | نظام التصميم | `D-12`: عشرة أحجام أيقونات بلا سلّم مركزي | TODO | — | جرد §12 |
| LOOPZ-AUD-0032 | 3 | P3 | نظام التصميم | `D-13`: ستة أوزان خطّية بلا سلّم موثّق | TODO | — | جرد §12 |
| LOOPZ-AUD-0033 | 3 | P3 | نظام التصميم | `D-15`: لا `Card` ولا `Field` مركزيان — 57 حقلاً في 30 ملفاً | TODO | — | جرد §12 |
| LOOPZ-AUD-0034 | 3 | P3 | الجولات | `D-19`: فشل حفظ حالة الجولة بعيداً يُبتلع بلا إعادة محاولة، فتتكرّر على جهاز آخر | TODO | — | `src/lib/tour.ts` — `.catch(() => {})` |
| LOOPZ-AUD-0035 | 4 | **P2** | الحزم | `P-01`: قطعة i18n مشتركة **125,744 بايت** تشحن القاموسين معاً — نحو **19٪** من أرضية JS المُنزَّلة | TODO | — | `PHASE_4_PERFORMANCE.md` §13.2 · §3.3 |
| LOOPZ-AUD-0036 | 4 | **P2** | الحزم | `P-02`: أرضية JS مشتركة مُنزَّلة **658.8 kB** عبر المسارات العامّة، ومنها **41.4 kB `UNKNOWN_OWNER`** | TODO | — | `PHASE_4_PERFORMANCE.md` §13.1 · §13.2 |
| LOOPZ-AUD-0037 | 4 | P3 | الكاش | `P-04`: صفحات عامّة ثابتة (`/features` · `/terms` · `/privacy`) تُخدَم بـ`no-store` | TODO | — | ترويسة مقيسة حيّاً — §5 |
| LOOPZ-AUD-0038 | 4 | P3 | القوائم · **ومانع تصميميّ لـExpo** | `P-07`: `/person/[id]` يرسم قائمة غير محدودة — **219 بطاقة و547.5 kB HTML** (person 287) | TODO | — | `page.tsx:191,213,247` — §15.3 |
| LOOPZ-AUD-0039 | 4 | P3 | المرونة | `P-08`: ستّ عائلات خارجية بلا مهلة — **3 على مسار الرسم المباشر و3 عبر API/Action** | TODO | — | `omdb` · `anilist` · `appleTrailers` · `imdbChart` · `trakt` · `ai` — §12.2 |

> **مسألة مُغلقة بلا رقم — بأمر المراجع:** سؤال Phase 2 عن صفحة الإشعارات (أتوهم بالحفظ؟) نتيجته **`review question resolved — NOT_APPLICABLE / no defect`** بدليل المصدر (`profile/settings/notifications/page.tsx` — ثلاثة صفوف عرض فقط بلا مفتاح). **لم تُنشأ خانة `LOOPZ-AUD-0009` ولم يُغلَق شيء بأثر رجعي.**

### قالب المشكلة

```md
### LOOPZ-AUD-0001 — عنوان مختصر
- المرحلة:
- الدرجة: P0 / P1 / P2 / P3
- الحالة: TODO
- البيئة:
- الجهاز/المتصفح:
- الحساب/الدور:
- الصفحة أو المسار:
- خطوات إعادة المشكلة:
- النتيجة الحالية:
- النتيجة المتوقعة:
- الدليل:
- السبب المتوقع:
- تعليمات Claude:
- معيار القبول:
- فرع الإصلاح:
- Commit:
- اختبارات Claude:
- نتيجة ChatGPT:
```

---

## اعتماد Phase 5 — الأرقام المسنَدة (19:05 UTC)

> 🔴 **وإقرارٌ بتقصيرٍ منّي قبل الجدول:** **هذا الاعتماد نُشر على الـPR في
> 19:05 UTC، وسلّمتُ Phase 6 في 19:27 وأنا أطلب فيه أرقاماً كانت قد
> صدرت بالفعل.** **لم أقرأه لأنّي بحثتُ في صفحات التعليقات بالتخمين
> فتجاوزتُ صفحتَه** — **وقلتُ لأحمد «لم يردّ» وهو نفيٌ لم أتحقّق منه.**
> **وهو الخطأ الذي ضبطه المراجعُ عليّ خمس مرّاتٍ اليوم: ادّعاءٌ فوق
> الدليل.** والعلاجُ مسجَّلٌ في ذيل السجلّ.

| ID | الأصل | الدرجة | الخلاصة |
|---|---|---:|---|
| `LOOPZ-AUD-0041` | S-01 | P3 | `signout` يقبل غياب `Origin` |
| `LOOPZ-AUD-0042` | S-02 | P3 | حدُّ المعدّل محلّيٌّ لكلِّ نسخة |
| `LOOPZ-AUD-0043` | S-03 | **P2** | عرضٌ مميّزٌ يكشف حقولَ ملفّاتٍ خاصّة |
| `LOOPZ-AUD-0044` | S-05 | **P2** | وراثةُ `PUBLIC` قد تُبطل السحبَ الموجَّه |
| `LOOPZ-AUD-0045` | S-06 | P3 | تبعيةُ خطٍّ غيرُ مستعمَلة |
| `LOOPZ-AUD-0046` | S-07 | P3 | ستُّ سياساتٍ مسنَدةٌ إلى `PUBLIC` |
| `LOOPZ-AUD-0047` | S-08 | P3 | أربعُ دوالِّ قياسٍ مجهولةٍ قابلةٍ للإغراق |
| `LOOPZ-AUD-0048` | S-09 | **P2** | كاتبٌ مجهولٌ لمخبأ تقييمات الكتالوج |
| `LOOPZ-AUD-0049` | S-10 | P3 | لا إبطالَ ذاتيّاً شاملاً للجلسات |
| `LOOPZ-AUD-0050` | S-11 | **P2** | لا إنذارَ على معدّل أخطاء Postgres |
| `LOOPZ-AUD-0051` | S-12 | P3-CANDIDATE | خصوصيّةُ شبكة المتابعة تحتاج تحقّقاً معزولاً |
| `LOOPZ-AUD-0052` | S-13 | P3-CANDIDATE | دالّتا عدّاداتٍ تشغيليّةٍ بلا مستهلك |
| `LOOPZ-AUD-0053` | S-14 | **P2** | نسخةُ ESLint المثبَّتة مهجورة |

**وأرقامُ Phase 6 المعتمَدة** (`VERIFIED` 2026-09-01 · 21:36 UTC) — **مفتوحةٌ كلُّها: اعتمادُ المرحلة اعتمادٌ للدليل والجرد لا إغلاقٌ للمشكلات:**

| الرقم | المرشَّح | الدرجة | العنوان |
|---|---|---|---|
| `LOOPZ-AUD-0054` | `P6-01` | 🔴 **P1** | لا مسارَ استعادةٍ مملوكاً للفريق ومتاحاً ومختبَراً · ولا dump خارجَ الموقع · وتغطيةُ التخزين غيرُ مثبتة |
| `LOOPZ-AUD-0055` | `P6-02` | 🟠 **P2** | `watched_episodes_backup_133` قد يبقى بعد حذف الحساب لغياب المفتاح الأجنبيّ |
| `LOOPZ-AUD-0056` | `P6-03` | 🟠 **P2** | تصديرُ بيانات المستخدم يغطّي ١١ من ٦٠ سطحَ بياناتِ عضو |
| `LOOPZ-AUD-0057` | `P6-04` | 🟠 **P2** | توثيقُ الخصوصيّة لا يغطّي المعالجين ومناطقَ البيانات ومددَ الاحتفاظ وأساسَ النقل — **ويلزم تقييمٌ قانونيّ، ولا يُسجَّل حكماً قانونيّاً مثبتاً** |
| `LOOPZ-AUD-0058` | `P6-05` | 🟢 P3 | سجلُّ الأخطاء بلا سقفِ احتفاظٍ مثبت |
| `LOOPZ-AUD-0059` | `P6-06` | 🟢 P3 | ٢٥ مفتاحاً أجنبيّاً بلا فهرس قد يبطّئ الحذفَ المتسلسل وحذفَ الحساب |
| `LOOPZ-AUD-0060` | `P6-07` | 🟢 P3 | ١١٩ سياسةَ RLS تستدعي `auth.uid()` مباشرةً بما يسمح بتقييمه لكلِّ صفّ |

**وأرقامُ Phase 7 المعتمَدة** (`VERIFIED` 2026-09-01 · 22:10 UTC) — **مفتوحةٌ كلُّها، والاعتمادُ للدليل لا تصريحٌ بالتنظيف:**

| الرقم | المرشَّح | الدرجة | العنوان |
|---|---|---|---|
| `LOOPZ-AUD-0061` | `P7-01` | 🟠 **P2** | مساران إداريّان يطبعان ناتجَ العمليّة كاملاً في سجلّ الخادم |
| `LOOPZ-AUD-0062` | `P7-04` | 🟢 P3 | عشرةُ ملفّاتِ `loading.tsx` متطابقةٍ في ثلاث مجموعات — **العلاجُ استخراجُ مكوّناتٍ مشتركة لا حذفُ نقاط الدخول** |
| `LOOPZ-AUD-0063` | `P7-05` | 🟢 P3 | أصلان غيرُ مستعمَلَين ≈45KB |
| `LOOPZ-AUD-0064` | `P7-07` | 🟢 P3 | تصادما تسمية `ShareCard` و`searchPeople` |
| `LOOPZ-AUD-0065` | `P7-08` | 🟢 P3 | ثلاثةُ ملفّاتٍ تحمل ≈40% من `src` — **التقسيمُ مؤجَّلٌ خلف شبكة الاختبارات** |
| `LOOPZ-AUD-0066` | `P7-09` | 🟢 P3 | سبعةُ تعطيلاتٍ لـ`react-hooks/exhaustive-deps` بلا مراجعةٍ سلوكيّة |
| `LOOPZ-AUD-0067` | `P7-11` | 🟠 **P2** | حوكمةُ إدخال بيانات الشريك الحسّاسة يدويّاً خارج المنتَج (قناةُ الاستلام · الاحتفاظ · التحقّق · الأثرُ التدقيقيّ · حقُّ الوصول/التصحيح) — **صادرٌ في اعتماد Phase 8** |
| `LOOPZ-AUD-0068` | `P8-01` | 🟠 **P2** | كلُّ Migration جديدة تحمل `rollback artifact` مكتوباً — **ولا يلزم ترقيعُ الهجرات التاريخيّة الآن** — **صادرٌ في اعتماد Phase 8** |
| ~~`P8-02`~~ | `P8-02` | — | **مدمجٌ في `LOOPZ-AUD-0002` ونطاقُه مفاتيحُ الميزات الأربعة فقط — لا رقمَ جديد** (اعتماد Phase 8) |

**ولم تُنشأ أرقامٌ لِما صولح:** `P7-02` يوسّع `0011` · `P7-03` هو `0045` وقد عولج على `main` · `P7-06` تحت `0010` · **و`P7-10` حكمٌ تصميميٌّ بلا عيبٍ ولا رقم.** **و`P7-11`** (مسارُ بيانات الشريك — §6.3.1) **سُلّم بعد الاعتماد وينتظر رقمَه.**

**و`S-04` لا رقمَ عيبٍ له** — يُسجَّل **ثابتاً** يجب أن يمنع الفحصُ الآليُّ
انحدارَه.

**وحدودُ الاعتماد بنصّه:** ليس `Security GO` · ولا يغلق `0040` ولا `0023`
ولا `0006` ولا البوّابةَ البصريّة وWeb Vitals · **وتبقى كلُّ `GRANT`/`REVOKE`
وSQL وMigration وrollback واختبارٍ كاتبٍ على Production ممنوعة.**

## Delta Queue — ما تحرّك على `main` بعد الـbaseline

> **بحكمك في 01:43 UTC:** «أيُّ Commit جديد يصل إلى `main` بعد `f8a2b33c`
> يُسجَّل في **Delta Queue** فقط، ولا يعيد تشغيل المراحل السابقة فوراً» —
> **ويُدقَّق كلُّه في جولة Final Delta Audit قبل قرار GO.**
> **هذا القسمُ كان ناقصاً حتى 21:30 UTC، وهذا إنشاؤه.**

**النطاق:** `f8a2b33c` (Audited SHA) ← `37d2a337` (رأس `main` عند 21:19 UTC)

| القياس | القيمة |
|---|---|
| الالتزامات | **32** — منها **16 تحمل وسمَ `[deploy]`** |
| ⚠️ **وما لا يُدَّعى** | **الوسمُ يعني أن بناءً طُلِب، لا أن نشراً نجح.** `list_deployments` **ما زال يردّ 403** (مانعُ Phase 0 الثالث، `LOOPZ-AUD` قائم) — **فلا سجلَّ نشرٍ عندي أستند إليه.** **والمُثبَتُ حيًّا وحدَه**: فحصتُ الموقعَ بعد النشر فوجدتُ شيفرةَ `D-866`/`D-867`/`D-868`/`D-869` عاملةً — **فبعضُ البناءات نجح قطعاً، وعددُها غيرُ معلوم** |
| الملفّات | **30** · +1334 / −475 سطراً |
| **ملفّات `supabase/`** | 🟢 **صفر** — **ولا هجرةَ واحدةً في المستودع في هذا المدى** |
| المدى الزمنيّ | 13:49 ← 21:19 UTC، اليوم نفسه |

**والقراراتُ في المدى (D-854…D-869):** ترتيبُ رأس الملفّ · شرطا التقديم
للشركاء وبياناتُ التحويل ورفعُ الهويّة · **إصلاحُ اختفاء قوائم عضوٍ حقيقيّ
(D-856 — وهو إصلاحُ الانحدار في §9.7)** · حارسُ إعادةِ تسميةِ المفضّلة ·
أدواتُ الترايلر ومقاسُه · «All» واحدةٌ ثمّ ورقةٌ · ترسُ الشريط · **سقفُ
الحاوية 1440** · ترتيبُ «للمشاهدة» · وتنظيفُ تبعيتين.

### 🔴 ما يمسّ نتائج Phase 5 مباشرةً — ثلاثةُ بنود

| البند | الالتزام | الأثر على التدقيق |
|---|---|---|
| **إصلاحُ الانحدار** | `1b9ccd95` | **مسجَّلٌ في §9.7** — حارسُ تطبيقٍ لا منحة. **ليس دليلَ اجتياز.** |
| **`S-06` عولج** | `37d2a337` | **التبعيةُ غيرُ المستعمَلة أُسقطت** من `package.json`. **والبندُ يبقى مفتوحاً في تقرير `f8a2b33c`** — الإغلاقُ يخصّ رأسَ `main` لا الـbaseline. |
| **`LOOPZ-AUD-0003` عولج** | `37d2a337` | `eslint-config-next` صار **16.3.0** مطابقاً لـ`next@16.3.0`. **نفسُ التحفّظ.** |

**وبوّاباتُ الجودة على رأس `main` بعد هذين:** `npm ci` من صفر · `tsc --noEmit`
**صفر خطأ** · `eslint .` **صفر خطأ و16 تحذيراً — وهو عددُ الأساس نفسُه** ·
`next build` نجح. **وفحصٌ حيٌّ بعد النشر:** الرئيسيةُ ترسم، **والخطوطُ
المحمَّلةُ Tajawal وPoppins فقط** (فالمُسقَطةُ لم تكن مستعمَلةً فعلاً)،
**وصورةُ OpenGraph تُبنى 200/PNG** (خطُّها ملفٌّ في `public/fonts` لا الحزمة).

### 🔴 و`S-14` — تصحيحُ معيارِ القبول بدليلٍ جديد

**معيارُ القبول الذي كتبتُه («ترقيةُ `eslint` إلى إصدارةٍ مدعومة») غيرُ
قابلٍ للتنفيذ اليوم — جرّبتُه فسقط.** `eslint@10.9.1` مثبَّتاً مع
`eslint-config-next@16.3.0` **يُفشل التدقيق كلَّه**:

```
TypeError: Error while loading rule 'react/display-name':
contextOrFilename.getFilename is not a function
```

**والسببُ بنيويّ**: إضافاتُ `eslint-config-next` (`eslint-plugin-react` ·
`eslint-plugin-import` · `eslint-plugin-jsx-a11y`) **مداها الأقصى `^9`** —
**فالإصدارةُ المهجورةُ هي الوحيدةُ التي يدعمها إعدادُ Next.**

**فالمعيارُ المصحَّح:** **تثبيتٌ موثَّقٌ على `eslint@9` مع تسجيل أنه لا
يتلقّى إصلاحات**، **ومتابعةُ `eslint-config-next` حتى تشحن إضافاتٍ متوافقةً
مع 10** — **والدرجةُ تبقى P2 لأن العجزَ أعلى في السلسلة لا عندنا.**
**أُعيدت التجربةُ ونُقضت في الحاوية، ولم يُشحن منها شيء.**

## سجل الجلسات — Append Only

### 2026-09-01 — ChatGPT

- نقل أحمد خارج دورة الاعتماد اليومية.
- جعل ChatGPT بوابة التحقق وClaude جهة التنفيذ.
- أبقى main وProduction ثابتين حتى بوابة GO.
- اعتمد التنبيه الحدثي عبر GitHub بدلاً من فحص غير مدعوم كل 10 دقائق.

### 2026-09-01 — ChatGPT

- أنشأ فرع `docs/pre-app-audit` والملفات الأساسية.
- لم يعدل كود التطبيق أو main.
- سلّم الدور إلى Claude.

### 2026-09-01 01:18 UTC — Claude

- قرأ الملفات الأربعة بالترتيب.
- نفّذ Phase 0 — Baseline جرداً محضاً: نقطة الرجوع، الفروع، الإصدارات، Scripts، البيئات، آخر Deploy، الأدوار، الموانع، وأوامر Phase 1 المقترحة.
- شغّل بوّابتَي الجودة على نقطة الرجوع: `tsc --noEmit` و`next build` — كلاهما نظيف.
- أضاف `PHASE_0_BASELINE.md`.
- لم يعدّل كود التطبيق ولم يلمس `main` ولم ينشر شيئاً.
- وضع Phase 0 بحالة `READY_FOR_REVIEW` وسلّم الدور إلى ChatGPT.

### 2026-09-01 02:06 UTC — Claude

- نفّذ Phase 1 — Full Inventory على `64bd1d41` جرداً محضاً (13 قسماً).
- صحّح خطأ Phase 0: المسارات 76 لا 81.
- أثبت مطابقة تامّة بين المصدر وجدول البناء، وصفر استدعاء RPC مكسور.
- حسم الفرعين المؤجَّلين بالدليل: أحدهما متجاوَز والآخر مفقود فعلاً.
- لم يعدّل كود التطبيق ولم يلمس `main` ولم يحذف شيئاً ولم ينشئ أي مورد.
- وضع Phase 1 بحالة `READY_FOR_REVIEW` وسلّم الدور إلى ChatGPT.

### 2026-09-01 02:33 UTC — Claude

- أعاد تسليم Phase 1 بعد `CHANGES_REQUESTED` على Audited SHA `f8a2b33c`.
- شغّل البوّابات الأربع على الـSHA المعتمد: `npm ci` · `npm run lint` · `tsc --noEmit` · `npm run build` — كلها exit 0، مع الأزمنة.
- قرأ مسارات API الواحد والعشرين كاملة وأزال كل `UNKNOWN` من جدولها.
- فصل تصنيف البناء عن `dynamic export`، وأضاف حالة `loading/error/not-found` لكل صفحة، وحسم كل تصنيف وصول.
- أكمل ربط خريطة الميزات بالمكوّنات والأفعال والجداول وRPC والتخزين.
- صحّح ثلاثة أخطاء من تسليماته السابقة.
- لم يعدّل كود التطبيق ولم يلمس `main` ولم يحذف ولم ينشئ أي مورد.

### 2026-09-01 02:41 UTC — Claude

- إعادة التسليم الثانية لـ Phase 1: أعاد الأقسام الأربعة الساقطة (§14–§17) دون المساس بما اجتاز.
- لم يُعد تشغيل البوّابات لأن الـAudited SHA لم يتغيّر.
- سجّل أن بنود «استعادة كلمة المرور وتغييرها وتغيير البريد» لا تنطبق على المشروع (Google وحده) وتحتاج شطباً من معايير Phase 2/5.
- لم يعدّل كود التطبيق ولم يلمس `main` ولم يحذف ولم ينشئ أي مورد.

### 2026-09-01 03:02 UTC — Claude

- نفّذ Phase 2 كزائر حقيقي في المتصفّح المدمّج، بعد إثبات أن متصفّح أحمد مسجَّل دخوله فلا يصلح.
- غطّى 49/49 صفحة بحالة صريحة، وأغلق مانع Phase 0 الثالث بإثبات أن Production يشغّل الـAudited SHA.
- سجّل تسع مشكلات `C-01`…`C-09`، ثلاث منها بجذر واحد.
- أثبت الكتم الافتراضي في الترايلرات، ووسم ستّة بنود `BLOCKED_BY_SURFACE` بدل ادّعائها.
- فحص ادّعاءً بدا عطلاً فثبت أنه سليم، وسجّله.
- لم يسجّل دخولاً ولم يلمس مساراً كاتباً ولم يعدّل كوداً ولم ينشئ مورداً.

### 2026-09-01 03:12 UTC — Claude

- أعاد تسليم Phase 2: مصفوفة 49 صفّاً بالحقول · قياس 768 · إزالة «PASS جزئياً» من الترايلرات.
- سجّل `LOOPZ-AUD-0014`…`0022` في سجل المشكلات.
- أثبت أن الدليل البصري `BLOCKED` بثلاثة أسطح مجرَّبة وثلاثة أخطاء حرفية، وقدّم ثلاثة خيارات بقرار موصى به.
- لم يستخدم جلسة أحمد ولم يسجّل خروجه ولم يكلّفه بشيء.

### 2026-09-01 03:30 UTC — Claude

- صحّح مصفوفة الـ49: صفّ لكل `page.tsx` بلا دمج، والvariants إلى جدول خارج العدّ، وحذف الجدول القديم المتعارض.
- أدرج `LOOPZ-AUD-0023` (P1) بحرف بلاغ ChatGPT، ولم ينسبه لنفسه.
- صحّح قراءته الخاطئة: «الكتم الافتراضي ناجح» صار قياساً صحيحاً بخلاصة مضلِّلة، وحكم الترايلرات صار `FAIL`.
- أنشأ Visual Verification Gate بخمسة عشر بنداً، صفر منها `PASS`.
- لم يصلح كوداً ولم يلمس `main` ولم يستخدم جلسة أحمد ولم يكلّفه بشيء.

### 2026-09-01 03:50 UTC — Claude

- نفّذ Phase 3 (التصميم والهوية والمحتوى والجولات والوصول الثابت) على `f8a2b33c` بتحليل المصدر.
- أوقف ثلاثة بلاغات كاذبة بالفحص قبل رفعها.
- سجّل عشرة مرشّحات `D-01`…`D-10`، ثلاثة منها P2.
- أثبت تكافؤ القاموسين 1278/1278 بصفر نقص، وحدّد أن `0017` عند حدّ GIS لا في القاموس.
- غطّى 49/49 مساراً وربط كل حكم بصريّ ببند من Visual Verification Gate.
- رحّل `0023` والبوّابة بلا تخفيف، ولم يصلح كوداً ولم يلمس `main`.

### 2026-09-01 04:12 UTC — Claude

- استكمل Phase 3 بعد `CHANGES_REQUESTED`: جدول تدقيق الرموز والمكوّنات · خريطة الهوية من المصدر · مصفوفة المحتوى · أجوبة الجولات · دليل `D-10`.
- سجّل تسعة مرشّحات جديدة `D-11`…`D-19`، أخطرها `D-11` (أصفران وأسودان، وثيم `loopz` يخالف قراره D-454).
- أغلق سؤالاً مفتوحاً من Phase 2 بدليل المصدر: صفحة الإشعارات لا توهم بالحفظ → اقتراح إغلاق `0009`.
- صحّح خطأ التوقيت في السجل، وأقرّ بسببه: استبدال لم يطابق ولم يُتحقَّق من أثره.
- سجّل `0024`…`0028` ورحّل `0023` والبوّابة بلا تخفيف.

### 2026-09-01 04:42 UTC — Claude

- نفّذ Phase 4 (الأداء والسرعة والاستجابة) على `f8a2b33c` — مسار B كاملاً، **ومسار A محجوب بكامله بقرار منهجي**.
- التزم القاعدة الصارمة: **صفر رقم زمنيّ في التقرير**، وكل ما هو زمن مكتوب `BLOCKED_BY_SURFACE` بسببه المرصود.
- سجّل سبعة مرشّحات `P-01`…`P-07`، اثنان منها P2 ولا P1 جديد.
- أثبت شحن القاموسين معاً بثلاثة مصادر مستقلّة، وربطه ببند مفتوح أصلاً في `18_Project_Context.md`.
- سجّل خمسة مواضع انضباط مقيسة، وأوقف ادّعاءً كاذباً سادساً بالفحص.
- نقل قرار ChatGPT في Phase 3 وأرقام `0029`…`0034` إلى سجل المشكلات.
- سجّل نتيجة §17 بوصفها `review question resolved — NOT_APPLICABLE / no defect` بلا إنشاء خانة `0009`.
- صحّح سطراً خاطئاً في مسوّدته قبل الرفع: `P-06` لا يخصّ البوّابة البصرية.
- حدّث قسم «الدور الحالي» الذي كان ما زال بأوامر Phase 0.
- لم يعدّل كوداً ولم يلمس `main` ولم يستخدم جلسة أحمد ولم يكلّفه بشيء.

### 2026-09-01 05:22 UTC — Claude

- أعاد تسليم Phase 4 بعد `CHANGES_REQUESTED`: التقرير من 12 قسماً إلى 18، بلا مساس بما اجتاز.
- أقرّ بالخطأ الجوهريّ: «الطلبات المكرّرة مقيسة» كانت جرداً سُمّي قياساً — وصُحّح وفُصل إلى `MEASURED` و`BLOCKED_BY_SURFACE`.
- قلب تشخيص `P-02` بالقياس المباشر: الأرضية المُنزَّلة 658.8 kB على تسعة مسارات، و`/features` تشحن صفر kB يخصّها.
- سمّى `P-07` بمسار بياناته وعدد عناصره: `/person/[id]` 219 بطاقة و547.5 kB.
- سجّل `P-08`: ستّ عائلات خارجية بلا مهلة، خمس منها على مسار رسم.
- **سحب `P-06` من مرشّحاته** بعد أن أثبت الفحص أن كل جلبٍ تابعٍ لمدخل محميّ.
- أوقف أربعة بلاغات كاذبة إضافية في إعادة التسليم وحدها.
- نفّذ قرار ChatGPT في Web Vitals: لا فتح للوحة، والأزمنة محجوبة، والقياس الصالح بوّابة قبل الـGO.
- لم يعدّل كوداً ولم يلمس `main` ولم يستخدم جلسة أحمد ولم يطلب صلاحية.

### 2026-09-01 05:41 UTC — Claude

- إعادة التسليم الثانية لـ Phase 4 بعد `CHANGES_REQUESTED` ثانٍ: ثلاثة تصحيحات توثيقية محدودة بلا إعادة ما اجتاز.
- وحّد ترقيم الأقسام والإحالات إلى `11.1`…`15.3` وفحصه آلياً: صفر إحالة معلَّقة.
- أعاد بناء مصفوفة API إلى واحد وعشرين صفّاً، صفٌّ لكل endpoint، بالأعمدة الستّة ودليل المصدر، و`/robots.txt` داخل العدّ.
- تتبّع مسار استدعاء كل عائلة خارجية بلا مهلة وصنّفها: ثلاث `DIRECT_RENDER_PATH` وثلاث `API_OR_ACTION` وصفر مجدوَلة — **والعدد الصحيح ثلاث لا خمس**.
- أقرّ بتجاوزين في الصياغة فوق الدليل وصحّحهما: «لا مسار يقبل حدّاً مفتوحاً» وعدد عائلات مسار الرسم.
- أضاف أبعاد `loopz-cover.jpg` (1600×533) من رأس SOF0.
- لم ينفّذ POST كاتباً ولم يعد القياسات المحجوبة ولم يطلب صلاحية.

### 2026-09-01 07:46 UTC — Claude

- 🔴 أغلق Pull Request رقم 19 بالخطأ عند 05:22 UTC: اختار «أوّل زرّ إرسال في النموذج» فكان `Close with comment` لا `Comment`.
- التعليق نُشر صحيحاً، والفرع ورأسه والتعليقات الأربعة والعشرون لم تتأثّر، ولم يُمسّ كود ولا `main`.
- أُعيد فتح الـPR عند 07:43 UTC بإذن أحمد الصريح — إعادة فتح PR تغييرُ حالة، وصلاحية GitHub للقراءة فقط.
- طبّق الدرس: اختيار الأزرار بمطابقة النصّ الحرفيّ وحدها، والتحقّق من الحالة بعد كل فعل مغيِّر.
- لا تغيير في مضمون Phase 4؛ الحالة تبقى `READY_FOR_REVIEW` على Commit `117c2436`.

### 2026-09-01 07:58 UTC — Claude

- إعادة التسليم الثالثة لـ Phase 4: تصحيح إحالات فقط، بلا مساس بتحليل أو قياس.
- أقرّ بسقوط ادّعائه «صفر إحالة معلَّقة»: فحصه أثبت وجود الهدف لا صحّته.
- صحّح تسع إحالات — واحدة ضبطها ChatGPT وثماني كشفها الفحص الدلاليّ — وسجّل السبب الجذريّ: إزاحة ترقيم شاملة طُبّقت على إحالات كانت أصلاً بالترقيم الجديد.
- سجّل نتيجة الفحص الأخير في §18 من التقرير: 42 إحالة · 37 داخلية مطابقة · 5 خارجية مُعلَّمة · صفر معلَّقة.
- صرّح باسم الملف في الإحالة إلى Phase 3 رفعاً للالتباس.
- لم يغيّر حالة الـPR ولم يعد ما اجتاز ولم يلمس `main`.

### 2026-09-01 08:04 UTC — Claude

- **Phase 4 اعتُمدت `VERIFIED`** على Commit `6a38d75a` بعد ثلاث إعادات تسليم.
- نقل أرقام `0035`…`0039` إلى سجل المشكلات، وسجّل ما لم يُرقَّم وسببه (`P-03` تابع لـ`0023` · `P-05` مقايضة موثّقة · `P-06` مسحوب).
- بدأ Phase 5 — Security & Protection ضمن حدود `Static / Read-only / Passive` الإلزامية.
- سجّل الحدود والممنوعات ومفردات الحالة قبل أي فحص، ليُحاسَب عليها لا عليّ بعدها.

### 2026-09-01 09:14 UTC — Claude

- سلّم Phase 5 بحالة `READY_FOR_REVIEW`، ورفع البلاغ مصغَّراً بلا تفصيل لأن المستودع عامّ.
- عرض على أحمد أوامر السحب وبيّن أن تشغيلها يخالف ثلاث قواعد قائمة، وخيّره — فأذن صراحةً، فنُفِّذ سحبُ صلاحيات تنفيذ لا غير.
- اكتشف بالتحقّق بعد التنفيذ أن وراثة `PUBLIC` تُبطل السحب الموجَّه، وسجّله `S-05`.

### 2026-09-01 09:36 UTC — Claude

- استلم مراجعتَي ChatGPT: Phase 5 `CHANGES_REQUESTED` بثمانية نواقص، والمعالجة `CHANGES_REQUESTED` بستّة اعتراضات.
- **قبل الحكم كاملاً**: المعالجة لا تُعتمد ضمن Phase 5، والإذن الخاصّ غير قابل للتحقّق من أدلة الـPR.
- **أوقف أي تغيير إضافي على Production**، ولم ينفّذ rollback ولا `GRANT` ولا `REVOKE` ولا Migration.
- **نقّح رأس الفرع العامّ**: حذف ملفّ SQL الأمنيّ، واستبدل تفصيل §4.3 بتجميع فئويّ آمن، وحجب أسماء مجموعة `search_path` والعرض المميَّز. بلا إعادة كتابة تاريخ Git.
- أقرّ بخطأ في الحكم لا في القياس: «الإفصاح بعد الإصلاح» تخصّ ثغرةً مغلقةً بذاتها لا حالةً من صنفٍ مفتوح.
- صحّح استنتاج `service_role` إلى الصياغة الأدقّ: لا تجاوز بمفتاح service-role في كود التطبيق، ومسار `SECURITY DEFINER` قائم وهو `LOOPZ-AUD-0040`.
- أجّل درجتَي `S-03` و`S-04` حتى يكتمل دليلهما بدل تثبيت درجة بلا إثبات.
- خفّض ادّعاء التحقّق الوظيفي إلى `BLOCKED_BY_TEST_ENV`: خمسة `GET` لا تثبت سلامة الـRPCs ولا صلاحيات المسجَّل.
- `LOOPZ-AUD-0040` تبقى مفتوحة P1 بشروطها الأربعة.

### 2026-09-01 10:02 UTC — Claude

- استكمل نواقص التحليل الثابت التي طلبها ChatGPT (§9 في التقرير): كلّه بيانات وصفية ومصدر، صفر استدعاء دالّة وصفر تغيير.
- §9.1 مصفوفة الوصول لكل الجداول الواحد والسبعين: صفر سياسة كتابة غير مقيَّدة بالمالك — كل خطر الكتابة منزاح إلى طبقة الدوالّ. (العدد صُحِّح لاحقاً إلى **73 جدولاً و128 سياسة**.)
- §9.2 صنّف الدوالّ القارئة بلا حارس: 46 قارئة، منها 17 تلمس جداول مستخدم وواحدة فقط تحترم is_private — فحتى 16 قد تتجاوز خصوصية القراءة (رأس LOOPZ-AUD-0040 المفتوح).
- §9.3 دورة الجلسة والحذف: الحذف محكَم (دالّة ممنوحة لـauthenticated وحده)، و«خروج من كل الأجهزة» فجوة ميزة لا عطل.
- §9.4 لا مجلّد .github ولا workflows؛ vercel.json بلا أسرار؛ إعدادات المشروع BLOCKED_BY_TEST_ENV.
- §9.5 تبعية غير مستخدمة واحدة (S-06). §9.6 حسم S-03 وS-04 بالدليل: لا دور غير مميّز يملك CREATE على public فS-04 نظافة لا ثغرة، والعرض حقول عامّة فS-03 ملاحظة تصميم.
- صحّح تناقضات السجل الثلاثة التي ضبطها ChatGPT (رأس الحالة، فقرة «النطاقات الثمانية»، استنتاج service_role).
- LOOPZ-AUD-0040 تبقى P1 مفتوحة، ومانعها الوحيد المتبقّي LOOPZ-AUD-0006 (بيئة معزولة).
- لم يُجرِ أيّ تغيير على Production، ولم يبدأ Phase 6.

## قرارات أحمد

| التاريخ | القرار | النطاق | الملاحظات |
|---|---|---|---|
| 2026-09-01 | أحمد خارج دورة الاعتماد التشغيلية | كامل التدقيق | ChatGPT يراجع وClaude ينفذ |
| 2026-09-01 | العمل الذاتي مع حواجز فنية | الفروع وPreview | main وProduction بعد بوابة GO |
| 2026-09-01 | إنشاء مساحة متابعة GitHub | docs/pre-app-audit | مرجع مشترك |
| 2026-09-02 | **`partner_details` يُملأ يدويّاً من لوحة Supabase** | بيانات الشركاء | **إفادةُ المالك رداً على سؤال Phase 7 — فالجدول `USED` لا ميّت.** والتبعاتُ الأربعُ في `PHASE_7_CODE_QUALITY.md` §6.3.1 و`P7-11` |

### 2026-09-01 17:20 UTC — Claude

- استوفى نواقص مراجعة 14:23 UTC الستّ. **كلّه بيانات وصفية ومصدر وسجلّات: صفر استدعاء دالّة تطبيقية · صفر كتابة · صفر DDL · صفر تغيير صلاحيات · صفر Deploy.**
- §9.1 صارت **128 صفّاً، صفّاً لكل سياسة** بـroles وcmd وUSING وWITH CHECK، **ومعها عمود المستهلِك** من الشيفرة، **و15 جدول صفر-سياسة مسمّاة**. أسماء الجداول صريحة لأن 173 ملف SQL في المستودع تحملها، وأسماء الدوالّ تبقى محجوبة لأنها سطح RPC.
- **بندان جديدان خرجا من العمل الصفّي نفسه:** `S-12` (خريطة المتابعة تُقرأ true لكل مصادَق، وعَلَم hide_follow_lists عمود لا شرط — صنف S-03 نفسه) و`S-13` (دالّتا عدّادات تشغيلية متاحتان للزائر بلا مستدعٍ في الشيفرة).
- §9.2 صارت صفّاً لكل FR-nn. **ونفيان بدليل:** صفر SQL ديناميكي في الـ130 المتاحة للزائر، وصفر قارئة تنادي كاتبة — وهما اعتراضاك الحرفيّان.
- 🔴 **تصحيح عدد نشرتُه:** «21 قارئة تلمس جداول مستخدم» **خطأ — الصحيح 32**؛ قائمة جداول المطابقة كانت أقصر من الواقع.
- 🔴 **تصحيح منهج:** أول regex كتبتُه اليوم صنّف دالّة كاتبة قارئةً لأن اسماً مستعاراً يفصل الجدول عن `set`. ضُبط وأُعيد التصنيف كلّه. النتيجة المنشورة لم تتأثر، والمنهج تأثر.
- **وتضييق رأس 0040 لا تخفيفه:** «حتى 16 قد تتجاوز الخصوصية» كان تعميماً؛ الفرز الصفّي يقول **تسع دوالّ تُخرج بيانات عضو بمعرّفه** (صُحِّح لاحقاً إلى **سبع** في جولة 17:45 — انظر السجل)، والباقي تجميع فوق الجمهور أو جواب بولياني أو محروس بـis_public. **P1 كما هي.**
- §9.5 اكتملت: **المانع ارتفع** — سجلّ npm صار مقروءاً. **صفر تبعية مهجورة وصفر متروكة الصيانة** في الثمانيَ عشرةَ، بتاريخ آخر نشر لكلٍّ منها وضبط صحّة على حزمة مهجورة معروفة. **وبند جديد `S-14`:** أربع تبعيات تطويرية متأخّرة إصدارةً رئيسية أو أكثر.
- §9.6 `S-04` بأدلّته الأربعة: aclexplode على كل مخطَّط · **TEMP ممنوح فعلاً لـanon وauthenticated — وهذا ما لم أوثّقه سابقاً** · المسار الفعّال بصيغه الست · **549 مرجع علاقة، صفر منها غير مؤهَّل إلى جدول public**. **وأسحب صياغة «ممتنع بالصلاحيات» — الصواب: ممكن ومُبطَل بالتأهيل**، فيتحوّل إلى ثابت واجب الحفظ بفحص آلي قبل الدمج.
- §9.7 دلتا الانحدار صارت قابلة للتحقق: SHA `1b9ccd95` ورابطه · السطح المتأثر · **وحالة الإنتاج الآن مغلقة**: آخر ساعة بأخطاء 11:00 UTC، **وصفر منذ 12:00 UTC** بالاستعلام الساعيّ لسجلّات Postgres · **والسحب قائم لم يُعكس** (EXECUTE لـanon = false، ولـauthenticated = true).
- **لم يُمسّ Audited SHA `f8a2b33c` ولا كود التطبيق من هذا الفرع، ولم يبدأ Phase 6.**

### 2026-09-01 17:45 UTC — Claude

- عالج تصحيحات الاتّساق الثلاثة في مراجعة 17:22 UTC. **توثيقٌ فقط: صفر كود · صفر SQL · صفر Deploy.**
- **1) عددُ القارئة الخاصّة بعضو: تسع ← سبع.** كتبتُ «تسع» وسردتُ سبعاً، وحسبتُ FR-17/FR-18 وهما تجميعيّتان وضعتُهما بنفسي تحت S-13. **رأسُ 0040 = سبعُ قراءاتٍ بمعرّف + FW-05 + V-01.**
- **2) الملخّصاتُ وُحِّدت مع §9 النهائي:** §8.1 (S-07 خمس ← **ست**) · §8.1.1 (46/17 ← **109/32 وسبعٌ بمعرّف**) · §10 (تعليلُ سقوط S-04 صار **التأهيل** لا منعَ CREATE، مع إثبات TEMP=true). **والسجلّ append-only لم يُمسّ.**
- **3) 🔴 فحصُ الإهجار كان على أحدث إصدارٍ لا على المثبَّت — والاعتراضُ أصاب صيداً:** أُعيد بصيغة `npm view <pkg>@<المثبَّت> deprecated` للثمانيَ عشرةَ، بضبطِ صحّةٍ مزدوج (`react@16.0.0` فراغ · `request@2.88.0` نصُّ إهجار). **والنتيجة: `eslint@9.39.5` — المثبَّتُ نفسُه — مهجورٌ صراحةً** («this version is no longer supported»)، **والصيغةُ الأولى كانت ستمرّره صامتاً.**
- **S-14 رُفعت P3 ← P2:** ليست تأخّرَ إصدارةٍ بل **مُدقِّقٌ مهجورٌ في سلسلة أدوات البناء** — لا يتلقّى إصلاحاتٍ ولا تصحيحاتِ قواعد. تطويريٌّ لا يُشحن، **فلا أثرَ على حزمة الإنتاج.**
- لم يُمسّ Audited SHA `f8a2b33c`، ولم تبدأ Phase 6.

### 2026-09-01 21:30 UTC — Claude

- **أنشأ Delta Queue** الذي أمرتَ به في 01:43 ولم يكن موجوداً: **32 التزاماً بعد `f8a2b33c` · 30 ملفاً · صفر ملفٍّ في `supabase/` · و16 تحمل وسمَ `[deploy]`.**
- **جرّب معيارَ قبول `S-14` فسقط**: `eslint@10` مع `eslint-config-next@16.3.0` يُفشل التدقيقَ كلَّه (`react/display-name` — `contextOrFilename.getFilename is not a function`)، **لأن إضافاتِ إعدادِ Next مداها `^9`**. **فالمهجورُ هو الوحيدُ المدعوم**، والمعيارُ صار تثبيتاً موثَّقاً ومتابعةً أعلى السلسلة. **التجربةُ نُقضت في الحاوية ولم يُشحن منها شيء.**
- **عولج `S-06` و`LOOPZ-AUD-0003` على `main`** (`37d2a337`) ببوّاباتٍ كاملة: `npm ci` من صفر · `tsc` صفر خطأ · `eslint` صفر خطأ و**16 تحذيراً وهو عددُ الأساس** · `next build` نجح · وفحصٌ حيٌّ بعد النشر (الخطوطُ المحمَّلةُ Tajawal وPoppins فقط · OG تُبنى 200/PNG). **والبندان يبقيان مفتوحَين في تقرير الـbaseline** — الإغلاقُ يخصّ رأسَ `main`.
- **وقاعدةُ `eslint` جديدةٌ في 16.3.0** رفعت تحذيراً على تحميلٍ كاملٍ بعد حذف الحساب; **السلوكُ مقصود** (`router.push` يُبقي مخبأَ موجِّهٍ لحسابٍ محذوف)، **فوُثِّق استثناءً بسطرٍ يشرح السبب** لا بإسكاتٍ عامّ.
- **لم يُمسّ Audited SHA `f8a2b33c` ولا فرعُ التدقيق بكود، ولم تبدأ Phase 6، ولا SQL ولا صلاحيات ولا rollback.**

### 2026-09-01 22:10 UTC — Claude

- عالج التصحيحات التوثيقية الستّ الأخيرة. **توثيقٌ فقط: صفر كود · صفر SQL · صفر Deploy.**
- **1)** «تسع دوال قراءة» ← **سبع** في §9.8 (كان قد صُحِّح في §9.2 وبقي هنا).
- **2)** سببُ سقوط `S-04` في §8.1 صار **تأهيلَ أسماء الجداول** لا منعَ `CREATE`، **مع تثبيت أن `TEMP` ممنوح** — ومعه معيارُ قبولٍ صريح (فحصٌ آليٌّ قبل الدمج).
- **3)** سطرُ التنبيهات «5 دوالّ بلا `search_path`» **لم يعد يُقرأ حكماً**: الجردُ المباشر يقول **170/170 مثبَّتة**، والمصدرُ الأحدثُ هو الجرد لا التنبيه.
- **4)** الأرقام وُحِّدت في §1 و§5 و§8.2 (والسجلُّ append-only بقي كما هو مع إشارةِ تصحيح). **⚠️ ووُحِّدت يومَها على «73» وهو خطأ — صُحِّح إلى 72 في جولة 22:45، انظر أدناه.**
- **5)** `LOOPZ-AUD-0005` نزلت من `P1-CANDIDATE` إلى **P3**: **دَينُ احتفاظٍ بنسخةٍ زائدة لا انكشافٌ مباشر** — **وتبقى واجبةَ الحذف وداخلَ نطاق `0040`.**
- **6)** 🔴 **سحبتُ ادّعاءً كتبتُه**: «16 بناءَ إنتاجٍ فعليّاً» **غيرُ مثبَت** — الوسمُ يعني أن بناءً طُلِب لا أن نشراً نجح، **و`list_deployments` ما زال 403** فلا سجلَّ نشرٍ عندي. **والمُثبَتُ حيًّا وحدَه**: شيفرةُ D-866…D-869 عاملةٌ على الموقع، **فبعضُ البناءات نجح قطعاً وعددُها غيرُ معلوم.**
- لم يُمسّ Audited SHA `f8a2b33c`، ولم تبدأ Phase 6.

### 2026-09-01 22:45 UTC — Claude

- عالج السطرَ التوثيقيَّ الأخير قبل اعتماد Phase 5: **جدولُ §4.1** كان ما زال يعرض 71/126.
- 🔴 **وأثناء تصحيحه ظهر خطأٌ أكبر — في رقمٍ كنتُ أنا نشرتُه، والمراجعُ كان سيعتمد عليه:** **«73 جدولاً» خطأ · والصحيح 72.** **عددتُ العرضَ `public_profiles` جدولاً وهو `relkind='v'` لا `'r'`** — **و73 عددُ العلاقات لا الجداول.** صُحِّح في §1 و§5 و§8.2 و§9.1 وفي رأس هذا الملفّ.
- **و«84 سياسة تستعمل `auth.uid()`» كانت ناقصةً لا خاطئة**: عدَّت `USING` وحدَها فأسقطت سياساتِ `INSERT` التي لا `USING` لها. **والعدُّ على الشرطين: 119 من 128** (85 في `USING` · 54 في `WITH CHECK`).
- **وجدولُ §4.1 أُعيد بناؤه من جردٍ مباشر**، وأُضيف إليه صفٌّ لم يكن: **9 سياساتٍ بلا `auth.uid()` إطلاقاً — وكلُّها `SELECT`**: ثلاثةُ جداولِ كتالوج · أسماءٌ موثَّقة · قائمتان عامّتان بعَلَمهما · صفُّ الشريك للإدارة · **وسياستا `true` وهما `S-12`**. **فصفر سياسةِ كتابةٍ بلا `auth.uid()`** — والخلاصةُ البنيويّة لم تتغيّر.
- **لم يُعَد التحليل، ولم تُمسّ Production، ولا الشيفرة، ولا Audited SHA.**

### 2026-09-02 00:20 UTC — Claude

- **Phase 6 مُسلَّمة** — `PHASE_6_DATA_PRIVACY.md`. **فحصٌ ثابتٌ وآمنٌ فقط كما أُمرت: صفر SQL كاتب · صفر DDL · صفر Migration · صفر صلاحيات · صفر حذف · صفر Deploy · صفر تغييرٍ على Production.** وكلُّ استعلام قراءةُ بياناتٍ وصفيّةٍ أو عدُّ صفوف، والمنهج معلَنٌ في رأس الملفّ.
- **لا تكرار لما مضى:** RLS والسياسات والعروض ودوالّ DEFINER وsearch_path والتخزين تُحال إلى Phase 5 §4 و§9.
- 🔴 **`P6-01` · P1 — أخطر ما وجدته في المرحلة، وليس في الشيفرة:** المشروع على الخطّة المجّانيّة، **والنسخ اليوميّة لخطط Pro فأعلى بنصّ توثيق Supabase**، **فلا نسخة احتياطية واحدة: RPO = RTO = ∞** — ولا PITR، ولا نسخ قابلة للتنزيل، **وتجربة الاستعادة `BLOCKED_BY_TEST_ENV`**. ويضاعف كل خطر آخر: `0040` و`FW-05` يصيران غير قابلَين للتراجع، **وخطة الرجوع عن هجرة خاطئة لا يمكن كتابتها أصلاً**.
- 🟠 **`P6-02` · P2** — `watched_episodes_backup_133` **بلا مفتاح أولي وبلا مفتاح أجنبي على user_id**: الجدول الوحيد الذي يحمل بيانات عضو ولا يرتبط به، **فصفوفه تنجو من حذف الحساب**. كامنٌ لا واقع: عضوان، وصفر صفٍّ لمستخدم محذوف. **ويرفع `LOOPZ-AUD-0005` من دَين احتفاظ إلى عيب في حق المحو** — ولا يناقض تخفيف درجته (ذاك عن الانكشاف، وهذا عن الحذف).
- 🟠 **`P6-03` · P2** — تنزيل البيانات يغطّي **11 جدولاً من 47**؛ تغيب تقييمات الحلقات والمراجعات والمنشورات والمجتمعات والمشاركات والاشتراك والشراكة. **والسبب البنيوي: الحذف يعتمد على المخطَّط فسلم، والتصدير على قائمة يدوية فسقط.**
- 🟠 **`P6-04` · P2** — القاعدة في `ap-south-1` (مومباي) لمنتَج موجَّه للسعودية، **ولا ذكر لمكان المعالجة ولا لمدة الاحتفاظ في سياسة الخصوصية**. **ولستُ محامياً — وصفُ واقعٍ لا رأي**، ومعيار القبول مراجعةُ مختصّ. **وترتيب البندين: النسخ أولاً، فالنقل مستحيل بلا نسخة.**
- 🟢 `P6-05`/`P6-06`/`P6-07` — سجلّ الأخطاء بلا احتفاظ · 25 مفتاحاً أجنبيّاً بلا فهرس (**والأثر على الحذف لا القراءة**) · 119 سياسة تُعيد تقييم الهوية لكل صفّ.
- ✅ **وما ثبت انضباطه:** 98 من 101 مفتاح بـCASCADE، **والثلاثة `SET NULL` صوابٌ لا سهو** (سجل المكافآت يبقى وصاحبه يُجهَّل) · حذف الحساب أمرٌ واحد يجرّ الباقي · **سجلّ أخطاء العميل مجهول الهوية بنيويّاً** (لا عمود مستخدم، وصفر نمط تسريب في 1610 صفّاً) · المخزنان مفصولان صحيحاً · ولا جمع موقع جغرافي إطلاقاً.
- **لم تبدأ Phase 7.**

### 2026-09-02 01:10 UTC — Claude · تصحيحُ خللٍ في المتابعة لا في التحليل

- 🔴 **فاتني اعتمادُ Phase 5 المنشورُ في 19:05 UTC** — **وسلّمتُ Phase 6 في 19:27 أطلب أرقاماً كانت قد صدرت.** السببُ: **كنتُ أتصفّح تعليقاتِ الـPR بالتخمين** (صفحةً صفحةً بحجمٍ واحد) فتجاوزتُ صفحتَه، **ثمّ قلتُ «لم يردّ» — وهو نفيٌ لم أتحقّق منه.**
- **وهو نمطُ اليوم نفسُه:** «21 قارئة» · «تسع دوال» · «16 نشراً» · «73 جدولاً» — **ادّعاءٌ فوق الدليل، هذه المرّة في المتابعة لا في القياس.**
- **العلاجُ المعتمَد من الآن:** **لا أقول «لم يردّ» إلّا بعد قراءة آخر صفحةٍ فعليّاً** (جلبُ آخر التعليقات بترتيبها لا بتخمين رقم الصفحة)، **وأفحص الـPR قبل كلِّ تسليمٍ وبعده لا عند الحاجة فقط.**
- **وسُجِّلت الأرقام `0041`…`0053` أعلاه**، و`S-04` ثابتاً بلا رقم.
- **وتنبيهُ اختلافٍ عن أمره في Phase 6:** أمر بنقل `LOOPZ-AUD-0005` **بوصفه P3 retention debt**؛ **وتقريري يرفعه** — لا لأنّه انكشاف، بل لأن **الجدول بلا مفتاحٍ أجنبيٍّ فصفوفُه تنجو من حذف الحساب** (`P6-02`). **التوصيفُ الذي أمر به صحيحٌ للانكشاف، والزيادةُ في المحو.** والقرارُ له.


### 2026-09-01 21:25 UTC — Claude · Phase 6 · التسليمُ الثاني (تصحيحُ المحاور الخمسة)

> **التوقيتُ من الآن بمنطقةٍ صريحة:** `UTC` أوّلاً وبينهما فرقُ ثلاثِ ساعاتٍ عن الرياض. **والقيدُ الذي كسرتُه سابقاً: لا أكتب وقتاً في المستقبل ولا وقتاً بعد وقتِ الالتزام.**

- **المحور ١ — `P6-01` أُعيدت صياغتُه، والحكمُ الأوّل كان أوسعَ من دليله.** كنتُ أنفي وجودَ النسخ (`RPO=RTO=∞`) **وأنا نفسي أقررتُ أنّي لم أفتح إعداداتِ النسخ**. الصياغةُ الجديدة **تفصل خمسةَ بنود**: نسخةُ المزوّد اليوميّة (**كامنةٌ لمشاريع الخطّة المجّانيّة — «up to 7 daily backups that will be available for you once you upgrade»**) · مسارٌ متاحٌ للفريق (**لا**) · تصديرٌ خارجيٌّ يملكه الفريق (**لا دليل — صفر سكربتٍ في `scripts/` و`supabase/` و`vercel.json`**) · **كائناتُ التخزين (خارجَ نسخِ القاعدة أصلاً بنصِّ التوثيق — حتّى بعد الترقية)** · وتجربةُ استعادة (`BLOCKED_BY_TEST_ENV`). **و`RPO`/`RTO` = `UNDEFINED / NOT_VERIFIED`.** **والدرجةُ تبقى P1** — لا لأنّ النسخَ معدومة، بل لأنّ **مسارَ الاستعادة غيرُ متاحٍ ولا مُختبَر**.
- **المحور ٢ — الملاحقُ الصفّيّةُ الثلاثةُ أُضيفت** (ملحق أ · ب · ج)، **وفيها استعلامُ كلِّ رقمٍ فوقه** فيُعاد العدُّ ولا يُصدَّق. **وصفر صفٍّ من بيانات الأعضاء فيها.**
- 🔄 **وخطآن من صنعي ظهرا وأنا أبني الملاحق:** **«٤٧ جدولَ بياناتِ عضو» صوابُها ٦٠** (٥٧ بمفتاحٍ أجنبيٍّ إلى `auth.users`/`profiles` · ١ بعمودِ هويّةٍ بلا مفتاح · ٢ عبر الأب) — **والسببُ أنّي عددتُ بلا تعريفٍ مكتوب، والتعريفُ الآن في رأس الملحق.** **و«٤٦ جدولاً يجرّها الحذف» صوابُها ٥٤ مباشرةً.** **فنسبةُ `P6-03` تصير ١١ من ٦٠ لا من ٤٧ — والفجوةُ أوسعُ ممّا قلت.**
- **المحور ٣ — مصفوفةُ المزوّدين (§4.8).** **وعبارةُ «لا يُخزَّن عندهم» سُحبت**: كانت تصف تطبيقَنا لا مزوّدينا، **و«نحن لا نخزّن ردَّهم» ≠ «هم لا يخزّنون طلبَنا»**. **وثلاثةُ مساراتٍ تخرج فيها بياناتٌ فعلاً وأُثبتت من الشيفرة**: **نصُّ العضو الحرّ إلى Gemini** · **ومتونُ منشوراته إلى DeepL** · **و`IP` جهازِه إلى YouTube** — **ولا تُذكر في سياسة الخصوصيّة، فأُضيفت إلى معيار قبول `P6-04`.** **والاحتفاظُ عند كلِّ مزوّدٍ `UNKNOWN` مُعلَنٌ لا مُخمَّن.**
- **المحور ٤ — المراقبةُ وبروتوكولُ الرجوع (§5).** **المراقبة:** نملك `runtime_errors` **ولا نملك جرساً واحداً** — صفرُ تنبيهٍ وصفرُ حصّةٍ مراقَبةٍ وصفرُ معدّلِ أخطاءٍ محسوبٍ وصفرُ مراقبةِ تخزين، **وسجلّاتُ Vercel محجوبةٌ (`403`)** وهي سببُ إنشاء الجدول أصلاً ⇢ `LOOPZ-AUD-0050`. **والبروتوكول:** `pre-check` + `snapshot` + **`rollback artifact` إلزاميٌّ قبل كلِّ هجرة** + عتباتُ إجهاض + `post-check` + **منعُ التنفيذ بلا بيئةٍ معزولة** — **مكتوبٌ غيرُ مُختبَر، واختبارُه `BLOCKED_BY_TEST_ENV` ولا أسمّيه ضماناً.**
- **المحور ٥ — التاريخ.** رأسُ التقرير ورأسُ هذا الملفّ **صارا وقتاً واحداً فعليّاً بمنطقةٍ صريحة**، **ورأسُ الحالة والدورُ صُحِّحا**، **وبندُ «المطلوب من Claude» كان ما يزال يقول Phase 5 و«لا تبدأ Phase 6» فصار Phase 6 و«لا تبدأ Phase 7».**
  > 🔄 **والتزامٌ تابعٌ صحّح التوقيت نفسَه:** كتبتُ أوّلاً 21:40/21:45 UTC **ثمّ وقع الالتزام `af82e7f` في 21:25 UTC** — **فكان التوثيقُ مستقبلاً عن الالتزام، وهي المخالفةُ التي ضبطها المراجعُ في الجولة السابقة.** **فأُخذ الوقتُ من `git log` لا من التقدير، ووُحِّد على 21:25 UTC.**
> 🔄 **وثالثةٌ ضبطها المراجعُ في 21:27 UTC:** **قسمُ `## Claude Check-in` كان ما يزال يقول `READY_FOR_REVIEW — Phase 5` ويصف Phase 4 آخرَ معتمَد** — **وهو موضعٌ ثالثٌ للعِلّة نفسِها: كتلةُ «الحالة النافذة» لم تتحرّك مع الرأس.** **صُحِّح في الالتزام `fd0c6b9` · 21:32 UTC، وأُضيف إليه تحذيرٌ مكتوبٌ يفصل «النافذ» عن «التاريخ» فلا تتكرّر رابعةً.**
  > 🔴 **وكتبتُ في ذلك الالتزام «21:35» ووقع في 21:32 — فوقعتُ في الخطأ نفسِه مرّةً ثالثةً في ساعةٍ واحدة.** **والعِلّةُ أنّي كنتُ أقدّر الوقتَ قبل الرفع بدل أن أقرأه بعده.** **والقاعدةُ المعتمَدة الآن: الوقتُ يُكتب من `git log` بعد وقوع الالتزام، لا يُقدَّر قبله.**

- ⚠️ **والسجلُّ التاريخيُّ لم يُمسّ:** مدخلتا **00:20** و**01:10 UTC** أعلاه تحملان الأرقامَ القديمة (٤٧ · `RPO=RTO=∞`) **وتبقيان كما هما — وهذه المدخلةُ تصحيحُهما لا حذفُهما.** **وتوقيتُهما نفسُه كان خطأً: كُتبتا بتوقيت الرياض موسوماً `UTC`.**
- **صفر تعديلٍ على شيفرة التطبيق · صفر SQL كاتب · صفر DDL · صفر Migration · صفر صلاحيات · صفر حذف · صفر Deploy · صفر تغييرٍ على Production.** **وكلُّ استعلامٍ قراءةُ بياناتٍ وصفيّة.**
- **لم تبدأ Phase 7.**

### 2026-09-01 21:58 UTC — Phase 7 مُسلَّمة — Claude

> **الوقتُ يُقرأ من `git log` بعد الالتزام لا يُقدَّر قبله** — القاعدةُ المعتمَدة بعد ثلاثِ مخالفاتٍ في ساعةٍ واحدة.

- **Phase 6 اعتُمدت `VERIFIED`** في 21:36 UTC على `af82e7f` + تصحيحات التنسيق حتى `f5e06d5`، **وأرقامُها `LOOPZ-AUD-0054`…`0060` نُقلت إلى سجلّ المشكلات أعلاه — مفتوحةً كلَّها.**
- **Phase 7 مُسلَّمة:** `PHASE_7_CODE_QUALITY.md`. **تدقيقٌ ثابتٌ وقراءةٌ فقط: صفر حذف · صفر تعديلِ شيفرة · صفر تغييرٍ على `dependencies` أو `config` أو `lockfile` · صفر تثبيتٍ دائم · صفر Deploy.**
- **والفحصُ وقع على الـAudited SHA `f8a2b33c` في شجرةِ عملٍ منفصلة لا على رأس فرع التوثيق** — **لأنّ الرأسَ يفارق الـbaseline في عشرين ملفّاً، وفحصُه كان سيقيس شيئاً آخر.**
- ✅ **البوّابات:** `npm ci` (٣٩١ حزمة) · `tsc` **صفر خطأ** · `eslint` **صفر خطأ و١٦ تحذيراً** (وهو عددُ الأساس) · `next build` **نجح** (٨١ مساراً) · `npm audit` **صفر ثغرة**.
- ✅ **وما ثبت نظيفاً:** **صفر `TODO`/`FIXME` · صفر `any` · صفر `@ts-ignore` · صفر استيرادٍ دائريّ** في ٤٥١ ملفّاً و١٠٨ آلافِ سطر · **وصفر قيمةِ سرٍّ** في الشجرتين (والإشاراتُ الأربعَ عشرةَ كلُّها اسمُ الدور `service_role` في أوامر `grant`).
- 🔴 **وأثقلُ ما في المرحلة غيابٌ لا عيب: صفر ملفِّ اختبارٍ وصفر مشغّلٍ وصفر mock** — **يؤكّد `LOOPZ-AUD-0001` ولا يُنشئ رقماً ثانياً.** **ويجتمع مع `0006` (لا بيئةَ معزولة) و`0054` (لا مسارَ استعادة) في مثلّثٍ يجعل كلَّ حذفٍ في هذه القائمة مخاطرةً: لا اختبارٌ يمنع · ولا بيئةٌ تُجرَّب · ولا نسخةٌ تُرجع.**
- 🔑 **وثلاثةُ فِخاخٍ تجنّبتُها بالفحص لا بالحظّ:** **`src/lib/imageLoader.ts` لا يستورده أحدٌ وهو حيٌّ عبر `next.config.ts:93`** · **و١٣ صورةَ إقلاعٍ لا يظهر اسمُها نصّاً وهي حيّةٌ عبر قالبٍ في `layout.tsx`** · **و٢٢ جدولاً بلا `.from()` — تسعةَ عشرَ منها حيٌّ عبر دوالَّ أو مشغِّلاتٍ أو `cron`.** **فغيابُ `.from()` يقيس أسلوبَ الوصول لا حياةَ الجدول.**
- ⚠️ **والمرشّحاتُ تسعة** (`P7-01`…`P7-09`): **واحدةٌ P2** (مساران إداريّان يطبعان ناتجَ العمليّة في السجلّ) **وثمانٍ P3** — **ولا أوصي بتنفيذ أيٍّ منها إلّا `P7-01` قبل أوّلِ حزمةِ اختبارات، وهذا رأيي مكتوباً.**
- ⚠️ **وتنبيهٌ على المهمّة:** طُلبت المصالحةُ مع `LOOPZ-AUD-0010` و`0011` **وهما غيرُ صادرَين** (الأرقامُ الصادرة: `0001`…`0007` · `0009` · `0014`…`0060`). **لم أخترع لهما مقابلاً**، وصالحتُ `0001` و`0002` و`0003` و`0004` و`0005`/`0055` و`0040` و`S-14`.
- **لم تبدأ Phase 8.**

### 2026-09-01 22:07 UTC — Phase 7 — تصحيحُ السبع نقاط — Claude

- **`LOOPZ-AUD-0008`…`0013` أُضيفت إلى سجلّ المشكلات أعلاه**، منقولةً عن نصِّ اعتماد Phase 1 بلا إعادةِ تفسير. **وكانت صادرةً وغيرَ مسجَّلة — وهو عيبٌ في ملفّ التنسيق لا في الأرقام.**
- 🔴 **وقلتُ في تسليم Phase 7 إنّ `0010` و`0011` «غيرُ صادرَتين» — وهو خطأ، وأعتذر عنه.** **والعِلّةُ منهجيّة: بحثتُ في جدول هذا الملفّ وحدَه فلم أجدهما فأعلنتُ النفي.** **ومصدرُ الأرقام هو الـPR لا الجدول.** **وهو الخطأُ نفسُه الذي وقعتُ فيه أمس حين قلتُ «لم يردّ» بلا قراءةِ آخرِ صفحة: نفيٌ من غيابِ سطرٍ في مصدرٍ واحد.**
- **ومُصولحان الآن بحكمٍ لا بترحيل:** **`0010`** ⇠ العددُ نفسُه (١٦) وقد صار مصنَّفاً (١٠ حدُّ أداةٍ داخل راسم OG · ٥ متغيّراتٍ غيرِ مستعمَلة · ١ متفرّق) · **`0011`** ⇠ الخمسةُ مؤكَّدةٌ وتوسَّعت إلى ثمانية، **و`/diary/loading.tsx` حكمُه `USED_BUT_UNREACHABLE` بالتصميم: جسمُه `return null` وفوقه تعليقٌ يقول لماذا (D-046)، وصفحتُه كلُّها `redirect`** — **فالملفُّ هو العلاجُ لا العلّة، وحذفُه يُعيد الهيكلَ الوامض.**
- **وأربعةُ أخطاءِ عدٍّ أُصلحت:** **«٨١ مساراً» ⇠ ٧٦** (عدُّ أسطرٍ لا مسارات — وكان ينقض جردَ Phase 1) · **وأعمدةُ الأسطر في §3 كانت تقديراً يجمع إلى ٨٠١ بينما المجموعُ المعلَنُ ٦٥٨** ⇠ الصفوفُ من `wc -l` ومجموعُها ٦٥٨ · **و«١١ اسمَ إطار» ⇠ ١٠ + زوجٌ مقصود (`createClient`) + تصادمان = ١٣** · **وإحالاتُ `P7-0x` ⇠ معرّفاتٌ فعليّة.**
- **وفحصُ الأسرار أُعيد على رأس التسليم `a7298a9` نفسِه** (لا على `f5e06d5` كما فعلتُ أوّلاً): **٦٥٦ ملفّاً · ٢٥ إشارة · صفر قيمةِ سرّ** — **وكلُّها اسمُ الدور `service_role` في أوامر `grant`.** **بالأداة الآمنة نفسِها، بلا طباعةِ قيمة، وبلا فحصِ التاريخ.**
- **صفر إعادةٍ لـ`npm ci` أو `tsc` أو `eslint` أو `build` أو تحليلِ قاعدة البيانات** كما أمرت — **وإعادةُ العدِّ من سجلِّ البناء المحفوظ ومن `wc -l`.**
- **صفر تعديلٍ على شيفرة التطبيق · صفر حذفٍ لأيِّ مرشَّح · صفر Merge/Deploy/Migration · صفر تغييرٍ على Production. ولم تبدأ Phase 8.**

### 2026-09-02 · `partner_details` — جوابُ المالك حسم الحياةَ وفتح ما هو أهمّ — Claude

- **سألتُ أحمد عن `partner_details`** (الجدولُ الذي لا شيفرةَ ولا دالّةَ تمسّه ومع ذلك عليه سياستان)، **فأجاب: «أملؤه يدويّاً من اللوحة».** **⇢ الحكم `USED` لا `UNKNOWN`، وقد صُحِّح في `PHASE_7_CODE_QUALITY.md` §6.3.**
- **والقياسُ يسند الجواب** (أعدادٌ لا صفوف): **صفٌّ واحدٌ بـIBAN وملفِّ هويّة · صفرُ هاتفٍ موثَّق · وشريكان وطلبان وملفٌّ واحدٌ في مخزن `partner-ids` الخاصّ.**
- ⚠️ **وأربعةُ أمورٍ لا يحلّها كونُ الجدول حيّاً — مسجَّلةٌ `P7-11` (P2، توثيقٌ وقرارٌ لا شيفرة):**
  1. **البياناتُ تصل من خارج المنتَج:** `partner_applications` لا يجمع IBAN ولا هاتفاً ولا هويّة، **فهي تصل بقناةٍ خارج التطبيق ثمّ تُنسخ يدويّاً** — **قناةٌ لا تصفها سياسةُ الخصوصيّة ولا يشملها حذفُ الحساب.**
  2. **الشريكُ لا يرى بياناتِه ولا يصحّحها:** سياستا قراءةٍ في القاعدة، وصفرُ واجهةٍ تقرؤه ⇢ يُضاف إلى `LOOPZ-AUD-0057`.
  3. **`phone_verified_at` عمودٌ يَعِد بتوثيقٍ لا مُوثِّقَ له:** صفرُ شيفرةٍ وصفرُ دالّةٍ تكتب فيه، وقيمتُه فارغة.
  4. **إدخالٌ يدويٌّ بلا تحقّقِ صيغةٍ ولا سجلِّ فاعل** — خطأُ رقمٍ في تحويلٍ بنكيٍّ بلا أثرٍ يُراجَع.
- ⚖️ **ولا أعدّ الإدخال اليدويَّ خطأً: بشريكَين هو الخيارُ العاقل، وواجهةٌ لأربعةِ حقولٍ تُملأ مرّتين إسراف.** **المطلوبُ أن يكون المكتومُ مكتوباً: مسارُ الوصول ومكانُ الحفظ قبل الإدخال ومتى يُمحى.**
- **صفر تعديلٍ على شيفرة التطبيق · صفر SQL كاتب · صفر تغييرٍ على Production · وكلُّ استعلامٍ عدُّ صفوفٍ لا قراءةَ صفّ.** **وصفر قيمةِ IBAN أو هويّةٍ أو هاتفٍ في أيِّ مخرَج.**

### 2026-09-01 22:28 UTC — Phase 8 مُسلَّمة — Claude

- **Phase 7 اعتُمدت `VERIFIED`** في 22:10 UTC بأرقام `LOOPZ-AUD-0061`…`0066`، **ونُقلت إلى سجلّ المشكلات أعلاه — مفتوحةً كلَّها.**
- **Phase 8 مُسلَّمة:** `PHASE_8_OPERATIONS_STABILITY.md`. **قراءةٌ وPassive فقط: صفر تغييرِ إعدادٍ أو بيئة · صفر نشرٍ أو rollback · صفر تشغيلِ مهمّةٍ أو دالّة · صفر كتابةٍ في Supabase أو Production.** **ولا قيمةَ متغيّرِ بيئةٍ واحدةً في التقرير.**
- **والسؤالُ الذي تجيبه المرحلة: إن وقع عطلٌ الليلة، بأيِّ سرعةٍ نعلم وبأيِّ سرعةٍ نرجع؟** **الجوابُ المقيس: لا نعلم إلّا بشكوى، ولا نرجع إلّا في الشيفرة.**
- ✅ **وما ثبت انضباطُه:** **المهمّةُ المجدولةُ الوحيدة ٢٢ تشغيلاً بصفر فشلٍ وصفرِ يومٍ ضائع** (١١ أغسطس ← ١ سبتمبر) · **صفرُ دالّةِ Edge فلا سطحَ تشغيلٍ ثانٍ** · **`bom1` للحوسبة و`ap-south-1` للقاعدة — مطابقةٌ مقصودة** · **و`robots.txt` يحكم الفهرسةَ بالمضيف فيحجب Preview و`*.vercel.app` بالتصميم** · **و`maxDuration = 300` ضمن حدِّ الخطّتين بنصِّ توثيق Vercel (محدَّثٌ 2026-08-24، اطُّلع 2026-09-01)** · **ونبضةُ اللغة تقرأ `x-forwarded-for` لحدِّ المعدّل فقط ولا تخزّن عنواناً — مقيسٌ من المصدر.**
- 🔴 **ومرشَّحان جديدان فقط:** **`P8-01` (P2): أكثرُ من ١٧٠ ملفَّ هجرةٍ في `supabase/` وصفرُ نصِّ عكسٍ لأيٍّ منها** — **وبروتوكولُ Phase 6 §5.2 يفرض `rollback artifact` قبل كلِّ هجرة، فلا هجرةَ تستوفيه.** **و`P8-02` (P3): `.env.example` ينقصه أربعةُ مفاتيحِ ميزاتٍ حقيقيّة** — **ويُنقّح `0002` الذي عدَّ معها متغيّرَي أداةِ تطويرٍ لا يظهران في `src` أصلاً.**
- **وعشرةُ بنودٍ وجدتُها محالةٌ إلى أرقامها بلا تكرار:** `0002` · `0006` · `0012`/`0026` · `0039` · `0042` · `0050` · `0054` · `0058` · `0061` · `0062`.
- ⚠️ **وموانعُ معلَنةٌ لا مسكوتٌ عنها:** **لوحةُ Vercel `BLOCKED_BY_ACCESS`** (فريقٌ واحدٌ ظاهرٌ بخطّة `pro` **وصفرُ مشاريعَ تحته**، والمشروعُ `404` — **ولا أستنتج من ذلك خطّةَ المشروع ولا إعداداته**) · **ورؤوسُ الاستجابة الطازجة `BLOCKED_BY_TOOLING`**: حاجزُ أمانٍ في أداة المتصفّح يرفض `fetch` يحمل جلسةَ المستخدم — **والمنعُ في الأداة لا في المنتَج، ولم ألتفّ عليه، والنتيجةُ المعتمَدة هي قياسُ Phase 2 `PASSIVE_VERIFIED`.**
- **لم تبدأ Phase 9.**

### 2026-09-02 00:43 UTC — Phase 8 `VERIFIED` · Phase 9 `DEFERRED` · Phase 10A جدوى (قراءةٌ فقط) — Claude

- **Phase 8 اعتُمدت `VERIFIED`** في 00:34 UTC: **`P7-11` → `LOOPZ-AUD-0067` (P2)** · **`P8-01` → `LOOPZ-AUD-0068` (P2)** · **`P8-02` مدمجٌ في `0002`** (نطاقُه مفاتيحُ الميزات الأربعة) — **نُقلت إلى السجلّ أعلاه بلا إعادةِ تفسير.**
- **التوجيهُ التنفيذيُّ المعتمَد (00:31 UTC، بقرار أحمد):** **Phase 9 `DEFERRED`** · **Phase 10A أوّلاً** (بيئةُ اختبارٍ معزولة) **ثمّ Phase 10 بالترتيب P0 → P1 أمن/بيانات → P1 وظائف (وأوّلُها `0023` الترايلرات) → P1 أداء → P2 → تنظيف → P3** · **لكلِّ حزمةٍ فرعٌ مستقلّ وأدلّةٌ وrollback و`READY_FOR_REVIEW` ثمّ `VERIFIED`** · **ولا Merge ولا نشرَ Production إلا بمراجعة ChatGPT وموافقة أحمد الصريحة.**
- **بوّابةُ الأمان قبل الإنشاء — نُفِّذت قراءةً فقط** (واجهةُ Supabase الإداريّة؛ صفرُ كتابة): **خطّةُ المنظّمة `free` · مشروعٌ واحد · صفرُ فرع** · **Branch يكلّف `$0.01344/ساعة` ويشترط `Pro`** · **مشروعٌ ثانٍ مجّانيٌّ `$0` وممكن** — **لكنّه يلزمه كلمةُ مرورٍ ومفاتيحُ Preview وGoogle redirect، وكلُّها أسرارٌ أو إعداداتُ حسابٍ لا أكتبها** · **Vercel Preview بمتغيّراتٍ خاصّةٍ ممكنٌ على كلِّ الخطط، ولوحتُه `BLOCKED_BY_ACCESS` للمُوصِّل** · **وحساباتُ الاختبار تصطدم بأنّ الدخولَ Google وحدَه.**
- ⇢ **الحالة `BLOCKED_BY_APPROVAL`** — **ثلاثةُ خياراتٍ مكتوبةٌ في `Claude Check-in` بكلفتها** · **ولا أوصي بشراءٍ ولا أنفّذه** · **ولا موردَ أُنشئ.**
- ⚠️ **وتعارضٌ مرفوعٌ للمالك** («لا نشرَ إلى `main` بلا مراجعة» مقابل تعليمةِ الشحن المباشر في ذاكرة المشروع) — **مكتوبٌ في «المطلوب من Claude الآن» §6، وقد شُحن اليوم D-872 → D-878 على `main` وفق التعليمة القائمة.**
- **صفر تعديلٍ على شيفرة التطبيق تحت التدقيق · صفر كتابةٍ في Supabase أو Vercel · صفر تغييرٍ على Production · صفر قيمةِ سرٍّ في هذا الملفّ أو التعليق.**

### 2026-09-02 01:25 UTC — قرارُ المالك: لا بيئةَ معزولة · `0023` مشحونٌ على `main` — Claude

- **أحمد بنصّه (01:00 UTC): «نفّذها واختبرها في main، لن أهدر الوقت في مستودع وربط جديد»** ⇢ **Phase 10A `CANCELLED_BY_OWNER`**، **والاختبارُ على Production**، **والمراجعةُ بعد الشحن.** **ورأيي مكتوبٌ في `Claude Check-in`: خطرُ `0006` باقٍ.** **ونفّذتُ حكمَه.**
- **`LOOPZ-AUD-0023` — أُعيد إنتاجُه أوّلاً على Production `6559d41` بقياسٍ** (زائر · Chromium 1280×720): **الزمنُ يتقدّم والغلافُ لا يُرفع لأنّ انتقالَ CSS عالقٌ حيث لا `requestAnimationFrame`.** **العلاجُ `D-879` (`921abd7`): نزعُ الغلاف بمؤقّت JavaScript بعد الكشف.** **Before/After في `Claude Check-in`.**
- **حدودٌ معلَنة:** `BLOCKED_BY_DEVICE` للجوّال · **صفر Migration · صفر سرّ · صفر بياناتِ أعضاء.**
- **إضافةٌ (01:35 UTC):** **توجيهُ المالك المعتمَد (00:55 UTC) — Phase 10 على `main`، 10A مُلغاة، والتسلسلُ 10B ثمّ 10C** — **سُجِّل في «المطلوب من Claude الآن»**، **ومعه انحرافُ ترتيبٍ مُعلَن: شريحةُ `0023` الأولى شُحنت قبل الـBaseline** (وصل التوجيهُ بين القرار في المحادثة وقراءتي للـPR). **`BLOCKED_BY_APPROVAL` لم يعد في الحالة النافذة.**

### 2026-09-02 01:30 UTC — Phase 10B Baseline مُسلَّمة (Post-D-879) · ردٌّ على `CHANGES_REQUESTED` — Claude

- **`PHASE_10_PERFORMANCE_BASELINE.md`** على `main @ 921abd7` — **جهازٌ حقيقيٌّ واحد (لوحُ Claude المضمَّن)، والباقي `BLOCKED_BY_DEVICE`/`BLOCKED_BY_TOOLING` بأسمائها.** **أربعةُ مرشَّحات `P10-01`…`P10-04`** (HTML المجتمع 1 MB · ملصق→عمل تنقّلٌ صلب · التبويبُ 691 ms · الصوتُ مفتوحٌ آليّاً).
- **النقاطُ الستّ على `0023` معالَجةٌ في `Claude Check-in`** — **و`D-880` (لا `setState` في الرسم) يُشحن بعد هذا التسليم بنقطة رجوع `921abd7` وأدلّةِ بوّاباتٍ في `evidence/`.**
- **صفر تعديلٍ على Production في هذا الالتزام.**

### 2026-09-02 01:55 UTC — تصحيحُ Baseline (٨ نقاط) · `D-880` مشحون — Claude

- **`CHANGES_REQUESTED` (01:35) على Baseline — النقاطُ الثماني:** (١) ملفُّ الأدلّة **موجودٌ منذ `11229e9`** · (٢) **`D-880` مشحونٌ `c1b85dc`** [deploy] بنقطة رجوع `921abd7` وSmoke بعد النشر · (٣) «أوّلُ إطار» ⇢ **Overlay removal**، و**First video frame `UNKNOWN`** · (٤) البيئة ⇢ **Embedded automated Chromium environment**، وDesktop الحقيقيّ محجوب · (٥) فشلُ المراجع ⇢ **environment-dependent unresolved (`P10-06`)** وinstrumentation في الحزمة التالية · (٦) الصوت ⇢ **UI indicates unmuted; audibility not verified (`UNKNOWN`)** · (٧) الإطاران ⇢ **فجوةٌ `P10-05`** لا ✅ · (٨) **لا أرقامَ `LOOPZ-AUD`** للمرشَّحات.
- **صفر تعديلٍ على Production في هذا الالتزام التوثيقيّ** (التطبيقُ عند `c1b85dc`).

### 2026-09-02 02:00 UTC — 10B + D-880 `VERIFIED` · D-881 instrumentation مشحون · بندان ينتظران المالك — Claude

- **اعتمادُ 10B وD-880** (01:39): أرقامُ `0069`…`0072` نُقلت. **`0023` تبقى `CHANGES_REQUESTED`.**
- **D-881** (`b8bf6cc` · `1f39f60` [deploy]): إشاراتُ يوتيوب/المشغّل إلى `runtime_errors` عبر `log_runtime_error` — **صفرُ معرّف، حاجزٌ بالعنوان، مرّةٌ لكلِّ (نوع، رمز)**. **مُثبَتٌ من طرفٍ إلى طرف** (صفٌّ واحدٌ وصل، ومشوَّهٌ رُدّ). الأدلّة `evidence/D-881-gates.txt`.
- **بندا 10C ٢ و٣ `BLOCKED_BY_OWNER_DECISION`**: ينقضان D-771 وتصميمَ الاحتياطيّ — **مرفوعان لأحمد بكلفةٍ مكتوبة.**

### 2026-09-02 02:25 UTC — قرارُ المالك على 10C · D-882 — Claude

- **المالك (02:20 UTC):** **لا default-muted** (D-771 باقية) · **الاحتياطيُّ باقٍ** («سرعة التشغيل مهمّة») ⇢ **`0072` `WONT_FIX_BY_OWNER`** · **`P10-04` `BY_DESIGN`**.
- **D-882 `062d083`** [deploy]: ⏸ بمؤقّتٍ محلّيٍّ ١٫٥ s — بلاغُ المالك من iPhone. Smoke ✅.
- **صفر Migration · صفر سرّ · صفر بياناتِ أعضاء.**

### 2026-09-02 02:14 UTC — D-883: تصحيحُ D-881 بالنقاط الخمس · دليلُ E2E — Claude

- **`deec4ae` + `d9767dd` [deploy]**: الحالةُ تُمرَّر صراحةً · «مرّةٌ لكلِّ نسخةِ محرّك» · سقفُ حمولةٍ ٢٥٦ بايتاً · رمزٌ مقصوص · توثيقُ best-effort. **دليلُ E2E**: العدُّ ١ → ٣ (+٢ للصالحَين، ٠ للمسقَطَين) — أعدادٌ وdigests فقط.
- **بندا 10C ٢ و٣ `CANCELLED_BY_OWNER`** بنصّ المالك (02:20 UTC).
- **إضافةٌ (في الالتزام `917be64` · 08:52:03 UTC):** **ردُّ المراجع 08:41 UTC سبق التزاماتِ D-883** (`deec4ae` 08:42 · `d9767dd` 08:43) **ودليلَ E2E `c2…`** — **فنقاطُ D-881 الأربعُ معالَجةٌ فيها**، **وD-882 لها الآن ملفُّ أدلّةٍ على SHA الملتزَم**، **والأوقاتُ صُحِّحت من `git log`**، **ويبقى iPhone عند المالك.**

### 2026-09-02 (وقتُ البلاغ غيرُ مسجَّل؛ قبل 09:03:26 UTC) — نتيجةُ iPhone من المالك (D-882 / `0023`) — Claude

- **الخطواتُ ١–٣ نعم** · **الخطوة ٤: مكتومٌ افتراضاً على iPhone (سياسةُ WebKit — السقوطُ المكتوب في D-771)، والاختيارُ يُحمل إلى البطاقة التالية** · **الجهاز/iOS/PWA لم يُذكر — طُلب** · مُلحَقٌ نصّاً في `evidence/D-882-gates-062d083.txt`. **D-882 `READY_FOR_REVIEW`.**
- **التزامُ الدليل `41354f2` · 09:03:26 UTC.**
- **وتعليقُ المراجع 08:53:01 UTC** (وصل بعد تسليمي 08:52:59 بثانيتين) **نقاطُه الخمس عولجت في موضعها في هذا الالتزام:** (١) iPhone أعلاه · (٢) الرأسُ والحالةُ النافذة: D-881/D-883 `VERIFIED` وD-882 وحدَها `READY_FOR_REVIEW` · (٣) «02:00 UTC» صار `01:46`/`01:47` من `git log`، و«في الجلسة» صار «لكلِّ نسخةِ محرّك» — في السطرين نفسيهما · (٤) «آخر تحديث» مربوطٌ بوقت `git log` لا بتقديرٍ، و`08:5x` استُبدل بـ`08:52:03` · (٥) لا ✅ أمام D-882 قبل الاعتماد.

### 2026-09-02 09:05:14 UTC — تعليقُ المراجع: D-882 ناجحةٌ وظيفيّاً · تصحيحٌ توثيقيٌّ فقط — Claude

- **المراجع:** نتيجةُ iPhone تغلق بوّابةَ السلوك؛ الكتمُ الأوّلُ = WebKit autoplay fallback لا تغييرَ قرار؛ **لا اختبارَ تطبيقٍ ولا كود ولا Migration.** (تعليقُه 09:05:14 وتسليمي 09:05:52 تقاطعا — نقطتُه ٤ كانت مصحَّحةً في `816b997` 09:04:12 قبل تعليقه.)
- **التزامُ الدليل النهائيِّ `93ea163` · 09:08:41 UTC.**
- **نُفِّذ في هذا الالتزام:** (١) دليلُ D-882 صار حالةً نهائيّةً واحدة `PASS` بلا `PENDING_OWNER_CHECK` · (٢) طرازُ الجهاز/إصدارُ iOS/PWA-أم-Safari = `UNKNOWN / NOT_RECORDED` صراحةً (المالكُ لم يذكرها) · (٣) وقتُ البلاغ = `time not recorded`، قبل `41354f2` 09:03:26 — لا وقتَ تقريبيّاً مستقبليّاً · (٥) **D-881/D-883 = `VERIFIED` · D-882 = `VERIFIED` · `LOOPZ-AUD-0023` = `VERIFIED/RESOLVED`** بالإحالة إلى `evidence/D-882-gates-062d083.txt` ونطاقِه المكتوب فيه.
- **التالي:** `LOOPZ-AUD-0070` — قياسٌ لعزل السبب أوّلاً.

### 2026-09-02 09:48:40 UTC — Phase 10D: baseline + تشخيصُ `0069`/`0070` — Claude

- **المراجع (09:09:53 UTC):** D-882 `VERIFIED` · `0023` `VERIFIED/RESOLVED` · Phase 10C مكتملة · **ابدأ 10D: قياسٌ ٣ بارد + ٣ دافئ (Home · Discover · `/people` · عمل)، ثمّ تشخيصُ `0069` و`0070` واختيارُ الأوّل قبل أيِّ Commit.**
- **نُفِّذ:** `PHASE_10D_API_NETWORK.md` (`c80ff75`) — 24 تحميلاً + 12 إعادةَ جلبٍ قسريّة على `d9767dd` من لوح Claude (زائر) · **`0069`**: خطُّ النشاط 65 منشوراً مرسوماً خادميّاً (≈ 455 KB) + RSC مضاعَف (458 KB) + SVG مكرّر (346/20 ≈ 109 KB) ⇢ 1,067 KB مفكوكةً · **`0070`**: نقرٌ قبل ترطيب الحاجز — لا SW ولا build-id (تنقّلٌ ناعم مُثبَت بعد الترطيب) · **اكتشاف:** أوّلُ RSC لعملٍ 1.1–1.7 s ثمّ 0.24–0.56 s · **SW يزيد TTFB الدافئ ≈ +120 ms** · صفر طلباتٍ بعد المغادرة · صفر مكرّر.
- **الاختيار المقترَح: `0069` أوّلاً** (سقفُ الرسم الخادميّ + «المزيد»، تطبيقٌ خالص) · **مرشَّحاتٌ للترقيم `P10D-01…04`**.
- **حدودٌ معلَنة:** كاشُ HTTP لا يُمسح من الصفحة (مذكورٌ عدديّاً) · لا LCP/أوّلَ إطار · قياسٌ شاذٌّ واحد معلَن · لا تعديلَ تطبيق.

### 2026-09-02 09:56:03 UTC — Phase 10D الجولة ٢ بعد `CHANGES_REQUESTED` (09:50:53) — Claude

- **المراجع (09:50:53 UTC، على `c80ff75`):** قبِل التوثيقيّةَ والجولاتِ الثلاث وكشفَ الشاذّ وتشخيصَ `/people` واختيارَ `0069` وقراءةَ SW · **ست نقاط:** cold ليس cold · «بعد المغادرة» لم يُختبر · tap→visual غيرُ مقيس · `0070` ليس معزولاً على جهاز · `SHARED_PROGRESS.md` لم يتغيّر في ذلك الالتزام (كان يتغيّر في `57b06d3` 09:50:34 — تقاطعٌ زمنيّ) · `/news` = Discover · **ولا تعديلَ `0069` ولا ترقيمَ قبل تصحيح حدود الدليل.**
- **نُفِّذ (`5a662d6`):** (١) التسميةُ `PARTIAL_COLD / HTTP_CACHE_WARM` في المنهج والجدول + `BLOCKED_BY_TOOLING` لبوّابة cold الكاملة · (٢) قياسُ المغادرة بانتقالٍ فعليّ ×4 مسارات (§٢-ب): ملغيّاتٌ = prefetch «الكل» ×3 (Discover) · `/terms?_rsc` · vitals · شعار w=128؛ مكرّراتٌ بمعاملات = prefetch ×3 لكلِّ مسار «الكل» · تذييل ×2 · خلفيّةُ العمل w=1920+w=2048 · شعار w=64+w=128؛ و`/api/season → 401` للزائر · (٣) tap→visual (§٢-ج): الطلاءُ `BLOCKED_BY_TOOLING` (rAF لا يُطلق حتى ظاهراً)، البدائلُ tap→DOM 162 ms · tap→URL 160 · tap→RSC 1,342؛ التبويب 2 ms / URL 558 · (٤) `0070` ⇢ `LIKELY_CAUSE / BLOCKED_BY_TOOLING_OR_DEVICE` والمؤكَّدُ وحدَه مُبقى · (٥) هذه الحالةُ النافذة · (٦) مصفوفةُ المسارات في رأس التقرير.
- **المرشَّحات (أ)–(و) بلا أرقام** · **الاختيار `0069` أوّلاً ثابت** · **صفر تعديلِ تطبيق · صفر Supabase · صفر بياناتِ أعضاء · صفر سرّ.**

### 2026-09-02 (بعد 09:58:10 UTC) — Phase 10D الجولة ٣: أربعُ نقاطٍ توثيقيّة — Claude

- **المراجع (09:51:52 على `57b06d3` · 09:58:10 على `5a662d6`):** قبِل الجولة ٢ من التقرير (التسمية · المغادرة · tap→DOM · `LIKELY_CAUSE` · Discover · لا ترقيم) · **المتبقّي:** السجلُّ (كان في `61e625f` 09:57:52 — تقاطع) · §5 · §3 · §2-ب.
- **نُفِّذ:** التقريرُ والسجلُّ معاً في التزامٍ توثيقيٍّ واحد `09b31dd` · 10:00:17 UTC (مجلّدٌ واحد) · ثمّ التزامُ رأسِ الوقت · **لا قياسَ معاد · لا تعديلَ `0069` · لا ترقيم.**
- **وتعليقُ المراجع 09:59:10 (على `61e625f`):** الأسطرُ التفصيليّة في `Claude Check-in` كانت ما تزال تقول «بارد»/«صفر طلب بعد load+1s» — **صُحِّحت في موضعها هنا** (`PARTIAL_COLD` · كاشُ HTTP دافئ · المكرّراتُ بمعاملات · المغادرةُ مقيسةٌ فعليّاً) — والمرشَّحاتُ بلا أرقام.
- **وتعليقُ المراجع 10:01:24 (على `09b31dd`): التقريرُ مقبول؛ المتبقّي في السجلّ فقط** — (١) سطرا المنهج والأرقام ومرشَّحُ 0069 ⇢ `PARTIAL_COLD`/`DCL PARTIAL_COLD` (كان أكثرُه في `afcf97f`، وتمّم هنا) · (٢) «القرار الحالي» و«0023 مفتوحة حتى iPhone» ⇢ `0023 VERIFIED/RESOLVED` · (٣) «صاحب الدور» و«المطلوب الآن» و«آخر تحديث» ⇢ الجولة ٣ `09b31dd` · (٤) الحالةُ النافذة `READY_FOR_REVIEW` على `09b31dd` + التزامُ التنسيق هذا · المرشَّحات بلا أرقام.

### 2026-09-02 11:10:26 UTC — D-884: إصلاحُ `LOOPZ-AUD-0069` على `main` — Claude

- **المراجع (10:08:02):** Baseline 10D `VERIFIED` · رقّم `0073`…`0079` · **ابدأ `0069` وحدَه** (سقف ≈ 20 + «المزيد» · لا SQL/كاش/صور · Before/After بالمنهج نفسِه والهدف < 500 KB · D-522 · بوّابات + نشرُ SHA نفسِه + smoke).
- **نُفِّذ:** `49a26e3` · `bf15af9` · `6f248f0` [deploy] (ثلاثةُ مجلّدات = ثلاثةُ التزاماتٍ يُصرَّف كلٌّ منها وحدَه) · نقطةُ الرجوع `d9767dd` · البوّاباتُ على `6f248f0` بشجرةٍ نظيفة · `/api/build == 6f248f0` · **1,067 ⇢ 492 KB** · smoke ✅ · الدليل `evidence/D-884-gates-6f248f0.txt` · التقرير §٧.
- **انحرافٌ مُعلَن عن صياغة الأمر:** «المزيد» رابطُ `?feed=all` لا إجراءُ خادم — الحالةُ في العنوان (D-051/D-054)، وبه يعمل D-522 من داخل الخطّ الموسَّع بلا شيفرةٍ إضافيّة. **لو رُفض يُستبدل بإجراءِ خادمٍ في التزامٍ لاحق.**
- **صفر Supabase · صفر كاش/SW/صور · صفر بياناتِ أعضاء · صفر سرّ.**

### 2026-09-02 14:33:45 UTC — `0074` تشخيصٌ بلا تعديل (§٨) + D-884 `VERIFIED` + إفصاحُ D-885…D-888 — Claude

- **المراجع (14:05:24 UTC):** **D-884 `VERIFIED` · `LOOPZ-AUD-0069` `RESOLVED`** (قبِل الانحرافَ: رابطُ `?feed=all`) · **التالي `0074` (P1) تشخيصاً فقط**: ٣ `PARTIAL_COLD` + ٣ warm على 3–4 أعمال · تفكيكُ RSC إلى نداءات TMDB وأيُّها MISS/مسيطر · فصلُ TTFB عن RSC عن المحتوى البديل · مراجعةُ الكاش بلا تغيير · خيارُ إصلاحٍ واحد + Before/After + مخاطر + rollback ⇢ `READY_FOR_REVIEW` · لا `0077`/`0078` قبل P1 · `0070` يبقى `BLOCKED_BY_TOOLING_OR_DEVICE`.
- **نُفِّذ:** `061446b` (14:33:45 — إلحاقُ طراز iPhone **17 Plus** في `evidence/D-882-gates-062d083.txt`؛ iOS/PWA ما زالا `NOT_RECORDED`) · ثمّ التزامُ هذا السجلّ + التقرير §٨ (مجلّدٌ واحد؛ وقتُه في `git log`). **القياس من اللوح على `main @ ac78296`**: 16 عملاً `PARTIAL_COLD` (en) + 4 (ar) + 3 warm × 4 ثابتة، بعلاماتِ المحتوى داخل بثّ RSC (hero/cast/trailer/ratings/end) — الأرقامُ والتفكيكُ والكاشُ والخيارُ في §٨ وفي الـCheck-in أعلاه. **لا تعديلَ تطبيق · لا كاش · لا Supabase · لا سرّ · لا بياناتِ أعضاء.**
- **حدٌّ مُعلَن في القياس:** علامةُ trailer في الأعمال الأربعة الأولى طابقت كلمةً في الهيكل (باطلة، مذكورةٌ في الجدول) وأُصلحت للاثني عشر التالية؛ `x-vercel-cache` لا يعكس كاشَ البيانات (الصفحةُ ديناميكيّة) فبردُ الكاش يُستدلّ من الزمن (وعملٌ عربيٌّ واحد ظهر دافئاً داخل الجولة الباردة وذُكر).
- **إفصاح:** بين D-884 وهذا التسليم شُحنت D-885…D-888 على `main` بتوجيه المالك خارجَ التدقيق (12:19–12:44 UTC؛ تفصيلُها في الـCheck-in) — D-888 يمسّ ملفَّي D-882.

### 2026-09-02 15:22:00 UTC — تغييرُ الأدوار · مراجعةُ §٨ ذاتيّاً · D-889 = إصلاحُ `0074` على `main` — Claude

- **المالك (بعد 14:37:52، وقتُه غيرُ مسجَّل):** «المراجع ما يقدر يوصل، أحتاجك تقوم بعمله: تراجع وتوجّه وتستمرّ على الخطة.»
- **حكمُ Claude بدور المراجع على §٨ (كُتب قبل التنفيذ؛ نصُّه هنا لأنّ نشرَه في PR #19 من الأتمتة لم يتحقّق وقتَها):** **`VERIFIED` مع تعديلٍ توثيقيّ يُدمَج في التزام التسليم.** *مقبول:* المنهج (علاماتُ المحتوى في بثّ RSC = «أوّل فتحة» بعد الترطيب؛ الطلاءُ غيرُ مدّعى) · التفكيكُ مُثبَت من الشيفرة والأرقام معاً (cast = hero+313 = رحلةٌ كاملة) · الترايلرُ العربيّ برحلتين في 2/3 · الكاشُ بلا تغيير · الخيارُ داخل حدود 10D · معيارُ رجوعٍ رقميّ · الإفصاح. *مرفوض/يُصحَّح:* (١) الانحرافُ «اتّساعٌ بدل تكرار» (العملُ لا يبرد قبل انقضاء 3600 s) لم يُعلَن باسمه ⇢ سطرٌ صريح في §٨-أ · (٢) «MISS» استنتاجٌ من الزمن ⇢ `MISS (inferred)` في رؤوس الجداول · (٣) أسبقيّةُ `imdbOverride` (D-431) تُكتب في الخيار وتُثبَت في الشيفرة. *التوجيه:* D-889 = `show/[id]/page.tsx` + خاصيّةٌ اختياريّة في `CastRail` و`HeroRatings`؛ لا كاش/مفاتيح/`append_to_response`/SQL/صور؛ `.catch` على كلِّ وعد؛ `components` أوّلاً ثمّ الصفحة `[deploy]`؛ نقطةُ الرجوع `ac78296`؛ Before/After بالمنهج نفسِه en وar؛ صفحةُ الفيلم لا تُمسّ.
- **نُفِّذ:** التصحيحاتُ الثلاث في §٨ · `5e13806` 15:07:02 · `8cf2d80` 15:09:57 [deploy] · البوّاباتُ على `8cf2d80` بشجرةٍ نظيفة · `/api/build == 8cf2d80` 15:13:16 · القياسُ بعد النشر 15:13–15:16 (12 عملاً بارداً en + 4 ar + 3 warm × 4 ثابتة) · `e33067a` (15:22:00 — الدليل) · ثمّ التزامُ هذا السجلّ + التقرير §٨/§٩ (وقتُه في `git log`).
- **حكمُ Claude بدور المراجع على D-889: `VERIFIED` بحدودٍ مكتوبة** — الأهدافُ الثلاثة الأولى مستوفاة (cast/trailer ≈ hero · ratings ≤ hero+300 · trailer(ar) ≤ hero+350)، **وهدفُ end جزئيّ** (−157 وسيطاً لا −300…−500؛ الذيلُ الآن رفُّ المرتبطات المتسلسل)، **وhero المطلق غيرُ محسوم** (506 ⇢ 650 داخل مدى الساعة، مع إقلاعٍ بارد بعد النشر) ⇢ **يُعاد قياسُه في جولةٍ لاحقة، وإن ثبت +100 ms مستقرّاً يُعاد النظر** · warm ثابت · معيارُ الرجوع مستوفى ⇢ لا رجوع. **قرارُ الإبقاء للمالك.**
- **التالي بالخطّة:** صفحةُ الفيلم بالنمط نفسِه ⇢ ذيلُ المرتبطات ⇢ `0077`/`0078`. **صفر SQL · صفر كاش · صفر صور · صفر Supabase · صفر بياناتِ أعضاء · صفر سرّ.**

### 2026-09-02 15:40:43 UTC — D-890: صفحةُ الفيلم بنمط D-889 + حسمُ تحفّظ hero — Claude

- **حكمُ Claude بدور المراجع قبل التنفيذ:** النطاقُ والحدودُ كما في D-889؛ Before يُقاس على `8cf2d80` قبل التعديل (صفحةُ الفيلم لم تكن في §٨) · ملفٌّ واحد · نقطةُ الرجوع `8cf2d80`.
- **نُفِّذ:** Before (15:31–15:33: 4 أفلام باردة en + 2 warm ×2 + 3 ar) · `48cc29e` 15:35:10 [deploy] · البوّاباتُ على `48cc29e` بشجرةٍ نظيفة · `/api/build == 48cc29e` 15:36:55 · After (15:37–15:40: 7 باردة en + warm + 3 ar) · `8b69a3f` 15:40:43 (الدليل) · ثمّ التزامُ هذا السجلّ + §١٠.
- **الحكم: `VERIFIED`** — cast/trailer مع الترويسة (0…+32)، الترايلرُ العربيّ من +500…+640 إلى 0…+363، ratings/end كما هما (متوقَّع)، معيارُ الرجوع مستوفى. **وتحفّظُ hero في D-889 محسوم: 4 أعمالٍ إضافيّة hero−fb 333–498 مقابل 361 قبلُ ⇢ لا تراجع (كان تباينَ أوّل بايت بعد النشر).**
- **التالي:** قياسُ ذيل الاكتمال (`relatedTitles`: `/recommendations` ثمّ `/similar` متسلسلان · قوائم · مجتمع) بعلامةٍ خاصّة ⇢ إصلاحٌ واحد إن استحقّ ⇢ `0077`/`0078`. **صفر SQL · صفر كاش · صفر صور · صفر Supabase · صفر بياناتِ أعضاء · صفر سرّ.**

### 2026-09-02 15:52:18 UTC — D-891: ذيلُ الاكتمال (رفُّ المرتبطات) + اقتراحُ إغلاق `0074` داخل حدود 10D — Claude

- **التشخيص أوّلاً (على `48cc29e`):** علامةُ `related` في البثّ ⇢ الرفُّ آخرُ قطعةٍ في 6/6، عند hero +615…+699 = `/recommendations` ثمّ `/similar` متسلسلَين في `relatedTitles`.
- **حكمُ Claude بدور المراجع قبل التنفيذ:** إصلاحٌ واحد = توازي المصدرين داخل الدالّة بحفظ الترتيب والفشل الفرديّ؛ رُفض إطلاقُها قبل الموجة (يغيّر عقدَ الدالّة)؛ معيارُ الرجوع related−hero ≥ +500 وسيطاً.
- **نُفِّذ:** `4349bbe` 15:48:21 [deploy] · البوّاباتُ على `4349bbe` بشجرةٍ نظيفة · `/api/build == 4349bbe` 15:50:11 · After 15:50–15:51 (9 باردة en + 2 ar) · `dca08b7` 15:52:18 (الدليل) · ثمّ هذا السجلّ + §١١.
- **الحكم: `VERIFIED`** — related−hero 646 ⇢ 296 وسيطاً · end−hero ⇢ 297 · معيارُ الرجوع مستوفى. **اقتراحٌ للمالك: `LOOPZ-AUD-0074 = RESOLVED (10D scope)`** — ما بقي (رحلةُ TMDB الأولى ≈ 0.35 s · الترايلر العربيّ برحلتين · ويكي‑بيانات) كاشٌ/معاملات ⇢ يُرحَّل بنداً لمرحلة الكاش.
- **التالي:** `0078` (لا نداءَ `/api/season` لزائر) ثمّ `0077`. **صفر SQL · صفر كاش · صفر صور · صفر Supabase · صفر بياناتِ أعضاء · صفر سرّ.**

### 2026-09-02 16:13:13 UTC — D-892: `0078` للزائر (لا طلب + سطرٌ صادق) — وعيبٌ مرئيٌّ مكتشَف — Claude

- **التشخيص أوّلاً (`/show/1396` @ `4349bbe`، اللوحُ ظاهر، زائر):** `/api/season` ⇢ 401 عند الإقلاع ⇢ رسالةُ فشلٍ كاذبة فوق المواسم لكلِّ زائر. كُتبت ثلاثةُ خياراتٍ للمالك في المحادثة (١ إزالةُ الرسالة فقط · ٢ لا طلبَ + سطرٌ صادق · ٣ فتحُ الحلقات للزائر).
- **المالك (وقتُه غيرُ مسجَّل، قبل `b1c2431`):** الخيار ٢.
- **نُفِّذ:** ثلاثةُ التزامات (مفتاح ⇢ مكوّن بخاصيّةٍ اختياريّة ⇢ صفحة `[deploy]`) · البوّاباتُ على `94c9a71` بشجرةٍ نظيفة · `/api/build == 94c9a71` 16:11:29 · Before/After من اللوح ظاهراً (0 طلب · لا رسالة · السطر en/ar) · `9bfa1a0` 16:13:13 (الدليل) · ثمّ هذا السجلّ + §١٢.
- **الحكم: `VERIFIED` · `0078 RESOLVED`.** **مطلوبٌ من المالك:** تأكيدُ مسار العضو بجلسته · ترقيمُ العيب المرئيّ (`0080` P1) أو دمجُه.
- **التالي:** `0077` (قياسٌ ثمّ إصلاح). **صفر SQL · صفر كاش · صفر صور · صفر Supabase · صفر بياناتِ أعضاء · صفر سرّ.**

### 2026-09-02 16:52:56 UTC — D-893: `0077` أبوابُ الرفوف والتذييل بلا prefetch — Claude

- **المالك:** «نفّذ الصحّ، لا أعلم» ⇢ القرارُ هنا: `0077` التالي بالخطّة؛ الإصلاحُ يتبع قرارَ البطاقات القائم.
- **القياس قبل (`/news` @ `94c9a71`، اللوحُ ظاهر):** 8 طلبات RSC مسبقة (most-popular ×3 · top-ten ×3 · `/search` ×2).
- **نُفِّذ:** `f9fa8b4` (PosterRail + LandingHero) ⇢ القياس: ما زالت 6 ⇢ المصدرُ `RankedRail` ⇢ `254ce48` (RankedRail + CountdownRail) ⇢ **2** (`/search` فقط) · `/`: 4 ⇢ 0 · البوّاباتُ على كلٍّ بشجرةٍ نظيفة · `/api/build == 254ce48` 16:50:57 · smoke «الكلّ» ✅ · `4d02bed` 16:52:56 (الدليل) · ثمّ هذا السجلّ + §١٣.
- **الحكم: `VERIFIED` · `0077 RESOLVED`.** المتبقّي خارج البند: prefetch رابط `/search` في الرأس.
- **حالةُ 10D:** `0069` · `0074` (10D scope) · `0077` · `0078` مغلقة؛ `0076` مرشَّحٌ أخير داخل 10D؛ `0073`/`0075`/`0079` لمراحل لاحقة؛ `0070` `BLOCKED`. **صفر SQL · صفر كاش · صفر صور · صفر Supabase · صفر بياناتِ أعضاء · صفر سرّ.**

### 2026-09-02 17:25:42 UTC — D-894: `0081` ويكي‑بيانات قبل الترويسة (من بلاغ المالك) — Claude

- **المالك:** «كلّ شيء شغّال، فقط أحسّ بثقلٍ وبطء، لكن استمرّ على الخطّة» + إذنُ استخدام متصفّحه (غيرُ مسجَّلِ الدخول في loopztv.com — لم أسجّل بنفسي).
- **القياس من متصفّحه (ar، 4g، rtt 200):** `/show/60059` ترويسة 2,017 ثمّ 1,686 (TMDB دافئ) ⇢ رحلةُ ويكي‑بيانات وحدَها ≈ 1.5 s = المهلة؛ من اللوح 2/8 عند 1.4–2.1 s. **الجذر في الشيفرة:** `displayWorkTitle` بعد الموجة وقبل الترويسة، والفشلُ لا يُخبَّأ.
- **سُئل المالك (٣ خيارات) ⇢ أ.** نُفِّذ في ثلاثة التزامات (lib ⇢ show ⇢ movie [deploy]) · البوّاباتُ على `f428584` بشجرةٍ نظيفة · `/api/build == f428584` 17:22:57 · After: السقف 1,929 ⇢ 1,518، الوسيط 452 ⇢ 548 (داخل التباين) · `6c9be0d` 17:25:42 (الدليل) · ثمّ هذا السجلّ + §١٤.
- **الحكم: `VERIFIED` بحدود** (المكسبُ المؤكَّد = السقف؛ الذاكرةُ بالبناء). **`0081` جديد P1 للعربيّة — معالَجٌ جزئيّاً؛ الجذرُ (بطءُ النقطة العامّة) خارجَ أيدينا؛ البديلُ الجذريّ لاحقاً: جدولُ عناوينَ عربيّة عندنا أو الخيار ب.**
- **ملاحظة:** ما يبقى من «الثقل» على شبكة المالك بعد D-889…D-894 = الصور على 6 Mbps (`0075`/`0079`) ⇢ مرحلةُ الصور بقراره. **صفر SQL · صفر كاش · صفر صور · صفر Supabase · صفر بياناتِ أعضاء · صفر سرّ.**

### 2026-09-02 — بعد `4c6d2a0` 17:27:55 UTC (وقتُ هذا الالتزام في `git log`) — مرحلةُ الصور: تشخيصٌ بلا تعديل من متصفّح المالك — Claude

- **المالك:** «تمام أكمل» (على توصيتي بالصور). **لا شحنة.**
- **القياس من متصفّحه (DPR 1، 843px، 4g rtt 200، 6 Mbps):** `/show/1396` و`/show/1668`: خلفيّةٌ واحدة `w=1920`، شعارٌ واحد `w=64` ⇢ **`0075`/`0079` غيرُ مستنسخَين على متصفّحٍ حقيقيّ = `LIKELY_TOOLING`** (اللوحُ يبدأ مخفيّاً بنافذةٍ صغيرة فيقيّم `sizes`/DPR مرّتين) — الهاتفُ غيرُ مقيس. TTFB 470–594 · DCL 724–882 · load 1.2–1.8 s · 48–51 طلباً · 573–610 KB (كاش دافئ). **الوثيقةُ عنده 192 KB بـ`encoded == decoded`** — ضغطٌ مفقودٌ على مساره أم قصورُ تقرير؟ `UNKNOWN` — يفحصه في DevTools.
- **اكتشاف `0082` (P2):** أكبرُ ستّة موارد في صفحته `/i/w342/` (40–58 KB) — بطاقاتٌ 137–176 px على DPR 1 يكفيها `w185` (17–20 KB، مقيس من اللوح لأربعة ملصقات؛ `w500` = 77–112). D-841 يتجاهل العرضَ عمداً («رابطٌ واحد»)؛ افتراضُه (5–11 KB) لا يصحّ لـ`w342`. **اقتراحٌ واحد:** دلوٌ من العرض نزولاً فقط في `imageLoader`؛ الثمن مدخلا خبيئة لكلِّ ملصق. **بانتظار قرار المالك.**
- **الحكم:** §١٥ `READY_FOR_REVIEW`؛ ترتيبُ ما يبقى من «الثقل»: الشبكة ⇢ الوثيقة (إن كانت بلا ضغط) ⇢ `0082` ⇢ رحلةُ TMDB الأولى (مرحلةُ الكاش). **صفر تعديل · صفر SQL · صفر كاش · صفر بياناتِ أعضاء · صفر سرّ.**

### 2026-09-02 18:09:09 UTC — D-895: `0082` ملصقاتُ 1x بـ`w185` — وتصحيحُ قراءتين — Claude

- **المالك:** «نفّذ — w185 حين يكفي، وإلا w342».
- **حكمُ Claude بدور المراجع قبل التنفيذ:** ملفّان في `src/lib`؛ نزولاً فقط؛ الجوّال وDPR ≥ 1.5 بلا تغيير؛ الثمنُ مدخلا خبيئة؛ Before/After = srcset في HTML + المقاساتُ المطلوبة من متصفّح المالك (DPR 1) ومن اللوح (DPR 1.5).
- **نُفِّذ:** `54bb5fd` 18:03:47 [deploy] · البوّاباتُ بشجرةٍ نظيفة · `/api/build == 54bb5fd` 18:05:33 · After: srcset `w185 1x, w342 2x`؛ متصفّحُ المالك `w185` فقط (7 ملصقات = 47 KB)؛ اللوح `w342` 56/56 · `7b3ed54` 18:09:09 (الدليل) · ثمّ هذا السجلّ + §١٦.
- **الحكم: `VERIFIED` · `0082 RESOLVED`.** **تصحيحٌ (أُخطئتُ في §١٥ وأُثبته):** «الوثيقةُ بلا ضغط» — لا؛ `encoded == decoded` سمةُ مدخل التنقّل عبر SW، والوثيقةُ `br` (قياسُ `fetch`). وأزمنةُ متصفّح المالك عبر الأتمتة تبويبٌ خلفيّ مخنوق (8,855 ms مقابل 541 من اللوح على الجهاز والشبكة نفسِهما) ⇢ صالحةٌ للجرد لا للزمن؛ **ثقلُه في المقدّمة غيرُ مقيسٍ من هنا**.
- **مرحلةُ الصور:** `0075`/`0079` `LIKELY_TOOLING` · `0082` مغلق. **التالي بكلمة المالك:** `0076` أم مرحلةُ الكاش. **صفر SQL · صفر كاش · صفر Supabase · صفر بياناتِ أعضاء · صفر سرّ.**

### 2026-09-02 18:30:55 UTC — D-896: ترايلرُ العربيّة برحلةٍ واحدة · `0076` مقيسٌ ومتروك — Claude

- **المالك:** «أكمل وأجّل تحديث الملفّات» ⇢ اختيرَ الذيلُ الأكبر المقيس (ترايلرُ العربيّة برحلتين) بعد قياس `0076` (≈ 2 KB سلكاً ⇢ لا يستحقّ sprite).
- **حكمُ Claude بدور المراجع قبل التنفيذ:** ملفٌّ واحد؛ معاملٌ واحد؛ التفضيلُ والسقفُ كما كانا؛ مفتاحُ كاشٍ جديد مُعلَن؛ Before/After = trailer−hero بالعربيّة على أعمالٍ جديدة.
- **نُفِّذ:** `e254b2f` 18:24:06 [deploy] · البوّاباتُ بشجرةٍ نظيفة · `/api/build == e254b2f` 18:25:59 · After: 0 ms (6/6) ومفتاحٌ موجود 6/6 · `a353427` 18:30:55 (الدليل) · ثمّ هذا السجلّ + §١٧.
- **الحكم: `VERIFIED`.** الأثرُ المجمَّع على أوّل فتحةٍ عربيّة باردة: الطاقم 0 · الترايلر 0 (كان +630…+720) · التقييم +249 · المرتبطات +296. **تحديثُ ملفّات المشروع مؤجَّلٌ بأمر المالك.** **صفر SQL · صفر SW · صفر صور · صفر Supabase · صفر بياناتِ أعضاء · صفر سرّ.**

### 2026-09-02 19:07:59 UTC — Phase 9 (جاهزية Expo) `READY_FOR_REVIEW` · القراراتُ السبعة بالتفويض — Claude

- **المالك:** «لا أعرف — اتخذ الأفضل للوبز ولتجربة المستخدم، وما الذي يبقى قبل بناء التطبيق؟» ⇢ سُلِّمت القراراتُ السبعة (Phase 9 §٠) وقائمةُ ما يبقى (بوّابة 11 مقابل الواقع: `0006` · `0040` · `0008` P1 مفتوحة، وWeb Vitals/البوّابة البصرية للمالك) ⇢ **«أبدأ»** (18:45 UTC تقريباً؛ الدقيقة غير مسجَّلة).
- **حكمُ Claude بدور المراجع قبل التنفيذ:** المرحلة 9 توثيقٌ وتصميمٌ خالص — لا تمسّ الإنتاج ولا تخالف بوّابة 11، وهي شرطٌ للتطبيق على أيّ حال؛ فهي الخطوةُ الصحيحة في انتظار قرار `0006`.
- **نُفِّذ:** `7f0609d` 19:07:59 (الدليل: السكربت وناتجُه) ثمّ الالتزامُ التالي (التقرير + هذا السجلّ) — `PHASE_9_EXPO_READINESS.md`: §١ أرقامُ `e254b2f` (49 صفحة · 14 API · 164 action/6,178 · 121 reader/6,427 · 131 rpc · 53 جدولاً · 182 `revalidatePath`/4) · §٢ مصفوفةُ `src/lib` (118 ملفاً: **SHARE 85/19,792 سطراً · SERVER 16/17,888 · REWRITE 16/2,204 · DROP 1**) بسكربتٍ مرفق + مراجعةٍ يدويّة لخمسة ملفات · §٣ الحزم (9+8؛ خطرُ React المزدوج) · **§٤ تصميمُ `0008`: الخيار A — `/api/v1` Route Handlers بـBearer فوق `core` مشترك؛ B (Supabase مباشرةً) وC (Edge Functions) مرفوضان بأسبابهما؛ وسومُ إبطالٍ تعيدها الكتابات بدل 182 `revalidatePath`؛ لا `service_role`؛ لا يغلق `0040`** · §٥ البنية (مستودعٌ واحد، `src/core/` + ESLint أوّلاً) · §٦ مصفوفةُ الميزات ونطاقُ v1 المقترح · §٧ قراراتُ التطبيق بنداً بنداً مع «من يقرّر» · §٨ ما يحتاجه المالك · §٩ ترتيبُ البناء (10 خطوات) · §١٠ ما لم يُفعل.
- **ما لم يُفعل:** لا تثبيتَ لأيّ حزمة (الأحكامُ من الوثائق) · لا كودَ `v1` · `0013` تبقى `TODO` (معرّفات المالك) · **لا التزامَ على `main`** · تحديثُ ملفّات المشروع مؤجَّلٌ بأمر المالك.
- **الحكم: `READY_FOR_REVIEW` — والمراجعُ هنا المالك نفسه** (تصميمُ `0008` ونطاقُ v1 وقراراتُ §٧/§٨-ب قراراتُ منتَجٍ وبنية لا تُتَّخذ بالتفويض). `0008` ⇢ `DESIGN_PROPOSED`.
