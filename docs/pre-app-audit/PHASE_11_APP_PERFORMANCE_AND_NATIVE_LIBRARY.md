# Phase 11 — أداءُ التطبيق الهجين + تجربةُ «المكتبة أصليّة»

> **الحالة:** `TODO` (خطّةٌ معتمدةٌ للتنفيذ بأمر المالك ٧ سبتمبر ٢٠٢٦ — «نحتاج نرفع
> سرعة الأداء وسرعة الاستجابة لأفضل مستوى، ونحوّل إحدى الصفحات native ونقارن،
> وابدأ بالمكتبة مطابقةً لتصميمها الحاليّ بنسبةٍ لا تقلّ عن ٩٨٪»).
> **المنفّذ:** Claude · **المراجع/البوّابة:** ChatGPT (بروتوكول `SHARED_PROGRESS.md`).
> **رأسُ `main` عند الكتابة:** `e117ea77` · التطبيق **1.2.1** (D-932) قيد مراجعة Play.
> **رقمُ القرار المحجوز:** D-935 (الأداء) · D-936 (المكتبة الأصليّة).
> **التوقيت:** المسودةُ بُدئت ≈12:40 UTC؛ **الالتزامُ الأوّل `40d15d4` عند 20:00:07 UTC** (وقتُ
> `git log` هو المرجع — وذكرُ 12:40 في رسالته خطأُ تسمية). **v2** بعد مراجعة ChatGPT
> `CHANGES_REQUESTED` (تعليق `5575170165`، 20:03:50 UTC): البنود ٣–٧ أدناه.

---

## ٠. الحقائقُ التي تحكم الخطّة (لا تُعاد مناقشتُها)

| # | الحقيقة | المصدر |
|---|---|---|
| ٠.١ | التطبيقُ **WebView على `loopztv.com`** (`apps/mobile/app/web.tsx`)؛ الأصليُّ ثلاثةُ مواضع فقط: الدخول، شاشةُ الانقطاع، الودجت | D-922 · D-925 · D-929 |
| ٠.٢ | **الجلسةُ كوكي الـWebView وحدَه** — لا جلسةَ أصليّةً ولا تجديدَ أصليّاً بعد التسليم | D-922 · D-932 |
| ٠.٣ | `/api/v1` قائمٌ ومختبَر (`home` · `discover` · `title` · `track/*` · `me/library`) ويقبل `Authorization: Bearer` والكوكي معاً | D-905 · `src/lib/supabase/server.ts` |
| ٠.٤ | القاعدةُ ليست عنقَ الزجاجة (متوسّط ١ م.ث)، **لكن ~٤٥٪ من عملها شارتان في الترويسة** (`unread_signals` + `unread_shares`) | D-924 (`/admin/health`) |
| ٠.٥ | زمنُ Vercel غيرُ مقيس: أدواتُه ترفض المشروع (403)، Speed Insights مغلقٌ بقرار كلفة | D-924 · D-931 · `18` |
| ٠.٦ | حكمُ المالك في D-922: «لازم يكون مطابق ١٠٠٪» ⇢ حُذفت الشاشاتُ الأصليّة (D-919/D-920) لأنّها اختلفت بصريّاً. **Phase 11-B تجربةٌ مقيسة لا نقضٌ لـD-922** — النقضُ إن وقع يكون بقراره بعد الأرقام | D-922 |
| ٠.٧ | مسارُ النشر: رقعُ D-913 إلى `main` بـ`[deploy]`؛ **كلُّ دفعةٍ تمسّ `apps/mobile/**` تبني وترسل إلى Alpha تلقائيّاً (~١٠ د)** — فتعديلاتُ التطبيق تُجمَع في دفعات | D-913 · D-915 |
| ٠.٨ | الحاويةُ لا تصل إلى `loopztv.com` ولا `expo.dev`؛ **القياسُ على الجهاز من متصفّح المالك أو من هاتفه** | `19` §3 |

⚠️ **تحدٍّ مسجَّلٌ للمالك (القاعدة ٧):** نسبةُ ٩٨٪ بلا تعريفٍ قابلٍ للقياس هي ما أسقط D-919/D-920 («ليه شكله يختلف»). **فالخطّةُ تُعرّف ٩٨٪ رقماً** (§٢.٣) قبل أن يُكتب سطرٌ أصليٌّ واحد، **وتضع بوّابةَ إيقافٍ مبكّر** (§٢.٦): إن لم يتفوّق الأصليُّ على الـWebView بفرقٍ محسوس **يُوقَف ويُسجَّل**، لا يُكمَل عناداً.

---

## ١. Phase 11-A — أداءُ الغلاف والويب داخلَه (D-935)

### ١.١ المبدأ
🔑 **يُقاس قبلُ وبعدُ على الجهاز نفسِه، ولا يُشحن تحسينٌ بلا رقمَين.** الغلافُ WebView، فـ٨٠٪ من سرعته سرعةُ الموقع؛ و٢٠٪ في إقلاع الغلاف نفسِه.

### ١.٢ خطُّ الأساس (11-A0) — قياسٌ بلا تعديل
**الأدوات (لا أداةَ مدفوعة)** — ولكلِّ مقياسٍ علامةُ بدايةٍ وعلامةُ نهايةٍ وأداةٌ مسمّاة؛
**ولا يُدمج رقمان من مصدرَين في رقمٍ واحد:**

- **إقلاعُ الغلاف (بارد):** `adb shell am force-stop com.loopztv.app` ⇢
  `adb shell am start -W -n com.loopztv.app/.MainActivity` ⇢ يُقرأ `TotalTime`
  (من الأمر إلى أوّل إطارٍ للنشاط). **هذا زمنُ الغلاف وحدَه** — لا يشمل الويب.
- **أوّلُ محتوى ويب:** علامةٌ صريحةٌ من الجسر: الغلافُ يسجّل `Date.now()` عند
  `onLoadStart` و`onLoadEnd` ويكتبهما في `console.log` (يظهران في `adb logcat
  -s ReactNativeJS`)، **والصفحةُ** تبثّ `{type:"perf", mark:"first-content", t}` عند
  أوّل `requestAnimationFrame` بعد ترطيب الرئيسيّة (`performance.mark`). **هذه
  أداةٌ وحدَها (A0-prep)**: تسجيلٌ لا سلوك، تُشحن قبل A0 في دفعة EAS واحدة،
  ولا تُعدّ «تحسيناً».
- **التنقّل والشبكة والتمرير:** `chrome://inspect` ⇢ WebView التطبيق **بعد
  الاتّصال** (لا يلتقط الإقلاع — لذلك العلامتان أعلاه): Performance recording
  لكلِّ انتقال (TTFB · LCP · long tasks) وNetwork لعدد الطلبات وحجمها وHIT/MISS،
  وFPS من Performance ⇢ Frames. **Lighthouse ليس بوّابة** داخل WebView — يُستعمل
  إن عمل كمؤشّرٍ إضافيّ فقط.
- **الخادم:** `/admin/health` (زمنُ القاعدة، نسبةُ الشارتَين) من متصفّح المالك.

**شروطُ كلِّ جولة تُسجَّل معها:** إصدارُ التطبيق (رمزُ الحزمة) · الجهاز والنظام · حالةُ
الدخول (زائر/مسجَّل، الحساب) · الشبكة (Wi-Fi/4G، المشغّل) · حالةُ الخبيئة (بعد
مسح بيانات التطبيق = بارد؛ ثانيةٌ فصاعداً = دافئ) · عددُ الجولات (٣، ويُسجَّل
الوسيط والمدى). **`evidence/PHASE_11_A0_<sha>.txt` يحمل الخامَ كلَّه لا الوسيطَ وحدَه.**

| المقياس | كيف يُقاس | يُسجَّل |
|---|---|---|
| **إقلاعُ الغلاف (بارد)** | `am force-stop` ⇢ `am start -W` ⇢ `TotalTime` | ms (٣ جولات، وسيط + مدى) |
| **أوّلُ محتوى ويب (بارد)** | علامةُ الجسر `first-content` − وقتُ `am start` (الساعةُ نفسُها: logcat) | ms، **رقمٌ منفصلٌ عن الأوّل** |
| **إقلاعٌ دافئ** | خلفيّةٌ ⇢ مقدّمة ⇢ `onLoadEnd` أو استئنافٌ بلا تحميل | ms |
| **التنقّل** | الرئيسيّة ⇢ المكتبة ⇢ اكتشف ⇢ عمل ⇢ رجوع | ms لكلِّ انتقال (TTFB + LCP) |
| **التمرير** | شبكةُ المكتبة (٥٠+ عنصر) · صفّ الترايلرات | FPS، long tasks > 50ms |
| **الشبكة** | عددُ الطلبات وحجمُها لكلِّ صفحة · نسبةُ cache HIT | من Network |
| **الخادم** | `/admin/health`: زمنُ القاعدة، نسبةُ الشارتَين | من الصفحة |
| **الجدار** | هل تظهر صفحةُ تحدّي Vercel في الغلاف؟ | نعم/لا |

**المخرَج:** جدولٌ في `SHARED_PROGRESS.md` (Claude Check-in) + `evidence/PHASE_11_A0_<sha>.txt`. **وكلُّ رقمٍ يُسمَّى بشروطه** (جهاز · شبكة · بارد/دافئ · زائر/مسجَّل).

### ١.٣ حزمُ التحسين — بترتيب الأثر المتوقَّع، حزمةٌ واحدةٌ في كلِّ دفعة

| # | الحزمة | ما يُغيَّر | لماذا | الملفّات | الكلفة | ما قد ينكسر |
|---|---|---|---|---|---|---|
| **A1** | **شارتا الترويسة** | عدُّ الجديد بسؤالٍ مباشرٍ بدل تشغيل `my_signals()` كاملةً ثمّ عدِّها؛ أو عدّادٌ مخزَّنٌ يُبطَل بالحدث | ~٤٥٪ من عمل القاعدة (D-924 §١) — **أكبرُ مكسبٍ في زمن الخادم لكلِّ صفحة** | `Navbar` · `src/lib/data.ts` · دالّةٌ SQL (DDL — المالكُ يشغّلها) | ½ يوم | رقمٌ خاطئٌ على الجرس ⇢ اختبارُ حالتَين قبل/بعد القراءة |
| **A2** | **RLS initplan** | `auth.uid()` ⇢ `(select auth.uid())` في ١١٩ سياسة | يُعاد تقييمُها لكلِّ صفّ؛ مستشارُ Supabase نفسُه | `supabase/security*.sql` — DDL على الإنتاج بيد المالك بعد `loopz-preview` | ½ يوم (+ تشغيلُه) | لا تغييرَ منطق — فرقُ سلوكٍ = خطأٌ في النسخ |
| **A3** | **خبيئةُ الموجِّه** | `staleTimes.dynamic` ١٨٠ ثانية قائمة ✅ — يُضاف **`prefetch` انتقائيّ**: تبويباتُ الشريط السفليّ تُحمَّل مسبقاً عند **أوّل لمسةٍ للشاشة** (لا عند التحويم — لا تحويمَ على الجوّال) | الرجوعُ فوريّ، والانتقالُ بين التبويبات من الذاكرة | `BottomNav` · `Link prefetch` | ¼ يوم | طلباتٌ زائدةٌ للزائر ⇢ يُقاس عددُها في A0/A1 |
| **A4** | **إقلاعُ الغلاف** | (أ) `cacheEnabled` + `cacheMode: LOAD_DEFAULT` صريحَين · (ب) **preconnect** إلى `loopztv.com` و`supabase.co` و`image.tmdb.org` في `<head>` · (ج) شاشةُ البداية تبقى حتى `onLoadEnd` (قائمة ✅) · (د) `androidLayerType="hardware"` | إقلاعُ WebView الأندرويد ٣٠٠–٦٠٠ م.ث قبل أوّل بايت؛ TLS ثلاثُ رحلاتٍ تُدفَع مسبقاً | `apps/mobile/app/web.tsx` · `src/app/layout.tsx` | ¼ يوم | **التزامٌ مستقلٌّ وقياسٌ مستقلّ**؛ يشترك مع A6 في **بناء EAS واحدٍ فقط** (الالتزامان متتاليان في دفعةٍ واحدة إلى `main` — البناءُ يُقاس مرّتين: A4 وحدَه على APK محلّيّ `preview`، ثمّ A4+A6 على Alpha) |
| **A5** | **الصور** | تأكيدُ `sizes` الصحيحة في شبكة المكتبة (D-895 عالج الرايل)؛ `priority` لأوّل صفٍّ فقط؛ **لا تحويلَ مدفوعاً لما دون w780** (D-841/D-931 ✅) | LCP في المكتبة صورةٌ دائماً | `LibraryGrid` · `PosterTile` | ¼ يوم | لا شيء — نزولٌ في الحجم فقط |
| **A6** | **الغلاف — الاستجابة** | (أ) `onShouldStartLoadWithRequest` لا يُنشئ `URL` لكلِّ طلبٍ فرعيّ (يُفلتر على التنقّلات الرئيسيّة فقط) · (ب) `pullToRefresh` يبقى · (ج) **فحصُ تحدّي Vercel** وإن ظهر ⇢ استثناءُ `LoopzApp/` في قاعدةٍ مخصّصة أو إيقافُ Bot Protection (D-931) | كلُّ طلبٍ فرعيّ (صور، خطوط) يمرّ من الجسر JS⇄Native | `web.tsx` · جدارُ Vercel من متصفّح المالك | ¼ يوم | ضبطُ الجدار يُنشر بخطوتين (Review ⇢ Publish) |
| **A7** | **الحراسة** | `web-vitals` خفيفةٌ ترسل LCP/INP/CLS إلى مسارٍ داخليٍّ يكتب في `runtime_errors`-like جدولٍ **موجود**؟ — **لا**: يُرفَض جدولٌ جديدٌ الآن؛ البديلُ **قياسٌ يدويٌّ موثَّق بعد كلِّ حزمة** (A0 يتكرّر) | صفحةٌ تعرض رقماً لا تقيسه أسوأُ من غيابه (D-063) | — | ٠ | — |

⚖️ **ما لا يُفعل عمداً:** لا Speed Insights مدفوع (قاعدةُ الكلفة 00 §21–26) · لا Service Worker جديد (الـPWA قائمة) · لا CDN ثانٍ · لا تقسيمَ أدوار «موقع للتطبيق» — **مصدرٌ واحد** (D-922).

### ١.٤ بوّاباتُ 11-A
- 🔑 **حزمةٌ واحدة = التزامٌ واحد = قياسٌ واحد.** لا يُدمج التزامان في قياس؛
  الاستثناءُ الوحيد **بناءُ EAS** (كلفةُ عدّاد) — يجوز أن يحمل بناءٌ واحدٌ التزامَي
  A4 وA6 **بشرط قياس A4 وحدَه على APK `preview` قبل ضمّ A6.**
- **A2 لا يبدأ قبل بوّابة §١.٥** (تكافؤُ السياسات على `loopz-preview`).
- كلُّ حزمة: نقطةُ رجوعٍ موثَّقة (SHA) · `tsc` + `eslint` + `next build` · رقعةُ D-913 مطابقةٌ بايتاً · **Before/After على الجهاز نفسِه** · Smoke على الإنتاج (`/api/build` من متصفّح المالك).
- **Regression ⇢ rollback فوراً** (توجيهُ 00:55 UTC في Phase 10).
- **هدفُ 11-A كاملةً** (يُراجَع بعد A0): إقلاعٌ بارد ≤ ٢٫٥ ث على STC 4G · التنقّلُ بين التبويبات ≤ ٦٠٠ م.ث دافئاً · تمريرُ المكتبة ≥ ٥٥ FPS · زمنُ القاعدة لكلِّ صفحةٍ −٤٠٪. **أرقامٌ مبدئيّةٌ حتى يثبت A0 — لا وعود.**

### ١.٥ بوّابةُ A2 — ١١٩ سياسة لا تُلمس بلا دليلِ تكافؤ
1. **جردٌ قبل/بعد:** `select polname, tablename, cmd, roles, qual, with_check from pg_policies order by 1,2` ⇢ ملفٌّ + `sha256` **قبل** التغيير على `loopz-preview`، ثمّ بعده؛ **الفرقُ المسموح به حرفيّاً: `auth.uid()` ⇢ `(select auth.uid())` وما عداه صفر.**
2. **مصفوفةُ أدوارٍ × حالات** على preview: لكلِّ جدولٍ متأثّر: `anon` · `authenticated` صاحبُ الصفّ · `authenticated` غيرُ صاحبه · `service_role`، × `select/insert/update/delete` ⇢ يسمح/يمنع **قبل وبعد متطابقان ١٠٠٪** (سكربت `psql` مولَّد، ناتجُه في `evidence/PHASE_11_A2_matrix_<sha>.txt`).
3. **زمنٌ قبل/بعد** على preview لاستعلامَين تمثيليَّين (`EXPLAIN ANALYZE`) — الدليلُ على أنّ المكسبَ حقيقيّ.
4. **التطبيقُ على الإنتاج بيد المالك الصريح** (`apply_migration` بعد «نفّذ» بنصّه)، **مع سكربتٍ عكسيٍّ جاهزٍ** (`(select auth.uid())` ⇢ `auth.uid()`) في `supabase/` قبل التشغيل.
5. **بعد الإنتاج:** الجردُ نفسُه على الإنتاج يطابق جردَ preview-بعد؛ و`runtime_errors` لا يرتفع خلال ساعة (درسُ `0040`: **نجاحُ البناء لا يثبت تكافؤَ RLS** — سحبُ صلاحيّات كسر مساراً حيّاً بـ١٥ ألف خطأ).

---

## ٢. Phase 11-B — المكتبةُ أصليّةً كتجربةٍ مقارنة (D-936)

### ٢.١ لماذا المكتبةُ أوّلاً (وليس الرئيسيّة)
- **بياناتُها صفرُ رحلاتٍ خارجيّة** (`/api/v1/me/library`: من `follows` وحدَه — D-905) ⇢ الفرقُ المقاس فرقُ **تصيير** لا شبكة.
- **صفحةٌ ذاتُ قائمةٍ طويلة** — حيث يُفترض أن يتفوّق `FlatList` الأصليّ على DOM.
- **مسجَّلةٌ دائماً** (لا حالةَ زائر) ⇢ حالةٌ واحدة أقلّ.

### ٢.٢ المعماريّة — بلا نقضٍ لـD-922
```
WebView (loopztv.com) ──bridge {type:"native", route:"library"}──▶ apps/mobile/app/library.tsx
        ▲                                                               │
        │  session: الكوكي (المالكُ الوحيد للتجديد)                     │ fetch /api/v1/me/library
        │                                                               │ Authorization: Bearer <access>
        └── {type:"session", access, exp} ◀── الصفحةُ تبثّ رمزَ الوصول ─┘  (قراءةٌ فقط، لا تجديد)
```
- 🔑 **الجلسةُ تبقى للـWebView**: الصفحةُ تبثّ **رمزَ الوصول وحدَه** (ساعةٌ) عبر الجسر عند التحميل وعند التجديد؛ الأصليُّ يستعمله `Bearer` **ولا يجدّد أبداً** — عند 401 يطلب من الصفحة رمزاً جديداً (`injectJavaScript`). **هذا ما بُني D-922 لحمايته** (رمزُ تجديدٍ واحدٌ بمالكٍ واحد).
- **الدخولُ إلى الشاشة**: زرُّ «المكتبة» في الشريط السفليّ للويب يبثّ `native` **حين يكون داخل الغلاف وعلمُ التجربة مفعّلاً** — **فالمقارنةُ A/B على الجهاز نفسِه بضغطة**، لا ببناءَين.
  🔑 **العلمُ محسوم: `profiles.is_admin`** (صفّان فقط: المالك وKHLD — D-923). لا عمودَ جديداً ولا إعدادَ مستخدم؛ الزرُّ الأصليُّ لا يُصيَّر أصلاً لغير الإدارة (شرطٌ خادميٌّ في `BottomNav`، لا `hidden` عميليّ) — **فالمختبِرون الاثنا عشر لا يرون التجربةَ ولا أثرَها**. عند B6 (أ) يُستبدل بعلمٍ صريح.

- **الكتابة** (تعليمُ حلقةٍ/إسقاط/متابعة) عبر `/api/v1/track/*` القائمة؛ **التفاؤلُ ثمّ الارتداد** كما في الويب.
- **الرجوعُ** من الأصليّة يعود إلى WebView دون إعادة تحميل (الـWebView تبقى مركَّبةً تحتها — `display:none` لا unmount).

### ٢.٢-ب عقدُ أمان B1 — ملزِمٌ قبل أوّل سطر (بطلب المراجع، مقبولٌ نصّاً)
1. **رمزُ الوصول في ذاكرة الغلاف فقط** (`useRef`/متغيّرُ وحدة): **لا SecureStore، لا AsyncStorage، لا سجلّات (`console.*`)، لا تحليلات، لا `runtime_errors`.** ويُمسح عند خروج التطبيق من المقدّمة أكثرَ من ٥ دقائق.
2. **تحقّقٌ صارمٌ من الرسالة**: `onMessage` يقبل `{type:"session", access, exp}` فقط إذا: `nativeEvent.url` مضيفُه في `INSIDE` (`loopztv.com` · `www.loopztv.com` · `meshahed.vercel.app`) **و**`typeof access === "string"` بثلاثة مقاطع JWT **و**`exp` عددٌ مستقبليّ. **أيُّ رسالةٍ أخرى تحمل حقلاً اسمُه `access` تُهمل وتُعدّ خطأً برمجيّاً (يُسجَّل النوعُ لا القيمة).** والرمزُ لا يُمرَّر إلى أيِّ رسالةٍ أو مسارٍ غيرِ `Authorization` لطلبات `loopztv.com/api/v1/*`.
3. **المسحُ الفوريّ**: الصفحةُ تبثّ `{type:"session:clear"}` عند `/auth/signout` وعند تغيّر `user.id` (المستمعُ نفسُه الذي يبثّ الرمز يراقب `onAuthStateChange`) ⇢ الغلافُ يمسح الرمزَ ويُغلق الشاشةَ الأصليّة إن كانت مفتوحة ويعود إلى WebView.
4. **401 = طلبُ رمزٍ جديد بمحاولةٍ محدودة**: الغلافُ يحقن `window.dispatchEvent(new Event("loopz:session-request"))` ⇢ الصفحةُ تبثّ رمزَها الحاليَّ (تجدّده هي عبر الكوكي إن لزم) ⇢ **محاولتان كحدٍّ أقصى** ثمّ رجوعٌ إلى WebView برسالة. **لا رمزَ تجديدٍ، ولا `/token`، ولا `/logout` من الأصليّ أبداً** — والدليلُ سجلُّ Supabase بعد B1: صفرُ نداءاتٍ لهما من `remote_addr` الجوال خارجَ تدفّق الويب.
5. **الدليلُ قبل `READY_FOR_REVIEW`**: `grep -rn "access" apps/mobile/src apps/mobile/app` يُظهر الاستعمالَ في موضعَين فقط (الاستلام والترويسة)، ولقطةٌ من سجلّات Supabase لساعةٍ من الاستعمال.

### ٢.٣ تعريفُ «٩٨٪» — رقمٌ لا انطباع
| البُعد | الأداة | العتبة |
|---|---|---|
| **بصريّ (pixel)** | لقطتان بالعرض نفسِه (١٠٨٠×٢٤٠٠) لثلاث حالات: «أشاهد» · «أكملت» · فارغة؛ **تُقنَّع صورُ البوسترات** ثمّ `pixelmatch` | **≤ ٢٪ بكسلاتٍ مختلفة** لكلِّ حالة |
| **الرموز (tokens)** | كلُّ لونٍ/نصف قطر/خطٍّ/مسافةٍ من `src/core/themes.ts` وقياساتِ `06_Component_Library.md` — **لا قيمةٌ مكتوبةٌ بيد** | ١٠٠٪ (قائمةُ فحصٍ مُرقَّمة) |
| **السلوك** | التبويباتُ الأربع · فلترُ المفضّلة · المجموعاتُ الآليّة · رفُّ القوائم العامّة · ذاكرةُ التمرير · ورقةُ ترتيب الرئيسيّة · RTL/LTR · `daylight` | قائمةُ فحصٍ: **كلُّ بندٍ ✅ أو `KNOWN_GAP` مكتوب** |
| **الخطوط** | Poppins للإنجليزيّة وTajawal للعربيّة (D-454) بالأوزان الأربعة نفسِها، مضمَّنةٌ في الحزمة | مطابقة |

**النسبةُ المُعلَنة = أدنى الأبعاد الأربعة، لا متوسّطُها.**

### ٢.٤ جدولُ التحويل (مراحل، كلُّ مرحلةٍ رقعةٌ ومراجعة)

| المرحلة | المحتوى | الملفّات | التقدير | بوّابةُ المراجعة |
|---|---|---|---|---|
| **B0** | **جردُ المكتبة الحاليّة**: كلُّ عنصرٍ في `src/app/library/page.tsx` (٥٢٢ سطراً) + `LibraryGrid` (٩٦٦) + `AutoGroups` + `LibraryFavFilter` + `PublicListsRail` + `HomeQueueSheetHost` + `ScrollMemory` ⇢ **قائمةُ التكافؤ** (ما يُنقل، ما يُعلَن `KNOWN_GAP`) + لقطاتُ المرجع الثلاث | `docs/pre-app-audit/PHASE_11_B0_LIBRARY_INVENTORY.md` | ½ يوم | ChatGPT: القائمةُ كاملةٌ ومُرقَّمة |
| **B1** | **جسرُ الجلسة والمسار**: بثُّ رمز الوصول من الصفحة · مسار `library` في `expo-router` · زرُّ التبديل خلف علم | `src/components/WidgetSync.tsx`-like مكوّنٌ للبثّ · `web.tsx` · `apps/mobile/app/library.tsx` (هيكل) | ١ يوم | 401 ⇢ رمزٌ جديد ⇢ نجاح، **بلا `/logout` ولا `/token` من الأصليّ** (سجلّاتُ Supabase) |
| **B2** | **الشبكةُ والبطاقة**: `FlatList` ثلاثيُّ الأعمدة · `PosterTile` من `core/themes` · شارةُ التقدّم · الحالاتُ الأربع (تبويبات = عائلة segmented الأصليّة الواحدة) | `apps/mobile/src/ui/*` (يُبعث `PosterTile`/`Rail` من تاريخ git `9cf7f108^` **بعد مطابقتها للرموز**) | ٢ أيّام | pixelmatch ≤ ٢٪ للحالتَين «أشاهد»/«أكملت» |
| **B3** | **الأفعال**: تعليمُ حلقة/موسم، إسقاط، إلغاءُ متابعة، فلترُ المفضّلة — تفاؤليّةٌ بارتدادٍ عبر `/api/v1/track/*` | `library.tsx` · `src/api.ts` (قائم) | ١ يوم | الفعلُ يظهر في الويب فوراً (مصدرٌ واحدٌ للحقيقة) |
| **B4** | **الملحقات**: المجموعاتُ الآليّة · رفُّ القوائم العامّة · ذاكرةُ التمرير · الحالةُ الفارغة · `daylight` · RTL/LTR | `library.tsx` · مكوّناتٌ فرعيّة | ١½ يوم | قائمةُ B0 كاملة |
| **B5** | **المقارنةُ المقيسة** على هاتفٍ واحد، الجلسةُ نفسُها، ٣ مرّات لكلّ: زمنُ «لمسةُ المكتبة ⇢ أوّلُ صفٍّ مرسوم» · FPS التمرير على ١٠٠ عنصر · الذاكرة · زمنُ الفعل (تعليمُ حلقة ⇢ تحديثُ الشارة) — **WebView مقابل أصليّ** | `evidence/PHASE_11_B5_<sha>.txt` + جدولٌ في `SHARED_PROGRESS` | ½ يوم | ChatGPT: الأرقامُ بشروطها |
| **B6** | **القرار** (المالك، بالأرقام): (أ) الأصليُّ أسرعُ بفرقٍ محسوس **و** ≥ ٩٨٪ ⇢ يُثبَّت خلف العلم ويُخطَّط للشاشة الثانية · (ب) أسرعُ لكن < ٩٨٪ ⇢ يُكمَل التكافؤ أو يُوقَف · (ج) لا فرقٌ محسوس ⇢ **يُوقَف ويُحذف** (D-922 يبقى) | `07` D-936 | — | قرارٌ مكتوبٌ بنصّ المالك |

**المجموع:** ~٧ أيّام عمل صافية.

### ٢.٥ ما يُعاد استعمالُه (لا يُبنى من الصفر)
- `apps/mobile/src/api.ts` + `contracts.ts` (قائمان، D-905) · `core/themes.ts` · `core/i18n` · `core/libraryStatus.ts` (منطقُ الحالة **نفسُه** في الويب والأصليّ — شرطُ تكافؤٍ لا زينة).
- شاشاتُ D-919/D-920 في تاريخ git (`git show 9cf7f108^:apps/mobile/...`) — **تُقرأ لا تُلصق**: سببُ حذفها أنّها اختلفت بصريّاً.

### ٢.٦ بوّابةُ الإيقاف المبكّر (تحمي وقتَ المالك)
بعد **B2** تُقاس شبكةٌ خامٌ بلا أفعال: إن كان زمنُ «لمسة ⇢ أوّلُ صفّ» في الأصليّ **≥ ٧٠٪** من زمن الـWebView (أي لا مكسبٌ يُذكر) **يُوقَف هنا** ويُسجَّل `DEFERRED` بالأرقام — **لا يُبنى B3–B4 لصفحةٍ لن تفوز.**

### ٢.٧ ما قد ينكسر — ومصدُّه
| الخطر | المصدّ |
|---|---|
| رمزُ تجديدٍ يُدوَّر من عميلَين (عطلُ D-932 نفسُه) | الأصليُّ لا يحمل رمزَ تجديدٍ **إطلاقاً**؛ فحصُ السجلّات بعد B1: صفر `/token` من الأصليّ |
| انزياحٌ بصريّ تدريجيّ بين النسختَين | كلُّ قيمةٍ من الرموز؛ pixelmatch في CI لاحقاً إن ثُبّتت |
| بناءاتُ EAS متعدّدة (كلُّ رقعةٍ تمسّ `apps/mobile`) | **دفعتان فقط** للتطبيق: B1+B2 ثمّ B3+B4؛ والباقي ويب |
| مختبِرو Alpha يرون شاشةً نصفَ مكتملة | خلف علمٍ لا يراه إلا المالك وKHLD حتى B5 |

---

## ٣. الجدولُ الزمنيّ (بافتراض جلسةِ عملٍ يوميّة)

| اليوم | التاريخ | العمل | المخرَج |
|---|---|---|---|
| ١ | الثلاثاء ٨ سبتمبر | **A0-prep** (علاماتُ الجسر — تسجيلٌ لا سلوك، بناءُ Alpha واحد) · **B0** الجرد · **11-A0** القياسُ على هاتف المالك حين يصل البناء (حضورُه ~٣٠ د: USB + `adb` + `chrome://inspect`) | `PHASE_11_B0` · جدولُ A0 (خام + وسيط) |
| ٢ | الأربعاء ٩ | **A1** الشارتان — التزامٌ وقياس (+ SQL يشغّلها المالك) · ثمّ **A3** prefetch — التزامٌ وقياس منفصلان | رقعتان `[deploy]` · Before/After لكلٍّ |
| ٣ | الخميس ١٠ | **A2** على `loopz-preview` فقط (بوّابة §١.٥: جردٌ + مصفوفة) — الإنتاجُ بيده بعد `VERIFIED` · **A4** التزامٌ + قياسٌ على APK `preview` · **A6** التزامٌ ⇢ بناءُ Alpha واحدٌ يحملهما | أدلّةُ A2 · رقعتان · بناء Alpha |
| ٤ | السبت ١٢ | **A5** الصور · إعادةُ قياس A0 ⇢ **تقريرُ 11-A** | `evidence/PHASE_11_A_AFTER` |
| ٥ | الأحد ١٣ | **B1** جسرُ الجلسة + المسار | رقعة + فحصُ سجلّات Supabase |
| ٦–٧ | الاثنين–الثلاثاء ١٤–١٥ | **B2** الشبكةُ والبطاقة ⇢ **بوّابةُ §٢.٦** | pixelmatch × ٢ حالة · قرارُ الاستمرار |
| ٨ | الأربعاء ١٦ | **B3** الأفعال | رقعة + بناء Alpha |
| ٩–١٠ | الخميس–السبت ١٧–١٩ | **B4** الملحقات + `daylight` + RTL/LTR | قائمةُ B0 كاملة |
| ١١ | الأحد ٢٠ | **B5** المقارنةُ المقيسة (حضورُ المالك ~٤٥ د) | جدولُ المقارنة |
| ١٢ | الاثنين ٢١ | **B6** القرار + تحديثُ `04`/`05`/`07`/`18` | D-935 · D-936 مكتوبان |

**ما يحتاج المالكَ حضوريّاً:** يومان فقط (١ و١١) + تشغيلُ DDL في يومَي ٢ و٣ + قرارُ B6. **كلُّ ما عداه يُنفَّذ بلا سؤال** (قاعدةُ ٤ سبتمبر).
⚠️ **الاعتمادُ الخارجيّ الوحيد:** مراجعةُ Play لـ1.2.1 — **إن تأخّرت لا يتأخّر A0**: يُقاس على 1.2.0 مع الدخول من الويب (الكوكي)، والفرقُ في الغلاف وحدَه.

---

## ٤. ما يحتاجه ChatGPT ليتابع ويقلّل الأخطاء

1. **كلُّ حزمةٍ لها سطرٌ في `Claude Check-in`** بالصيغة القائمة: `D-xxx — <اسم> (<sha> [deploy] · نقطةُ الرجوع <sha> · /api/build == <sha> · البوّابات) + Before/After`.
2. **الأدلّةُ ملفّاتٌ** في `evidence/PHASE_11_*` لا فقراتٌ إنشائيّة.
3. **بوّاباتُ المراجعة** التي تحتاج `VERIFIED` قبل المتابعة: **A0** (قبل أيِّ تحسين) · **B0** (قبل أيِّ سطرٍ أصليّ) · **§٢.٦** (بعد B2) · **B5** (قبل B6).
4. **قرارا §٤.٤ — صدرا في مراجعة `5575170165` (20:03:50 UTC):**
   - ✅ **pixelmatch ≤ ٢٪ مقبولٌ مبدئيّاً** كتعريفِ «٩٨٪» **بشرط** أن يثبّت B0: الجهازَ، الأبعادَ (١٠٨٠×٢٤٠٠)، safe-area، اللغةَ، والمحتوى نفسَه (الحسابُ نفسُه، الترتيبُ نفسُه) — **وأن يقتصر القناعُ على صور البوسترات الديناميكيّة لا على أيِّ عنصرِ واجهة.**
   - ✅ **«Bearer بلا تجديد» مقبولٌ لهذه التجربة فقط** تحت عقد §٢.٢-ب؛ **لا يُعتمد نمطاً دائماً** للشاشات القادمة حتى يجتاز B1 ويثبت سجلُّ Supabase صفرَ refresh/logout من الأصليّ — حينها يُقترح لـ`07`.
5. **ما لا يُطلب من المالك**: كلُّ ما في §١ و§٢ عدا DDL والحضور للقياس والقرار.
