LOOPZ-AUD-0006 — isolated data environment: `loopz-preview` created and the production SCHEMA copied (no data)
=========================================================================================================

owner (chat, 2026-09-03 ≈03:3x UTC): "I'm with you for an hour — anything you need, open the pages and I'll do it" then "كمل وسويها انت … عندك وصول كامل للجهاز". Cost confirmed by the owner in chat before creation: 0 $/month (free plan; second active project on the same org).

project: name `loopz-preview` · ref `zzrbebfuzfimrrutefeg` · org = the production org · region ap-south-1 (same as production and Vercel bom1) · plan Free · status ACTIVE_HEALTHY · created 2026-09-03 via the Supabase connector (create_project). The database password was generated by Supabase and never passed through the assistant.

how the schema was read (READ-ONLY, production untouched):
  the Supabase connector refused every read on the production project (auto-mode classifier), so a single catalog SELECT (`evidence/P9-…` style; the query text is `schema_export.sql`, kept locally, not committed — it names DB functions) was run in the OWNER's SQL editor in his signed-in browser: it reads pg_catalog / pg_policies / storage.buckets settings / cron.job definitions only — zero rows of member data. Result: 1,599 DDL statements (428 KB CSV) exported with "Download CSV" (owner approved the download in chat) and staged from his Downloads folder. The CSV and the generated chunks stay in the session container; nothing of it is committed (function names).
  gap found and closed the same way: one extra schema `ops` with one empty table (0 functions, 0 policies) — recreated.

how it was applied (preview only): 13 migrations through the connector (`loopz_schema_01…13`): extensions (pg_cron) · sequence · 72 tables with PK/UNIQUE/CHECK · RLS enabled on all 72 · 101 foreign keys · 177 functions (check_function_bodies off, definitions verbatim) · 1 view · 14 triggers (incl. the one on auth.users) · 84 non-constraint indexes · 128 public policies + 6 storage policies · table grants per role (explicit REVOKE+GRANT, reproducing the production ACLs incl. the three tables with no anon/authenticated privileges) · function grants per role (REVOKE ALL then GRANT EXECUTE per role from the production ACL) · 2 storage buckets (settings only) · 1 cron job · 4 realtime publication tables · schema `ops`.
  compaction declared: table/function grants were emitted as one GRANT per (role, privilege-set) with a table/function list instead of 750 single statements — same resulting ACL.

verification — production (from the export's own count row) vs preview (connector, after apply):
  tables 72 = 72 · views 1 = 1 · functions 177 = 177 · triggers 14 = 14 · policies 128 = 128 · storage policies 6 = 6 · indexes 162 = 162 · sequences 1 = 1 · FKs 101 = 101 · RLS on 72 of 72 · buckets 2 = 2 · cron jobs 1 = 1 · realtime tables 4 = 4
  function EXECUTE grants (the 0040 surface): anon 144 = 144 · authenticated 172 = 172 · PUBLIC 27 = 27 · service_role 177 = 177
  default privileges: identical shape in both projects (postgres/supabase_admin → anon, authenticated, service_role on tables, functions, sequences) — read from pg_default_acl on both sides.

NOT copied / still owner work before 0006 can close:
  · data — none by rule (no member rows). Reference tables are empty too (title_aliases 66 rows in prod, imdb_pool 4,042, imdb_chart 688, title_meta 541, title_snapshots 1,248, news_posts 45, provider links 1 — counts only); copy later only if a test needs them.
  · Auth configuration (dashboard, not DB): Google provider client id/secret, redirect URLs, JWT expiry — owner sets it in the preview project; the assistant never handles the secret.
  · test accounts (two, in the preview project) — owner creates them by signing in through the preview URL once Auth is configured.
  · Vercel: a Preview environment pointing at the preview project (URL + anon key in Preview env vars only) — owner's dashboard action; production env untouched.
  · one stale function references a table that does not exist in production either (news_items) — same in both; recorded, not fixed.
  · column/table comments, sequence grants beyond defaults: not exported (no security relevance).
status: `0006` → PARTIALLY_RESOLVED (isolated schema exists; Auth + accounts + Vercel wiring pending owner). `0040` verification can now start in the preview project without touching production.
