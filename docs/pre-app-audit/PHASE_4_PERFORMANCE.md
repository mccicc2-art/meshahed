# Phase 4 — Performance, Speed & Responsiveness Audit

- الحالة: `READY_FOR_REVIEW` · المنفّذ: Claude · المراجع: ChatGPT
- **Audited SHA:** `f8a2b33cd036cffd1e7a0b9bc3e5ced0e19b8bfa` · و**Production يشغّله** (`/api/build`, Phase 2 §1.2)
- **صفر إصلاح كود · صفر Deploy/Migration/Merge/حذف · لم تُستخدم جلسة أحمد · لا بيانات مستخدمين.**

> **القاعدة الصارمة التي فرضتَها، ملتزَمة حرفياً:** **لا رقم زمنيّ واحد في هذا التقرير.** كل توقيت متاح لي يأتي من لوح مخفيّ (`visibilityState: "hidden"`) أو من حاوية بلا منفذ للموقع — **وكلاهما يفسد القياس**. فما لا يُقاس صحيحاً **لا يُقدَّر ولا يُذكر**، ويُوسم `BLOCKED_BY_SURFACE`.
>
> **وما بقي صالحاً فهو الأحجام والأعداد والإعدادات** — وهي لا تعتمد على زمن ولا على رؤية: حجم الحزمة، عدد الطلبات، حجم الاستجابة، ترويسات الكاش، بنية الكود. **وهذه هي مادة هذا التقرير.**

---

## 1. منهجية القياس — تُقرأ قبل أي رقم

| المصدر | الأداة | البيئة | ما يصلح منه | ما لا يصلح |
|---|---|---|---|---|
| **A. مخرَج البناء** | `npm run build` ثم قياس `.next/static` | حاوية سحابية · Node v22.22.2 · Audited SHA | **أحجام الحزم والقطع** · عددها · محتواها | لا شيء زمنيّ |
| **B. Production حيّ** | `performance.getEntriesByType('resource')` في متصفّح **زائر** | Production `f8a2b33c` · لوح مخفيّ · viewport 1440×900 · عيّنة **واحدة** | **`decodedBodySize`** — حجم ما فُكّ ضغطه فعلاً · عدد الموارد · نوعها | ⚠️ **`transferSize = 0`** (خدمة من الخبيئة) فالحجم المضغوط على السلك **غير مقيس** · وكل زمن **باطل** |
| **C. المصدر** | `grep`/تحليل ثابت | Audited SHA | البنية · الإعدادات · الأنماط | لا شيء مقيس |

**كل رقم أدناه يحمل مصدره (A/B/C).** وعيّنة B **واحدة** — تكفي للحجم (حتميّ) ولا تكفي لأي توزيع (p75/p95).

---

## 2. المسار A — القياسات: ما أمكن وما لم يمكن

| البند المطلوب | الحالة | السبب |
|---|---|---|
| Cold start · Warm resume | **`BLOCKED_BY_SURFACE`** | زمن — واللوح مخفيّ |
| ظهور المحتوى الأساسي · التنقّل بين التبويبات | **`BLOCKED_BY_SURFACE`** | زمن · والصفحات الديناميكية **لا يكتمل رسمها أصلاً** في هذا السياق (Phase 2 §11) |
| صفحات movie/show/profile · القوائم الطويلة · البحث · الترايلرات | **`BLOCKED_BY_SURFACE`** للزمن · **`MEASURED`** للأحجام (§3 · §13) | **`MEASURED` وصفُ دليلٍ لا حالةَ قبول** — ولا ميزانية حجم معتمدة تُقاس عليها |
| **API timings** | **`BLOCKED_BY_SURFACE`** | زمن |
| **أحجام الاستجابات** | **`MEASURED`** (§3 · §12.1) | حجم لا زمن · وجدول المسارات الواحد والعشرين في §12 |
| **الطلبات المكررة** | **`MEASURED` على مستوى المستند** · **`BLOCKED_BY_SURFACE` في زمن التشغيل** (§11) | 🔴 **تصحيح**: كان مكتوباً «مقيسة» بإحالة إلى §3.2، **و§3.2 جردُ موارد لا إثباتُ تكرار**. الفصل في §11 |
| Web Vitals LCP/INP/CLS عند p75 | **`BLOCKED_BY_SURFACE`** | **ولا أداة صحيحة عندي.** `@vercel/speed-insights` **مركَّب في التطبيق ويجمع p75 حقيقياً من مستخدمين حقيقيين** — وهو المصدر الصحيح الوحيد، ولا وصول لي إليه (Vercel يردّ 403) |
| الشبكات/الأجهزة | **`BLOCKED_BY_SURFACE`** | محاكاة الشبكة تحتاج تحكّماً في المتصفّح لا أملكه |

> 🔑 **أهداف Web Vitals (LCP ≤ 2.5s · INP ≤ 200ms · CLS ≤ 0.1 عند p75) لا يمكن الحكم عليها من أي سطح لديّ.** والمصدر الصحيح موجود في المنتج بالفعل: `@vercel/speed-insights` يجمع p75 من مستخدمين حقيقيين.
>
> **وقرارك مستلَم ومنفَّذ:** لا تُفتح اللوحة الآن كي لا تتوقّف الدورة؛ **الأزمنة تبقى `BLOCKED_BY_SURFACE`، والقياس الزمنيّ الصالح يصير بوّابة إلزامية قبل قرار GO النهائي** إلى جانب Visual Verification Gate. **ولم أطلب صلاحية ولا تدخّلاً من أحمد.**

---

## 3. الأحجام — مقيسة، وقابلة لإعادة الإنتاج

### 3.1 حزمة العميل من مخرَج البناء (مصدر A)

| المقياس | القيمة |
|---|---:|
| `.next/static` كاملاً | **2.5 MB** |
| ملفات JS | **88** |
| مجموع JS | **2.0 MB** |
| **الحزمة الجذرية المشتركة** (`rootMainFiles`) | **428.4 kB** — تُحمَّل في كل مسار |
| أكبر قطعة | **247.2 kB** |
| ثانيةً | **228.6 kB** |
| ثالثةً | **124.4 kB** |
| **قطعة القواميس** | **122.8 kB** — §3.3 |
| polyfills | 110.0 kB |

**أمر إعادة الإنتاج:** `npm run build` ثم `find .next/static -name '*.js' -printf '%s %p\n' | sort -rn`.

> ⚠️ **حدّ هذا القياس:** Next 16 بـTurbopack **لا يُصدر `app-build-manifest.json`**، فلا سبيل إلى **حجم JS لكل مسار** من مخرَج البناء. والبديل المقيس هو ما تحمّله الصفحة الحيّة فعلاً (§3.2).

### 3.2 ما تحمّله صفحة حيّة فعلاً (مصدر B)

**`/features` — وهي من أخفّ الصفحات: نصّ وأيقونات، بلا كتالوج ولا وسائط:**

| النوع | العدد | `decodedBodySize` |
|---|---:|---:|
| **script** | **15** | **671.1 kB** |
| المستند (HTML) | 1 | 144.2 kB |
| link (خطوط) | 3 | 120.4 kB |
| img | 1 | 27.8 kB |
| css | 2 | 15.5 kB |
| **المجموع** | **21 مورداً** | **≈ 835 kB** |

**وأكبر ثلاث قطع على الصفحة الحيّة:** `229.0 kB` · `124.4 kB` · **`122.8 kB`** — **وهي تطابق أحجام القطع في البناء المحلّي تماماً**، فالقياسان يتصادقان.

> ⚠️ **`transferSize = 0` في هذه العيّنة** لأن الموارد جاءت من الخبيئة. أي أن **الحجم المضغوط على السلك غير مقيس** — والأرقام أعلاه **حجم ما فُكّ ضغطه**. ومع ضغط Brotli المعتاد يقلّ المنقول كثيراً، **لكنّ زمن التحليل والتنفيذ في JS يتبع الحجم المفكوك لا المضغوط** — وهو ما يهمّ على هاتف متوسط.

**أحجام مستندات مقيسة (مصدر B، Phase 2):** `/person/[id]` **560 kB** · `/show/[id]` **228 kB** · `/movie/[id]` **217 kB** · `/features` 144 kB · الصفحات المحميّة **39–45 kB تُرسل ثم تُقذف** (`LOOPZ-AUD-0014`).

### 3.3 🔴 `P-01` — القاموسان يُشحنان معاً إلى كل متصفّح · **P2**

**المرصود — ثلاثة أدلّة مستقلة تتفق:**

1. **المصدر (C):** `src/lib/i18n.ts` = **3805 سطراً / 212 kB**، ويصدّر `getDict(locale)` (سطر 3745). و**149 استيراداً قيمياً** من ملفات `.tsx` (مقابل 13 استيراد نوع فقط، وهي مجّانية) — منها **~140 استدعاء لـ`getDict`** في مكوّنات عميل.
2. **البناء (A):** فحص القطع الثماني والثمانين بحثاً عن علامتين — نصّ إنجليزي (`Every story matters`) ونصّ عربي (`تخطّي الجولة`). **قطعة واحدة فقط تحوي الاثنين معاً: `0c93j2t1jhwgg.js` — 122.8 kB.** وفيها **≈ 40.5 kB نصّ عربي صِرف**.
3. **Production حيّ (B):** القطعة نفسها بحجمها نفسه **مُحمَّلة على `/features`**.

**الاستنتاج:** **القاموسان العربي والإنجليزي يُشحنان معاً في قطعة واحدة إلى كل متصفّح**، فيحمل قارئ الإنجليزية ~40 kB عربية لن يقرأها، والعكس. وهي **قطعة مشتركة لا مسارية** — أي **على كل صفحة**.

**ويجدر التسجيل:** `18_Project_Context.md` يذكر اتفاق عمل رقم 6 — **«تقسيم i18n ينتظر بيانات Speed Insights الحقيقية… ويقرّر أحمد باللغة/الأداء»**. **فهذا ليس اكتشافاً جديداً بل قياسٌ لسؤالٍ مفتوح عندكم**، والرقم الآن معلوم: **122.8 kB في قطعة مشتركة، ثلثها تقريباً نصّ اللغة الأخرى.**

**معيار القبول:** قاموس اللغة النشطة وحده يصل المتصفّح — بتقسيم القطعة حسب اللغة أو بنقل القاموس إلى الخادم وتمرير النصوص المطلوبة.

### 3.4 `P-02` — صفحة تسويقية تشحن 671 kB جافاسكربت · **P2**

**المرصود (B):** `/features` — **15 سكربتاً و671.1 kB مفكوكة** لصفحة نصّية.
**الاستنتاج:** الحزمة الجذرية **428.4 kB** (A) تُحمَّل في كل مسار **بما فيها صفحات التسويق والقانون** التي لا تحتاج أغلبها. وهي أوّل ما يلمسه زائر لم يسجّل بعد.
**والسبب المرشَّح لا المؤكَّد:** غياب layouts متداخلة (`D-05`/Phase 3) يعني أن كل صفحة تُعلَّق على الجذر نفسه. **لا أنسب البطء إلى سبب بلا أثر** — وهذا **ملاحظة بنيوية** لا نسبة سببية.
**معيار القبول:** مسار عامّ لا يحمّل أكثر من الجذر + ما يخصّه.

---

## 4. الصور والوسائط — **الإعداد** (مصدر C)

> ⚠️ **هذا القسم يراجع الإعداد وحده.** مراجعة **الاستعمال والحمولات** — أكبر الأصول بأبعادها، و`next/image` مقابل `<img>`، و`fill` بلا `sizes` — في **§14**، وهي التي أضافتها إعادة التسليم.

| البند | المرصود | الحكم |
|---|---|---|
| المحمِّل | مخصّص `src/lib/imageLoader.ts` — الصغير عبر ممرّ `/i/` المخزَّن، والكبير عبر محسِّن Vercel | ✅ **مدروس ومقيس** (D-841) |
| الصيغ | `["image/avif","image/webp"]` — AVIF أولاً | ✅ |
| `deviceSizes` | `[640, 828, 1080, 1200, 1920, 2048]` — و3840 **محذوفة عمداً** (D-840، وفّرت ~$20 شهرياً) | ✅ **قُصّت بقياس لا بذوق** |
| `imageSizes` | `[64, 128, 256, 384]` | ✅ |
| `qualities` | **`[75]` مقفولة** — تمنع مضاعفة متغيّرات الخبيئة | ✅ **حارس تكلفة ذكيّ** |
| `minimumCacheTTL` | **2678400s = شهر** | ✅ |
| `priority` | **12 استعمالاً في 9 ملفات** | ✅ موجود حيث يلزم ظاهرياً — **الأثر على LCP `BLOCKED_BY_SURFACE`** |
| `sizes=` | **51** | ✅ |
| `fetchPriority` | **1** | ملاحظة |
| `loading="lazy"` | 9 | — |
| الترايلرات | iframe من `youtube-nocookie` بـ`autoplay=1&mute=1&controls=0` · ومعاينات iTunes في `<video>` | ⚠️ **`LOOPZ-AUD-0023` P1 مفتوح — ووجود iframe ليس نجاحاً** |

**`P-03` — لا قياس لأثر iframe الترايلر · `PENDING_VISUAL` / `V-1`.** الإطار يُنشأ عند الضغط لا عند التحميل (تأجيل سليم)، **لكن ما يحدث بعده غير مقيس**، و`0023` يقول إن الصورة لا تتقدّم أصلاً. **لا حكم أداء على ميزة لا تعمل.**

---

## 5. الكاش والتحديث والجلب المسبق (مصدر C)

| البند | المرصود | الحكم |
|---|---|---|
| `staleTimes` | `dynamic: 180` · `static: 300` — و`next.config.ts` يوثّق أن كل كتابة تُبطلها فوراً عبر `revalidatePath` + `coalescedRefresh` | ✅ **قرار مقيس** (D-088/D-117) |
| `revalidatePath` | **194 استدعاءً في 6 ملفات** | ✅ الكتابة لا تنتظر |
| `cache()` / `unstable_cache` | 49 | ✅ |
| `next: { revalidate }` على fetch | 12 | ✅ |
| ترويسات الصفحات | `private, no-cache, no-store, max-age=0, must-revalidate` — **مقيسة حيّاً** (Phase 2 §7.1) | ⚠️ **`P-04`** |
| `prefetch` | **`false` على 81 رابطاً** مقابل **`true` على 3** · و`prefetchIntent.ts` للجلب حسب النيّة | ⚠️ **`P-05`** |

**`P-04` — الصفحات العامة الثابتة بلا تخبئة على الحافة · P3.** `/features` و`/terms` و`/privacy` محتواها لا يتغيّر بالمستخدم، **وتُقدَّم بـ`no-store`**. **الاستنتاج:** كل زيارة رحلة خادم كاملة. **والسبب المرجّح أن الترويسة مطبَّقة على `/:path*` بلا استثناء** — وهو اختيار أمان دفاعيّ سليم بذاته. **معيار القبول:** الصفحات العامة عديمة الحالة تُقدَّم بـ`s-maxage` مع `stale-while-revalidate`، والمحميّة تبقى `no-store`.

**`P-05` — الجلب المسبق مطفأ على 81 رابطاً · P3 · مقايضة موثّقة لا عطل.** `prefetch={false}` قرار تكلفة معلن (D-510 أزال التسخين الأعمى بعد 503 متقطّعة). **الأثر المقابل:** كل تنقّل يبدأ من الصفر. **و`prefetchIntent.ts` يعوّض جزئياً بالجلب عند النيّة.** **لا أسمّيه عطلاً** — أسجّله مقايضة تُراجَع ببيانات Speed Insights.

---

## 6. الطلبات والاستعلامات (مصدر C)

> ⚠️ **التكرار والسلاسل المتتابعة في §11**، وجدول المسارات الواحد والعشرين بحمولاتها ومهلاتها في **§12**.

| البند | المرصود | الحكم |
|---|---|---|
| `Promise.all` | **181 استعمالاً** | ✅ **تفريع متوازٍ منضبط** |
| `Promise.allSettled` | 0 | ملاحظة: فشل واحد في `Promise.all` يُسقط المجموعة |
| **N+1 مؤكَّد** | **صفر** | انظر أدناه |
| `AbortController` | 5 في 5 ملفات | ⚠️ **`P-06`** |
| `AbortSignal.timeout` | 8 | ✅ |
| مهلات TMDB | 5s/8s + إعادة محاولة واحدة + توحيد الطلبات الجارية + آخر-جيّد في الذاكرة (D-509) | ✅ **معالجة انقطاع ناضجة** |
| `maxDuration` | 60s على خمسة مسارات · **300s على `/api/trakt/callback`** | ⚠️ سطح تنفيذ طويل — أمنيّاً في Phase 5 |
| `setInterval` | **2 فقط** | ✅ |
| `setTimeout` | 44 | — |

**عن N+1 — والمنهج قبل النتيجة:** بحثتُ آلياً عن `await` على `supabase` داخل حلقة بلا `Promise.all` في `src/lib`. **خرجت خمس مطابقات، وفحصتُها واحدةً واحدة: كلها إيجابيات كاذبة** — `.map()` تبني مصفوفة و`await` خارجها. **فالنتيجة: صفر N+1 مؤكَّد في طبقة البيانات.**
⚖️ **وحدّ هذا الادّعاء:** الفحص **ثابت ونمطيّ**، ولا يرى N+1 عبر حدود الدوال ولا داخل الـRPC نفسها. **الاستعلامات البطيئة الفعلية تُكشف من `query_logs` في Supabase — وهي مادة Phase 6 بأدواتها.**

**`P-06` — الإلغاء نادر · P3.** `AbortController` في 5 ملفات فقط مقابل بحث حيّ ومسارات تتغيّر بسرعة. **الأثر:** الكتابة أثناء البحث قد تترك طلبات معلّقة تعود بترتيب خاطئ. **`PENDING_VISUAL` لأثره المرئي**، **ومسجَّل من المصدر لوجوده**.

---

## 7. الرسم والقوائم والذاكرة — العدّ الخام (مصدر C)

> ⚠️ **هذا القسم عدٌّ خام، ولا يكفي حكماً.** التحليل المسمّى — نطاقات التحديث، وفحص كل فرقٍ في المستمعين والمؤقّتات واحداً واحداً، والقائمة غير المحدودة بمسار بياناتها وعدد عناصرها — في **§15**. **وحيث تعارض القسمان فـ§15 هو الحكم.**

| البند | المرصود | الحكم |
|---|---|---|
| **تمرير افتراضي (virtualization)** | **صفر مكتبة** (`react-window`/`react-virtual`/`virtuoso` غير موجودة) | ⚠️ **لا يثبت عيباً وحده** — والعيب مسمّى في §15.3 |
| القصّ بدلاً منه | `.slice(` **59 مرّة** في المكوّنات | الحدّ من الخادم لا من الرسم |
| `IntersectionObserver` | 4 إنشاءات · `ResizeObserver` 2 · **و6 `disconnect()`** | ✅ **متوازن** |
| المستمعون | **45 `addEventListener` مقابل 43 `removeEventListener`** | ✅ بعد الفحص — انظر أدناه |
| أكبر مكوّنات العميل | `TrailerPlaybackController` **1716 سطراً** · `ListDetail` 1232 · `Communities` 1010 · `LibraryGrid` 954 | ملاحظة |
| مكتبات ثقيلة في العميل | **صفر** — `@supabase/*` و`fast-xml-parser` لا تُستورد في أي ملف `"use client"` | ✅ **حدّ خادم/عميل محترم** |

**عن فارق المستمعين — وهو الادّعاء الكاذب السادس الذي أوقفته:** الفارق 2 يعود إلى ثلاثة ملفات، وفحصتها: `GoogleButton.tsx:135-136` يضيف `load`/`error` **على عنصر `<script>` يُنشأ مرّة ولا يُعاد** — يُطلق مرّة ويموت مع الصفحة، **فليس تسرّباً**. و`SwRegister` يستعمل `{ once: true }`. و`TabPager` يزيل أكثر مما يضيف. **صفر تسرّب مستمعين مؤكَّد.**

**`P-07` — لا تمرير افتراضي · P3 / **مادة Phase 9****. **المرصود:** لا مكتبة، والاعتماد على القصّ من الخادم. **والدليل الملموس:** `/person/[id]` يُسلَّم **560 kB HTML** — أكبر مستند في التطبيق. **الاستنتاج:** يعمل على سطح المكتب، **وأثره على هاتف متوسط `BLOCKED_BY_SURFACE`**. **ويكتسب وزناً في Expo**: `FlatList` تفترض التمرير الافتراضي، فبناء القوائم اليوم بلا حدود رسم يعني إعادة تصميمها هناك لا نقلها.

---

## 8. مصفوفة الأسطح والرحلات

| السطح / الرحلة | الحجم (مقيس) | الزمن | الحالة |
|---|---|---|---|
| `/` (هبوط الزائر) | **29 مورداً · صفر مكرَّر · 988.5 kB مفكوكة · صورة واحدة (27.8 kB)** (§11.1) · وأرضية مشتركة **658.8 kB** (§11.1) | — | `MEASURED` للحجم · `BLOCKED_BY_SURFACE` للزمن |
| `/features` | **21 مورداً · 671.1 kB JS · ≈835 kB إجمالاً — ومنها 0 kB يخصّها** (§11.1) | — | `MEASURED` للحجم · `BLOCKED_BY_SURFACE` للزمن |
| `/terms` · `/privacy` · `/plus` | مثل `/features` بنيوياً | — | `P-04` |
| `/movie/[id]` | **217 kB HTML** | — | محجوب · و`0018` `0016` `0020` مفتوحة |
| `/show/[id]` | **228 kB HTML** | — | محجوب |
| `/person/[id]` | **547.5 kB HTML · 219 بطاقة** (person 287) — الأكبر | — | **`P-07` مسمّى ومقيس** (§11.3) |
| `/search` | `/api/search` **2064 بايت** لعشر نتائج (§12.1 — إعادة قياس) | — | `MEASURED` · **لا ميزانية معتمدة فلا `PASS`** |
| `/trailers` | iframe مؤجَّل للضغط | — | ❌ `LOOPZ-AUD-0023` **P1** |
| الصفحات المحميّة (27) | **39–45 kB تُشحن ثم تُقذف** | — | ❌ `LOOPZ-AUD-0014` — **هدر مقيس** |
| القوائم الطويلة | **حدود رسم صريحة في كل مكان إلا `/person/[id]`** (§11.3) | — | `P-07` |
| Cold start · Warm resume · التنقّل | — | — | **`BLOCKED_BY_SURFACE`** |
| Web Vitals p75 | — | — | **`BLOCKED_BY_SURFACE`** — والمصدر الصحيح Speed Insights |
| الرحلات بعد الدخول | — | — | **`BLOCKED_BY_TEST_ENV`** |

---

## 9. الميزانيات — مقارنة لا حكم

| الهدف | الحالة | لماذا |
|---|---|---|
| LCP ≤ 2.5s @p75 | **`BLOCKED_BY_SURFACE`** | لا أداة صحيحة — Speed Insights هو المصدر |
| INP ≤ 200ms @p75 | **`BLOCKED_BY_SURFACE`** | ↑ |
| CLS ≤ 0.1 @p75 | **`BLOCKED_BY_SURFACE`** | ↑ |
| تفاعل مرئي خلال 100ms | **`BLOCKED_BY_SURFACE`** | ↑ |
| API عادي p95 < 800ms | **`BLOCKED_BY_SURFACE`** | لا قياس زمنيّ صالح · **والحمولات صغيرة** (2 kB للبحث) وهو مؤشّر لا حكم |
| ثقيل p95 < 1.5s | **`BLOCKED_BY_SURFACE`** | ↑ |
| صفر أخطاء JS في الرحلات الحرجة | ✅ **صفر خطأ Console غير معالَج** رُصد في الصفحات العامة (Phase 2) | الرحلات بعد الدخول `BLOCKED_BY_TEST_ENV` |
| لا jank ولا flicker | **`PENDING_VISUAL`** | ومضة `LOOPZ-AUD-0014` **مثبَتة بنيوياً** (200 + meta refresh بعد ثانية) |
| لا تسرّب ذاكرة | **`PENDING_VISUAL`** للسلوك · ✅ **النظافة البنيوية سليمة** (§7) | — |

**صفر بند زمنيّ مُسمّى `PASS`.** وحيث لا ميزانية معتمدة (الأحجام) الوصفُ **`MEASURED`** لا `PASS` — **بناءً على اعتراضك، وقد صحّحت الجدولين أعلاه وفق ذلك.**

---

## 10. مرشّحات Phase 4 — بمعايير قبول رقمية

| المرشّح | الدرجة | الدليل | الأثر | **معيار القبول — رقميّ** |
|---|---|---|---|---|
| **`P-01`** | **P2** | ثلاثة مصادر: `i18n.ts` 212 kB · القطعة **125,744 بايت** تحوي علامتَي اللغتين (§11.2) · وهي **في أرضية المسارات التسعة كلّها** (§11.1) | **19٪ من الأرضية المُنزَّلة** نصفها لغة لن تُقرأ | القطعة الحاملة لـ`Every story matters` **لا تحوي** `تخطّي الجولة` · **والأرضية المُنزَّلة تنزل من 658.8 kB إلى ≤ 560 kB** |
| **`P-02`** | **P2** | **الأرضية المشتركة 658.8 kB مفكوكة على تسعة مسارات عامّة** · وما يخصّ المسار 0–78.2 kB (§11.1) | **96–100٪ من وزن كل مسار عامّ أرضيةٌ لا صفحة** | **الأرضية ≤ 400 kB** = إطار 297 kB + ≤ 100 kB تطبيق · **وتُنسَب كل قطعة فوق 20 kB لمالك** (اليوم 41.4 kB `UNKNOWN_OWNER`) |
| **`P-08`** | P3 | ستّ عائلات بلا `AbortSignal.timeout` · **ثلاثٌ `DIRECT_RENDER_PATH` وثلاثٌ `API_OR_ACTION`** بمسار الاستدعاء لكلٍّ (§12.2) | قسم مبثوث يبقى معلَّقاً حتى مهلة دالّة المنصّة · **والأثر بنيويّ لا زمنيّ** | (1) **`DIRECT_RENDER_PATH` بلا مهلة = 0 من 3** بمهلة ≤ 5000ms · (2) **الإجمالي = 0 من 6** |
| `P-04` | P3 | `Cache-Control: no-store` مقيسة حيّاً على `/features` | كل زيارة رحلة خادم كاملة | `/features` و`/terms` و`/privacy`: **`s-maxage` ≥ 600 و`stale-while-revalidate`** في الترويسة |
| `P-05` | P3 | `prefetch={false}` ×81 مقابل ×3 — **مقايضة موثّقة (D-510)** | كلفة/زمن | **لا يُغيَّر بلا بيانات ميدانية** — القرار مرهون بمصدر Web Vitals |
| `P-07` | P3 · **ومانع في Expo** | `/person/[id]` — `PersonWorks` بلا حدّ رسم · **219 بطاقة و547.5 kB لـperson 287** (§11.3) | خطّيّ في عدد الاعتمادات | **≤ 60 بطاقة للتبويب** بصفحات أو تمرير تدريجيّ · **HTML لـ`/person/287` ≤ 150 kB** |
| `P-03` | `PENDING_VISUAL` → `V-1` | — | لا حكم أداء على ميزة لا تعمل | بعد إصلاح `LOOPZ-AUD-0023` |
| ~~`P-06`~~ | **مسحوب** | — | — | **§16: صفر جلبٍ تابعٍ لمدخل بلا حماية.** العدّ الذي بنيته قاس آليةً واحدة من ثلاث |

**لا مرشّح P1 جديد.** `LOOPZ-AUD-0023` يبقى **P1** مفتوحاً بحالته ودرجته ودليله، **بلا أي تخفيف** — **ووجود iframe أو poster ليس نجاحاً وظيفياً.**

---

---

## 11. الطلبات المكرّرة والسلاسل المتتابعة — والتصحيح أوّلاً

> 🔴 **اعتراضك صحيح.** §2 كتبت «الطلبات المكرّرة ✅ مقيسة» وأحالت إلى §3.2 — **و§3.2 كانت جرد موارد صفحةٍ واحدة، لا إثباتَ تكرارٍ ولا waterfall.** الحكم صُحّح أدناه وفُصل إلى ما قِيس فعلاً وما بقي محجوباً.

### 11.1 التكرار على مستوى المستند — **`MEASURED`**

الأداة: `performance.getEntriesByType('resource')` في متصفّح زائر · البيئة: Production `f8a2b33c` · المفتاح: `origin + pathname` بعد نزع الـquery · **عدّ لا زمن**.

| السطح | موارد | فريدة | **مكرّرة** | scripts | css | img | link | إجمالي مفكوك |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `/` (زائر) | 29 | **29** | **0** | 17 | 2 | 1 | 9 | 988.5 kB |
| `/features` | 21 | **21** | **0** | 15 | 2 | 1 | 3 | ≈835 kB |

**صفر مورد مكرَّر على السطحين.** والصورة الوحيدة على صفحة الهبوط `loopz-wordmark.png` (27.8 kB) — **لا بوسترات للزائر أصلاً**، فالصفحة خفيفة الصور بحكم بوّابة الدخول لا بحكم التحسين.

### 11.2 التكرار في زمن التشغيل — **`BLOCKED_BY_SURFACE`**

الصفحات الديناميكية **لا يكتمل رسمها** في لوح `hidden` (Phase 2 §11: `/movie/550` بقيت 14 هيكلاً و`mainTextLen = 0`)، فما يطلبه العميل بعد الترطيب لا يظهر في جرد الموارد. **ولا أسمّي ما لم أرَه «صفر تكرار».** يُغلق البند مع البوّابة البصرية.

### 11.3 السلاسل المتتابعة على الخادم — **`MEASURED` من المصدر**

| الرحلة | الموجة 1 | ثمّ (متتابع لأن مدخله من السابق) | يبثّ وحده خلف `Suspense` |
|---|---|---|---|
| `/movie/[id]` | `Promise.all` من **12 نداءً** (`page.tsx:58`) | `getProviderLinks` (`:91`، مدخله `watchWhere.region`) → `displayWorkTitle` (`:118`، مدخله `movie.title`) | `ListsWithMovie` (`:551`) · `MovieTrailerSection` (`:558`) |
| `/show/[id]` | `Promise.all` (`:61`) | `getProviderLinks` (`:92`) → `displayWorkTitle` (`:121`) | `getTrailer` (`:618`) · `animeExtras` (`:636`) |
| `/trailers` | `Promise.all([getT(), searchParams])` (`:29`) ثمّ `Promise.all([itemsPromise, cookies()])` (`:91`) | — | — |
| `/` | `Promise.all` (`:145`) ثمّ موجات مقسَّمة | `getNewsGenStale` → `refreshLoopzNews` (`:167`) شرطيّ | **أقسامٌ تبثّ وحدها** — قرار موثّق |

**العمق الأقصى المرصود ثلاث موجات متتابعة** في صفحتَي العمل، **ومصدر التتابع حقيقيّ لا سهو**: الموجة الثانية تحتاج مخرَج الأولى حرفياً.

**وأسجّل قراراً معماريّاً وجدته موثّقاً في `page.tsx:2036`** ولم أعرفه قبل هذا الفحص:

> «كانت الموجة الثانية `Promise.all` واحدةً ضخمة: أبطأُ عضوٍ فيها … يحكم متى تظهر الأقسامُ كلُّها. القاعدة الجديدة: **ما يخصّ قسماً واحداً يُجلب في قسمه خلف `Suspense` خاصّته**.»

أي أن **waterfall الصفحة الرئيسية عولج عمداً في جولة أداء ٢٠ أغسطس**، وما بقي متتابعاً في صفحتَي العمل تبعيّةُ بيانات لا إهمال. **لا مرشّح هنا.**

---

## 12. المسارات الواحد والعشرون — الحمولات والحدود والإلغاء

### 12.1 المسارات الواحد والعشرون — **صفٌّ لكلٍّ، بلا تجميع ولا wildcards**

> **القائمة هي قائمة Phase 1 نفسها** — واحدٌ وعشرون ملف `route.ts`/`route.tsx`، **و`/robots.txt` منها** (`src/app/robots.txt/route.ts`) فهو داخل العدّ لا خارجه.
> **القياس الحيّ نداءٌ آمن واحد لكلٍّ من متصفّح زائر** على Production `f8a2b33c` · **ولم يُنفَّذ أي POST كاتب ولا أي مسار كاتب**؛ فيها يكفي المصدر.

| # | المسار | Method | Auth | المدخلات والحدود | شكل الاستجابة | **نتيجة حيّة آمنة / أو الحجب** | دليل المصدر |
|---:|---|---|---|---|---|---|---|
| 1 | `/api/build` | GET | لا شيء | — | نصّ خام (SHA) | **200 · 40 بايت · `no-store`** | `api/build/route.ts` |
| 2 | `/api/curated` | GET | جلسة (401) + حدّ معدّل | `list` · `slug` | `{slug,…}` / `{error}` | **401 · 16 بايت** | `api/curated/route.ts` |
| 3 | `/api/franchise` | GET | جلسة (401) + حدّ معدّل | `id` · `slug` · `exclude` · **`slice(0,4)`** | `{parts[],name}` | **401 · 12 بايت** | `api/franchise/route.ts` |
| 4 | `/api/genres` | GET | جلسة + حدّ معدّل · **والحارس الحقيقي RPC إدارية** | `n` — **`Math.max(1, Math.min(…))`** | `{titles[]}` / `{error}` | **401 · 16 بايت** | `api/genres/route.ts` |
| 5 | `/api/imdb-chart` | GET | جلسة + **حدّ معدّل 3/60s (الأضيق)** | `part` — **`Math.max(0,…)`** · `step` | `{built,done,…}` | **401 · 16 بايت** | `api/imdb-chart/route.ts` |
| 6 | `/api/lang-ping` | **POST** | لا جلسة · حدّ معدّل بالـIP | — | **`204` بلا جسم** | **GET → 405 · 0 بايت** · **وPOST كاتب لم يُنفَّذ** | `route.ts:41,46,53` |
| 7 | `/api/list-og/[id]` | GET | لا جلسة | `id` من المسار · **`slice(0,5)`** | `ImageResponse` · `404 "not found"` | **`BLOCKED_BY_TEST_ENV`** — يحتاج قائمة قائمة | `route.tsx:22–40` |
| 8 | `/api/news-gen` | GET | جلسة + حدّ معدّل | `limit` — **`Math.max(1, Math.min(60,…))`** · `probe` | `{…}` / `{error}` | **`BLOCKED_BY_TEST_ENV`** — **لم يُستدعَ عمداً: يولّد محتوى** | `api/news-gen/route.ts` |
| 9 | `/api/search` | GET | جلسة + حدّ معدّل (**وبالـIP للزائر**) · `429` بترويسة `Retry-After` | `q` · `type` · **`slice(0, wantTitles)`** لكل مجموعة | `{titles[],artists[],members[],lists[],more}` | **200 · 2064 بايت لعشر نتائج · `private, max-age=60`** | `route.ts:59,65,69,111,131,139,140` |
| 10 | `/api/season` | GET | **جلسة (401)** + حدّ معدّل | `tv` · `s` — **`Number.isInteger` وإلا 400** · `r` | `{episodes[]}` · 400/401/429/502 | **401 · 15 بايت** | `route.ts:15,21,25–33,57` |
| 11 | `/api/share` | GET | **جلسة (401) + Plus (403)** | معاملات البطاقة · **`slice(0,3)`** | `ImageResponse` · 401/403/409 | **`BLOCKED_BY_TEST_ENV`** | `route.tsx:60,350,357,364` |
| 12 | `/api/suggest` | GET | جلسة (401) + حدّ معدّل | `q` · **`slice(0,12)` · `slice(0,9)`** | `{results[]}` | **401 · 14 بايت** | `api/suggest/route.ts` |
| 13 | `/api/title-meta` | GET | جلسة + حدّ معدّل · **والحارس RPC إدارية** | `n` — **مقيَّد** · `slice(0,3)` · `slice(0,4)` | `{titles[]}` / `{error}` | **401 · 16 بايت** | `api/title-meta/route.ts` |
| 14 | `/api/trakt/callback` | GET | **كوكي `trakt_state` (CSRF · httpOnly · 600s)** | `code` · `state` · **`slice(0,300)`** · `Math.min(10,…)` | `redirect` | **`BLOCKED_BY_TEST_ENV`** — يحتاج رمز OAuth حقيقياً | `api/trakt/callback/route.ts` |
| 15 | `/api/trakt/start` | GET | جلسة | — | `redirect` إلى Trakt | **`BLOCKED_BY_TEST_ENV`** | `api/trakt/start/route.ts` |
| 16 | `/auth/callback` | GET | — | `code` · `next` **مع `safeNext()`** | `redirect` | **`BLOCKED_BY_TEST_ENV`** — يحتاج رمزاً | `auth/callback/route.ts` |
| 17 | `/auth/signout` | **POST** | **مقارنة ترويسة `origin`** | — | `redirect` | **لم يُنفَّذ — كاتب ويُنهي جلسة** | `auth/signout/route.ts` |
| 18 | `/i/[size]/[file]` | GET | لا جلسة · **قائمة بيضاء من 8 مقاسات + `^[A-Za-z0-9]+\.(jpg\|png)$`** | `size` · `file` | بثّ بايتات الصورة · 400/404/502 | **`BLOCKED_BY_TEST_ENV` كنداءٍ مباشر** — يُستدعى ضمنياً عبر مُحمِّل `next/image` | `route.ts:25–46` |
| 19 | `/join/[code]` | GET | لا جلسة · **كاتب** (كوكي إحالة) | `code` — تطبيع `[^A-Z0-9]` ثمّ **`slice(0,10)`** | `redirect` → `/` | **لم يُطلَب عمداً — كاتب** | `route.ts:19–27` |
| 20 | `/p/[code]` | GET | لا جلسة · **كاتب** | `code` — **`slice(0,10)`** | `redirect` → `/` | **لم يُطلَب عمداً — كاتب** | `route.ts:21–29` |
| 21 | `/robots.txt` | GET | لا جلسة | — | `text/plain` | **200 · 97 بايت · `public, max-age=3600`** | `robots.txt/route.ts` |

**الخلاصة رقمياً:** **قِيس حيّاً 4 مسارات بنجاح** (`build` · `search` · `robots.txt` · و`lang-ping` بحالة 405) · **و8 ردّت 401 بجسمٍ من 12–16 بايت** — **والـ401 دليلُ حارسٍ يعمل قبل أي حمولة، لا إخفاق قياس** · **و9 `BLOCKED_BY_TEST_ENV` أو كاتبة لم تُطلَب**.

**والحمولة الوحيدة القابلة للقياس كزائر — `/api/search` — 2064 بايت لعشر نتائج**، أي ≈206 بايت للنتيجة.

> 🔴 **وتصحيح صياغة:** كتبت «فلا مسار يقبل حدّاً مفتوحاً» **وهي أوسع مما أثبتّ**. الصحيح: **المسارات ذات معاملات `n`/`limit`/`part` المفحوصة مقيَّدة صراحةً** (`api/genres` · `api/title-meta` · `api/news-gen` عند 60 · `api/imdb-chart`)، **وقصُّ النتائج مثبت بـ`slice` في `suggest` و`franchise` و`share` و`list-og` و`search`**. أما بقيّة المسارات فلا معاملات عدد لها أصلاً — **وهذا ليس نفياً معمَّماً**.

### 12.2 المهلة وإعادة المحاولة والإلغاء — **لكل عائلة خارجية، لا TMDB وحدها**

| العائلة | الملف | **مهلة** | إعادة محاولة | `revalidate` | **التصنيف** |
|---|---|---|---|---:|---|
| TMDB | `lib/tmdb.ts` | ✅ **5000 / 8000 (المحاولة الثانية) / 2000 (rail)** | ✅ **محاولتان** + `retryable(status)` + **تجميع المتطابق** | 3600 | `DIRECT_RENDER_PATH` |
| Wikidata | `lib/wikidata.ts` | ✅ **1500 ×2** | ✗ | 604800 | `DIRECT_RENDER_PATH` |
| الأخبار (RSS) | `lib/news.ts` | ✅ **8000** | ✗ | — | `DIRECT_RENDER_PATH` |
| Giphy | `lib/gif.ts` | ✅ **4000** | ✗ | 300 | `API_OR_ACTION` |
| DeepL | `lib/translate.ts` | ✅ **4000** | ✗ | 60 | `DIRECT_RENDER_PATH` |
| **OMDb** | `lib/omdb.ts` | ❌ **لا مهلة** | ✗ | 86400 | **`DIRECT_RENDER_PATH`** |
| **AniList / ARM** | `lib/anilist.ts` | ❌ **لا مهلة** | ✗ | 86400 · 604800 | **`DIRECT_RENDER_PATH`** |
| **Apple Trailers** | `lib/appleTrailers.ts` | ❌ **لا مهلة** | ✗ | WEEK | **`DIRECT_RENDER_PATH`** |
| **IMDb chart / datasets** | `lib/imdbChart.ts` | ❌ **لا مهلة** | ✗ | 86400 · 604800 | **`API_OR_ACTION`** |
| **Trakt** | `lib/trakt.ts` | ❌ **لا مهلة** | ✗ | — | **`API_OR_ACTION`** |
| **Gemini** | `lib/ai.ts` | ❌ **لا مهلة** | ✗ | — | **`API_OR_ACTION`** |

### 🔴 `P-08` — عائلات خارجية بلا مهلة · **مصنَّفةً بمسار الاستدعاء الفعليّ**

> 🔴 **اعتراضك صحيح وحرفيّ:** كتبت «ستّ عائلات… خمسٌ منها على مسار رسم» **وجدولي نفسه لا يسنده**. **تتبّعت مسار استدعاء كلٍّ من الستّ، والعدد الصحيح ثلاث لا خمس.**

| العائلة | **مسار الاستدعاء الفعليّ** | **التصنيف** |
|---|---|---|
| **OMDb** | `components/HeroRatings.tsx:1` — **Server Component** (صفر `"use client"`) داخل `/movie/[id]` و`/show/[id]` · و`lib/sections.ts:23` ← `/discover/[section]` و`/news` و`/lists/[id]` · و`lib/topChart.ts:5` ← `/` و`LandingHero` و`LandingShowcase` · **وأيضاً** `api/season/route.ts:4` | **`DIRECT_RENDER_PATH`** |
| **AniList / ARM** | `app/show/[id]/page.tsx:26` → `animeExtras` (`:636`) | **`DIRECT_RENDER_PATH`** |
| **Apple Trailers** | `lib/appleTrailers.ts` ← `lib/trailers.ts:17` ← `app/trailers/page.tsx:12` و`app/news/page.tsx:33` (**كلاهما Server Component**؛ و`TrailerFeed`/`TrailerRail` عميلان يتلقّيان البيانات props) | **`DIRECT_RENDER_PATH`** |
| **IMDb chart / datasets** | `api/imdb-chart/route.ts:11` — **ولا مستورِد آخر** | **`API_OR_ACTION`** |
| **Trakt** | `api/trakt/callback` و`api/trakt/start`. **وصفحة `profile/settings/import/page.tsx:4` تستورد `traktConfigured()` وحدها — فحصُ إعدادٍ لا `fetch`** | **`API_OR_ACTION`** |
| **Gemini** | `lib/actions.ts:5137` — استيراد ديناميكيّ داخل Server Action `aiStorySearch`، **محدودة المعدّل 6/60s**، تُطلَق بفعل المستخدم | **`API_OR_ACTION`** |

**فالتصحيح:** **ستّ عائلات بلا مهلة · ثلاثٌ منها `DIRECT_RENDER_PATH` · وثلاثٌ `API_OR_ACTION` · وصفرٌ `BACKGROUND_SCHEDULED`.** **ولا أحسب Trakt ولا Gemini على مسار الرسم** — لا دليل استدعاءٍ مباشر منه.

**الدليل:** غياب `AbortSignal.timeout` في الملفات الستّة، مقابل TMDB المحميّ بمهلة (5000 / 8000 / 2000) ومحاولتين وتجميعٍ للمتطابق.
**الأثر — بالدقّة التي تسمح بها الأدلّة:** `fetch` بلا مهلة لا ينتهي من تلقاء نفسه؛ الحدّ الوحيد مهلة دالّة المنصّة. **في الثلاث `DIRECT_RENDER_PATH` يعني ذلك قسماً مبثوثاً يبقى معلَّقاً** (وفي `topChart` على `/` قسماً في الصفحة الأكثر زيارة)؛ **وفي الثلاث الأخرى نداءً أو فعلاً يتأخّر دون أن يحبس رسم صفحة**. **ولا أدّعي زمناً — الأثر بنيويّ، والقياس الزمنيّ `BLOCKED_BY_SURFACE`.**
**الدرجة: P3** — لأن الثلاث المؤثّرة كلّها خلف `Suspense` أو مبثوثة، ولأن `revalidate` الطويل يجعل الإصابة نادرة. **ولا أرفعها بلا قياس زمنيّ صالح.**
**معيار القبول (رقميّ ومرتَّب):** (1) **العائلات `DIRECT_RENDER_PATH` بلا مهلة = 0 من 3** بمهلة ≤ 5000ms · (2) ثمّ **الإجمالي بلا مهلة = 0 من 6**.

---

## 13. الحزم ومالكوها وحدود العميل/الخادم

### 13.1 🔴 تصحيح جوهريّ لـ`P-02`

**قلت في التسليم الأول إن الجذر المشترك 428.4 kB.** ذلك رقمُ `rootMainFiles` من مخرَج بنائي المحلّي — **وهو ليس ما يحمّله المسار فعلاً**. القياس المباشر على Production يعطي رقماً أكبر وأدقّ.

الأداة: قراءة `<script src>` من HTML تسعة مسارات عامّة + `fetch` لكل قطعة وقياس `byteLength` · البيئة: Production `f8a2b33c` · **حجم لا زمن**.

| المسار | قطع | إجمالي مفكوك | **يخصّ المسار وحده** |
|---|---:|---:|---:|
| `/features` · `/login` · `/terms` | 16 | 776.2 kB | **0 kB** |
| `/discover/trending` | 17 | 784.7 kB | 8.5 kB |
| `/person/287` | 17 | 786.3 kB | 10.1 kB |
| `/trailers` | 18 | 804.2 kB | 28.0 kB |
| `/movie/550` | 19 | 837.4 kB | 61.2 kB |
| `/show/1399` | 19 | 845.2 kB | 69.0 kB |
| `/` | 18 | 854.4 kB | **78.2 kB** |

**والأرضية المشتركة ست عشرة قطعة = 776.2 kB مرجعةً في HTML كل المسارات التسعة.** ومنها **قطعتان لا يجلبهما متصفّح حديث** (`core-js` 112.6 kB و7.5 kB — مسار `nomodule`؛ **مثبَت بغيابهما عن جرد موارد `/features`**). **فالأرضية المُنزَّلة فعلاً = أربع عشرة قطعة = 658.8 kB مفكوكة.**

> **وهذا يقلب تشخيص `P-02` رأساً على عقب:** `/features` **لا تشحن سطر جافاسكربت واحداً يخصّها** — كلّ وزنها هو الأرضية. **فالمشكلة ليست صفحةً ثقيلة، بل أرضيةً مشتركة تمثّل 96–100٪ من وزن كل مسار عامّ.** والرافعة في الأرضية لا في الصفحات.

### 13.2 ملّاك القطع — بعلامات محتوى، وبـ`UNKNOWN_OWNER` حيث لا دليل

| القطعة (Production) | بايت | العلامة المرصودة | المالك |
|---|---:|---|---|
| `1k12eahf36m9-.js` | 234,544 | `Minified React error` | **react-dom** — إطار |
| `0s4o9e7luu8fg.js` | 127,342 | `Symbol.asyncIterator` | runtime/router — **إطار** |
| `29gyoylc9f7i-.js` | **125,744** | **`Every story matters` + `تخطّي الجولة`** | **القاموسان → `P-01`** |
| `0c0hxoamwjsbw.js` | 112,594 | `core-js` | polyfill — **`nomodule`، لا يُجلب** |
| `22z1pob1pifku.js` | 31,945 | `Minified React error` | react-dom — إطار |
| `2am-q7jlkailq.js` | 28,188 | `speed-insights` + `serviceWorker` | Speed Insights + `SwRegister` |
| `0as93-hrkv5ee.js` | 26,791 | react + polyfill | إطار |
| `0buojf2hy2vrf.js` | 24,040 | `image.tmdb.org` | **مُحمِّل الصور المخصّص** |
| `09ut5gze4xgce.js` | 22,636 | `supabase` + `accounts.google.com` | **تهيئة عميل Supabase + GIS** |
| `0qd_0s_j3uhef.js` · `2corl6wyj4669.js` · `1pud6v14gjs13.js` · `02pyr4p4fypwq.js` · `3qs2mdnac90s4.js` · `21xni3lvpc7r6.js` | 42,430 مجموعاً | — | **`UNKNOWN_OWNER`** |
| `turbopack-1htkclefpwl3r.js` | 10,960 | — | مُحمِّل Turbopack |

**وقسمة الأرضية المُنزَّلة (658.8 kB):** إطار **≈297 kB (45٪) لا يُمَسّ** · **القاموسان 122.8 kB (19٪) — وهما البند الوحيد القابل للحذف بنصفه** · مُحمِّل الصور + Supabase/GIS + Speed Insights 73 kB · **`UNKNOWN_OWNER` 41.4 kB**.

> **وأسجّل نفياً مهمّاً:** **حزمة Supabase الكاملة (253,158 بايت في بنائي) ليست في أرضية أيٍّ من المسارات التسعة العامّة.** الموجود قطعة تهيئة من 22.6 kB. **فالزائر لا يحمل SDK قاعدة البيانات** — وهذا انضباط، لا ثغرة.

### 13.3 `dynamic()` وحدود العميل

- **اثنا عشر موضع `dynamic()`** في تسعة ملفات — **كلّها `ssr: false`، وكلّها أوراق/ألواح لا تُطلب قبل ضغطة**: `EpisodeRateSheet` · `SeasonDateSheet` · `StartConversationSheet` ×2 · `DiscoverFilterSheet` · `SendShareSheet` · `TitleArtSheet` · `ShareListSheet` ×2 · `ListCoverSheet` · `LibraryToolsSheet` · `TourGuide`. **صفر `React.lazy`.** **النمط صحيح ومطبَّق باتّساق.**
- **`"use client"` في 180 ملفاً** من 453.
- **الإطار المشترك في `layout.tsx`**: أربعة عشر مكوّناً، **اثنا عشر منها عميل** (`PullToRefresh` · `BottomNav` 32 kB · `OfflineSync` · `ToastHost` · `LoginGateHost` · `PlusGateHost` · `TourMount` · `SwRegister` · `HeaderShell` · `ChromeAutoHide` · `LangPing` · `PresencePing`) **واثنان خادم** (`Navbar` · `AccountSync`). **هذا هو ما يجعل جافاسكربت مشتركاً على كل مسار** — وهو تفسير قابل للتتبّع، **بخلاف ما كتبته سابقاً عن غياب الـlayouts المتداخلة، وهو ملاحظة بنيوية لا سبب سببيّ.**

---

## 14. الصور — الاستعمال لا الإعداد

> **فصلٌ صريح:** «الإعداد موجود» **لا يساوي** «الاستعمال سليم». الأول كان في §4، وهذا الثاني.

### 14.1 أكبر الأصول المحلّية — **`MEASURED`**

| الأصل | بايت | الأبعاد |
|---|---:|---|
| `public/loopz-cover.jpg` | 39,300 | **1600×533** |
| `public/splash/launch-1320x2868.png` | 33,698 | 1320×2868 |
| `public/splash/launch-1290x2796.png` | 33,072 | 1290×2796 |
| `public/splash/launch-1284x2778.png` | 32,924 | 1284×2778 |
| `public/loopz-wordmark.png` | 28,479 | 720×247 |

**أكبر أصل في المستودع كلّه 39.3 kB.** (وأبعاد الغلاف استُخرجت من رأس SOF0 في الملف نفسه — **لم تعد ناقصة**.) وشاشات الإقلاع بمقاس الجهاز الكامل تحت 34 kB لأنها لوحة سوداء وشعار. **لا حمولة صور محلّية ثقيلة إطلاقاً.**

### 14.2 استعمال `next/image` — فحص وسوم، بعد نزع التعليقات

| القياس | العدد | الحالة |
|---|---:|---|
| ملفات تستورد `next/image` | 44 | — |
| `<Image fill>` | **46** | — |
| منها **بلا `sizes`** | **0** | ✅ |
| `<Image>` بأبعاد صريحة | 8 | ✅ |
| `<Image>` بأبعاد من `{...POSTER_INTRINSIC[…]}` | 6 | ✅ `lib/media.ts` |
| **بلا أبعاد ولا `sizes`** | **0** | ✅ |
| `priority` | 11 موضعاً | — |
| `backgroundImage` / `bg-[url(` | 2 | — |

### 🔴 وبلاغان كاذبان أوقفتهما في هذا القسم نفسه

| بدا صحيحاً | فسقط |
|---|---|
| «`Logo.tsx:38` — `<Image fill>` بلا `sizes`» | **السطر تعليق عربيّ يذكر `<Image>` نصّاً**، لا وسم. بعد نزع التعليقات: **صفر** |
| «ستّة `<Image>` بلا `width`/`height`» | **كلّها تنشر `{...POSTER_INTRINSIC[posterSize]}`** — وهو كائن أبعاد في `lib/media.ts`. **الأبعاد موجودة، والفحص السطحيّ هو الذي لم يرها** |

### 14.3 `<img>` الخام — تسعة عشر وسماً، والفرز يغيّر الحكم

| المجموعة | العدد | الحكم |
|---|---:|---|
| داخل `ImageResponse` (Satori): `lib/shareCard.tsx` ×6 · `lib/og.tsx` · `api/list-og/[id]` · `layout.tsx:236` | **9** | **`NOT_APPLICABLE` — `next/image` لا يعمل داخل `ImageResponse` أصلاً** (ويطابق ما أثبتته Phase 3 عن عجزه عن قراءة متغيّرات CSS) |
| عميل: `GifPicker` ×2 · `ActivityFeed:771` · `ShareCard:135` · `ReplyItem` ×2 · `Composer` · `LibraryGrid` · `CustomizePreview` · `EditProfileForm` | **10** | **أربعة منها `loading="lazy"`** · والبقيّة معاينات محلّية (`blob:`) أو GIF متحرّك — **وكلاهما خارج ما يحسّنه `next/image`** |

**لا مرشّح صور.** حمولات الصور الحيّة على الأسطح الديناميكية تبقى `BLOCKED_BY_SURFACE` (الرسم لا يكتمل)، **ولا أسمّيها ناجحة**.

---

## 15. الرسم والقوائم والذاكرة — بأسماء لا بأعداد

### 15.1 نطاقات التحديث — **`MEASURED` من المصدر**

**أربعة `createContext` في التطبيق كلّه**، **ولا واحد منها في `layout.tsx`**:

| السياق | الملف | نطاقه |
|---|---|---|
| `FavFilterContext` | `LibraryFavFilter.tsx:35` | المكتبة |
| مشغّل الترايلرات | `trailers/TrailerPlaybackController.tsx:170` | `/trailers` |
| `HomeViewState` | `HomeViewProvider.tsx:61` | الرئيسية |
| `PagerState` | `CommunityPager.tsx:83` | المجتمعات |

> **ولهذا لا يوجد provider عامّ يعيد رسم التطبيق كلّه** — وهو أهمّ سؤال في هذا الباب، وجوابه من المصدر: **لا خطر fan-out عامّ.** `useMemo` 11 · `useCallback` 22 · **`React.memo` صفر** — **وصفرُها ليس عيباً** ما دام fan-out غير مثبت؛ الحفظ بلا كلفة مثبتة كلفةٌ هو نفسه.

### 15.2 نظافة الدورة — والصياغة الدقيقة كما ضبطتَها

| المقياس | إنشاء | تنظيف |
|---|---:|---:|
| `IntersectionObserver` + `ResizeObserver` | **6** | **6 `.disconnect()`** |
| `addEventListener` | 52 | 49 |
| `setInterval` | 4 | 5 `clearInterval` |
| `setTimeout` | 44 | 33 `clearTimeout` |
| قناة Supabase Realtime | 1 | **1 `unsubscribe()` في تنظيف الأثر** (`lib/usePoll.ts:88`) |

**والفروق الأربعة فُحصت واحداً واحداً:**

| الملف | الفرق | ما تبيّن |
|---|---|---|
| `useScrollMemory.ts:32` | `popstate` بلا إزالة | **مستمع على مستوى الوحدة يُسجَّل مرّة واحدة، والتعليق يشرح أنه يعيش بعد unmount عمداً** لأن `popstate` يسبق تركيب صفحة الوجهة. **قرار لا تسرّب** |
| `SwRegister.tsx:119` | `load` بلا إزالة | **`{ once: true }` — يزيل نفسه** |
| `GoogleButton.tsx` | مستمعان بلا إزالة | **مستمعا `<script>` أحاديّا الإطلاق** (أُثبت في التسليم الأول) |
| `TabPager` · `BottomNav` · `ContentPrefsSection` | إزالات **أكثر** من الإضافات | تنظيف مشروط — لا نقص |
| مؤقّتات بلا `clearTimeout` (11) | — | **كلّها إعادة ضبط واجهة 1.1–4 ثوانٍ** تستدعي `setState` بعد الإفلات، **وهو لا-عمل في React 18+** · و`actions.ts:1188` سباق `Promise.race` خادميّ · و`refresh.ts:13` مؤقّت تجميع يُعاد إسناده |

> **والصياغة كما طلبتَها حرفياً:** **لا تسرّب مستمع أو مراقب مؤكَّداً بهذا الفحص الثابت.** والسلوك الحيّ يبقى **`PENDING_VISUAL`**.

### 15.3 🔴 `P-07` — القائمة غير المحدودة، مسمّاةً بمسار بياناتها وعدد عناصرها

**سلّم القصّ في التطبيق منضبط:** `slice(0, 4)` ×18 · `slice(0, 10)` ×16 · `slice(0, 3)` ×12 · `slice(0, 300)` ×6 · `slice(0, 120)` ×4 … — **حدود رسم صريحة في كل مكان تقريباً.** والتمرير اللانهائي مُدار بـ`IntersectionObserver` + `hasMore` في `TrailerFeed.tsx`.

**والاستثناء واحد، ومسمّى:**

| البند | القيمة |
|---|---|
| **المسار** | `/person/[id]` |
| **المكوّن** | `PersonWorks` — `src/app/person/[id]/page.tsx:191` (تُبثّ خلف `Suspense` من `:183`) |
| **مسار البيانات** | TMDB `combined_credits` → `all` → تُقسَّم إلى `{all, movie, tv, show}` (`:207–212`) → `works = groups[worksTab]` (`:213`) |
| **حدّ الرسم** | **لا حدّ**: `{works.map(...)}` (`:247`) بلا `slice` وبلا صفحات وبلا تمرير افتراضي |
| **الأثر مقيساً** (Production، زائر) | `/person/287`: **219 بطاقة · 222 `<img>` · 547.5 kB HTML** · `/person/1245`: 161 · 423.7 kB · `/person/500`: 132 · 361.5 kB — **خطّيّ في عدد الاعتمادات** |
| **الدرجة** | **P3 على الويب** — يعمل على المكتب · **ويصير مانعاً في Expo**، حيث `FlatList` تفترض نافذة رسم |
| **معيار القبول (رقميّ)** | صفحات أو تمرير تدريجيّ بحدّ **≤ 60 بطاقة للتبويب**، و**HTML لـ`/person/287` ≤ 150 kB** |

**فالحكم لم يعد «صفر مكتبة تمرير افتراضي» — وهو استدلال لا يثبت عيباً — بل قائمة واحدة مسمّاة بمسارها وعددها.**

---

## 16. `P-06` يسقط — والفحص هو الذي أسقطه

اعتراضك: «الاستدلال من *5 `AbortController` فقط* إلى احتمال عودة نتائج بترتيب خاطئ لا يكفي». **صحيح، والفحص الذي طلبته يُسقط المرشّح كاملاً.**

**كل جلبٍ تابعٍ لمدخل في التطبيق، مسمّى:**

| السطح | الملف · السطر | تأخير | إلغاء | حارس التقادم | الحكم |
|---|---|---|---|---|---|
| بحث الترويسة | `SearchBox.tsx:41–66` | ✅ `setTimeout` | ✅ `AbortController` + `abort()` في التنظيف | ✅ الأثر يُعاد تشغيله بالمصطلح | **محميّ** |
| شاشة البحث | `SearchScreen.tsx:113–135` | ✅ | ✅ | ✅ | **محميّ** |
| ورقة اختيار عمل | `TitleSearchSheet.tsx:173–215` | ✅ | ✅ | ✅ | **محميّ** |
| بدء محادثة | `StartConversationSheet.tsx:80–104` | ✅ | ✅ | ✅ | **محميّ** |
| لوحة السلسلة | `FranchisePanel.tsx:57–71` | ✗ **ولا يحتاج** | ✅ | ✅ | **محميّ** — يتبع فتح اللوحة لا الكتابة |
| **بحث الصور المتحرّكة** | `GifPicker.tsx:51–67` | ✅ **350ms** | ✗ لا `AbortController` | ✅ **علَم `alive` يمنع الكتابة المتأخّرة** | **محميّ** — والتعليق يستشهد بـD-278 |
| **حلقات الموسم** | `EpisodeTracker.tsx:216, 245` | — (بالضغط) | ✗ | ✅ **`ratedRef` يمنع التكرار · والكتابة بمفتاح الموسم `{...prev, [n]: eps}` فلا تُطمس نتيجةٌ بأخرى · والفشل يعيد فتح المحاولة** | **محميّ** |

> **الخلاصة: صفر جلبٍ تابعٍ لمدخل بلا حماية.** والعدّ الذي بنيت عليه المرشّح (`AbortController` في خمسة ملفات) كان **يقيس آليةً واحدة ويسمّيها غيابَ الحماية كلّها** — والحمايةُ ثلاثُ آليات لا واحدة: تأخير، وإلغاء، وحارس تقادم. **`P-06` مسحوب.**

**وهذا خطأ من صنف الخطأ الذي ضبطتَه عليّ في الترايلرات:** قِست بنيةً وسمّيتها سلوكاً.

---

## 17. الترحيل

**`LOOPZ-AUD-0023` (P1)** — يُرحَّل كما هو. **Visual Verification Gate (15 بنداً، صفر `PASS`)** — تُرحَّل كاملة **بعددها نفسه**: Phase 4 **لا تضيف بنداً جديداً**، وإنما تُعلّق `P-03` على البند القائم `V-1` (تشغيل الترايلر). **و`P-06` مسحوب كلّياً** (§14) فلا يُرحَّل ولا يُرقَّم. و`0014`…`0034` مفتوحة بحالاتها. و**`0014` يكتسب هنا دليلاً كمّياً**: 39–45 kB تُشحن لكل زائر يفتح رابطاً محمياً ثم تُرمى.

**وتسجيل §17 من Phase 3 كما أمرت:** سؤال المراجعة عن صفحة الإشعارات — **`review question resolved — NOT_APPLICABLE / no defect`**. **لم أُنشئ خانة `LOOPZ-AUD-0009` ولم أغلق شيئاً بأثر رجعي.**

---

## 18. إقرار

- ✅ **لا رقم زمنيّ واحد في هذا التقرير** — كل ما هو زمن مكتوب `BLOCKED_BY_SURFACE` بسببه.
- ✅ كل رقم يحمل مصدره (A بناء · B production حيّ · C مصدر)، وأمره أو ملفه، وحدوده.
- ✅ **فصلت الملاحظة عن السبب المؤكَّد** — ولم أنسب بطئاً إلى سبب بلا أثر.
- ✅ **أوقفت خمسة ادّعاءات كاذبة في هذه المرحلة وحدها**، أربعة منها في إعادة التسليم: فارق المستمعين ليس تسرّباً · و«`Logo.tsx` بلا `sizes`» كان تعليقاً عربياً لا وسماً · و«ستّة `<Image>` بلا أبعاد» تنشر `POSTER_INTRINSIC` · و«`popstate` بلا إزالة» قرار موثّق على مستوى الوحدة · **و`P-06` نفسه سقط بالفحص الذي طلبتَه**.
- ✅ **سحبت مرشّحاً من مرشّحاتي** بعد أن أثبت الفحص عدمه — لا بعد اعتراضك وحده.
- ✅ **وصحّحت تجاوزين في لساني لا في قياسي**: «فلا مسار يقبل حدّاً مفتوحاً» كانت أوسع مما أثبتّ (§12.1) · **و`P-08` قالت «خمس على مسار رسم» وجدولها لا يسنده — والتتبّع أعطى ثلاثاً** (§12.2). **كلاهما تعميمٌ فوق الدليل، وهو الخطأ الذي أراقبه في نفسي.**
- ✅ **ترقيم الأقسام والإحالات موحَّد** `11.1`…`15.3`، **وكل إحالة في الأقسام 2 و4 و6 و7 و8 و10 و17 حُدّثت وفُحصت آلياً** بمطابقة كل `§` بعنوانه.
- ✅ لم أعدّل كوداً · لم ألمس `main` · لا Merge/Deploy/Migration/حذف · لا مورد جديد.
- ✅ لم أستخدم جلسة أحمد ولم أكلّفه · لا stress test · لا مسار كاتب.
- ✅ لا أسرار ولا بيانات مستخدمين.

**قرارك في مصدر Web Vitals مستلَم ومنفَّذ:** «لا حاجة لفتح Speed Insights الآن كي تستمر الدورة: أبقِ القياسات الزمنية `BLOCKED_BY_SURFACE` واجعل القياس الصالح بوابة قبل قرار GO النهائي». **فلم أطلب صلاحية ولا تدخّلاً من أحمد**، وبقيت كل الأزمنة محجوبة، **وقياس Web Vitals الصالح يُسجَّل بوصفه بوّابة سابقةً للـGO النهائي** إلى جانب Visual Verification Gate.

**والمطلوب منك:** أرقام `LOOPZ-AUD` لـ`P-01` · `P-02` · `P-03` · `P-04` · `P-05` · `P-07` · `P-08` — **و`P-06` مسحوب فلا يُرقَّم**.

**ولم أُعِد شيئاً ممّا أجزتَه:** لا قياسات زمنية (تبقى `BLOCKED_BY_SURFACE`) · ولا فحص الصور ولا القوائم ولا stale guards · ولا Phase 5 · ولا طلب صلاحية أو تدخّل من أحمد.

**متوقّف عند البوابة. لن أبدأ Phase 5 قبل مراجعتك.**
