# SHARED_PROGRESS — ملحق Phase 11 (٧ سبتمبر ٢٠٢٦)

> ⚠️ **لماذا ملفٌّ جانبيّ؟** `SHARED_PROGRESS.md` ١٩٠ كيلوبايت من العربيّة المشكولة، وأداةُ
> الدفع من الجلسة تعيد كتابةَ الملفّ نصّاً كاملاً — **وهذا يُفقده تشكيلَه بصمت** (D-913).
> الحلُّ الدائم (D-937: `patches/target` في مسار الرقع ليطبّق على `docs/pre-app-audit`)
> **محجوبٌ بصلاحيّة**: `GITHUB_TOKEN` وتطبيقُ GitHub كلاهما يرفضان تعديلَ
> `.github/workflows/*` (403). **بانتظار المالك** (منحُ `Workflows: read/write` للتطبيق،
> أو تطبيقُ التعديل بيده). **حتى ذلك: هذا الملفُّ هو الحالةُ النافذة لـPhase 11، ويُدمج في
> `SHARED_PROGRESS.md` بالرقعة فور فتح الطريق. لا سطرَ تاريخيٍّ يُمسّ.**

## القيمُ النافذة لجدول «بيانات العمل» (تحلّ محلّ الصفوف الخمسة نفسِها)

| الحقل | القيمة |
|---|---|
| المرحلة الحالية | **Phase 11 — خطّةٌ `CHANGES_REQUESTED` ⇢ v2 مُسلَّمة** (`PHASE_11_APP_PERFORMANCE_AND_NATIVE_LIBRARY.md`): 11-A أداءُ الغلاف الهجين (D-935) · 11-B المكتبةُ أصليّةً كتجربةٍ مقيسة خلف `is_admin` (D-936). **`main @ e117ea77`** (D-932 إصلاحُ دخول التطبيق — 1.2.1 في مراجعة Play · D-933 · D-934). **السابق (Phase 10/`0040`) محفوظٌ في سجلّ الجلسات.** |
| القرار الحالي | **Phase 11: بانتظار `VERIFIED` على v2 قبل A0/B0** — لا A1 ولا B1 قبل بوّابتَيهما. **`0040` B-2 ما زالت مفتوحةً** بانتظار `SUPABASE_SERVICE_ROLE_KEY` في Vercel (لم يتغيّر). |
| آخر تحديث | **2026-09-07 · ≈20:35 UTC** — Phase 11 v2 بعد مراجعة `5575170165` (20:03:50 UTC)؛ الالتزامُ الأوّل `40d15d4` كان **20:00:07 UTC** (رسالتُه تحمل 12:40 خطأً — وقتُ بدء المسودة). |
| صاحب الدور التالي | **ChatGPT:** مراجعةُ Phase 11 v2 (البنود ١–٧ من `5575170165`) ⇢ `VERIFIED` أو `CHANGES_REQUESTED`. **بعدها Claude:** A0-prep + B0 ثمّ A0 بحضور المالك (٨ سبتمبر). **المالك:** (أ) صلاحيّةُ Workflows للتطبيق أو تطبيقُ D-937 بيده · (ب) `SUPABASE_SERVICE_ROLE_KEY` (0040 B-2) — كلاهما غيرُ مانعٍ لمراجعة v2. |
| المطلوب الآن | **مراجعةُ Phase 11 v2** · ثمّ A0-prep/B0 · فتحُ طريق D-937 · **متغيّرُ Vercel ⇢ B-2** يبقى في الطابور |

## Claude Check-in — الحالةُ النافذة

- الحالة: **Phase 11 — v2 `READY_FOR_REVIEW`** (20:35 UTC): خطّةُ أداءِ الغلاف (11-A · D-935) وتجربةِ المكتبة الأصليّة (11-B · D-936) في `PHASE_11_APP_PERFORMANCE_AND_NATIVE_LIBRARY.md`، مُعدَّلةٌ على البنود السبعة من مراجعة `5575170165`: (١) الحالةُ مُزامَنة — في هذا الملحق للسبب أعلاه · (٢) التوقيتُ مصحَّح (الالتزامُ 20:00:07 UTC) · (٣) A0 بعلاماتٍ صريحة (`am start -W` للغلاف، علامةُ جسرٍ `first-content` للويب، `chrome://inspect` للتنقّل؛ Lighthouse ليس بوّابة؛ شروطُ الجولة تُسجَّل) · (٤) حزمة = التزام = قياس، وبناءُ EAS الواحد لـA4+A6 يسبقه قياسُ A4 على APK `preview`؛ A2 لا يبدأ قبل §١.٥ · (٥) بوّابةُ A2 §١.٥: جردُ `pg_policies` بـsha256 + مصفوفةُ أدوار×حالات على preview + `EXPLAIN ANALYZE` + سكربتٌ عكسيّ + تطبيقُ المالك الصريح · (٦) عقدُ أمان B1 §٢.٢-ب (ذاكرةٌ فقط، تحقّقُ مصدرٍ ونوع، `session:clear`، 401 بمحاولتَين، لا `/token` ولا `/logout`، دليلُ `grep` + سجلّ Supabase) والعلمُ محسوم `profiles.is_admin` (المالك وKHLD فقط، شرطٌ خادميّ) · (٧) القراران مسجَّلان في §٤.٤ (٢٪ بشروط B0 وقناعٍ للبوسترات فقط؛ Bearer بلا تجديد لهذه التجربة فقط). **الحالةُ السابقة (Phase 10D · D-884…D-896) باقيةٌ في `SHARED_PROGRESS.md` كما هي.**

## سجلّ الجلسات — Append Only (يُنقل نصّاً إلى أسفل `SHARED_PROGRESS.md`)

### 2026-09-07 20:00:07 UTC (الالتزام؛ المسودةُ من ≈12:40) → 20:35 UTC (v2) — Phase 11: خطّةُ أداء الغلاف الهجين (D-935) + تجربةُ المكتبة الأصليّة (D-936) — Claude
- **ما سبقها على `main` اليوم (مسارُ الرقع، مطابقةٌ بايتاً):** D-932 `aad09c4c` — **دخولُ التطبيق كان يُلغى بعد ثانيتين**: `signOut({scope:"local"})` في الغلاف ينادي `/logout` ويُلغي الجلسةَ المسلَّمة (سجلّاتُ Supabase 10:43–10:44Z: login ⇢ logout ⇢ `session_not_found`)؛ العميلُ الأصليُّ صار ذاكرةً فقط بلا تجديدٍ ولا `signOut` — **1.2.1 في مراجعة Play** · D-933 `f5763372` شريطُ مستوى الصوت · D-934 `e117ea77` ضغطةٌ مزدوجة ±٥ ث.
- **الخطّة `40d15d4`** ثمّ **مراجعةُ ChatGPT `CHANGES_REQUESTED` (`5575170165`)** بسبعة بنود — **كلُّها مقبولةٌ ومطبَّقة في v2**. لا اعتراضَ على أيٍّ منها؛ البندُ ٢ خطأٌ منّي في تسمية وقت الالتزام.
- **مانعٌ أدواتيّ مكتشَف (D-937):** مسارُ الرقع لا يعرف إلا `main`؛ التعديلُ الذي يضيف `patches/target` **رُفض مرّتين** — عبر المسار نفسِه (`GITHUB_TOKEN` لا يملك `workflows`) وعبر تطبيق GitHub (`403 Resource not accessible by integration`). الفرعُ `patch/d937-lane-target` باقٍ للفحص، **والملفُّ المعدَّل جاهزٌ عند Claude**. **الطلبُ للمالك: صلاحيّةُ `Workflows: read/write` للتطبيق «Claude Github MCP Connector»، أو لصقُ التعديل بيده** — ثمّ يُدمج هذا الملحقُ في `SHARED_PROGRESS.md` بالرقعة ويُحذف.
- **صاحبُ الدور التالي:** ChatGPT (مراجعة v2). **A0/B0 لا يبدآن قبل `VERIFIED`.**
