# Phase 2 — Functional & UX Journeys (زائر · قراءة آمنة)

- الحالة: `READY_FOR_REVIEW` — **إعادة تسليم بعد `CHANGES_REQUESTED`** · المنفّذ: Claude · المراجع: ChatGPT
- **Audited SHA:** `f8a2b33cd036cffd1e7a0b9bc3e5ced0e19b8bfa` — **ومؤكَّد أنه ما تشغّله Production** (انظر §1.2).
- التاريخ: 2026-09-01 UTC
- **صفر تسجيل دخول · صفر POST/PUT/PATCH/DELETE · صفر كتابة في قاعدة البيانات · صفر تعديل كود · صفر Deploy · صفر مورد جديد.**

---

## 1. سطح الفحص — ولماذا هذا لا ذاك

### 1.1 المتصفّح المستخدم

| المتصفّح | الحالة | القرار |
|---|---|---|
| Chrome عند أحمد (Browser 1) | **مسجَّل دخوله في `loopztv.com`** — كوكيز `sb-uvgmvrdrxzpudoldjxaa-auth-token.0/.1` حيّة | **لم يُستخدم لفحص الزائر.** ولم أسجّل خروجه: جلسته ليست ملكي |
| المتصفّح المدمّج في تطبيق Claude | **زائر حقيقي** — لا كوكي جلسة، ملفّ تعريف مستقل | **هو سطح Phase 2** |

هذه ليست تفصيلة إجرائية: **لو فحصتُ في متصفّح أحمد لكان كل ما أسمّيه «تجربة الزائر» كذباً**، لأن كل صفحة كانت سترسم بجلسته وبياناته وثيمه ولغته.

### 1.2 ما أغلقه هذا الفحص من موانع Phase 0

**`GET /api/build` من متصفّح الزائر يُعيد:**

```
f8a2b33cd036cffd1e7a0b9bc3e5ced0e19b8bfa
```

**أي أن Production يشغّل الـAudited SHA بعينه.** كان هذا `UNKNOWN` في Phase 0 (Vercel MCP يردّ 403، وأداة الجلب محجوبة بـ`Disallow: /api/`). **المانع رقم 3 من Phase 0 مُغلَق الآن بدليل من المنصّة نفسها لا من المستودع.**

### 1.3 حدود هذا السطح — تُقال قبل النتائج لا بعدها

لوح المتصفّح المدمّج **مخفيّ**، ويترتّب على ذلك ثلاثة قيود أعلنها صراحةً بدل أن أدّعي فوقها:

| القيد | الأثر | كيف عالجته |
|---|---|---|
| `computer{action:"screenshot"}` يفشل بـ`UnknownVizError` | **لا دليل بصريّ ولا فحص تباين ولا zoom 200% ولا تحقّق RTL بصريّاً** | كل بند بصريّ وُسم `BLOCKED_BY_SURFACE` مع سببه — لم أدّعِ نجاحه ولا فشله |
| `element.innerText` يعيد فراغاً (لا تخطيط مرئي) | قياس النصّ عبره **يعطي نتائج كاذبة** | استُخدم `textContent` في كل قياس نصّي. (وهذا نفسه أنقذني من خطأ: أول قراءة للرئيسية أعطت «نصّ الصفحة = Home» وكاد يُسجَّل كعطل — وهو أثرُ أداة لا عطلُ منتَج) |
| اللوح خلفيّ فقد يُخنَق مؤقّتاً | **كل ادّعاء زمنيّ غير موثوق** | لا رقم زمنيّ في هذا التقرير. الأداء بند Phase 4 بأدواته |

**القياسات التي تعتمد على DOM وHTTP (الحالة، التحويل، الترويسات، الفيض الأفقي، أسماء العناصر، الميتاداتا) دقيقة على هذا السطح** — وهي عماد ما يلي.

---

## 2. مصفوفة الـ49 صفحة — الحالة كزائر

**المنهج:** `fetch(path, {redirect:'manual'})` من أصل الموقع نفسه لكل مسار، ثم تصفّح حيّ للصفحات العامة. عمود «الآلية» يميّز التحويل الحقيقي (3xx) من **التحويل اللَيّن** (200 + `<meta http-equiv="refresh">`).

### 2.1 الصفحات المحميّة (27) — كلها نمط واحد

`/activity` · `/calendar` · `/library` · `/lists` · `/messages` · `/profile` · `/profile/edit` · `/profile/settings` وأبناؤها الأربعة عشر · `/ratings` · `/reports` · `/statistics` · `/stats` · `/welcome`

| القياس | القيمة |
|---|---|
| HTTP status كزائر | **200** — لا 307 ولا 302 |
| آلية الحماية | `<meta id="__next-page-redirect" http-equiv="refresh" content="1;url=/login"/>` |
| حجم ما يُسلَّم للزائر قبل القذف | **≈ 40–45 ك.ب HTML كاملة** |
| زمن البقاء على الصفحة الخطأ | **ثانية واحدة معلنة في الـmeta** |
| الحالة | **`FAIL` — انظر `C-01`** |

### 2.2 الصفحات الإدارية (3)

`/admin/links` · `/admin/partners` · `/admin/verify`

| القياس | القيمة |
|---|---|
| HTTP status كزائر | **200** |
| المحتوى المرسوم | `Home Discover Community Search Sign in EN Sign in` — **الترويسة وحدها، وجسم فارغ** |
| تحويل | **لا شيء** |
| الحالة | **`FAIL` — انظر `C-02`.** الصلاحية نفسها سليمة (لا تسريب محتوى إداري)، **والعطل في العرض لا في الحماية** |

### 2.3 الصفحات العامة (16) — كلها `PASS` في الوصول

| المسار | status | ملاحظة |
|---|---|---|
| `/` | 200 | يرسم صفحة هبوط كاملة للزائر: عنوان · «See Loopz before you sign in» · «What is Loopz?» · **أسئلة شائعة بثمانية عناوين** · «Start free» · «Continue with Google» · روابط أعمال حقيقية |
| `/login` | 200 | يعمل — **مع عطل لغة، انظر `C-04`** |
| `/features` `/plus` `/terms` `/privacy` | 200 | تسويق وقانون، ميتاداتا سليمة |
| `/search` | 200 | يعمل للزائر · `/api/search` مفتوح للزائر بحدّ IP |
| `/trailers` | 200 | يعمل — **مع فراغ بلا حالة فارغة، انظر `C-08`** |
| `/discover/[section]` | 200 | يعمل — **والقسم المجهول لا يُنكَر، انظر `C-09`** |
| `/movie/[id]` `/show/[id]` `/person/[id]` | 200 | تعمل — **بلا ميتاداتا للاثنين الأولين، انظر `C-05`** |
| `/talk/[type]/[id]` · `/u/[username]` · `/news` · `/people` | 200 | تعمل للزائر |
| `/diary` | 200 → `1;url=/activity` | **ليست صفحة**: جسمها كلّه `redirect("/activity")`. والزائر يُقذف مرتين: `/diary` → `/activity` → `/login` |

### 2.4 المسارات غير الصفحية

| المسار | status | ملاحظة |
|---|---|---|
| `/robots.txt` | 200 | `Allow: /` + `Disallow: /api/` و`/auth/` + Sitemap — سليم |
| `/manifest.webmanifest` | 200 | سليم |
| `/opengraph-image` | 200 | سليم |
| `/sitemap.xml` | 200 | **أربعة روابط فقط — انظر `C-06`** |
| `/api/build` | 200 | يُعيد SHA الإنتاج |
| مسار غير موجود | **404** | **وبواجهة 404 صحيحة**: «Page not found — That link doesn't point anywhere. Try searching or head back home.» ✅ |
| `/p/[code]` · `/join/[code]` | — | **لم تُطلَب عمداً**: كلاهما يكتب كوكي إحالة، و`/p/[code]` يستدعي `bump_partner_click` أي يكتب في القاعدة. خارج نطاق «القراءة الآمنة» بأمرك |
| دورات المشغّل (`/api/genres` · `/api/title-meta` · `/api/imdb-chart` · `/api/news-gen` · `/api/curated`) | — | **لم تُطلَب عمداً** — كاتبة |

**التغطية: 49/49 صفحة بحالة صريحة، وصفر مسار بلا نتيجة.**

---

## 3. المشكلات — **الأرقام معتمدة من ChatGPT**

| المرشّح | الرقم المعتمد | | المرشّح | الرقم المعتمد |
|---|---|---|---|---|
| `C-01` | `LOOPZ-AUD-0014` | | `C-06` | `LOOPZ-AUD-0019` |
| `C-02` | `LOOPZ-AUD-0015` | | `C-07` | `LOOPZ-AUD-0020` |
| `C-03` | `LOOPZ-AUD-0016` | | `C-08` | `LOOPZ-AUD-0021` |
| `C-04` | `LOOPZ-AUD-0017` | | `C-09` | `LOOPZ-AUD-0022` |
| `C-05` | `LOOPZ-AUD-0018` | | | |

### `C-01` — الصفحات المحميّة تُسلِّم صفحةً كاملة للزائر ثم تقذفه بعد ثانية · **P2**

- **النطاق:** 27 صفحة محميّة.
- **الحالي:** `GET /library` كزائر ← **200** + **45 ك.ب HTML** + `<meta http-equiv="refresh" content="1;url=/login">`.
- **المتوقّع:** **307/302** إلى `/login` بلا جسم.
- **الأثر ثلاثيّ:** ① الزائر يرى هيكل صفحة لا يملكها **ثانيةً كاملة** — ومضة تُقرأ كعطل. ② محرّكات البحث ترى **200 لا تحويلاً**، فتفهرس صفحات محميّة فارغة. ③ **45 ك.ب تُرسل بلا فائدة** في كل مرة.
- **السبب المرجّح:** `redirect()` يُستدعى **بعد** بدء البثّ (`getUser()` قبله)، فيسقط Next إلى meta-refresh لأن الترويسات أُرسلت. الحلّ المعروف: حسم الجلسة قبل أي بثّ — في `src/proxy.ts` أو في مقدّمة الصفحة.
- **الدليل:** `fetch('/library',{redirect:'manual'})` → `status:200`, والـHTML يحوي `__next-page-redirect ... content="1;url=/login"`. متكرّر على 27 مساراً.

### `C-02` — `/admin/*` كزائر: 200 وصفحة فارغة بلا 404 · **P2**

- **الحالي:** الترويسة وحدها، جسم فارغ، لا تحويل، **status 200**.
- **المتوقّع:** واجهة 404 نفسها التي يعرضها مسار غير موجود (وهي **موجودة وتعمل**، §2.4).
- **الأثر:** ليس تسريباً — لا محتوى إداريّ يظهر، والصلاحية في القاعدة سليمة (`am_admin()`). لكنه **صفحة بيضاء تُقرأ كعطل**، و**soft-404** لمحرّكات البحث.
- **السبب المرجّح:** نفس سبب `C-01` — `notFound()` بعد بدء البثّ لا يستطيع تبديل الاستجابة.

### `C-03` — عملٌ غير موجود يُقدَّم كعطل مؤقّت · **P2**

- **الحالي:** `GET /movie/999999999` ← **200** + «**Couldn't load this movie right now. Please try again shortly.**»
- **المتوقّع:** 404 وواجهة «غير موجود».
- **الأثر:** الرسالة **تَعِد بما لن يحدث**: لا «shortly» ينفع لعمل غير موجود. والمستخدم ينتظر ويعيد المحاولة بلا طائل. وللمحرّكات: soft-200 على صفحة لا محتوى لها.
- **ملاحظة تمييز مهمّة:** الرسالة نفسها **صحيحة تماماً** لو كان TMDB متعطّلاً فعلاً. المشكلة أن **الحالتين لا تُفرَّقان**: «غير موجود» و«تعذّر الجلب» تُعرضان بالنصّ نفسه.

### `C-04` — زرّ تسجيل الدخول عربيّ في صفحة إنجليزية · **P2**

- **الصفحة:** `/login`، و`<html lang="en" dir="ltr">`، وكل نصّها إنجليزي.
- **الحالي:** الزرّ إطار `iframe` من `https://accounts.google.com/gsi/button` ونصّه **«المواصلة باستخدام Google»**، وعنوان الإطار **«زر تسجيل الدخول باستخدام حساب Google»**، واسمه للقارئ الصوتي **«المواصلة باستخدام Googleيفتح الرابط في علامة تبويب جديدة»** (بلا فاصل).
- **والدليل على أنه خلل لا سياسة:** في **الجلسة نفسها** ترسم `/` زرّها الداخلي **«Continue with Google»** بالإنجليزية. فالتطبيق يعرف لغة الزائر، وGIS وحده لا يعرفها.
- **الأثر:** **أهمّ سطح تحويل في المنتج يخاطب الزائر الإنجليزي بلغة لا يقرؤها.**
- **الإصلاح المرجّح:** تمرير `locale`/`hl` إلى GIS من نفس مصدر لغة الواجهة.

### `C-05` — 37 من 49 صفحة بلا ميتاداتا، ومنها صفحات الأعمال · **P2**

| الحالة | الصفحات |
|---|---|
| **لديها ميتاداتا (12)** | `/` · `/features` · `/plus` · `/terms` · `/privacy` · `/login` · `/person/[id]` · `/lists/[id]` · `/post/[key]` · `/talk/[type]/[id]` · `/review/[type]/[id]/[user]` · `/u/[username]/stats` |
| **بلا ميتاداتا (37)** | ومنها: **`/movie/[id]`** · **`/show/[id]`** · **`/u/[username]`** · `/search` · `/news` · `/people` · `/trailers` · `/discover/[section]` |

- **الدليل الحيّ:** `GET /movie/550` → `<title>Loopz — track your shows & movies</title>` و`og:title` المطابق. اسم «Fight Club» موجود في الصفحة **داخل حمولة JSON فقط**، لا في عنوان ولا في وسم مشاركة.
- **والفجوة تكشف نفسها بأنها سهو لا قرار:** `/person/[id]` تضع `title: person.name` ✅ · `/lists/[id]` لديها ✅ · **و`/u/[username]/stats` لديها بينما أمّها `/u/[username]` ليس لديها** ❌. الابن يفعلها والأب لا.
- **الأثر:** كل رابط فيلم أو مسلسل يُشارَك في واتساب أو X يظهر بعنوان **«Loopz — track your shows & movies»** بدل اسم العمل. وهذه أكثر الروابط مشاركةً في منتج كهذا.

### `C-06` — `sitemap.xml` يعرض أربعة روابط فقط · **P2**

- **الحالي:** `/` · `/features` · `/privacy` · `/terms`. المصدر `src/app/sitemap.ts` يعدّها يدوياً.
- **الأثر:** تطبيق كتالوج بصفحات أعمال وأشخاص وقوائم وملفّات عامة **لا يعرض منها شيئاً واحداً** لمحرّكات البحث.
- **ويجتمع مع `C-05`:** الصفحات غير مفهرسة **ولا** تحمل عنواناً خاصاً بها لو فُهرست.
- **ملاحظة إنصاف:** هذا قد يكون قراراً مقصوداً (كلفة زحف، أو تجنّب فهرسة كتالوج TMDB). **إن كان قراراً فليُوثَّق في `07_Design_Decisions.md`**؛ وإن لم يكن فهو أكبر فجوة نموّ في المنتج.

### `C-07` — فيض أفقي في صفحة العمل على الجوال · **P3**

- **الشرط:** 390×844، `/movie/550`، **أثناء حالة الهيكل (skeleton)**.
- **القياس:** `documentElement.scrollWidth = 415` مقابل `clientWidth = 390` — **فيض 25px في المستند نفسه**، لا داخل شريط تمرير أفقي.
- **المتجاوِز:** عنصر `<a class="relative flex min-w-0 items-center justify-center rounded-2x…">` عند `right = 415`، و`overflow-x` لأبيه `visible`.
- **حدّ الادّعاء:** قيس **في حالة الهيكل**؛ لم أستطع تأكيد بقائه بعد اكتمال الرسم على هذا السطح.
- **وللمقارنة — وهذا يقوّي القياس لا يضعفه:** `/` عند 390 لا فيض فيه (`scrollWidth == clientWidth == 390`) رغم احتوائه على أشرطة تمرير أفقية. أي أن **الأشرطة لا تنفخ الرقم**، والفيض في `/movie/[id]` حقيقي.

### `C-08` — تبويب «For You» في الترايلرات فارغ للزائر بلا حالة فارغة · **P3**

- **الحالي:** `/trailers` كزائر ← التبويبات الخمسة تظهر (`For You · Trending · Movies · Shows · Anime`)، ثم **فراغ**: صفر `<video>`، صفر بطاقة، **وصفر نصّ يشرح**.
- **والمقارنة تثبت أن المحرّك سليم:** `/trailers?tab=trending` **يعمل** — عنصر `<video>` و42 صورة.
- **الأثر:** الزائر بلا سجلّ مشاهدة يهبط على تبويب لا يمكن أن يملأ نفسه، فيرى صفحة معطوبة. والحلّ إمّا حالة فارغة تشرح وتدعو، أو تحويل الزائر إلى «Trending» افتراضياً.

### `C-09` — قسم استكشاف مجهول لا يُنكَر · **P3**

- **الحالي:** `/discover/not-a-real-section` ← **200**، وجسم الاستجابة **مطابق حرفياً** لـ`/discover/movies` (قورنا نصّاً: `identical: true`).
- **المتوقّع:** 404 — والكود فيه `notFound()` فعلاً.
- **السبب المرجّح:** نفس سبب `C-01` و`C-02` — `notFound()` بعد بدء البثّ.

> **ثلاث مشكلات بجذر واحد:** `C-01` و`C-02` و`C-09` كلها أعراضُ أن `redirect()`/`notFound()` تُستدعى بعد بدء بثّ الاستجابة. **إصلاح واحد في مكان الحسم قد يغلق ثلاثتها** — وهذا يخصّك أنت في ترتيب Phase 10.

---

## 4. تقرير الترايلرات المستقلّ (أولويتك P1)

| البند المطلوب | النتيجة | الدليل |
|---|---|---|
| ظهور الفيديو لا الصوت وحده | **`PASS` جزئياً** | `/trailers?tab=trending` يُنشئ `<video>` حقيقياً + 42 صورة |
| **الافتراضي Muted للمستخدم الجديد** | ✅ **`PASS`** | `video.muted === true` · `autoplay === false` · وكوكي `loopz_trailer_sound` **غير موجود أصلاً** لزائر جديد. **يطابق السلوك المتوقّع الذي حدّدته، لا سلوك `mccicc2-art-patch-1`** |
| تذكّر آخر اختيار للصوت محلياً | **`BLOCKED_BY_SURFACE`** | يحتاج ضغط زرّ الصوت ثم إعادة تحميل ومراقبة الكوكي — والتفاعل غير موثوق بلا رؤية |
| زرّ الصوت والزمن يعملان | **`BLOCKED_BY_SURFACE`** | لا لقطة ولا تفاعل موثوق |
| التمرير يشغّل الحالي ويوقف السابق | **`BLOCKED_BY_SURFACE`** | يعتمد `IntersectionObserver` وهو معطَّل المعنى في لوح مخفيّ |
| حفظ موضع Discover عند العودة | **`BLOCKED_BY_SURFACE`** | يحتاج تمريراً حقيقياً ورجوعاً |
| التوقّف عند background/navigation | **`BLOCKED_BY_SURFACE`** | اللوح نفسه في الخلفية — الشرط ملوَّث |
| تبويب «For You» للزائر | ❌ **`FAIL`** | `C-08` |

**الخلاصة الصادقة:** البند الوحيد الذي طلبتَه صراحةً وكان قابلاً للإثبات على هذا السطح — **الكتم الافتراضي** — **مثبَت وناجح**. والستّة الباقية تحتاج متصفّحاً مرئيّاً، ولا أدّعي فيها نجاحاً ولا فشلاً. **من يستطيع إثباتها في دقيقتين هو أحمد على جهازه.**

---

## 5. المقاسات

| المقاس | ما قيس | النتيجة |
|---|---|---|
| **1440×900** | `/` · `/login` · `/features` · `/trailers` · `/movie/550` | لا فيض أفقي في أيٍّ منها |
| **390×844** | `/` | ✅ لا فيض (`390 == 390`) · عنصر تفاعلي واحد تحت 44px وهو **رابط التخطّي المخفيّ 1×1** وهذا صحيح لا عطل |
| **390×844** | `/movie/550` | ❌ **فيض 25px** — `C-07` |
| **390×844** | `/trailers?tab=trending` | لا فيض · **7 من 11 عنصراً تفاعلياً مرئياً أصغر من 44×44** — مرشّح وصول، انظر §6 |
| **768×1024** | — | **`BLOCKED_BY_SURFACE`**: بلا لقطات، قياس اللوحي يضيف أرقاماً بلا معنى بصريّ. أُجري القياس البنيوي على الطرفين (390 و1440) وهما يحدّان المدى |

**RTL:** حاولت التبديل محلياً بكتابة كوكي `locale=ar` من المتصفّح (كتابة محلية مسموحة بأمرك) وإعادة التحميل — **ولم تتبدّل الصفحة**، بقيت `lang="en" dir="ltr"`. لا أعرف بعد أهو لأن اسم/قيمة الكوكي غير كافيين وحدهما أم لأن التبديل يمرّ بمسار خادم آخر. **`UNKNOWN` — يُحسم بقراءة `src/lib/locale.ts` في Phase 3، ولم أُرِد تخميناً هنا.**

---

## 6. الوصول — ما يُقاس بالـDOM فقط

**`/features` كخطّ أساس نظيف:**

| المقياس | النتيجة |
|---|---|
| `<h1>` واحد | ✅ |
| قفزات ترتيب العناوين | ✅ لا شيء |
| صور بلا سمة `alt` | ✅ صفر |
| أزرار بلا اسم متاح | ✅ صفر |
| روابط بلا اسم متاح | ✅ صفر |
| رابط تخطّي إلى `#main` | ✅ موجود |
| معلَم `<main>` | ✅ موجود |

**وعلى الرئيسية:** كل أيقونات التنقّل تحمل `aria-label` صريحة (`Discover` · `Community` · `Search` · `Sign in` · `Loopz`)، وزرّ اللغة يحمل `aria-label="Interface language"` و`aria-haspopup="listbox"` و`aria-expanded`. **هذه بنية وصول مبنيّة بعناية، لا صدفة.**

**ومرشّح واحد:** في `/trailers?tab=trending` على 390×844، **7 من 11 عنصراً تفاعلياً مرئياً أصغر من 44×44** (توصية WCAG 2.5.5 / إرشاد Apple). يستحق قياساً بصريّاً قبل الحكم — قد يكون بعضها متجاوراً ضمن هدف أكبر.

**`BLOCKED_BY_SURFACE`:** التباين اللوني · تركيز لوحة المفاتيح المرئي · zoom 200% · Reduced Motion المرئي · قارئ الشاشة. **كلها تحتاج رؤية.**

---

## 7. الترويسات وAPIs العامة — فحص غير كاتب وبمعدّل منخفض

### 7.1 ترويسات الأمن على Production — **كلها حاضرة وصحيحة**

| الترويسة | القيمة الحيّة |
|---|---|
| `Content-Security-Policy` | حاضرة · `default-src 'self'` · `frame-ancestors 'none'` · `object-src 'none'` |
| `Strict-Transport-Security` | `max-age=63072000; includeSubDomains; preload` — **سنتان + preload** |
| `X-Frame-Options` | `DENY` |
| `X-Content-Type-Options` | `nosniff` |
| `Referrer-Policy` | `strict-origin-when-cross-origin` |
| `Permissions-Policy` | `camera=(), microphone=(), geolocation=(), payment=(), usb=()` |
| `Cross-Origin-Opener-Policy` | `same-origin-allow-popups` |
| `Cache-Control` على صفحة عامة | `private, no-cache, no-store, max-age=0, must-revalidate` |

**ما وُعد به في `next.config.ts` مُسلَّم فعلاً في الإنتاج.** (وبقاء `'unsafe-inline'` في `script-src` هو `LOOPZ-AUD-0007` المسجَّل، لا اكتشاف جديد.)

> ⚠️ **ملاحظة أداء لا أمن، للتوثيق:** `Cache-Control: private, no-store` على صفحة عامة ثابتة مثل `/features` يمنع أي تخبئة على الحافة. مادة **Phase 4** لا Phase 2، ولا أحكم عليها هنا.

### 7.2 المسارات العامة غير الكاتبة

| المسار | النتيجة | الحكم |
|---|---|---|
| `GET /api/build` | 200 · `f8a2b33c…` | ✅ ويغلق مانع Phase 0 |
| `GET /api/search?q=fight` | 200 · نتائج حقيقية · 2081 بايت | ✅ **مفتوح للزائر عمداً** كما وثّقت Phase 1 |
| `GET /api/search?q=f` (حرف واحد) | 200 · `{"titles":[],"artists":[],"members":[],"lists":[]}` | ✅ **تحقّق «حرفان على الأقل» يعمل حيّاً** |
| `GET /api/suggest?q=fight` كزائر | **401** · `{"results":[]}` | ✅ **فرض الجلسة يعمل حيّاً** — ويطابق جدول Phase 1 حرفاً |
| `GET /robots.txt` | 200 · `Allow: /` + `Disallow: /api/` و`/auth/` | ✅ |

**لم يُجرَ أي stress test ولا محاولة تجاوز لحدّ المعدّل**، ولا لمس مسار كاتب واحد.

### 7.3 ادّعاء فحصته فلم يصمد — يُسجَّل لأنه كاد يصير بلاغاً كاذباً

`/api/search` يُعيد روابط ملصقات **مباشرةً إلى `https://image.tmdb.org/...`**، بينما `18_Project_Context.md` يقول إن الطلب المباشر لهذا النطاق **محجوب عن مستخدمينا** (D-726). بدا هذا عطلاً واضحاً.

**وهو ليس كذلك.** `src/components/SearchScreen.tsx:466` يمرّر الرابط إلى `next/image`، و`next/image` يمرّ بالمحمِّل المخصّص `src/lib/imageLoader.ts` الذي يحوّل المقاسات الصغيرة (ومنها `w185`) إلى الممرّ المخزَّن `/i/…`. **فالرابط المباشر لا يُطلَب أبداً.**

سجّلته لأن **الادّعاء الذي لم يُفحص أخطر من العطل الذي لم يُكتشف.**

---

## 8. ما لا يمكن إثباته — بلا تجميل

### 8.1 `BLOCKED_BY_TEST_ENV` — يحتاج حساباً

| الرحلة | الدور المطلوب |
|---|---|
| إنشاء حساب ← إكمال الملف ← الاستخدام | Free |
| بقاء الجلسة بعد الإغلاق والعودة | Free |
| تتبّع حلقة/موسم · Rewatch · التقييم وتعديله | Free |
| تعليق ← تعديل ← حذف · وظهور الأثر في ثلاثة أسطح | Free |
| متابعة · حساب خاص · قبول/رفض الطلب | Free + Private |
| إنشاء قائمة · إضافة · ترتيب · حذف | Free |
| فتح إشعار والوصول للمحتوى الصحيح | Free |
| **صفحة الإشعارات: هل تُظهر أنها غير مفعّلة أم تُوهم بالحفظ؟** (بندك 7) | Free — **محميّة، ولا تُرى كزائر** |
| فروق Plus مقابل Free | Plus + Free |
| مسار التوثيق والشراكة | Verified · Partner · Admin |
| عزل A/B وIDOR | زوج A/B |
| تنزيل البيانات وحذف الحساب | Free |
| **الجولات التعريفية** | Free — تبدأ بعد الدخول |

### 8.2 `BLOCKED_BY_SURFACE` — يحتاج متصفّحاً مرئيّاً

سلوك تشغيل الترايلر والتمرير والصوت · حفظ موضع التمرير · Back/Forward البصريّ · التباين · تركيز لوحة المفاتيح المرئي · zoom 200% · Reduced Motion · أهداف اللمس بصريّاً · تصادم العناصر الثابتة · Safe Area · قياس اللوحي · أي دليل بصريّ.

**كلها تُغلَق بجلسة واحدة على جهاز أحمد** — أو بلوح متصفّح مرئيّ.

### 8.3 ولم أفعل هذا عمداً

`/p/[code]` و`/join/[code]` (كتابة كوكي + `bump_partner_click`) · دورات المشغّل الخمس · أي POST · أي تسجيل دخول · أي تجاوز لحماية · أي stress test.

---

## 9. ما ثبت أنه **يعمل** — لأن التقرير الذي لا يقول إلا العيوب يكذب أيضاً

1. **Production يشغّل الـAudited SHA بعينه** — وأغلق مانع Phase 0.
2. **ترويسات الأمن السبع كلها مُسلَّمة حيّاً.**
3. **404 الحقيقي يعمل بواجهة صحيحة ومفيدة** ونصّ يقترح البحث والعودة.
4. **`/` صفحة هبوط كاملة للزائر**: أسئلة شائعة بثمانية عناوين، ودعوتان للتسجيل، وأعمال حقيقية.
5. **حدّ المعدّل وفرض الجلسة يعملان حيّاً** — `/api/suggest` يردّ 401 للزائر.
6. **تحقّق المدخلات يعمل حيّاً** — استعلام بحرف واحد يُعيد فارغاً.
7. **الكتم الافتراضي في الترايلرات مثبَت** ومطابق للسلوك الذي حدّدته.
8. **بنية الوصول مبنيّة بعناية**: رابط تخطّي، معلَم `main`، `aria-label` على كل أيقونة، `aria-haspopup`/`aria-expanded` على القوائم، صفر صورة بلا `alt` على الصفحة المفحوصة.
9. **صفر خطأ Console غير معالَج** رُصد في الصفحات العامة المتصفَّحة.

---

## 10. مصفوفة الـ49 صفحة — صفّاً صفّاً

**مفاتيح الحالة:** `PASS` = السلوك المرصود مطابق للمتوقّع · `FAIL` = مخالف · `BLOCKED_BY_TEST_ENV` = يحتاج حساباً · `BLOCKED_BY_SURFACE` = يحتاج سطحاً مرئيّاً (انظر §11) · `N/A` = لا ينطبق.

**أدلّة مشتركة تُشار بالرمز بدل التكرار:**

- **`E-GUARD`** — `fetch(path,{redirect:'manual'})` ← `status:200` + `<meta id="__next-page-redirect" http-equiv="refresh" content="1;url=/login">` · حجم الجسم 39–45 ك.ب. مطبَّق على 27 مساراً.
- **`E-ADMIN`** — `status:200` · جسم مرسوم = `Home Discover Community Search Sign in EN Sign in` · لا تحويل.
- **`E-PUB`** — `status:200` · تصفّح حيّ كزائر · لا خطأ Console غير معالَج · لا طلب شبكة فاشل رُصد.
- **`E-SHELL`** — الصفحة تُرجع 200 وهيكلاً، **ولا يكتمل رسم محتواها في سطح الفحص** (`document.visibilityState === "hidden"`، §11). القياس البنيوي صالح، وتقييم المحتوى `BLOCKED_BY_SURFACE`.

### 10.1 الصفحات المحميّة (27) — `FAIL` بـ`LOOPZ-AUD-0014`

| # | Route | status | الآلية | المحتوى للزائر | loading | error | not-found | Console | Network | الحالة |
|---:|---|:--:|---|---|---|---|---|:--:|:--:|---|
| 1 | `/` | 200 | — (صفحة هبوط) | **كاملة**: عنوان · «See Loopz before you sign in» · «What is Loopz?» · **8 أسئلة شائعة** · «Start free» · «Continue with Google» · أعمال حقيقية | local | **local** | **local** | نظيف | نظيف | **`PASS`** |
| 2 | `/activity` | 200 | `E-GUARD` | هيكل ثم قذف بعد ثانية | local | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |
| 3 | `/calendar` | 200 | `E-GUARD` | ↑ | inherit ← `/` | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |
| 4 | `/library` | 200 | `E-GUARD` | ↑ | local | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |
| 5 | `/lists` | 200 | `E-GUARD` | ↑ | local | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |
| 6 | `/messages` | 200 | `E-GUARD` | ↑ | local | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |
| 7 | `/profile` | 200 | `E-GUARD` | ↑ | inherit ← `/` | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |
| 8 | `/profile/edit` | 200 | `E-GUARD` | ↑ | inherit ← `/` | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |
| 9 | `/profile/settings` | 200 | `E-GUARD` | ↑ | local | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |
| 10–23 | `/profile/settings/{about,account,appearance,billing,content,help,home,import,invites,notifications,privacy,verify}` + `/reports` + `/welcome` | 200 | `E-GUARD` | ↑ | inherit ← `/profile/settings` (والأخيران ← `/`) | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |
| 24 | `/ratings` | 200 | `E-GUARD` | ↑ | local | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |
| 25 | `/statistics` | 200 | `E-GUARD` | ↑ | inherit ← `/` | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |
| 26 | `/stats` | 200 | `E-GUARD` | ↑ | local | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |
| 27 | `/diary` | 200 | `1;url=/activity` ثم `E-GUARD` | **قذفتان متتاليتان**: `/diary` → `/activity` → `/login` | local (**لن يُرى**) | inherit ← `/` | inherit ← `/` | نظيف | نظيف | `FAIL` |

**رحلات هذه الصفحات بعد الدخول:** `BLOCKED_BY_TEST_ENV` — الدور المطلوب: Free (وPlus للفروق).

### 10.2 الصفحات الإدارية (3) — `FAIL` بـ`LOOPZ-AUD-0015`

| # | Route | status | المحتوى | loading/error/not-found | Console | Network | الحالة |
|---:|---|:--:|---|---|:--:|:--:|---|
| 28 | `/admin/links` | 200 | `E-ADMIN` — ترويسة وجسم فارغ، **لا 404** | inherit ← `/` (الثلاثة) | نظيف | نظيف | `FAIL` |
| 29 | `/admin/partners` | 200 | `E-ADMIN` | ↑ | نظيف | نظيف | `FAIL` |
| 30 | `/admin/verify` | 200 | `E-ADMIN` | ↑ | نظيف | نظيف | `FAIL` |

**التحقق من الصلاحية نفسها** (أن الإداري يرى المحتوى وغيره لا): `BLOCKED_BY_TEST_ENV` — الدور: Admin.

### 10.3 الصفحات العامة (19) — التفصيل

| # | Route | status | المحتوى كزائر | حالة فارغة/تحميل/خطأ | Console | Network | الحالة |
|---:|---|:--:|---|---|:--:|:--:|---|
| 31 | `/login` | 200 | يرسم كاملاً · زرّ GIS | لا حاجة | نظيف | نظيف | **`FAIL`** — `LOOPZ-AUD-0017` (زرّ عربيّ في صفحة إنجليزية) |
| 32 | `/features` | 200 | **يرسم كاملاً** · عنوان `Features — Loopz` | لا حاجة | نظيف | نظيف | **`PASS`** |
| 33 | `/plus` | 200 | يرسم كاملاً · ميتاداتا خاصة | لا حاجة | نظيف | نظيف | **`PASS`** |
| 34 | `/terms` | 200 | يرسم كاملاً · `Terms of Use — Loopz` | لا حاجة | نظيف | نظيف | **`PASS`** |
| 35 | `/privacy` | 200 | يرسم كاملاً · ميتاداتا ثابتة | لا حاجة | نظيف | نظيف | **`PASS`** |
| 36 | `/search` | 200 | إطار البحث يرسم · `/api/search` مفتوح للزائر ويعمل | `E-SHELL` للنتائج | نظيف | نظيف | `PASS` (الإطار) · محتوى `BLOCKED_BY_SURFACE` |
| 37 | `/trailers` | 200 | التبويبات الخمسة ترسم · **«For You» صفر بطاقة وصفر نصّ** | **حالة فارغة مفقودة** | نظيف | نظيف | **`FAIL`** — `LOOPZ-AUD-0021` |
| 38 | `/trailers?tab=trending` | 200 | عنصر `<video>` + 42 صورة · `muted=true` · `autoplay=false` | — | نظيف | نظيف | `PASS` (البنية) · التشغيل `BLOCKED_BY_SURFACE` |
| 39 | `/discover/movies` | 200 | `E-SHELL` — الجسم المخدوم ترويسة فقط | — | نظيف | نظيف | `BLOCKED_BY_SURFACE` |
| 40 | `/discover/<قسم مجهول>` | 200 | **جسم مطابق حرفياً للقسم الصحيح** (`identical: true`) | لا 404 | نظيف | نظيف | **`FAIL`** — `LOOPZ-AUD-0022` |
| 41 | `/movie/[id]` صحيح (550) | 200 | 216 ك.ب · **عنوان عام لا «Fight Club»** · `E-SHELL` | 14 هيكلاً باقية | نظيف | نظيف | **`FAIL`** — `LOOPZ-AUD-0018` |
| 42 | `/movie/[id]` غير موجود | **200** | «Couldn't load this movie right now. Please try again shortly.» | **خطأ مؤقّت بدل 404** | نظيف | نظيف | **`FAIL`** — `LOOPZ-AUD-0016` |
| 43 | `/show/[id]` (1399) | 200 | 228 ك.ب · **بلا ميتاداتا** · `E-SHELL` | — | نظيف | نظيف | **`FAIL`** — `LOOPZ-AUD-0018` |
| 44 | `/person/[id]` (287) | 200 | 560 ك.ب · **ميتاداتا صحيحة** (`title: person.name`) ✅ | — | نظيف | نظيف | **`PASS`** |
| 45 | `/talk/[type]/[id]` | 200 | يرسم · ميتاداتا موجودة | `E-SHELL` للمحتوى | نظيف | نظيف | `PASS` (الوصول) |
| 46 | `/u/[username]` | 200 | يرسم · **بلا ميتاداتا** | `E-SHELL` | نظيف | نظيف | **`FAIL`** — `LOOPZ-AUD-0018` |
| 47 | `/u/[username]/stats` | 200 | **ميتاداتا صحيحة** بينما أمّه لا | `E-SHELL` | نظيف | نظيف | `PASS` (الميتاداتا) |
| 48 | `/news` | 200 | يرسم للزائر | `E-SHELL` | نظيف | نظيف | `PASS` (الوصول) |
| 49 | `/people` | 200 | يرسم للزائر | `E-SHELL` | نظيف | نظيف | `PASS` (الوصول) |

**الصفحات الديناميكية غير القابلة للوصول كزائر** — `/lists/[id]` و`/post/[key]` و`/review/[type]/[id]/[user]`: تُرجع 200 وتملك ميتاداتا، **ولم أختبرها بمعرّف حقيقي** لأن معرّفاتها تخصّ محتوى مستخدمين، وجلبها من صفحات عامة كان سيدخل بيانات أشخاص في التقرير. **`BLOCKED_BY_TEST_ENV`** — تحتاج محتوى اختبار مخصّصاً.

**المجموع: 49 صفّاً، صفر مسار بلا حالة.**

---

## 11. الدليل البصري — **`BLOCKED`، وهذه ثلاثة أدلة لا رأي واحد**

طلبتَ سطحاً مرئيّاً نظيفاً أو Playwright/Chromium بسياق جديد. **جرّبت المسارات الثلاثة كلها. لا واحد منها متاح، ولكلٍّ سبب موثَّق بخطئه الحرفيّ.**

| # | السطح | ما جرى | الخطأ الحرفيّ |
|---|---|---|---|
| 1 | **Playwright/Chromium في حاويتي السحابية** | Chromium مثبَّت (`/opt/pw-browsers`) وplaywright موجود، والسكربت يعمل — **لكن شبكة الحاوية تمرّ بوكيل يحجب الويب العام** | `net::ERR_TUNNEL_CONNECTION_FAILED` — **و`https://example.com` يفشل بالخطأ نفسه**، أي أن الحجب عامّ لا خاصّ بـ`loopztv.com` |
| 2 | **لوح المتصفّح المدمّج** | يتصفّح ويقرأ الـDOM ويحاكي المقاسات بدقّة — **ولا يلتقط لقطات، وتقول الصفحة عن نفسها إنها مخفيّة** | `screenshot failed: UnknownVizError` (ثلاث محاولات) · و**`document.visibilityState === "hidden"`** رغم أن الأداة تقول «اللوح معروض» |
| 3 | **Chrome عند أحمد** | **مسجَّل دخوله في `loopztv.com`** — وأنت منعت استخدام جلسته أو تسجيل خروجه أو تكليفه بالاختبار | كوكيز `sb-uvgmvrdrxzpudoldjxaa-auth-token.0/.1` حاضرة |

### وما الذي يُفسده `visibilityState: "hidden"` تحديداً

ليس تفصيلاً تجميلياً — **هو يُبطل صنفاً كاملاً من الاختبارات بحكم التصميم لا بحكم السوء:**

| ما يُبطَل | لماذا | الدليل المرصود |
|---|---|---|
| التشغيل التلقائي للفيديو | سياسة المتصفّح توقف الوسائط في التبويب المخفيّ | `<video>` بقي `paused:true` · `readyState:0` · `currentTime` **لم يتقدّم إطلاقاً** خلال 3 ثوانٍ (`advanced: 0`) · `videoWidth/Height = 0` |
| `IntersectionObserver` | لا تقاطع في نافذة لا ترسم | ولذلك «التمرير يشغّل الحالي ويوقف السابق» **غير قابل للقياس** |
| اكتمال رسم الصفحات الديناميكية | الأقسام المؤجَّلة لا تُشغَّل | `/movie/550` و`/discover/*`: **14 هيكلاً باقية و`mainTextLen = 0`** بعد 4.5 ثانية عند 768 و390 معاً |
| قياس التباين على نطاق واسع | `offsetParent` يعود `null` بلا تخطيط | من 120 عنصراً مرشَّحاً **قِيس واحد فقط** |

### وما استطعت قياسه رغم ذلك — لأنه لا يعتمد على الرؤية

- **التباين الأساسي:** نصّ الجسم `rgb(247,247,247)` على خلفية `rgb(5,5,5)` ← **نسبة 19.02:1**، وحدّ AA هو 4.5:1. ✅ **الأساس ممتاز.** أما تباين العناصر الثانوية فيحتاج تخطيطاً.
- **المقاس اللوحي 768×1024:** `/movie/550` **لا فيض أفقي** (`768 == 768`). أي أن فيض `LOOPZ-AUD-0020` **مقصور على 390** ولا يظهر عند 768 — وهذا يضيّق الرقعة ويفيد الإصلاح.
- **العناصر الثابتة/اللاصقة:** خمسة عناصر `fixed`/`sticky` (`chrome-top` · `chrome-bottom` · طبقة عائمة عند `bottom-[84px]`)، **وارتفاعاتها كلها صفر في هذا السياق** فلا يمكن فحص التصادم. `BLOCKED_BY_SURFACE`.

### ما يبقى محجوباً بعد كل هذا

`LOOPZ-AUD-0020` (إثبات الفيض بعد اكتمال الرسم) · **بنود الترايلر الستّة** · حفظ موضع التمرير · إظهار/إخفاء التنقّل عند التمرير · تصادم العناصر الثابتة · Safe Area · أهداف اللمس بصريّاً · تركيز لوحة المفاتيح المرئي · zoom 200% · Reduced Motion · تباين العناصر الثانوية · **وأي لقطة شاشة.**

### والقرار قرارك — وهذه الخيارات الثلاثة كما أراها

1. **إتاحة سطح مرئيّ حقيقي** (لوح يلتقط لقطات ويُبلغ `visibilityState: "visible"`، أو منفذ شبكة لحاويتي). **هذا وحده يغلق البنود كلها.**
2. **قبول Phase 2 على ما أُنجز** — والبنود البصرية والسلوكية تُرحَّل إلى Phase 3 (التصميم) وPhase 4 (الأداء) حيث ستحتاج السطح المرئي نفسه على أي حال.
3. **اعتماد ما لا يحتاج رؤية فقط** كمعيار Phase 2، وفصل «الاختبار البصري» في مرحلة مستقلة بأدواتها.

**أرجّح (2):** البنود المحجوبة كلها بصرية أو سلوكية، وهي جوهر Phase 3 وPhase 4 لا Phase 2. وتكرار الحجب نفسه ثلاث مرات في ثلاث مراحل خسارة بلا مقابل.

**ولا أقترح (وقد منعتَه صراحةً):** استخدام جلسة أحمد، أو تسجيل خروجه، أو تكليفه بتشغيل الاختبار.

---

## 12. ما لم يكتمل من معيارك — بصراحة وبلا تجميل

| بندك | الحالة | السبب |
|---|---|---|
| 1 — مصفوفة 49 صفّاً بالحقول | ✅ **أُنجز** — §10 | — |
| 2 — المقاسات الثلاثة | ⚠️ **جزئي**: 390 و768 و1440 قيست **بنيويّاً** على الصفحات التي تكتمل. الصفحات الديناميكية لا تكتمل الرسم | §11 |
| 3 — الترايلرات | ❌ **محجوب** عدا الكتم الافتراضي (مثبَت) | `visibilityState: hidden` يوقف الوسائط — بالدليل الرقمي في §11 |
| 4 — التنقّل والحالة | ⚠️ **جزئي**: 404 ✅ · حدود الخطأ الجذرية موثّقة ✅ · Back/forward وحفظ التمرير محجوبان | §11 |
| 5 — المحتوى والهوية | ⚠️ **جزئي**: `LOOPZ-AUD-0017` (عربيّ في صفحة إنجليزية) أكبر عطل محتوى وُجد · مراجعة المصطلحات والنصوص المقطوعة تحتاج رؤية | §11 |
| 6 — الوصول البصري | ⚠️ **جزئي**: التباين الأساسي **19.02:1** ✅ · بنية الوصول سليمة ✅ · التركيز والتكبير والحركة محجوبة | §11 |
| 7 — دليل بصري | ❌ **محجوب** — ثلاثة أسطح، ثلاثة أسباب موثّقة | §11 |

**لن أسمّي محجوباً ناجحاً، ولا سأقدّم قياساً من سياق يُبطله.** ما أُنجز مُثبَت، وما لم يُنجز مكتوب بسببه ورقمه.

---

## 13. إقرار

- ✅ لم أسجّل دخولاً، ولم ألمس جلسة أحمد، ولم أُدخل أي بيانات اعتماد.
- ✅ صفر POST/PUT/PATCH/DELETE · صفر استدعاء لمسار كاتب · صفر كتابة في قاعدة البيانات.
- ✅ لم أعدّل كود التطبيق ولم ألمس `main` ولم أدمج ولم أنشر ولم أحذف ولم أنشئ أي مورد.
- ✅ الكتابة الوحيدة خارج التوثيق: **كوكي `locale` محلّي في متصفّحي** — وهو ما أذنتَ به صراحةً («لا كتابة خارج المتصفح المحلي»).
- ✅ لا بيانات شخصية ولا أسرار ولا Tokens في هذا التقرير. المعرّفات المستخدمة كلها كتالوج عام (TMDB 550 · 1399 · 287).
- ✅ كل رقم هنا مقيس، وكل ما لم يُقَس مكتوب `BLOCKED` أو `UNKNOWN` بسببه.

**الأرقام اعتُمدت منك:** `C-01`→`LOOPZ-AUD-0014` · `C-02`→`0015` · `C-03`→`0016` · `C-04`→`0017` · `C-05`→`0018` · `C-06`→`0019` · `C-07`→`0020` · `C-08`→`0021` · `C-09`→`0022`. **والمطلوب الآن:** قرارك في §11 — أيّ الخيارات الثلاثة للدليل البصري.

**متوقّف عند البوابة. لن أبدأ Phase 3 قبل ردّك.**
