# Phase 3 — Design, Identity & Content Audit

- الحالة: `READY_FOR_REVIEW` — **إعادة تسليم بعد `CHANGES_REQUESTED`** · المنفّذ: Claude · المراجع: ChatGPT
- **Audited SHA:** `f8a2b33cd036cffd1e7a0b9bc3e5ced0e19b8bfa`
- المنهج: **تحليل مصدر + DOM/HTTP آمن**. لم أُعِد محاولة الأسطح المرئية التي ثبت تعذّرها (Phase 2 §11).
- **صفر إصلاح كود · صفر حذف · صفر كتابة في قاعدة البيانات · صفر Deploy · صفر مورد · لم يُمسّ `main` · لم تُستخدم جلسة أحمد.**

> **قاعدة الفصل في هذا التقرير:** كل بند مقسوم إلى **المرصود** (حقيقة بأمرها أو ملفها وسطرها) و**الاستنتاج** (حكمي) و**التوصية**. وحيث يحتاج الحكم رؤيةً فعلية فهو **`PENDING_VISUAL`** مربوط ببند من Visual Verification Gate — **ولا يُسمّى `PASS` أبداً**.

---

## 0. ثلاثة ادّعاءات فحصتُها فسقطت — قبل النتائج

أسجّلها أولاً لأن **الادّعاء الذي لم يُفحص أخطر من العطل الذي لم يُكتشف**، وقد كدت أرفع ثلاثة بلاغات كاذبة في هذه المرحلة وحدها.

| الادّعاء الذي بدا صحيحاً | لماذا سقط |
|---|---|
| «اسم العلامة القديم مسرَّب: **90 إشارة إلى `مشاهد` في `src`**» | **كلها الاسم العربي الشائع** (مشاهدة · أكمل المشاهدة · وقت المشاهدة). **صفر إشارة إلى العلامة القديمة.** والفحص بالكلمة المجرّدة كان سيولّد بلاغاً كاذباً بحجم كبير |
| «**الإشعارات المنبثقة (toasts) غير مقروءة لقارئ الشاشة**: `aria-live` مرّة واحدة في كل المستودع» | **`ToastHost.tsx:47` يضع `role={tone==='error' ? 'alert' : 'status'}`** — وكلاهما منطقة حيّة ضمنياً. **الوصول سليم، والعدّ بـ`aria-live` وحده كان مضلِّلاً** |
| «**315 `<button>` مقابل 15 ملفاً يستورد مصنع الأزرار** → تجاوز واسع للنظام» | القياس الدقيق: **أربعة أسطر فقط** في المستودع كلّه يعيد فيها زرٌّ بناء توقيع المصنع بيده، وثلاثة منها رقائق/مجزّأة لها مصنعها في `controls.ts`. **الانضباط أعلى بكثير مما أوحى الرقم الخام** |

---

## 1. نظام التصميم

### 1.1 المرصود

| العنصر | الحقيقة | المصدر |
|---|---|---|
| رموز CSS | **75 متغيّراً** في `:root` | `src/app/globals.css` (808 سطراً) |
| مصنع الأزرار | **واحد**: `buttonClass()` + `<Button>` · 5 صيغ (`primary` `inverse` `surface` `ghost` `danger`) · 4 مقاسات (`xs` `sm` `md` `lg`) | `src/components/ui/Button.tsx` |
| حالات الزرّ | `active:scale-[0.98]` · `disabled:opacity-60` · `disabled:pointer-events-none` · `hover:brightness-110` — **في `BASE` لا في المنادي** | نفس الملف |
| عائلتا التحكّم | **مجزّأة** (`segmentedTrack` · `segmentedItem`) و**رقاقة** (`chipClass` · `chipOutline`) — كما تقتضي القاعدة | `src/components/ui/controls.ts` |
| الأوراق والنوافذ | **تنفيذ واحد**: `Sheet.tsx` (479 سطراً · 7 تصديرات). والوحيد الآخر الذي يحمل `role="dialog"` هو `TourGuide.tsx` وهو حالة خاصة | `src/components/ui/Sheet.tsx` |
| مضيف الإشعارات | **واحد**: `ToastHost.tsx` + `src/lib/toast.ts` بحدث `loopz:toast` | — |
| مجموعة الأيقونات | **واحدة**: `Icon.tsx` (596 سطراً). و`<svg>` خارجها في **10 ملفات فقط**، والمفحوص منها مبرَّر: شعار Loopz · حرف Google · دوّار السحب · علامات التقييم | — |
| الثيمات | **سبعة**، منها `daylight` النهاري | `src/lib/themes.ts` |
| Safe areas | **39 استعمالاً** لـ`env(safe-area-inset-*)` | — |
| الأرقام | **137 استعمالاً** لـ`tabular-nums` · `Intl.DateTimeFormat` ×29 · `Intl.DisplayNames` ×8 · `Intl.Collator` ×3 | — |

### 1.2 🔴 `D-01` — سلّمان متوازيان للزوايا، والقاعدة تقول سلّم واحد · **P2**

**المرصود** — `src/app/globals.css`، عشرة رموز بخمس قيم فقط:

| القيمة | الرموز التي تشير إليها |
|---:|---|
| **14px** | `--radius-card` · `--radius-2xl` · `--radius-3xl` |
| **12px** | `--radius-poster` · `--radius-xl` |
| **10px** | `--radius-control` · `--radius-lg` · `--radius-md` |
| 8px | `--radius-sm` |
| 22px | `--radius-sheet` |

**الاستنتاج:** يتعايش **سلّمان**: دلاليّ (`card` · `control` · `poster` · `sheet`) وقياسيّ (`sm`…`3xl`). و`00_Project_Rules` يقول: سلّم زوايا **واحد**، و«نسخة ثانية من أيٍّ منها عطل». وأثره العملي ليس جمالياً: **`--radius-2xl` و`--radius-3xl` تعدان بتدرّج لا وجود له** — كلاهما 14px مثل `card` — فمن يكتب `rounded-3xl` يظنّ أنه يكبّر الزاوية **ولا يتغيّر شيء**. وهذا يولّد نسخاً متباعدة عند أول تعديل.

**التوصية:** الإبقاء على الدلاليّ وحده (`card` `control` `poster` `sheet`) وإلغاء القياسيّ، أو العكس — لا الاثنان. **معيار القبول:** كل قيمة زاوية في `src` تعود إلى رمز واحد لا مرادف له.

### 1.3 🔴 `D-02` — والسلّم نفسه مكرَّر في المقاسات النصّية · **P2**

**المرصود:**

| القيمة | الرموز |
|---:|---|
| 12px | `--text-12` · `--text-xs` |
| 14px | `--text-14` · `--text-sm` |
| 20px | `--text-20` · `--text-xl` |
| 24px | `--text-24` · `--text-2xl` |
| بلا مرادف | `--text-15` · `--text-22` · `--text-base` · `--text-lg` · `--text-3xl` |

**واستثناء يستحق نظراً:** كل الرموز `calc(Npx * var(--fs))` **إلا `--text-base` فهو `max(16px, calc(16px * var(--fs)))`**.

**الاستنتاج:** تكرار كتكرار الزوايا. **والاستثناء ليس خطأً بل قرار غير موثّق**: `--text-base` وحده **لا ينزل تحت 16px** مهما صغّر المستخدم الخط. فمن يخفّض `--fs` يجد النصوص كلها تصغر **إلا واحدة** — سلوك يبدو عطلاً وهو على الأرجح حماية مقصودة (16px حدّ منع تكبير iOS التلقائي في الحقول).

**التوصية:** توحيد السلّم، **وتوثيق استثناء `--text-base` في `07_Design_Decisions.md`** — أو تعميمه على كل رمز يُستعمل في حقول الإدخال. **معيار القبول:** رمز واحد لكل قيمة، والاستثناء مكتوب بسببه.

### 1.4 `D-03` — الصور المفقودة بلا معالجة موحّدة · **P3**

**المرصود:** `onError` في **4 مواضع فقط** في 229 مكوّناً، و**صفر** استعمال لـ`placeholder`/`blurDataURL`.

**الاستنتاج:** ملصق TMDB الساقط (رابط ميت أو حجب شبكي) **لا يملك مساراً موحّداً**. والتطبيق يعتمد على `bg-surface-2` تحت الصورة كخلفية — وهو حلّ بصري كافٍ غالباً، لكنه ليس معالجةً صريحة.

**`PENDING_VISUAL` — مرتبط بـ`V-15`.** لا أحكم بلا رؤية.

### 1.5 ما ثبت انضباطه — ويستحق أن يُقال

مصنع أزرار واحد وحالاته في `BASE` لا عند المنادي · ورقة واحدة · مضيف إشعارات واحد · مجموعة أيقونات واحدة · عائلتا تحكّم كما تقتضي القاعدة · و**أربعة أسطر فقط** في المستودع كلّه تتجاوز المصنع. **هذا نظام تصميم محكوم، والخللان أعلاه في طبقة الرموز لا في الالتزام بها.**

---

## 2. البنية المتكررة

### 2.1 المرصود

| العنصر | الحقيقة |
|---|---|
| Layout | **واحد** على الجذر — `src/app/layout.tsx`. **لا layouts متداخلة إطلاقاً** |
| الترويسة | `HeaderShell` · `HomeHeader` · `HeaderTrailing` · `NavTitle` · `NavLinks` · `NavAvatar` · `Navbar` |
| التنقّل السفلي | `BottomNav.tsx` + `ChromeAutoHide.tsx` (إظهار/إخفاء عند التمرير) |
| Safe areas | 39 استعمالاً · و`main` يحمل `pb-[calc(71px+max(0.5rem,env(safe-area-inset-bottom)/2))]` |
| حدود الخطأ | **الجذر وحده**: `error.tsx` · `global-error.tsx` · `not-found.tsx` · `loading.tsx`. **صفر حدّ خطأ محلّي في أي من الـ49 صفحة** |
| `loading.tsx` | 21 مساراً فرعياً |

### 2.2 🔴 `D-04` — لا حدود خطأ محلّية في التطبيق كلّه · **P2**

**المرصود:** جدول §7 يُظهر `error` = `local` في مسار واحد (`/`) و`inherit ← /` في الثمانية والأربعين الباقية. أي أن **حدّ الخطأ الوحيد هو الجذر**.

**الاستنتاج:** أي فشل رسم في قسم واحد — شريط تفاعلي، بطاقة إحصاء، خيط تعليقات — **يُسقط الإطار كلّه** إلى شاشة خطأ الجذر بدل أن يعزل الجزء المعطوب. وفي تطبيق تعتمد صفحاته على TMDB وSupabase معاً، الفشل الجزئي هو **الحالة المتوقّعة لا الاستثناء**.

**ويجتمع مع `LOOPZ-AUD-0014`:** الصفحة المحميّة تُرسل كاملة ثم تُقذف، والصفحة المعطوبة جزئياً تسقط كاملة. **الاثنان عرضان لغياب حدود على مستوى القسم.**

**التوصية:** `error.tsx` على الأقل في العائلات الثقيلة: `/movie/[id]` · `/show/[id]` · `/u/[username]` · `/talk/[type]/[id]` · `/statistics`. **معيار القبول:** فشل قسم واحد يُظهر خطأً محصوراً فيه، وباقي الصفحة يعمل.

### 2.3 `D-05` — انعدام الـlayouts المتداخلة · **P3 · ملاحظة معمارية لا عطل**

**المرصود:** layout واحد للتطبيق كلّه، بينما `/profile/settings/*` أربع عشرة صفحة تتقاسم ترويسة وإطاراً.

**الاستنتاج:** كل صفحة تعيد بناء إطارها. هذا **يعمل** ولا أثر له على المستخدم اليوم، **لكنه يضاعف كلفة أي تغيير في الإطار المشترك**، وهو أحد أسباب أن `loading.tsx` صار محلّياً في 21 موضعاً بدل أن يُورَّث من layout قسم.

**التوصية:** `layout.tsx` لـ`/profile/settings` على الأقل. **ليست عطلاً — دَينٌ يُسدَّد قبل التطبيق لا بعده.**

### 2.4 `PENDING_VISUAL`

تصادم العناصر الثابتة · Safe Area على جهاز حقيقي · النصوص الطويلة والأسماء الطويلة · عرض البوسترات · اتساق المكوّن عبر الصفحات بصرياً → **`V-8` · `V-9` · `V-15`**.

---

## 3. الهوية

### 3.1 المرصود

| العنصر | الحقيقة | المصدر |
|---|---|---|
| الأصفر والأسود | `accent: #FFD400` · `background: #050505` · `foreground: #F7F7F7` | `src/lib/themes.ts` (ثيم `loopz`) |
| رمز `on-accent` | `#050505` — **موجود لأن تباين الأبيض على الأصفر ~1.6 والأسود عليه ~14** (التعليق في الملف نفسه) | نفس الملف |
| تباين النصّ الأساسي | **19.02:1** مقيس حيّاً على Production (الحدّ AA = 4.5) | Phase 2 §6 |
| أصول العلامة | `loopz-mark.png` · `loopz-mark-192.png` · `loopz-wordmark.png` · `loopz-cover.jpg` · `icon-192` · `icon-512` · `icon-maskable-512` · `apple-icon` · `splash/` | `public/` |
| `Every story matters` | **يُرسَم فعلاً** — `LandingHero.tsx:108` عبر `t.footer` | — |
| أسماء قديمة في نصوص المستخدم | **صفر** | §0 |

### 3.2 `Every story matters` — **`PASS`، والقرار موثّق**

العبارة كانت تحت كل شاشة ثم **حُذفت بأمر أحمد** (D-437)، **وبقيت في مكان واحد يُقرأ فيه**: صفحة الهبوط تحت زرّ الدخول. والتعليق في `layout.tsx:293` يوثّق السبب والثمن: «شعارٌ يُعاد تحت كلِّ شاشةٍ يُقرأ مرّةً ثم لا يُقرأ، وثمنُه ٩٠px من ذيل كلِّ صفحةٍ على الجوّال».

**هذا ليس نقصاً بل قرار مكتوب بسببه وثمنه.** سجّلته لأن معيارك يسأل عن العبارة، والجواب: **موجودة، مرّة واحدة، عن عمد.**

### 3.3 `D-06` — اسم العلامة القديم باقٍ في مُعرّف داخلي · **P3**

**المرصود:** `src/app/globals.css:539` و`:741` — `@keyframes meshahed-pulse` وإحالتها.

**الاستنتاج:** **غير مرئي للمستخدم إطلاقاً** — اسم رمز CSS. لكنه آخر أثر للاسم القديم في الكود، ومعيارك يسأل عن «منع ظهور أسماء أو شعارات قديمة».

**التوصية:** إعادة تسمية إلى `loopz-pulse` في تنظيف Phase 7. **P3، وأصرّح بأنه تجميليّ.**

> وللإنصاف: بقيّة إشارات `meshahed` في `src` (خمس) كلها **تعليقات تشرح النطاق القديم `meshahed.vercel.app`** الذي ما يزال يخدم عمداً — أي **توثيق لقرار، لا بقايا علامة**.

### 3.4 `PENDING_VISUAL`

شارات Plus/Partner/Verified بصرياً · App Icon وFavicon وOG وSplash كما تُرى · تناسق الأصفر عبر الثيمات السبعة → **`V-13` · `V-15`**.

---

## 4. المحتوى

### 4.1 🟢 طبقة الترجمة — نتيجة قوية بالقياس

| المقياس | النتيجة |
|---|---|
| مفاتيح القاموس العربي | **1278** |
| مفاتيح القاموس الإنجليزي | **1278** |
| **مفاتيح ناقصة في أيٍّ منهما** | **صفر** |
| قيم متطابقة بين اللغتين | 12 — **وكلها أسماء علم مشروعة**: `Loopz` · `Every story matters` · `Trakt` · `Letterboxd` · `Simkl` · `TV Time` |
| نصّ عربي داخل القاموس الإنجليزي | **واحد**: `arabicLang = "العربية"` — **وهو الصواب**: مبدّل اللغة يعرض كل لغة باسمها |

**الاستنتاج:** **تكافؤ تامّ 1278/1278 وصفر تسريب ترجمة.** هذا مستوى نادر في تطبيق بهذا الحجم.

**وهذا يضع `LOOPZ-AUD-0017` في نصابه الصحيح:** الزرّ العربي في صفحة `/login` الإنجليزية **لا يأتي من قاموس التطبيق** — يأتي من **إطار Google GIS** الذي لا يُمرَّر إليه `locale`. أي أن **الخلل عند حدّ طرف ثالث، لا في طبقة الترجمة**. وهذا يغيّر مكان الإصلاح: تمرير `hl` إلى GIS، لا مراجعة القاموس.

### 4.2 🟢 نسب المصادر والشروط والخصوصية — `PASS`

**المرصود:** `terms/page.tsx:64-65` ينسب لـTMDB بالعربية والإنجليزية معاً («لا يملكها ولا يدّعي ملكيتها، ودقّتها ليست مضمونةً منّا»). و`privacy/page.tsx:73-74` يفصّل كل معالج بيانات — Supabase · Vercel · Google · TMDB · Speed Insights — **ويصرّح: «لا توجد أي أداة تتبّعٍ إعلانية في Loopz»**. و`:91-92` يوضّح أن ملف الاستيراد **يُقرأ داخل المتصفّح ولا يُرفع**، وأن ما يغادر الجهاز أسماء الأعمال فقط.

**الاستنتاج:** نسب المصادر والإفصاح **مكتملان ودقيقان وبلغتين**. وهذه مادة جاهزة لإفصاحات App Store وGoogle Play في Phase 9.

### 4.3 التاريخ والوقت والأرقام

**المرصود:** `Intl.DateTimeFormat` ×29 · `Intl.DisplayNames` ×8 · `Intl.Collator` ×3 · `tabular-nums` ×137 · وعمود `profiles.timezone` وملفّ `src/lib/zone.ts`.

**الاستنتاج:** التنسيق يمرّ بـ`Intl` لا بسلاسل مكتوبة بيد، والأرقام مُثبّتة العرض. **بنية سليمة.** وصحّة النواتج المعروضة نفسها **`PENDING_VISUAL`** — لا أحكم على تنسيقٍ لم أره.

### 4.4 `PENDING_VISUAL`

المصطلحات وسلاسة الصياغة · النصوص المقطوعة · وضوح أسماء الأزرار · الحالات الفارغة بصرياً → **`V-15`**.
وواحدة **`FAIL` معروفة سلفاً:** `LOOPZ-AUD-0021` — تبويب «For You» فارغ للزائر بلا حالة فارغة.

---

## 5. الجولات التعريفية

### 5.1 المرصود

| العنصر | الحقيقة | المصدر |
|---|---|---|
| عدد الجولات | **اثنتان**: `basics` (6 خطوات) و`details` (8 خطوات) | `src/lib/tour.ts:57` |
| هدف الخطوة | **مسار صفحة (`path`)** لا عنصر في الصفحة | `TourStep` |
| مسارات `basics` | `/` → `/search` → `/library` → `/news` → `/people` → `/profile/settings` | — |
| مسارات `details` | `/` → `/library` → `/news` → `/lists` → `/calendar` → `/statistics` → `/messages` → `/profile/settings/billing` | — |
| أدوات التحكّم | `tourSkip` («تخطّي الجولة» / «Skip tour») · `tourNext` («التالي» / «Next») · `tourFinish` («إنهاء» / «Done») | `i18n.ts:118-121` و`2277-2280` |
| **«رجوع»** | **غير موجود** | — |
| حفظ الإكمال | `persistTourState` → `localStorage` **و**`profiles.ui_state` عبر `updateUiState` | `tour.ts:110-125` |
| منع التكرار | `TOUR_VERSION = 2` — من أنهى القديمة يُعرض عليه الجديد **مرّة واحدة** | `tour.ts:24` |
| الوصول | `role="dialog"` **بلا `aria-modal`** · `aria-label={step.title}` · تركيز على البطاقة | `TourGuide.tsx:21,58,82` |

### 5.2 🔴 `D-07` — لا زرّ «رجوع» في الجولة · **P3**

**المرصود:** ثلاث أدوات فقط: تخطّي · التالي · إنهاء. ولا مفتاح `tourBack` في القاموس ولا عنصر في `TourGuide.tsx`.

**الاستنتاج:** في جولة من **ثماني خطوات** (`details`)، من فاتته خطوة **لا يملك إلا التخطّي والبدء من جديد**. ومعيارك يسأل عن Skip/Next/**Back**/Finish صراحةً — وثلاثة من أربعة موجودة.

**التوصية:** `tourBack` معطَّل في الخطوة الأولى. **معيار القبول:** الرجوع يعيد الخطوة السابقة ويحافظ على الحالة، ولا يظهر في الخطوة الأولى.

### 5.3 `D-08` — الجولة تشير إلى صفحات لا إلى عناصر · **P3 · ملاحظة تصميم**

**المرصود:** `TourStep` يحمل `path` فقط، بلا محدِّد عنصر.

**الاستنتاج:** الجولة **رحلة بين صفحات** لا إشارة إلى أزرار. ومعيارك يسأل عن «صحة العناصر المشار إليها» — والجواب: **لا عناصر مشار إليها أصلاً.** وهذا اختيار مشروع (وأمتن: لا ينكسر عند تغيير الواجهة)، لكنه يعني أن الجولة **تصف ولا تُري**.

**التوصية:** لا تغيير — **بل توثيق القرار في `07_Design_Decisions.md`** كي لا يُقرأ لاحقاً كنقص.

### 5.4 `D-09` — نافذة الجولة لا تحبس التركيز · **P3**

**المرصود:** `role="dialog"` بلا `aria-modal`، والتعليق في `TourGuide.tsx:21` يقرّ بذلك صراحةً: «(لا يحبس شيئاً)».

**الاستنتاج:** **قرار موثّق لا سهو.** وثمنه: مستخدم لوحة المفاتيح أو قارئ الشاشة قد يخرج بـTab إلى محتوى الصفحة خلف الجولة. ولأن الجولة **غير حاجزة** بطبيعتها فقد يكون هذا مقصوداً تماماً.

**`PENDING_VISUAL` — `V-10`.** لا يُحسم بلا اختبار تركيز حقيقي.

### 5.5 `BLOCKED_BY_TEST_ENV`

تشغيل الجولة لمستخدم جديد · حفظ الإكمال · عدم التكرار · إعادة التشغيل من الإعدادات · فرق المجاني وPlus (`details` تنتهي عند `/profile/settings/billing`) — **كلها تبدأ بعد تسجيل الدخول.**

---

## 6. الوصول الثابت

### 6.1 المرصود

| السمة | العدد في `src` |
|---|---:|
| `aria-label` | **221** |
| `aria-hidden` | 199 |
| `alt=` | 111 |
| `role=` | 77 |
| `aria-pressed` | 47 |
| `aria-expanded` | 15 |
| `aria-labelledby` | 11 |
| `prefers-reduced-motion` | **11** |
| `focus-visible` | 11 |
| `aria-current` | 10 |
| `sr-only` | 10 |
| `tabIndex` | 10 |
| `aria-live` | 1 |
| `aria-describedby` | **0** |

وعلى Production حيّاً (Phase 2 §6): `/features` — عنوان `h1` واحد · **صفر قفزة في ترتيب العناوين** · **صفر صورة بلا `alt`** · **صفر زرّ أو رابط بلا اسم متاح** · رابط تخطّي إلى `#main` · معلَم `<main>`.

### 6.2 الاستنتاج

**بنية وصول مبنيّة بعناية لا بالصدفة.** و`aria-live: 1` **ليس نقصاً** — الإشعارات تستعمل `role="alert"`/`role="status"` وهما منطقتان حيّتان ضمنياً (§0).

**و`aria-describedby: 0` ليس عطلاً بذاته**، لكنه يعني أن الحقول التي لها نصّ مساعد أو رسالة خطأ **لا تربطها برمجياً بالحقل**. قارئ الشاشة يقرأ الاسم ولا يقرأ الشرح.

### 6.3 `D-10` — لا ربط بين الحقول ونصوصها المساعدة · **P3**

**المرصود:** `aria-describedby` = صفر في 453 ملفاً.
**التوصية:** ربط رسائل التحقّق والنصوص المساعدة في نماذج الإعدادات والملف الشخصي. **معيار القبول:** كل حقل له نصّ مساعد أو خطأ يربطه بـ`aria-describedby`.

### 6.4 `PENDING_VISUAL`

تركيز لوحة المفاتيح المرئي (`V-10`) · تكبير النصّ 200% (`V-11`) · Reduced Motion فعلياً (`V-12`) · أهداف اللمس (`V-9`) · تباين العناصر الثانوية (`V-13`) · عدم الاعتماد على اللون وحده (`V-15`).

---

## 7. تغطية الأسطح الـ49

**القاعدة:** كل مسار ممثَّل — مباشرةً أو بمرجع مكوّن مشترك واضح. **لا مسار مُسقَط.**

| المجموعة | المسارات | التمثيل في هذا التقرير |
|---|---|---|
| **الإطار المشترك — يشمل الـ49 كلها** | جميع المسارات | `layout.tsx` (§2.1) · `HeaderShell`/`Navbar`/`BottomNav`/`ChromeAutoHide` (§2.1) · `globals.css` 75 رمزاً (§1.1) · `Icon.tsx` · `ToastHost` · `error`/`not-found`/`loading` الجذرية (§2.2) · قاموس 1278 مفتاحاً (§4.1) |
| صفحة الهبوط (1) | `/` | §3.2 (`Every story matters` · `LandingHero:108`) · §2.2 (حدّ خطأ محلّي — **الوحيدة**) |
| المحميّة (25) | `/activity` `/calendar` `/library` `/lists` `/messages` `/profile` `/profile/edit` `/profile/settings` + 12 ابناً `/ratings` `/reports` `/statistics` `/stats` `/welcome` | §2.2 (بلا حدّ خطأ محلّي) · §2.3 (بلا layout قسم — يخصّ `/profile/settings/*`) · §5.5 (`BLOCKED_BY_TEST_ENV`) |
| `/diary` | `/diary` | §2.2 · وPhase 2 §10.1: إعادة توجيه لا صفحة |
| الإدارية (3) | `/admin/links` `/admin/partners` `/admin/verify` | §2.2 · Phase 2 `LOOPZ-AUD-0015` |
| التسويق والقانون (4) | `/features` `/plus` `/terms` `/privacy` | §4.2 (نسب TMDB والخصوصية) · §6.1 (خطّ أساس الوصول المقيس) |
| الدخول (1) | `/login` | §4.1 (`0017` عند حدّ GIS لا في القاموس) |
| الأعمال والأشخاص (3) | `/movie/[id]` `/show/[id]` `/person/[id]` | §1.4 (الصور المفقودة) · §2.2 (حدّ الخطأ) · Phase 2 `0016` `0018` `0020` |
| الاستكشاف والبحث والترايلرات (3) | `/discover/[section]` `/search` `/trailers` | §2.2 · §4.4 (`0021`) · Phase 2 `0022` `0023` |
| الاجتماعي والمحتوى (7) | `/news` `/people` `/u/[username]` `/u/[username]/stats` `/talk/[type]/[id]` `/lists/[id]` `/post/[key]` | §1.1 (عائلتا التحكّم) · §2.2 · Phase 2 `0018` · `BLOCKED_BY_TEST_ENV` للثلاثة الأخيرة |
| المراجعات (1) | `/review/[type]/[id]/[user]` | §2.2 · `BLOCKED_BY_TEST_ENV` |
| **المجموع** | **49** | **صفر مُسقَط** |

**والجولات تعبر أحد عشر مساراً فريداً** (`basics` 6 خطوات · `details` 8 · واتحادهما 11): `/` `/search` `/library` `/news` `/people` `/profile/settings` `/lists` `/calendar` `/statistics` `/messages` `/profile/settings/billing` (§5.1).

---

## 8. جدول الحالات

| البند | الحالة |
|---|---|
| مصنع أزرار واحد وحالاته مركزية | ✅ `PASS` |
| ورقة واحدة · مضيف إشعارات واحد · أيقونات واحدة · عائلتا تحكّم | ✅ `PASS` |
| تكافؤ القاموسين 1278/1278 بصفر نقص | ✅ `PASS` |
| نسب TMDB والشروط والخصوصية بلغتين | ✅ `PASS` |
| `Every story matters` — مرّة واحدة بقرار موثّق | ✅ `PASS` |
| صفر اسم علامة قديم في نصّ مستخدم | ✅ `PASS` |
| بنية الوصول الثابتة (221 `aria-label` · صفر صورة بلا `alt` على المقيس) | ✅ `PASS` |
| تنسيق التاريخ والأرقام عبر `Intl` و`tabular-nums` | ✅ `PASS` |
| `D-01` سلّم زوايا مزدوج | ❌ `FAIL` · **P2** |
| `D-02` سلّم مقاسات نصّية مزدوج + استثناء غير موثّق | ❌ `FAIL` · **P2** |
| `D-04` لا حدود خطأ محلّية | ❌ `FAIL` · **P2** |
| `D-06` `meshahed-pulse` في CSS | ❌ `FAIL` · P3 |
| `D-07` لا «رجوع» في الجولة | ❌ `FAIL` · P3 |
| `D-10` `aria-describedby` صفر | ❌ `FAIL` · P3 |
| `D-03` معالجة الصور المفقودة | ⏳ `PENDING_VISUAL` — `V-15` |
| `D-05` لا layouts متداخلة | ⚠️ دَين معماري · P3 |
| `D-08` الجولة تشير لصفحات لا عناصر | ⚠️ قرار يحتاج توثيقاً · P3 |
| `D-09` الجولة لا تحبس التركيز | ⏳ `PENDING_VISUAL` — `V-10` |
| التركيز · التكبير · الحركة · اللمس · التباين الثانوي · النصوص المقطوعة | ⏳ `PENDING_VISUAL` |
| الجولات فعلياً · فروق Plus · نماذج الإعدادات | 🚫 `BLOCKED_BY_TEST_ENV` |

---

## 9. المرشّحات مرتّبة — بلا أرقام (الترقيم لك)

**P1:** لا مرشّح جديد من Phase 3. **و`LOOPZ-AUD-0023` يبقى P1 مفتوحاً بحالته كما هي.**

**P2:** `D-01` سلّم الزوايا المزدوج · `D-02` سلّم المقاسات المزدوج + استثناء `--text-base` غير الموثّق · `D-04` غياب حدود الخطأ المحلّية.

**P3:** `D-06` `meshahed-pulse` · `D-07` لا «رجوع» في الجولة · `D-10` `aria-describedby` صفر · `D-03` الصور المفقودة (`PENDING_VISUAL`) · `D-05` دَين الـlayouts · `D-08` توثيق قرار الجولة · `D-09` حبس التركيز (`PENDING_VISUAL`).

---

## 10. ترحيل صريح — بلا تخفيف

**`LOOPZ-AUD-0023` — P1 — تشغيل الترايلر معطوب.** يُرحَّل بحالته ودرجته ودليله **دون أي تخفيف**. ولم تجد Phase 3 ما يناقضه أو يخفّفه.

**Visual Verification Gate — خمسة عشر بنداً، مانع للـGO.** ترحّل كما هي: `V-1` و`V-2` **`FAIL` مؤكَّدان**، والثلاثة عشر الباقية `PENDING_VISUAL`. **وأضافت Phase 3 إليها بنوداً جديدة** ربطتها بالبنود القائمة: `D-03` → `V-15` · `D-09` → `V-10`.

**`LOOPZ-AUD-0014`…`0022`** تبقى مفتوحة بحالاتها. و**`0017` تتحدّد أدقّ**: الخلل عند حدّ GIS لا في قاموس التطبيق (§4.1) — وهذا **ينقل مكان الإصلاح، لا يخفّض درجته**.

---

---

# إعادة التسليم — استكمال ما لم يُفحص

**اعتراضك الجوهري صحيح:** «المشكلة ليست غياب لقطات؛ أجزاء واسعة قابلة للفحص من المصدر ولم تُفحص». اختبأت خلف `PENDING_VISUAL` حيث كان المصدر يكفي. الأقسام أدناه تُصلح ذلك، **ولا تعيد ما اجتاز**.

**وأقرّ بخطأ ثانٍ ضبطتَه:** `SHARED_PROGRESS.md` كان يحمل `01:18 UTC` بينما التسليم `03:50`. السبب أنّ استبدالاً نصّياً لم يطابق فمرّ صامتاً، **ولم أتحقّق من نتيجته**. صُحّح، **وصار التحقّق من أثر كل تعديل خطوةً لا اختياراً**.

---

## 12. جدول تدقيق الرموز والمكوّنات — استكمال §1

### 12.1 سلّم المسافات والشبكة والمحاذاة

| البند | المرصود | الحالة |
|---|---|---|
| رموز مسافات في `globals.css` | **صفر** (`--space-*` · `--gap-*` · `--grid-*` غير موجودة) | ملاحظة لا عطل — المسافات من سلّم Tailwind الافتراضي |
| السلّم المستعمل فعلياً | `3` (466) · `2` (422) · `1` (295) · `4` (264) · `1.5` (230) · `2.5` (159) · `5` (133) · `0.5` (123) · `3.5` (91) · `6` (71) · `8` (47) · `10` (34) · `16` (28) | ✅ **سلّم متّسق** — درجات Tailwind القياسية بلا اختراع |
| مسافات خام `p-[..]` `m-[..]` `gap-[..]` | **53** | ⚠️ منخفض مقابل آلاف الاستعمالات — مقبول |
| الشبكة | `posterGrid` مركزيّ في `controls.ts:161` | ✅ مصدر واحد |
| المحاذاة والاتجاه | `ps-`/`pe-`/`start-`/`end-` منطقية لا `left/right` — و`dir="auto"` في `ToastHost:52` | ✅ RTL/LTR بالبناء لا بالاستثناء |

### 12.2 الخطوط والأوزان وأحجام الأيقونات

| البند | المرصود | الحالة |
|---|---|---|
| الخطوط | Cairo · Tajawal · Poppins عبر `@fontsource` + `public/fonts` · ورمزان `--font-sans` و`--fs-ui`/`--fs-content` | ✅ |
| الأوزان المستعملة | `bold` 309 · `semibold` 148 · `medium` 28 · `extrabold` 24 · `normal` 12 · `black` 3 | ⚠️ **ستّة أوزان** — و`extrabold` (24) و`black` (3) استعمالهما نادر جداً |
| أحجام الأيقونات | 18 (80) · 16 (64) · 14 (46) · 15 (29) · 13 (29) · 20 (27) · 44 (16) · 26 (15) · 12 (15) · 40 (10) | ⚠️ **عشرة أحجام على الأقل، منها 13 و15 و26** — درجات وسيطة بلا سلّم |

**`D-12` — سلّم الأيقونات غير معرَّف · P3.** المرصود: عشرة أحجام دون رمز أو ثابت يجمعها، و`13` و`15` و`26` درجات وسيطة تُكتب بيدٍ عند المنادي. **الاستنتاج:** لا يوجد سلّم أيقونات — وهو نفس النمط الذي وُجد مصنع الأزرار ليقتله. **التوصية:** ثابت `ICON_SIZES` بأربع أو خمس درجات. **معيار القبول:** كل `size` في `<Icon>` من مجموعة مسمّاة.

**`D-13` — ستّة أوزان خطّية بلا سلّم موثّق · P3.** `extrabold` و`black` معاً 27 استعمالاً مقابل 457 لـ`bold`/`semibold`. **التوصية:** تثبيت ثلاثة أوزان (normal · semibold · bold) وتوثيق أي استثناء.

### 12.3 الظلال والشفافية

| البند | المرصود | الحالة |
|---|---|---|
| ظلال قياسية | `shadow-2xl` 8 · `lg` 5 · `xl` 2 · `sm` 1 — **16 استعمالاً فقط** | ✅ الظلّ نادر عمداً — يتّسق مع «العمق بفارق الإضاءة لا بالظلال» (تعليق `themes.ts:69`) |
| ظلال خام `shadow-[..]` | **11** | ⚠️ قريب من عدد القياسية — انظر `D-14` |
| الشفافية | `opacity-50` 29 · `70` 27 · `60` 24 · `80` 23 · `0` 10 · `40` 9 · `100` 7 · `45` 5 | ⚠️ **`opacity-45` درجة وحيدة شاذّة** عن سلّم الخمسات/العشرات |

**`D-14` — الظلال الخام تكاد تعادل القياسية · P3.** 11 خام مقابل 16 قياسياً. **التوصية:** رمزان أو ثلاثة (`--shadow-card` · `--shadow-sheet` · `--shadow-pop`).

### 12.4 الكروت والحقول وحالات التحميل

| البند | المرصود | الحالة |
|---|---|---|
| الكروت | `--radius-card` + `bg-surface`/`surface-2` + `border-border` — نمط متكرر بلا مكوّن `Card` مركزي | ⚠️ **`D-15` · P3** |
| الحقول | **57 `<input>`/`<textarea>` في 30 ملفاً** · بلا مكوّن `Input` مركزي · و`<label htmlFor>` مستعمل (مثال `EditProfileForm:468`) | ⚠️ **`D-15`** |
| حالة التحميل | `.skeleton` — **100 استعمال** وتعريف واحد في `globals.css` | ✅ **مصدر واحد** |
| `aria-busy` | **1** فقط رغم 100 هيكل | ⚠️ جزء من `D-10` |
| زرّ في حالة تحميل | `disabled:opacity-60` + `disabled:pointer-events-none` في `BASE` | ✅ مركزيّ |

**`D-15` — لا مكوّن مركزي للكرت ولا للحقل · P3.** المرصود: مصنع أزرار وورقة وأيقونات ورقائق ومجزّأة كلها مركزية، **والكرت والحقل وحدهما بلا مصنع** — 57 حقلاً في 30 ملفاً يكرّر كلٌّ منها أصنافه. **الاستنتاج:** هذا **أوسع باب لتباعد النسخ** في نظام منضبط بغير ذلك. **معيار القبول:** `<Card>` و`<Field>` مركزيان، وصفر تكرار لأصناف الحقل عند المنادي.

### 12.5 hover / pressed / disabled خارج مصنع الأزرار

| المرصود | العدد |
|---|---:|
| `hover:` خارج `Button.tsx` | كثيف ومتّسق (`hover:border-accent` · `hover:brightness-*` · `hover:text-foreground`) |
| `active:scale-*` | نمط الضغط نفسه المستعمل في `BASE` |
| `disabled:` | مركزيّ في `BASE`، ومكرَّر يدوياً في الرقائق عبر `controls.ts` |
| `focus-visible` | **11** |

**الحالة:** ✅ الحالات متّسقة نمطاً، **و`focus-visible` = 11 هو الرقم المقلق** — يُحسم بصرياً في `V-10`.

### 12.6 القيم الخام التي تتجاوز الرموز — العدّ والمسارات

| النوع | العدد | الحكم |
|---|---:|---|
| `text-[..]` | 185 | ⚠️ الأعلى — يتجاوز سلّم `--text-*` |
| `w-[..]` `h-[..]` `min/max` | 185 | مقبول (أبعاد بطاقات وملصقات) |
| **ألوان hex خام في `.tsx`** | **108 في 17 ملفاً** | 🔴 **`D-11`** |
| ألوان عبر الرموز `var(--..)` | **307** | النسبة 3:1 لصالح الرموز |
| `p-[..]` `m-[..]` `gap-[..]` | 53 | مقبول |
| `shadow-[..]` | 11 | `D-14` |
| `rounded-[..]` | 9 | مقبول |

**ملفات الألوان الخام (17):** `stats/FullStatsTabs` · `LandingHero` · `ReportView` · `Logo` · `LandingContent` · `GoogleButton` · `ContinueCard` · `ListDetail` · `PeopleBoard` · `RatingMarks` · `AccountIdentity` · `app/opengraph-image` · `app/api/list-og/[id]/route` · `app/global-error` · `app/layout` · `lib/og` · `lib/shareCard`.

**واستثناءات مشروعة منها — أُثبتت لا افتُرضت:** `opengraph-image.tsx` و`api/list-og/[id]/route.tsx` و`lib/og.tsx` و`lib/shareCard.tsx` تعمل داخل `ImageResponse`، **وهو محرّك لا يقرأ متغيّرات CSS إطلاقاً** فالقيمة الخام فيه ضرورة. و`global-error.tsx` يُرسم **خارج غلاف التطبيق** فلا Tailwind ولا رموز. و`GoogleButton` يحمل ألوان شعار Google الرسمية.

**فالفائض الحقيقي المرشَّح للتوحيد:** `FullStatsTabs` · `LandingHero` · `LandingContent` · `ReportView` · `ContinueCard` · `ListDetail` · `PeopleBoard` · `RatingMarks` · `AccountIdentity` · `Logo`.

### 12.7 🔴 `D-11` — الأصفر مرقومان، والقرار الذي منع ذلك مخروق في ثيمه نفسه · **P2**

**المرصود — بالسطر:**

| المصدر | القيمة |
|---|---|
| `themes.ts:72` — تعليق الهوية | «الهوية الرسمية: أصفر **`#FFD200`** على أسود **`#0D0D0D`** (كتيّب العلامة)» |
| `themes.ts:99` — ثيم `loopz` | `accent: "#FFD400"` |
| `themes.ts:110` | `verified: "#FFD400"` |
| `themes.ts:114` — ثيم `loopz` | `brand: ["#FFD200", "#FBBF24", "#F59E0B"]` |
| `themes.ts:292` | `DEFAULT_BRAND = ["#FFD400", …]` |
| `themes.ts:289` — نصّ **D-454** | «`--brand-1` هو `--accent` نفسُه في كلِّ تدرّجٍ في التطبيق — **ولو بقي `#FFD200` لجرى في شريط التقدّم لونٌ وفي الأيقونة فوقه لونٌ آخر**» |

**والمقارنة عبر الثيمات السبعة:**

| الثيم | `accent` | `brand[0]` |
|---|---|---|
| **`loopz`** | `#FFD400` | **`#FFD200`** ← **الوحيد المخالف** |
| `amber` `ocean` `violet` `crimson` `forest` `daylight` | كلٌّ لونه | **غير معرّف** → يرث `DEFAULT_BRAND` |

**الاستنتاج:** D-454 اتُّخذ **تحديداً** لمنع «لونٍ في الشريط وآخر في الأيقونة»، **والثيم الافتراضي — الذي كُتب القرار من أجله — هو الوحيد الذي ما زال يخالفه.** الستّة الأخرى تمتثل لأنها لا تعرّف `brand` أصلاً. وأثره ليس نظرياً: **كل مستخدم جديد يفتح Loopz على هذا الثيم بالذات.**

**وأثر ثانٍ مقيس:** `#FFD200` حيّ في `api/list-og/[id]/route.tsx:92` — **فبطاقة مشاركة القائمة تُرسَم بالأصفر القديم بينما التطبيق يرسم الجديد.**

**وأسودان كذلك:** `manifest.ts` يضع `background_color` و`theme_color` = `#0D0D0D` (أسود الكتيّب)، بينما خلفية التطبيق `#050505` (اختير للـOLED، `themes.ts:69`). **فشاشة إقلاع التطبيق المثبَّت تفتح بدرجة وتُسلّم لدرجة أخرى.**

**التوصية:** حسم واحد — إما يُحدَّث الكتيّب إلى `#FFD400`/`#050505`، أو يُرجَع التطبيق. **معيار القبول:** قيمة صفراء واحدة وقيمة سوداء واحدة عبر `themes.ts` و`manifest.ts` وبطاقات OG.

### 12.8 اتساق المكوّنات المشتركة — من المصدر

| المكوّن | التنفيذات | الحالة |
|---|---:|---|
| مصنع الأزرار | 1 · و**4 أسطر** فقط تتجاوزه في المستودع كلّه | ✅ |
| الورقة/النافذة | 1 (`Sheet.tsx`) + `TourGuide` كحالة خاصة | ✅ |
| مضيف الإشعارات | 1 | ✅ |
| الأيقونات | 1 (`Icon.tsx`) · و`<svg>` خارجها في 10 ملفات كلها مبرَّرة | ✅ |
| المجزّأة والرقائق | 1 لكلٍّ في `controls.ts` | ✅ |
| الهيكل (skeleton) | تعريف 1 · 100 استعمال | ✅ |
| **الكرت** | **بلا مصنع** | ⚠️ `D-15` |
| **الحقل** | **بلا مصنع** — 57 في 30 ملفاً | ⚠️ `D-15` |

---

## 13. خريطة الهوية من المصدر — استكمال §3

### 13.1 التوصيل — أين تُستعمل الأصول فعلاً

| الأصل | موصول في | التحقّق |
|---|---|---|
| `icon-192.png` · `icon-512.png` · `icon-maskable-512.png` | `manifest.ts` — بمقاسات وأنواع و`purpose` صريحة (`any` و`maskable`) | ✅ **موصولة بمقاساتها** |
| `apple-icon.png` | `layout.tsx:92` — `sizes: "180x180"` · `type: "image/png"` | ✅ |
| Favicon | `layout.tsx:90` كتلة `icons` | ✅ |
| OG وTwitter | `layout.tsx:94` و`:103` + `metadataBase: new URL("https://loopztv.com")` | ✅ **مطلق للكاشطات** |
| صورة OG المولّدة | `app/opengraph-image.tsx` (`ImageResponse`) | ✅ |
| Splash | `public/splash/` + `<meta name="apple-mobile-web-app-capable" content="yes">` يدوياً في `layout.tsx:160` **لأن Next أسقطه من `appleWebApp.capable`** والتعليق يوثّق أن `apple-touch-startup-image` لا يعمل بدونه | ✅ **موصول، وبمعالجة واعية لعطل معروف** |
| `loopz-mark.png` · `loopz-mark-192.png` · `loopz-wordmark.png` | **لا إشارة إليها في `src`** | ⚠️ **`D-16`** |
| `loopz-cover.jpg` | يُستعمل كغلاف افتراضي | — |
| علامة OO | `Logo.tsx` — SVG داخل الكود لا ملف | ✅ |

**`D-16` — ثلاثة أصول علامة غير موصولة · P3.** `loopz-mark.png` · `loopz-mark-192.png` · `loopz-wordmark.png` لا تُستدعى من أي ملف في `src`. **الاستنتاج:** إمّا بقايا، أو تُستعمل خارج الكود (لوحات، متاجر). **لا أحكم بالموت** — بروتوكول الحذف يقتضي إثباتاً ثانياً. **مرشّح Phase 7 لا الآن.**

### 13.2 Plus / Partner / Verified — **`PASS` بدليل المصدر**

**سؤالك الدقيق:** هل تظهر Partner وPlus معاً؟ **الجواب: لا، والمنع مقصود وموثّق.**

| الطبقة | المصدر | السلوك |
|---|---|---|
| المنطق | `plan.ts:55` | `isPartner = asPlan(plan)==="partner" && isPlus(p)` — **الشراكة تشترط Plus بنيوياً** |
| العرض | `AccountIdentity.tsx:254` | `const tier = isPartner ? "partner" : isPlus ? "plus" : null` — **درجة واحدة لا شارتان** |
| التوثيق | `AccountIdentity.tsx:258` | «لأنّ `isPartner` تشترط `isPlus` أصلاً في `plan.ts`» (D-145) |
| الترتيب | `AccountIdentity.tsx:307` | `verified` شارة مستقلة تُرسم إلى جانب الدرجة — **وهذا صحيح**: التوثيق ليس خطّة |
| Founder | `isFounder` منفصل + `FOUNDER_PLUS_UNTIL` | مستقل عن الدرجة |

**والقاعدة مطبَّقة في طبقتين لا واحدة** — المنطق والعرض. `PASS`.

### 13.3 أسماء قديمة خارج `meshahed-pulse`

**المرصود:** فحص `src` و`public` — **صفر اسم علامة قديم في أي نصّ مستخدم أو اسم ملف أصل**. الإشارات الخمس اللاتينية كلها **تعليقات تشرح النطاق القديم `meshahed.vercel.app`** الذي ما يزال يخدم عمداً (`robots.txt/route.ts:6` · `site.ts:4` · `siteOrigin.ts:8` · `ShareListSheet.tsx:61`) — **توثيق قرار لا بقايا علامة**. والوحيد المرشَّح للتنظيف هو `meshahed-pulse` (`LOOPZ-AUD-0027`).

---

## 14. مصفوفة المحتوى — استكمال §4

| السطح | المفاتيح/النصّ | المصدر | الحالة | الملاحظة ومعيار القبول |
|---|---|---|---|---|
| رسائل الخطأ | `errPickImage` · `errTooLarge` («الحد الأقصى ٢ ميجابايت») · `errUpload` · `errSave` · `errSaveShort` · `errViewSave` · `xError` · `errorTitle`/`errorBody`/`errorRetry`/`errorHome` | `i18n.ts:374-428` · `2140-2144` | ✅ `PASS` | **تقول السبب وتقترح فعلاً** — «حاول مجدداً» و«الرجوع للرئيسية» |
| رسائل النجاح | 22 مفتاحاً بـ`saved`/`Saved` · و`blockedToast: "تمّ الحظر"` | — | ✅ `PASS` | قصيرة ومؤكِّدة |
| الحالات الفارغة | **96 مفتاحاً** بـ`empty`/`Empty` · مثال `notifEmpty: "لا إشعارات بعد — حين يتابعك أحدٌ أو يعجبه رأيك سيظهر هنا"` | — | ✅ `PASS` | **تشرح وتَعِد** لا تكتفي بالنفي. **والاستثناء الوحيد المعروف `LOOPZ-AUD-0021`** — تبويب «For You» بلا حالة فارغة |
| حذف الحساب | `deleteAccountTitle` · `deleteAccountDesc` · `deleteAccountBtn: "حذف الحساب نهائياً"` · **`deleteAccountConfirm: "متأكد؟ اضغط مرة أخرى — لا رجوع بعدها"`** · `deleteAccountBusy` | `i18n.ts:1631-1636` · `3335+` | ✅ `PASS` | **تأكيد بضغطتين ونصّ لا يخفي النهائية.** والدالة `delete_my_account` موجودة في القاعدة |
| الإبلاغ | `reportUserOption`/`Title`/`Body` · `reportLabel`/`reportTitle` · **26 مفتاحاً** | `i18n.ts:518-520` · `646-647` | ✅ `PASS` | مسارات إبلاغ لكل نوع محتوى (رأي · ردّ · قائمة · خبر · منشور) |
| الحظر | `blockOption` · `blockConfirmTitle` · `blockConfirmBody` · `blockConfirmButton` · `blockedToast` · `blockedListTitle` · `blockedEmpty` — **18 مفتاحاً** | `i18n.ts:522-529` | ✅ `PASS` | تأكيد + قائمة محظورين + حالة فارغة |
| Spoilers | **8 مفاتيح** | — | ✅ موجود | التطبيق الفعلي `PENDING_VISUAL` |
| الإشعارات | **22 مفتاحاً** — `notifFollow` · `notifRequest` · `notifLike` · `notifReply` · `notifTalkReply` · `notifListReview` … | `i18n.ts:743-773` | ✅ `PASS` | صيغ دالّية بمتغيّرات لا سلاسل مركّبة |
| **البريد** | **لا مفاتيح بريد ولا قوالب** | — | ✅ **متّسق** | لا مرسِل بريد أصلاً (D-462) — **فغياب النصّ صواب لا نقص** |
| وضوح الأزرار | `deleteAccountBtn: "حذف الحساب نهائياً"` · `blockConfirmButton: "احظر"` · `errorRetry: "حاول مجدداً"` | — | ✅ `PASS` | **أفعال صريحة لا «موافق»/«إلغاء»** |
| نسب TMDB | `terms:64-65` بلغتين | — | ✅ `PASS` | §4.2 |
| **نسب الصور والترايلرات** | الملصقات ضمن نصّ TMDB · **والترايلرات لا نصّ نسب خاصاً بها** رغم أنها من **YouTube** و**iTunes** | `terms/page.tsx` | ⚠️ **`D-17` · P3** | انظر أدناه |
| اتساق المصطلحات | تكافؤ 1278/1278 · صفر تسريب · 12 متطابقة كلها أسماء علم | §4.1 | ✅ `PASS` | القصّ والتخطيط `PENDING_VISUAL` |

**`D-17` — الترايلرات بلا نسب مصدر · P3.** **المرصود:** `terms` ينسب لـTMDB «أسماء الأعمال وملصقاتها وأوصافها وتقييماتها»، **ولا يذكر الترايلرات** — وهي تُبَثّ من `youtube-nocookie.com` و`s.ytimg.com` (`next.config.ts` CSP) ومن `video-ssl.itunes.apple.com` (D-758). **الاستنتاج:** محتوى طرف ثالث يُعرَض داخل المنتج بلا نسب ولا إحالة لشروطه. **ويكتسب وزناً في Phase 9:** متجرا التطبيقات يسألان عن مصادر الوسائط صراحةً. **معيار القبول:** فقرة في `/terms` بلغتين تذكر YouTube وApple كمصدرَي معاينات.

---

## 15. الجولات من المصدر — استكمال §5

**تصحيح §7:** العبارة «الجولات تعبر ثمانية من هذه المسارات» **خطأ**. الصواب: `basics` **6 خطوات** و`details` **8 خطوات**، **واتحاد مساراتهما 11 مساراً فريداً**: `/` · `/search` · `/library` · `/news` · `/people` · `/profile/settings` · `/lists` · `/calendar` · `/statistics` · `/messages` · `/profile/settings/billing`. (`/` · `/library` · `/news` مشتركة بين الجولتين.)

| سؤالك | الجواب من المصدر | الحالة |
|---|---|---|
| **هل توجد إعادة تشغيل من الإعدادات، وما مسارها؟** | **نعم.** `settings/HelpTourRows.tsx:40` يبثّ `TOUR_START_EVENT`، و`TourMount.tsx:84` يستمع. المسار: **`/profile/settings/help`** | ✅ `PASS` |
| **هل تتغيّر الجولة حسب اللغة؟** | **نعم** — كل خطوة `title`/`body` دالّة تأخذ `Dict`، فالنصّ يتبع لغة الواجهة | ✅ `PASS` |
| **حسب الثيم؟** | **لا** — لا إشارة للثيم في `tour.ts` ولا في `TourGuide.tsx` | ✅ **صحيح**: الجولة تستعمل رموز الثيم فتتبعه تلقائياً بلا فرع |
| **حسب الشاشة؟** | **لا فرع** — الخطوات نفسها على كل مقاس | ⚠️ **`D-18` · P3** |
| **حسب الخطة Free/Plus؟** | **لا فرع.** و`details` تنتهي عند `/profile/settings/billing` بخطوة `tourPlusTitle` — **تُعرض للمجاني والمشترك سواءً** | ⚠️ **`D-18`** |
| **هل حفظ الإكمال مكتمل محلياً وبعيداً؟** | **نعم، طبقتان:** `persistTourState` (`tour.ts:123-125`) يكتب `localStorage` **ثم** `updateUiState({tour})` → `profiles.ui_state` | ✅ `PASS` |
| **ومعالجة الفشل؟** | `void updateUiState(...).catch(() => {})` — **الفشل البعيد مبتلَع صامتاً والمحلّي يبقى** | ⚠️ **`D-19` · P3** |
| **هل يوجد منطق يمنع التكرار بعد `TOUR_VERSION`؟** | **نعم.** `TOUR_VERSION = 2`، والتعليق (`tour.ts:12-24`) يوضّح أنه يرتفع حين تتغيّر البنية جوهرياً فيُعرض الجديد **مرّة واحدة**. و`TourMount.tsx:63` يكتب `{v: TOUR_VERSION, id:"basics", i:0, s:"suggested"}` | ✅ `PASS` |

**`D-18` — الجولة لا تفرّق بين الشاشات ولا بين الخطط · P3.** **الأثر الملموس:** المستخدم المجاني يُقاد إلى خطوة عن Plus تنتهي بصفحة الفوترة. **قد يكون مقصوداً** (تعريف بالمزايا)، **وقد يُقرأ ترويجاً داخل جولة تعليمية**. **التوصية:** قرار صريح موثّق — لا إصلاح أعمى.

**`D-19` — فشل حفظ الجولة بعيداً مبتلَع · P3.** `.catch(() => {})`. **الأثر:** من أنهى الجولة على جهاز وفشل الحفظ البعيد **يراها من جديد على جهاز آخر**. **والابتلاع مقصود** (ألّا يُسقط فشلُ حفظٍ تجربةَ المستخدم) — **لكن بلا أي محاولة إعادة**. **التوصية:** إعادة محاولة واحدة أو مزامنة عند الإقلاع التالي.

---

## 16. `D-10` — الدليل التطبيقي الذي طلبتَه

**اعتراضك صحيح:** `aria-describedby = 0` وحده لا يثبت وجود حقول لها نصّ مساعد غير مربوط. **وهذه الأمثلة الفعلية:**

| الحقل/السطح | النصّ غير المربوط | المصدر |
|---|---|---|
| إعادة تموضع صورة الغلاف | `<p class="text-12 text-muted">{t.repositionHint}</p>` — **تلميح إرشادي بلا `id` ولا ربط** | `src/components/settings/EditProfileForm.tsx:461` |
| نموذج الملف الشخصي كاملاً | خطأ الحفظ يُعرض في `<Alert>{error}</Alert>` **ويُمرَّر إليه التركيز بالتمرير لا بالربط** (`:146` `scrollIntoView`) — فقارئ الشاشة لا يعرف أيّ حقل يخصّه | `EditProfileForm.tsx:315-317` · `:146` |
| كل الحقول | **`aria-invalid` = صفر** في 57 حقلاً موزّعة على 30 ملفاً | فحص المستودع كله |

**والتسمية سليمة:** `<label htmlFor="ep-name">` مستعمل (`:468`) — فالمشكلة **ليست في الاسم بل في الوصف وحالة الخطأ**.

**فيبقى `D-10` عطلاً مسجَّلاً** بدرجة **P3**، بمعيار قبول: كل حقل له تلميح أو رسالة خطأ يربطه بـ`aria-describedby`، وكل حقل مرفوض يحمل `aria-invalid="true"`.

**وأضيف `aria-busy`:** استعمال واحد مقابل **100** هيكل تحميل — الهياكل تتغيّر بصرياً ولا تُعلن للقارئ. جزء من `D-10` بمعيار القبول نفسه.

---

## 17. إغلاق مسألة مفتوحة من Phase 2 — `LOOPZ-AUD-0009`

سألتَ في Phase 2 (بندك 7): **هل صفحة الإشعارات تُظهر أنها غير مفعّلة، أم تعرض تحكماً يوحي بالحفظ دون تخزين؟** لم أستطع الجواب كزائر لأنها محميّة. **والمصدر يجيب قاطعاً:**

`src/app/profile/settings/notifications/page.tsx` — ثلاثة صفوف **للعرض فقط، بلا أي مفتاح أو مدخل**:

| الصفّ | القيمة المعروضة |
|---|---|
| `setNotifInApp` | **`t.setPlanActive`** — «مفعّل» |
| `setNotifPush` | **`t.settingsSoonShort`** — «قريباً» |
| `setNotifEmail` | **`t.settingsSoonShort`** — «قريباً» |

**النتيجة: لا وهم بالحفظ إطلاقاً.** الصفحة **تصرّح** بأن الدفع والبريد غير مفعّلين، **ولا تعرض تحكّماً واحداً**. و`SettingsRow` صفّ عرض لا إدخال.

**التوصية: إغلاق `LOOPZ-AUD-0009` بوصفه لا-عطل**، مع تسجيل السبب: **الواجهة لا تدّعي ما لا تملك** — وهو نفس النمط الموثّق في D-462.

---

## 18. المرشّحات بعد إعادة التسليم

**المرقَّمة منك:** `0024` (`D-01`) · `0025` (`D-02`) · `0026` (`D-04`) · `0027` (`D-06`) · `0028` (`D-07`).

**جديدة تنتظر الترقيم:**

| المرشّح | الدرجة | الخلاصة |
|---|---|---|
| `D-11` | **P2** | **أصفران وأسودان**: الكتيّب `#FFD200`/`#0D0D0D` والتطبيق `#FFD400`/`#050505` · و**ثيم `loopz` وحده يخالف قراره D-454** (`accent` ≠ `brand[0]`) · و`#FFD200` حيّ في بطاقة OG للقوائم · و`manifest` يفتح بأسود مختلف عن خلفية التطبيق |
| `D-12` | P3 | عشرة أحجام أيقونات بلا سلّم (13 · 15 · 26 وسيطة) |
| `D-13` | P3 | ستّة أوزان خطّية، `extrabold` و`black` نادران |
| `D-14` | P3 | 11 ظلّاً خاماً مقابل 16 قياسياً |
| `D-15` | P3 | **لا مصنع للكرت ولا للحقل** — 57 حقلاً في 30 ملفاً · أوسع باب لتباعد النسخ |
| `D-16` | P3 | ثلاثة أصول علامة غير موصولة — **مرشّح Phase 7 لا حكم بالموت** |
| `D-17` | P3 | **الترايلرات بلا نسب** رغم أنها من YouTube وApple — يكتسب وزناً في Phase 9 |
| `D-18` | P3 | الجولة لا تفرّق بين الشاشات ولا الخطط · المجاني يُقاد إلى صفحة الفوترة |
| `D-19` | P3 | فشل حفظ الجولة بعيداً مبتلَع بلا إعادة محاولة |
| `D-10` | P3 | **مثبَت الآن بأمثلة** — `repositionHint` غير مربوط · `Alert` غير مربوط · `aria-invalid` = 0 · `aria-busy` = 1 مقابل 100 هيكل |

**وإغلاق مقترح:** `LOOPZ-AUD-0009` → **لا-عطل** بدليل §17.

**ولا مرشّح P1 جديد.** `LOOPZ-AUD-0023` يبقى P1 مفتوحاً بحالته، وVisual Verification Gate تبقى مانعة — **بلا أي تخفيف**.


---

## 19. إقرار

- ✅ لم أعدّل أي ملف في `src/` أو `supabase/` ولا أي إعداد تطبيق.
- ✅ لم ألمس `main` ولم أدمج ولم أنشر ولم أحذف ولم أشغّل Migration.
- ✅ لم أنشئ حساباً ولا بيانات ولا أي مورد.
- ✅ لم أستخدم جلسة أحمد ولم أسجّل خروجه ولم أطلب منه اختباراً.
- ✅ لم أُعِد محاولة الأسطح المرئية التي ثبت تعذّرها.
- ✅ لا أسرار ولا Tokens ولا بيانات مستخدمين في هذا التقرير.
- ✅ كل ادّعاء جوهري له ملف وسطر أو أمر قابل للتكرار، وكل ما يحتاج رؤية مكتوب `PENDING_VISUAL` **ولم يُسمَّ `PASS`**.

**متوقّف عند البوابة. لن أبدأ Phase 4 قبل مراجعتك.**
