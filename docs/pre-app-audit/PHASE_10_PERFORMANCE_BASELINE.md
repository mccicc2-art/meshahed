# Phase 10B — Performance P0 Baseline · **Post-D-879** (`main @ 921abd7`)

> **الحالة:** `READY_FOR_REVIEW` (المراجعةُ الثانية — **مُصحَّحٌ بالنقاط الثماني من `CHANGES_REQUESTED` 01:35 UTC**) · **الوقت:** 2026-09-02 · 01:55 UTC · **النسخة المقيسة:** `main @ 921abd7` (`/api/build == 921abd7`) — **وهي بعد شريحة `D-879` (غلافُ الترايلر) لا قبلها**، **ولا يُدَّعى قياسُ pre-fix مفقود.** **الزائرُ (غير مسجَّل)** في كلِّ القياسات — لا حسابَ اختبارٍ بعد (قرارُ المالك).
> **قراءةٌ فقط:** صفر تعديلٍ على الشيفرة أو الإعدادات أو البيانات أثناء القياس.

## ٠. البيئةُ والحدودُ — قبل أيِّ رقم

| البُعد | الحال |
|---|---|
| **بيئةُ القياس** | **Embedded automated Chromium environment** — لوحُ Claude المضمَّن في تطبيق سطح المكتب (Chromium · Windows · 1280×720 CSS px · `1x`) · **بيئةٌ آليّةٌ مضمَّنة، لا جهازُ مستخدمٍ نهائيّ** |
| **Desktop Chrome حقيقيّ** | 🔴 **`BLOCKED_BY_DEVICE`** — لم يُقَس |
| **iPhone PWA / Safari** | 🔴 **`BLOCKED_BY_DEVICE`** — لا جهازَ لديّ · **مطلوبٌ من المالك** (§٧) |
| **Android Chrome** | 🔴 **`BLOCKED_BY_DEVICE`** |
| **ثلاثةُ مقاسات** | 🔴 **`BLOCKED_BY_TOOLING`** — اللوحُ بمقاسٍ واحد ولا يُعاد تحجيمُه من الأداة |
| **شبكةٌ بطيئةٌ محاكية** | 🔴 **`BLOCKED_BY_TOOLING`** — لا throttling في اللوح |
| **Cold launch** (كاشٌ فارغ) | 🔴 **`BLOCKED_BY_TOOLING`** — لا مسحَ كاشٍ من الأداة، **وعاملُ الخدمة يخدم الحزمَ cache-first** — **فكلُّ ما هنا Warm** (`jsCached = 100%`) |
| **LCP · FCP · CLS · INP** | 🔴 **`BLOCKED_BY_TOOLING`** — **اللوحُ يحمّل الصفحةَ مخفيّاً ولا يرسم إلا حين يُعرَض**: قِيس FCP/LCP = **3820 ms** على `/news` وهو **لحظةُ إظهار اللوح لا لحظةَ الرسم** (`DCL = 819 ms` في الطلب نفسه) — **فالرقمُ أداةٌ لا منتَج، ويُرفض.** CLS المقيس = 0 في كلِّ الصفحات (قد يكون لأنّ الرسمَ لم يقع) — **`UNKNOWN`.** |
| **ما يُقاس فعليّاً هنا** | ✅ TTFB · DCL · load · حجمُ HTML · عددُ الطلبات · JS/CSS (decoded) · الطلباتُ الخارجيّة · Tap→أوّلُ تغييرٍ في DOM · التنقّلُ الناعم/الصلب · scroll restoration · الترايلرات (البدء · أوّلُ إطار · الصوت · المشغّلاتُ الحيّة) **بعد إظهار اللوح** |

**والقاعدة:** **رقمٌ لم يُقَس لا يُقدَّر** — الخانةُ إمّا رقمٌ مقيسٌ وإمّا وسمُ حجب.

## ١. الصفحاتُ — الشبكةُ والخادم (زائر · Warm · لوحٌ مخفيٌّ أثناء التحميل)

| المسار | TTFB | DCL | load | HTML (نقل) | الطلبات | JS ملفّات / decoded | CSS decoded | صورٌ حُمِّلت | Supabase / API من العميل | ملاحظة |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|---|
| `/` (الجذر — معرضُ الزائر) | 223 ms | 310 | 310 | **251 KB** | 30 | 18 / 761 KB | 112 KB | 8 | 0 / 0 | خطّان + CSS واحد |
| `/news?tab=shows` — قراءةٌ ١ | 173 | 742 | 747 | 264 KB | 26 | 20 / — | — | 2 | 0 / 0 | |
| `/news?tab=shows` — إعادةُ تحميل | 206 | 822 | 829 | 263 KB | 26 | 20 / — | — | 2 | 0 / 0 | |
| `/news?tab=shows` — إعادةٌ **واللوحُ مرئيّ** | 203 | 819 | 819 | 264 KB | **93** | 20 | — | **56 (202 KB)** | 0 / 0 | **الصورُ تُطلب حين يُرسم وحدَه** — 31 صورةً فوق الطيّة |
| `/people` (المجتمع) | 180 | 831 | 831 | 🔴 **1,069 KB** | 28 | 20 / 774 KB | — | 4 | 0 / 0 | **أكبرُ HTML بأربعة أضعاف الجذر — للزائر** |
| `/search` | 241 | 314 | 314 | 47 KB | 22 | 17 / 697 KB | — | 1 | 0 / 0 | |
| `/show/1396` (عمل) | 176 | 1,111 | 1,529 | 198 KB | 27 | 20 / 751 KB | — | 3 | 0 / 0 | **أبطأُ DCL** |
| `/trailers?tab=trending` | 186 | 346 | 347 | 218 KB | 26 | 18 / 711 KB | — | — | 0 / 0 | **صفرُ iframe ما دام اللوحُ مخفيّاً** ✅ |
| `/library` (زائر) | — | — | — | — | — | — | — | — | — | يحوّل إلى `/login` — لم يُقَس (يلزم حساب) |

**قراءاتٌ من الجدول (لا أحكام):**
- **TTFB 173–241 ms** في كلِّ الصفحات — **دون هدف ≤ 800 ms** بفارقٍ واسع. **`dns_tcp = 0`** (اتّصالٌ مُعاد).
- **حجمُ JS المفكوك ~700–775 KB لكلِّ صفحة (17–20 ملفّاً)** — **كلُّه من كاش عامل الخدمة** (`transfer = 0`)، **فوزنُه يُدفع مرّةً ثمّ لا يعبر** (D-652 مُعاد تأكيدُه).
- 🔴 **`/people` HTML = 1,069 KB للزائر** — **مرشَّحُ P2 Network الأوّل** (يُقاس ماذا يحمله قبل أن يُحكم).
- **`/show` DCL 1.1 s وload 1.5 s** — أثقلُ صفحة تحميلاً أوّليّاً.
- **صفرُ طلبِ Supabase أو `/api` من العميل في كلِّ الصفحات (زائر)** — **الخادمُ يقرأ** (RSC).
- **`/news` بعد الرسم: 93 طلباً منها 56 صورةً (202 KB)** — **الصورُ لا تُطلب قبل الرسم (lazy) ✅** — **و31 صورةً فوق الطيّة عند 1280×720.**

## ٢. التفاعل (لوحٌ مرئيّ)

| القياس | النتيجة | الهدف | الطريقة |
|---|---:|---:|---|
| **Tap → أوّلُ تغييرٍ في DOM** (رقاقة «Movies» في `/trailers`) | **36 ms** | ≤ 100 ms | `MutationObserver` من لحظة `click()` |
| **Tap → تغيّرُ الرابط** (تنقّلٌ ناعم `/trailers?tab=movies`) | **691 ms** | < 300 ms | استطلاعُ `location.href` |
| **رجوعٌ من عمل إلى اكتشف** | **`back_forward` (bfcache)** · **scroll = 876 من 900** | مُستعاد | `history.back()` |
| 🔴 **اكتشف → عمل (نقرُ ملصق)** | **تنقّلٌ صلبٌ — وثيقةٌ جديدة** (`navigation.name = /show/…` · علامةُ `window` ضاعت) | ناعم | **`<Link prefetch={false}>` في `PosterCard`** ومع ذلك وثيقةٌ كاملة — **السببُ غيرُ معزول** (مرشَّحان: mismatch في build id مع HTML من كاش عامل الخدمة · أو سقوطُ Next إلى MPA) — **`UNKNOWN` يُعزل قبل أيِّ إصلاح** |

## ٣. الترايلرات (`/trailers?tab=trending` · زائر · لوحٌ مرئيّ · Post-D-879)

| القياس | النتيجة | الهدف | ملاحظة |
|---|---:|---:|---|
| **مشغّلاتٌ حيّة واللوحُ مخفيّ** | **0 iframe** | 1 | ✅ لا يُنشأ مشغّلٌ قبل الرؤية |
| **iframes بعد الرؤية** | **2** (نشطٌ + احتياطيٌّ) | **1 حيّ والباقي thumbnail/metadata** | 🔴 **فجوةُ تصميم/ذاكرةٍ تحتاج قراراً** (طلبُ المراجع ٧): **إطارُ الاحتياطيّ مشغّلُ يوتيوب كاملٌ محمَّلٌ ومُوقَف** (`standbyStateChange` يوقفه) — **لا metadata فقط** · **يُسجَّل `P10-05` ولا يُوسَم ✅** |
| **`<video>` عناصر** | 1 (`display:none`، لمزوّد الملفّ) | — | لا يعمل في يوتيوب |
| **First video frame** (أوّلُ إطارٍ مرسومٌ من يوتيوب) | 🔴 **`UNKNOWN` / `BLOCKED_BY_TOOLING`** — لا قياسَ للإطار نفسه من الأداة · **لقطةٌ واحدةٌ بعد ~10 s أظهرت مشهداً من الفيديو لا الملصق** (دليلُ وجودٍ لا زمن) | — | (طلبُ المراجع ٣) |
| **زمنُ البدء** (من إظهار اللوح إلى أوّل عدّاد) | **≈ 2 s** (قراءةٌ ١) · **≤ 4.3 s من بدء التحميل** (قراءةٌ ٢، مشتقٌّ من العدّاد `0:12` عند 16.3 s) | — | ⚠️ **مشتقٌّ**: كمونُ الأداة سبق أوّلَ عيّنة |
| **Overlay removal** (نزعُ الغلاف `hidden`) | **≤ 400 ms بعد البدء** (D-879) — `hidden=true · display:none` مقيس | — | **يثبت نزعَ الغلاف لا رسمَ الإطار** (طلبُ المراجع ٣) · **قبل D-879 (على `6559d41`): لم يقع خلال 70 s** |
| **الصوت** | **UI state indicates unmuted; audibility not independently verified** — زرٌّ باسم `Mute` معروضٌ بعد التشغيل الآليّ للزائر | افتراضٌ muted أوّلَ مرّة | ⚠️ **`UNKNOWN`** — مخالفةُ default-muted **مرشَّحٌ يحتاج تحقّقاً سمعيّاً** (طلبُ المراجع ٦) |
| **التقطّع** | `UNKNOWN` | — | لا قياسَ إطاراتٍ من الأداة |
| **فحصُ المراجع** («spinner ثمّ زرُّ تشغيل») | 🔴 **environment-dependent unresolved failure** — أُعيد إنتاجُه من المراجع على Production ولم يُعد إنتاجُه في هذه البيئة (التشغيلُ آليٌّ ناجح) | — | **لا يُلغى بعدم إعادة إنتاجه** (طلبُ المراجع ٥) · **العلاج في حزمة الإصلاح التالية: instrumentation آمنٌ لرموز أخطاء يوتيوب وحالات المشغّل** (`onError.data` · `phase`) يُكتب في `runtime_errors` بلا بياناتِ مستخدم |

## ٤. الذاكرة والمشغّلات
| القياس | النتيجة |
|---|---|
| مشغّلاتُ يوتيوب في الخلفيّة (صفحاتٌ أخرى) | **0** — `/`, `/news`, `/people`, `/search`, `/show`: صفرُ iframe |
| ذاكرةُ الصفحة | `BLOCKED_BY_TOOLING` (لا `performance.memory` موثوق) |

## ٥. مقابلَ ميزانيّة القبول

| المؤشّر | الهدف | Baseline | الحكم |
|---|---:|---:|---|
| LCP | ≤ 2.5 s | — | `BLOCKED_BY_TOOLING` |
| INP | ≤ 200 ms | — | `BLOCKED_BY_TOOLING` (بديلٌ: Tap→DOM 36 ms) |
| CLS | ≤ 0.1 | 0 (غيرُ موثوق) | `UNKNOWN` |
| TTFB | ≤ 800 ms | **173–241 ms** | ✅ داخل الهدف |
| Tap → feedback | ≤ 100 ms | **36 ms** | ✅ (بديل DOM) |
| navigation بعد التجهيز | < 300 ms | **691 ms** (ناعم) · **وثيقةٌ كاملة** (ملصق→عمل) | 🔴 خارج الهدف |
| Prefetch متزامن | ≤ 2 | 0 (`prefetch={false}`) | ✅ |
| مشغّلاتُ Trailer حيّة | 1 | **2 iframes** (1 يعمل + 1 احتياطيٌّ كامل) | 🔴 فجوةٌ تحتاج قراراً (`P10-05`) |

## ٦. المرشَّحاتُ التي تخرج من هذه القاعدة (**بلا أرقام `LOOPZ-AUD` حتّى يعتمدها المراجع** — طلبُه ٨)
| المعرّف | الدرجة المقترَحة | ما قِيس |
|---|---|---|
| **`P10-01`** | P2 Network | `/people` HTML 1,069 KB للزائر |
| **`P10-02`** | P2 Network/React | ملصق → عمل تنقّلٌ صلب رغم `<Link>` — **يُعزل السبب أوّلاً** |
| **`P10-03`** | P3 | التنقّلُ الناعم 691 ms للتبويب |
| **`P10-04`** | `UNKNOWN` | الصوت: واجهةٌ تقول «مفتوح» بعد التشغيل الآليّ — **لم يُتحقَّق سمعيّاً** |
| **`P10-05`** | تصميم/ذاكرة | **إطاران ليوتيوب** (نشطٌ + احتياطيٌّ كاملٌ موقوف) حيث تقول الخطّة «مشغّلٌ حيٌّ واحد والباقي thumbnail/metadata» — **قرارٌ: يبقى الاحتياطيُّ أم يُستبدل بـmetadata** |
| **`P10-06`** | وظيفيّ · `0023` | **environment-dependent unresolved failure**: spinner ثمّ زرُّ تشغيل عند المراجع — **instrumentation في الحزمة التالية** |

## ٧. المطلوبُ من المالك — خطواتٌ قصيرة على iPhone
1. افتح `loopztv.com/trailers?tab=trending` في Safari (زائراً أو مسجَّلاً).
2. عدّ بصوتٍ عالٍ من لحظة الظهور حتى **تتحرّك الصورة** — واكتب الرقم.
3. هل ظهر **الملصق فوق الفيديو** بعد أن بدأ العدّاد؟ (نعم/لا)
4. اضغط الفيديو مرّتين: هل توقّف؟ وثبّت إصبعك يميناً ثانيتين: هل ظهر «2×»؟
5. أرسل لقطةً واحدةً لكلِّ خطوة.

**صفر تعديلٍ على الشيفرة أو Production أثناء هذه المرحلة · صفر سرّ · صفر بياناتِ أعضاء.**
