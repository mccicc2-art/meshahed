/**
 * 🆕 **عتبةُ الأصوات لكلِّ صنف — موضعٌ واحدٌ للحقيقة** (D-385).
 *
 * **ملفٌّ صغيرٌ بلا تبعيّات عمداً**: يقرؤه `topChart` على الخادم
 * **و`universes` التي تُستورَد في مكوّناتِ عميل** (`curatedName` في
 * الجرس) — **فلو سكن الرقمُ في `topChart` لسحب `data.ts` ومعه عميلُ
 * Supabase إلى حزمة المتصفّح.** **والورقةُ لا تحمل شجرة.**
 *
 * ⚖️ **والأنمي عشرةُ آلافٍ وحدَه** — نقضٌ مسجَّلٌ لتطبيق D-323/D-365
 * عليه، **بموافقة أحمد على البابِ الثاني من D-382**، **والقاعدةُ الأمُّ
 * باقية**: العتبةُ تُقاس بالبِركة التي تقف عليها (D-378).
 * **والمقيس**: `≥20k = 136` · `≥15k = 165` · **`≥10k = 188`** · `≥5k = 206`.
 *
 * ⚠️ **ونظيرُه في جسم `build_imdb_chart`** (الهجرة ١١٥) — **رقمان في
 * موضعين لأن أحدهما في القاعدة والآخر في الشيفرة**، **ومن غيّر أحدَهما
 * غيّر الآخر في الدفعة نفسِها.**
 */
export const MIN_CHART_VOTES = { movie: 25_000, tv: 20_000, anime: 10_000 } as const;

export function minChartVotes(kind: "movie" | "tv" | "anime"): number {
  return MIN_CHART_VOTES[kind];
}
