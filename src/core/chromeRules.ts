/**
 * قواعدُ «كسوة» التطبيق حسب المسار — مصدرٌ واحدٌ للحقيقة.
 *
 * (جولة ١٩ أغسطس ليلاً، طلبُ أحمد: «اجعل التحكم في السلوك مركزيًا بدل
 * توزيع الشروط داخل كل صفحة».)
 *
 * كان قرارُ «هل يعمل السحبُ للتحديث هنا؟» شرطاً داخل `PullToRefresh`
 * وقرارُ «هل تظهر الترويسة؟» شرطاً داخل `HeaderShell` وثالثاً داخل
 * `BottomNav` — **وثلاثةُ أماكن لسؤالٍ واحد تفترق يومَ يُعدَّل أحدُها
 * ويُنسى الباقي.** هذا الملفُّ يجيب وحدَه، والمكوّناتُ تسأل.
 *
 * **ولماذا قوائمُ سماحٍ لا قوائمُ منع (في السحب):** صفحةٌ جديدةٌ تُنشأ
 * غداً يجب أن تُولد بلا سحبٍ للتحديث حتى يُقرَّر لها — **والافتراضُ
 * الآمن هو الأقلُّ سلوكاً.**
 */

/**
 * **السحبُ للتحديث في سطحٍ واحدٍ فقط** — المجتمعُ `/people`:
 * خلاصةٌ اجتماعيّةٌ تتجدّد بفعل الآخرين، والسحبُ سؤالُ «ماذا استجدّ؟»
 * بمعناه الحرفيّ.
 *
 * ⚖️ 🆕 **واكتشفُ خرج منه** (D-680، بلاغُ أحمد بلقطةِ الدوّارة فوق
 * تبويباته: «إلغي التحديث من ديسكفري بالكامل — كل ما أبغى ألفّ يمين
 * ويسار بشكل مائل يطلع لي») — **نقضٌ لنصف طلبه الأوّل («احصره في
 * Community وDiscover») بيده وبعد التجربة**: تبويباتُ اكتشف تُقلَب
 * بالسحب الأفقيّ، **وإصبعٌ يميل قليلاً وهو يقلب تبويباً يوقظ سحبَ
 * التحديث** — **وإصبعٌ واحدٌ لا يملك نيّتين** (D-440). والمجتمعُ
 * تبويباتُه تُسحب أيضاً، **لكنّ صاحبَ الحكم أبقاه** — يُراجَع ببلاغٍ
 * مثلِه إن أزعج.
 *
 * **وكلُّ ما عداه بلا سحبٍ** — الرئيسيةُ والمكتبةُ واكتشفُ تتجدّد عند
 * كلِّ تأشيرةٍ وعودةٍ للتبويب، وصفحاتُ التفاصيل ثابتةُ المحتوى،
 * والإعداداتُ نماذجُ لا خلاصات.
 */
export function allowsPullToRefresh(pathname: string): boolean {
  return pathname === "/people";
}

/**
 * **الترويسةُ والشريطُ السفليُّ يختبئان مع النزول ويعودان مع الرجوع** —
 * إلا حيث يكون الثباتُ هو الصواب:
 *
 * - **الإعداداتُ وكلُّ فروعها**: رحلةٌ لها بدايةٌ ونهاية، وترويستُها
 *   تحمل زرَّ الرجوع — **وإخفاءُ باب الخروج عن ساحبٍ يقرأ نموذجاً
 *   طويلاً حبسٌ لا ترتيب.**
 * - `/login` و`/welcome`: شاشتان قصيرتان مركّزتان بلا كسوةٍ أصلاً.
 *
 * **والفروعُ تدخل تلقائيّاً** (`startsWith`) — مسارُ إعداداتٍ يُنشأ
 * غداً يرث الاستثناءَ بلا سطرٍ جديد.
 */
export function allowsAutoHideChrome(pathname: string): boolean {
  return !(
    pathname === "/profile/edit" ||
    pathname === "/login" ||
    pathname === "/welcome" ||
    pathname.startsWith("/profile/settings")
  );
}

/**
 * 🆕 **أين يُخفى شريطُ التطبيق العلويّ** (D-493) — لأن للصفحة ترويستَها
 * الداخليّة برجوعٍ واسمٍ وفعل. **وترويستان في شاشةٍ واحدة تجعلان سهمَي
 * رجوعٍ وعنوانين**، ولا يعرف القارئُ أيَّهما يخرج به (حجّةُ D-462).
 *
 * ⚠️ **والشريطُ السفليُّ لا يتبع هذه القاعدة**: الإعداداتُ رحلةٌ تُغلق
 * فيُحذف (D-462)، **و`/stats` وجهةٌ داخل المكتبة** — يبقى الدوك فيها
 * ليعود منها القارئُ إلى حيث كان.
 */
/**
 * 🆕 **وشريطُ الرئيسية على الجوّال وحدَه** (D-536، طلبُ أحمد بلقطتين:
 * «رجّع التصميم إلى الهوم»).
 *
 * ⚖️ **وهذا نقضٌ مسجَّلٌ لـD-502** — **وحجّتُه كانت صحيحةً يوم كُتبت**:
 * شريطان علويّان يجعلان الظرفَ يقفز والعلامةَ تتبدّل في كلِّ تنقّل.
 * **والذي تبدّل أنّ أحمد رأى الشكلين معاً واختار** — **وقرارُ المالك في
 * الشكل يعلو استنتاجَ الاتّساق** (D-475: الارتدادُ رخيص، وغيرُ المكتوب
 * غالٍ). **والثمنُ يُقال ولا يُخبَّأ**: الرمزُ في كلِّ صفحةٍ ووردماركٌ في
 * الرئيسية، **والأيقونةُ الأخيرة صورةٌ هناك وترسٌ هنا.**
 *
 * ⚠️ **وعلى الجوّال وحدَه**: الشريطُ الواسع يحمل الروابطَ والبحث،
 * **وإخفاؤه على اللوح يقطع التنقّلَ كلَّه** — فالقاعدةُ تُطبَّق
 * بـ`hidden md:contents` لا بـ`return null` (وهو حرفُ D-122).
 */
export function hidesAppHeaderOnMobile(pathname: string): boolean {
  return pathname === "/";
}

export function hidesAppHeader(pathname: string): boolean {
  return (
    pathname === "/profile/edit" ||
    pathname === "/stats" ||
    /* 🔴 🆕 **وابنتا `/stats`** (D-804، لقطتا أحمد المحوَّطتان): «صلّح
       المساحة العلوية». **«تقريرك» و«الإحصائيات الكاملة» تُفتحان من
       `/stats` ولهما ترويستُهما** (رجوعٌ · اسمٌ · فعل) — **وشريطُ
       التطبيق فوقهما ترويستان وسهما رجوعٍ وعنوانان** (حجّةُ
       D-493/D-462 بحرفها).
       📏 **والثمنُ كان مقيساً في الشاشة**: `SettingsHeader` تحجز
       `--safe-top` **وشريطُ التطبيق يحجزه أيضاً** — **فالفجوةُ فوق
       الاسم كانت ضِعفَ ما تحتاجه**، **و«الفراغُ» الذي حوّطه ليس تنضيداً
       بل بابٌ نُسي مفتوحاً.** */
    pathname === "/reports" ||
    pathname === "/statistics" ||
    /* 🆕 **وتقويمُ أعمالك** (D-828) — **الحجّةُ نفسُها**: تُفتح من
       عنوان شريط الأسبوع ولها ترويستُها (رجوعٌ · اسمٌ · مِقودُ شهر)،
       **وشريطُ التطبيق فوقها ترويستان وسهما رجوعٍ وعنوانان.** */
    pathname === "/calendar" ||
    /* 🆕 **والبحثُ صفحةٌ كاملة تُفتح فوق التطبيق** (D-534، تصميمُ أحمد):
       ترويستُها رجوعٌ واسم (`SettingsHeader`)، **وشريطُ التطبيق يحمل حقلَ
       بحثٍ ثانياً وجرساً وصورة** — **حقلا بحثٍ في شاشةِ بحثٍ واحدة هو
       العطلُ بعينه** (القاعدة ٦، وحجّةُ D-462/D-493 بحرفها). */
    pathname === "/search" ||
    /* 🆕 **وصفحةُ الملفّ العامّ** (D-643، حكمُ أحمد: «إذا دخلت على حساب
       شخص أخفِ الهيدر اللي فوق والدوك»): **هي صفحةُ تفصيلٍ تُفتح فوق
       التطبيق لا وجهةٌ فيه** — **ولها ترويستُها**: غلافٌ يملأ العرض،
       ورجوعٌ ومشاركةٌ ونقاطٌ في زاويتيه. **وشريطُ التطبيق فوق ذلك يضع
       صورةً ثانيةً للحساب فوق صورةِ صاحب الصفحة** — **وجهان لشخصين في
       شريطٍ واحد.** (وهي حجّةُ D-493/D-534 نفسُها: **ترويستان في شاشةٍ
       واحدةٍ سهما رجوعٍ وعنوانان.**) */
    pathname.startsWith("/u/") ||
    /* 🆕 **وصفحةُ القائمة** (D-681، لقطتا أحمد: «خلي كل شي مطابق ١٠٠٪»)
       — **الحجّةُ نفسُها**: صفحةُ تفصيلٍ لها ترويستُها (رجوعٌ · شعارٌ ·
       مشاركةٌ ونقاط)، **وشريطُ التطبيق فوقها ترويستان.**
       ⚠️ **و`/lists` المجرّدةُ ليست منها** — وجهةٌ في التطبيق لا صفحةُ
       تفصيل (البادئةُ بشرطةٍ تفصل الاثنتين). */
    pathname.startsWith("/lists/") ||
    pathname.startsWith("/profile/settings")
  );
}
