/**
 * 🔴 🆕 **جسرٌ رابعٌ مكتوبٌ بيد — حين تسقط الثلاثة** (D-431).
 *
 * ================= لماذا وُجد هذا الملفّ =================
 *
 * **قال أحمد: «كلّهم يعرفونه، أنت الي ما عرفت تسحب بياناته».** **وكان
 * محقّاً، وقِيس:** «في ال لا لا لاند» **موجودٌ في IMDb** باسم
 * `Fi Al La La Land` (2017 · 6.6 · 886 صوتاً · `tt8903114`).
 *
 * **وجسورُنا الثلاثة سقطت كلُّها، وكلُّ واحدٍ لسببه:**
 * 1. `external_ids.imdb_id` — **TMDB لا تحمله لهذا العمل** (قِيس).
 * 2. البحثُ بالاسم في OMDb (D-414) — **TMDB تكتبه `Fi El` وIMDb تكتبه
 *    `Fi Al`**، **وحرفٌ واحدٌ يمنع المطابقة.**
 * 3. `alternative_titles` (D-430) — **قِيس على TMDB نفسِها: صفر عناوين
 *    بديلة.** **والجسرُ سليمٌ ويخدم غيرَه، لكنه لا يعبر فوق لا شيء.**
 *
 * ================= ولماذا خريطةٌ لا محاولةٌ رابعة =================
 *
 * **لأن الثلاثةَ كانت تخميناً، وهذا يقين.** **ومحاولةٌ رابعةٌ ذكيّةٌ
 * تخمّن أذكى، وتخطئ يومَ يختلف حرفان لا حرف.**
 *
 * **والقاموسُ المكتوبُ بيدٍ عرفُ هذا المستودع** (`awards.ts` ·
 * `famousLists.ts` · `universes.ts` · `EDITORIAL` في D-427): **حين يكون
 * المصدرُ حكماً بشريّاً، يُكتب حكمُ إنسانٍ ولا يُصطنع.**
 *
 * ⚠️ **وهو أوّلُ ما يُسأل لا آخرُه**: **صفٌّ مكتوبٌ بيدٍ أوثقُ من ثلاث
 * رحلاتِ شبكة، وأرخصُ منها** — **فالسطرُ الذي نعرفه لا يُدفع ثمنُ
 * البحث عنه.**
 *
 * ⚠️ **وثمنُه معلَن**: **يتقادم إن غيّر IMDb معرّفاً** (وهي لا تفعل)،
 * **وينمو سطراً سطراً لا وحدَه** — **فمن وجد عملاً بلا تقييمٍ يعرفه
 * IMDb، سطرُه هنا.**
 *
 * **المفتاح `"<tv|movie>:<tmdbId>"`**, والقيمةُ `tt` بأرقامها.
 */
export const IMDB_OVERRIDES: Record<string, string> = {
  /* في ال لا لا لاند — TMDB: `Fi El La La Land` · IMDb: `Fi Al La La Land` */
  "tv:72337": "tt8903114",
};

/** **الغيابُ يعني «اسأل الجسورَ الثلاثة» لا خطأً** (D-063) */
export function imdbOverride(mediaType: "tv" | "movie", tmdbId: number): string | null {
  const v = IMDB_OVERRIDES[`${mediaType}:${tmdbId}`];
  return v && /^tt\d+$/.test(v) ? v : null;
}
