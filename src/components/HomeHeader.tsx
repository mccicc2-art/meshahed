import Link from "next/link";
import Image from "next/image";
import { getDict, type Locale } from "@/lib/i18n";
import { Icon, type IconName } from "./Icon";
import { Avatar } from "./Avatar";
import { HomeGreeting } from "./HomeGreeting";
import { HomeViewSwitch } from "./HomeViewSwitch";
import { HeaderTrailing } from "./HeaderTrailing";
import { LogoWordmark } from "./Logo";
/**
 * 🆕 **خانةُ بطاقة الأرقام — تسكن مع راسمها** (D-497): كانت تُستورد
 * نوعاً من `ProfileHeader`، **وتلك بلا قارئٍ منذ D-438 وحكمُها الحذف**
 * (D-214) — **والنوعُ يُنقل أوّلاً ثم يُحذف الملفّ.**
 */
/* ===== 🆕 وصفةُ الغلاف — خطّان رسمهما أحمد (D-553) =====

   **بلاغُه بلقطةٍ عليها خطّان**: «الأحمر هو نهاية الغلاف، والأزرق هو
   بداية الظلام».

   🔴 **وكنتُ فهمتُها مقلوبةً في D-540→D-551**: بنيتُ **حجاباً أسودَ
   يبدأ من رأس الشاشة بـ٩٦٪ ويخفّ نازلاً** — **أي أنّ الظلامَ يبدأ في
   الأعلى وينتهي في الأسفل** — **وهو عكسُ ما رسمه بالضبط.** **ورسمُه
   أوضحُ من كلِّ ما كتبتُه**: الصورةُ تُرى كاملةً غيرَ مظلَّمة، **ثمّ
   عند الخطّ الأزرق تبدأ تُظلم**، **وعند الأحمر تكون قد انتهت.**

   **والقياسُ يعطي الخطّين رقمين** (آيفون، `--safe-top: 47`):
   الأزرق ≈ **١٧٨px** · الأحمر ≈ **٢٠١px** — **وبطاقةُ الأرقام
   ١٧٣→٢٢٥ ومنتصفُها ١٩٩**. **فالأحمرُ منتصفُ البطاقة**، وهو
   `calc(var(--safe-top) + 9.5rem)` بالضبط في الشاشتين (٠+١٥٢ و٤٧+١٥٢).

   🔑 **وقناعٌ واحدٌ يفعل الاثنين ولا حجابَ أصلاً**: **خلفيّةُ الصفحة
   سوداء** — **فتلاشي الصورة فوق الأسود هو الإظلامُ نفسُه.** **لا
   طبقتان تتنازعان، ولا حجابٌ يظلم ما لم يطلب أحدٌ إظلامَه.**

   ⚠️ **والثمنُ يُقال ولا يُخبَّأ**: **لم يبقَ فوق الصورة حجابٌ يحمي
   الأبيض** — أيقوناتُ الشريط والوردمارك وسطرُ الترحيب **بيضاءُ على
   فنٍّ مجهولِ الألوان** (تحذيرُ D-501). **غلافٌ فاتحٌ سيبتلعها.**
   **والعلاجُ سطرٌ واحد** (حجابٌ خفيفٌ خلف الشريط وحدَه) **يُكتب متى
   رآها أحمد تختفي — لا قبل.**

   **٨٩٪ من ١٩٩ = ١٧٧٫١** — الأزرقُ، **مشتقٌّ من قياسه لا مذوق.** */
/** **القناعُ**: الصورةُ تامّةٌ إلى الأزرق، ثمّ تذوب إلى العدم عند الأحمر */
const COVER_FADE = "linear-gradient(180deg, #000 0%, #000 89%, transparent 100%)";

export interface HeaderStat {
  key: string;
  icon: IconName;
  value: string;
  label: string;
  href?: string;
  /** لون الأيقونة — ثابت لا يتبع الثيم، فالخانة تُعرف بلونها قبل كلمتها */
  color?: string;
}

/**
 * ترويسةُ الرئيسية — **الرئيسيةُ صفحةُ مكتبتك لا صفحةُ حسابك** (D-434).
 *
 * **ما سقط ولماذا:** كانت الرئيسية تفتح بترويسة الحساب كاملةً — غلافٌ
 * بارتفاع ١٦٠px واسمٌ ونبذةٌ ومتابعون ومستوى — **فأوّلُ ما يراه صاحبُ
 * الحساب هو صورةُ نفسه، وأوّلُ محتوًى حقيقيّ تحت منتصف الشاشة.**
 * **وتلك ترويسةُ ملفٍّ عامّ يقرؤه الآخرون**، ومكانُها الملفُّ العامّ
 * (`/u/<username>`) — **والرئيسيةُ تُفتح لتُستأنَف حلقة، لا لتُقرأ سيرة.**
 *
 * ⚖️ **ونقضٌ مسجَّل**: «الملفّ الشخصيّ صار جزءاً من الصفحة الرئيسية».
 * **والثمنُ مدفوعٌ في الدفعة نفسِها**: `/profile` صار يحوّل إلى ملفّك
 * العامّ لا إلى الجذر، **فما سقط رسمُه لم يسقط بابُه.**
 *
 * **وثلاثةُ صفوفٍ لا أكثر:** العلامةُ وأدواتُها · التحيّةُ ومبدّلُ العرض ·
 * بطاقةُ رقمين. **وكلُّها معاً أقصرُ من الغلاف وحدَه.**
 */
export function HomeHeader({
  displayName,
  avatarUrl,
  avatarPos,
  coverUrl,
  coverPos,
  unreadSignals = 0,
  unreadShares = 0,
  stats,
  showStats,
  levelPercent,
  locale,
}: {
  displayName: string;
  /** ⚖️ 🆕 **يُقرأ مرّةً أخرى** (D-536): الصورةُ عادت إلى صفّ الترحيب */
  avatarUrl?: string | null;
  avatarPos?: number | null;
  /**
   * 🆕 **غلافُ ملفّك خلفَ ترويسة الرئيسية** (D-540، طلبُ أحمد: «صورة
   * الخلفية في البروفايل أبغاها تكون هنا أيضاً»).
   *
   * **ولا نداءَ جديد**: `profiles` تُقرأ للصفحة أصلاً وفيها العمودان
   * (D-470). **وغيابُه يعني ألّا شيء** — لا تدرّجَ بديلاً ولا سطحاً
   * رماديّاً (D-034/D-222): **ترويسةُ الرئيسية تعمل بلا صورةٍ منذ
   * D-434، فلا تُخترع لها خلفيّة.**
   */
  coverUrl?: string | null;
  coverPos?: number | null;
  /** 🆕 **عدّادان لا مجموع** (D-536) — لكلِّ بابٍ رقمُه */
  unreadSignals?: number;
  unreadShares?: number;
  /** خاناتُ بطاقة الأرقام — من التخصيص، اثنتان إلى أربع (D-152) */
  stats: HeaderStat[];
  showStats: boolean;
  /** ⚖️ 🆕 **والهلالُ عاد معها** (D-536/D-439) — **٠ يعني «لا هلال»** */
  levelPercent?: number;
  locale: Locale;
}) {
  const t = getDict(locale);

  return (
    /* ⚖️ 🆕 **وترويسةُ الرئيسية الخاصّةُ سقطت** (D-502، طلبُ أحمد
       بلقطتين: «الرسائل ليه مكانها في كل الصفحات ما هو مطابق لمكانها في
       الهوم؟ حتى الشعار والصورة الشخصية — لازم كلها في نفس المكان عشان
       وقت الانتقال بين الصفحات يطلع شكلها ثابت»).

       **كان في التطبيق شريطان علويّان لا واحد**: هذا (وردمارك + ظرف +
       ترس) في الرئيسية على الجوّال، **و`Navbar` (رمز + عنوان + ظرف +
       صورة) في كلِّ صفحةٍ سواها** — **فالظرفُ يقفز أفقيّاً، والعلامةُ
       تتبدّل شكلاً، والأيقونةُ الأخيرة تتبدّل معنًى** في كلِّ تنقّل.
       **وهذا نقضٌ لشطرٍ من D-122** (إخفاءُ الشريط في الرئيسية على
       الجوّال): **حجّتُه أن الشريط هناك لا يحمل إلا أيقونة**، **وقد
       صار يحمل الظرفَ والصورةَ معاً** — فماتت الحجّة.

       **والباقي هنا محتوًى يجري مع الصفحة** كما ثبّت D-479. */
    <>
    {/* ===== 🆕 الغلافُ خلفَ الترويسة (D-540) =====

        **ولماذا طبقةٌ مطلقةٌ بلا غلافٍ يحملها:** صفُّ العلامة `sticky`،
        **وأيُّ حاويةٍ بارتفاعه تصير سقفاً لالتصاقه** (قاعدةُ
        `HeaderShell` بحرفها). **وهذه الطبقةُ لا أبَ موضوعاً لها**،
        فتُرسى إلى صندوق الاحتواء الابتدائيّ — **أي رأسُ المستند بعرض
        الشاشة** — **وهو بالضبط ما يريده غلافٌ يخرج عن الهوامش**، ولا
        يمسّ التصاقَ الشريط بحرف.

        **والوصفةُ وصفةُ غلاف الملفّ نفسُها** (`‎/u/[username]`): صورةٌ
        `fill` بموضعٍ رأسيٍّ يختاره صاحبُها، **ثمّ حجابٌ متدرّج**،
        **ثمّ ذوبانٌ إلى لون الخلفية في الأسفل**. **ولا نظامَ بصريٌّ
        ثانٍ لغلافٍ واحد** (القاعدة ٦).

        ⚠️ **والذوبانُ يقع قبل صفِّ الترحيب لا بعده**: تحته نصٌّ بألوان
        الثيم (وثيمُ النهار فاتح)، **ونصٌّ ثيميٌّ على صورةٍ يُقرأ في
        ثيمٍ ويختفي في آخر** (D-501). **فما يقف على الصورة هو الشريطُ
        وحدَه، وهو أبيضُ صريحٌ في الثيمين.** ===== */}
    {coverUrl && (
      <div
        aria-hidden
        /* 🔴 **ولا `z` سالبة** (مقيسٌ على المنشور قبل الإصلاح): طبقةٌ
           بـ`-z-10` تُرسم **قبل خلفيّات الكتل العاديّة** في ترتيب
           الطلاء، **وخلفيّةُ `body` صمّاء** (`rgb(5,5,5)`) — **فكانت
           تُغطّي الصورةَ تغطيةً كاملة** والشجرةُ تقول إنها موجودةٌ
           ومحمَّلة. **فالطبقةُ في مستواها الطبيعيّ، وما فوقها يُرفع
           بـ`relative`** — وهو ما تفعله الترويسةُ أصلاً بـ`z-30`. */
        /* 🆕 **الارتفاعُ يتبع المنطقةَ الآمنة لا رقماً ثابتاً** (D-543):
           الشريطُ نفسُه ارتفاعُه `--safe-top + 64px`، **فغلافٌ بارتفاعٍ
           ثابتٍ يقع تحته على الآيفون ويفيض عنه في المتصفّح.** */
        className="pointer-events-none absolute inset-x-0 top-0 overflow-hidden"
        /* **ينتهي عند الخطّ الأحمر** — منتصفُ بطاقة الأرقام (D-553) */
        style={{ height: "calc(var(--safe-top) + 9.5rem)" }}
      >
        {/* 🆕 **والصورةُ تذوب بقناعٍ لا بطبقةٍ فوقها** (D-543): **حجابٌ
            ثانٍ إلى لون الخلفية كان يجتمع مع الحجاب الأوّل عند صفِّ
            الترحيب فيُظلمه مرّتين** — **والقناعُ يُنقص شفافيّةَ الصورة
            نفسِها** فيظهر ما تحتها بلا أن يُضاف سواد. **وليس مرشّحاً**
            (`filter`) ولا تغبيشاً: لا خلطَ ألوانٍ ولا تشويه. */}
        <Image
          src={coverUrl}
          alt=""
          fill
          priority
          sizes="100vw"
          className="object-cover"
          style={{
            objectPosition: `50% ${coverPos ?? 30}%`,
            maskImage: COVER_FADE,
            WebkitMaskImage: COVER_FADE,
          }}
        />
        {/* ⚖️ 🔴 **والحجابُ سقط كلَّه** (D-553): كان يُظلم من الأعلى
            إلى الأسفل — **عكسَ ما رسمه أحمد بخطّيه** — **والقناعُ فوق
            خلفيّةٍ سوداء يفعل الإظلامَ وحدَه.** **وطبقتان تفعلان شيئاً
            واحداً إحداهما زائدة** (القاعدة ٦). */}
      </div>
    )}

    {/* ===== ⚖️ 🆕 صفُّ العلامة وأدواتُها — عادَ إلى الرئيسية (D-536)
        =====

        **طلبُ أحمد بلقطتين: «رجّع التصميم إلى الهوم».** **وهو نقضٌ
        مسجَّلٌ لـD-502** التي حذفت هذا الصفَّ لتوحيد الشريط —
        **وحجّتُها كانت ثبات موضع الظرف والعلامة بين الصفحات**،
        **والذي تبدّل أنّ المالك رأى الشكلين واختار.** **والثمنُ
        يُقال**: وردماركٌ هنا ورمزٌ هناك، وترسٌ هنا وصورةٌ هناك.

        **وعلى الجوّال وحدَه** (`md:hidden`) — **والشريطُ الواسع باقٍ
        كما هو** بروابطه وبحثه (`hidesAppHeaderOnMobile` في
        `chromeRules`)، **فالقرارُ في ملفِّ القواعد لا في مكوّنين.**

        **و`chrome-top` كي يختبئ مع النزول** كبقيّة الأشرطة (D-479)،
        **وحشوةُ `--safe-top`** لأنه أوّلُ ما تحت ساعة النظام في
        التطبيق المثبَّت (D-040). ===== */}
    {/* ⚖️ 🆕 **وخلفيّةُ الشريط تتبع وجودَ الغلاف** (D-540 ثمّ D-543):
        **صمّاءُ بلا صورة** كما كانت، **ولا شيءَ إطلاقاً فوق الغلاف** —
        **والحجابُ الذي يجعل أيقوناتِه مقروءةً يرسمه الغلافُ نفسُه**
        (٩٦٪ عند الحافّة العليا)، **لا سطحٌ ثانٍ تحت الشريط.** */}
    <header
      /* ⚖️ 🆕 **ولا تغبيشَ ولا خلفيّةَ فوق الغلاف** (D-543، طلبُ أحمد
         بالنصّ: «احذف backdrop-filter وblur وأيّ طبقة تسبّب خلط أو
         تشويه ألوان الغلاف»). **وكان `backdrop-blur-xl` مع لونٍ ممزوج
         يشتري وضوحَ الأيقونات حين يعود الشريطُ بالتمرير فوق الملصقات**
         — **والثمنُ أنّ الغلافَ يُرى مغبَّشاً مشوَّهَ اللون في رأس
         الصفحة، وهو أوّلُ ما تقع عليه العين.** **والمالك رأى الاثنين
         واختار.**
         ⚠️ **وما سقط معه يُقال**: الشريطُ الآن شفّافٌ تماماً، **فإن
         عاد بالتمرير فوق صورةٍ فاتحة صارت أيقوناتُه البيضاءُ على
         فاتح.** **و`chrome-top` يُخفيه مع النزول** فالنافذةُ ضيّقة —
         **لكنها ليست معدومة.** */
      /* 🆕 **وارتفاعُه ارتفاعُ الشريط العامّ بالضبط** (D-543):
         `10 + 44 + 10 = 64` — **وهو `h-16` في `Navbar` حرفاً.**
         **فمركزُ الأيقونة `--safe-top + 32` في الشريطين**، ولا يقفز
         شيءٌ رأسيّاً في الانتقال. **وكانت `0.5rem` تعطي ٣٠ فيزيح
         بكسلين.** */
      className={`chrome-top md:hidden sticky top-0 z-30 -mx-4 px-4 -mt-6 pt-[calc(var(--safe-top)+0.625rem)] pb-2.5 ${
        coverUrl ? "" : "bg-[color:var(--background)]"
      }`}
    >
      <div className="flex items-center gap-1">
        <Link href="/" prefetch={false} aria-label={t.brand} className="shrink-0 -ms-0.5">
          {/* **الكلمةُ المرسومة لا الرمز** — هي علامةُ الرئيسية في
              تصميم أحمد، **والرمزُ يبقى للشريط الضيّق في بقيّة
              الصفحات** حيث يجاوره عنوانُ الصفحة. */}
          {/* **والعلامةُ على فنٍّ لا تتبع الثيم** (D-405): `on="art"`
              يمنع قلبَها في الثيم النهاريّ — **فوق الصورة هي بيضاءُ
              دائماً**، وبلا صورةٍ تتبع الثيم كما كانت. */}
          <LogoWordmark size={30} on={coverUrl ? "art" : "surface"} />
        </Link>

        {/* 🆕 **والطرفُ صفٌّ واحدٌ مشترك** (D-541، بلاغُ أحمد بلقطتين:
            «مكان الرسالة والجرس لازم يطابق موقعها في كل الصفحات»):
            **المسافةُ والمقاسُ يُكتبان في `HeaderTrailing` مرّةً**،
            **وهذا الشريطُ و`Navbar` يقرآن منها** — **فلا يقفز الجرسُ
            ٥px ولا الظرفُ ٣ في الانتقال.** **والخانةُ الأخيرة وحدَها
            تختلف معنًى** (ترسٌ هنا، صورةٌ هناك) **وصندوقُها ٤٠×٤٠
            في الحالين وإلّا انكسر التطابق.** */}
        <div className="ms-auto">
          <HeaderTrailing
            unreadSignals={unreadSignals}
            unreadShares={unreadShares}
            locale={locale}
          >
            <Link
              href="/profile/settings"
              prefetch={false}
              aria-label={t.settingsNavHeading}
              title={t.settingsNavHeading}
              /* 🆕 **٤٤×٤٤ و٢٤ للأيقونة** (D-543) — **نفسُ مقاسِ الجرس
                 والظرف بالضبط**، فالخانةُ الأخيرة تختلف معنًى لا مقاساً. */
              className={`shrink-0 grid place-items-center w-11 h-11 rounded-full hover:bg-surface-2 active:scale-95 transition ${
                coverUrl ? "text-white/90 hover:text-white" : "text-foreground/80 hover:text-foreground"
              }`}
            >
              <Icon name="settings" size={24} />
            </Link>
          </HeaderTrailing>
        </div>
      </div>
    </header>

    {/* ===== ما بقي محتوًى عاديّاً يتحرّك مع الصفحة (طلبُ أحمد ١٩
        أغسطس): التحيّةُ والصورةُ والأرقامُ ومبدّلُ العرض تُقرأ عند
        الدخول وتغادر بالتمرير — **لا سقفَ يتكرّر في كلِّ شاشة.** ===== */}
    {/* **و`relative` ترفع ما تحت الشريط فوق الغلاف** — بلا رقمٍ: عنصرٌ
        موضوعٌ متأخّرٌ في الشجرة يعلو عنصراً موضوعاً قبله (D-540). */}
    <div className="relative space-y-2.5">
      {/* ===== التحيّةُ ومبدّلُ العرض ===== */}
      <div className="flex items-center gap-3">
        {/* ⚖️ 🆕 **وصورةُ الترحيب عادت ومعها الهلال** (D-536/D-439،
            لقطةُ أحمد) — **بعد أن أسقطتهما D-502.**

            **والهلالُ مجّانيٌّ هنا وحدَه**: نسبةُ المستوى تُحسب من
            أرقامٍ تقرؤها هذه الصفحةُ أصلاً (`watch_summary` والأفلام
            المشاهَدة)، **ورسمُه حول صورة الشريط العلويّ كان سيكلّف
            نداءَي عدٍّ في كلِّ مسار** — **وهذا هو نصُّ ثمنِ D-502**،
            **والموضعُ الذي لا يدفعه هو هذا.**

            **ودائرةٌ واحدةٌ بتدرّجٍ مخروطيّ** لا حلقةُ SVG: قوسٌ بلونِ
            الهوية إلى النسبة ثم رماديُّ الحدّ — **ولا مكوّنَ جديد.**
            **و`0` تعني ألّا هلال** فلا تُرسم حلقةٌ فارغةٌ تعد بشيء
            (D-222). */}
        <Link
          href="/profile"
          prefetch={false}
          aria-label={t.profile}
          title={displayName || t.profile}
          className="shrink-0 rounded-full p-[2px] active:scale-95 transition"
          style={
            levelPercent && levelPercent > 0
              ? {
                  background: `conic-gradient(var(--accent) ${levelPercent}%, var(--border) 0)`,
                }
              : undefined
          }
        >
          <span className="block rounded-full p-[2px] bg-[color:var(--background)]">
            <Avatar
              src={avatarUrl}
              name={displayName}
              size={44}
              posY={avatarPos}
              alt={t.avatarAlt}
            />
          </span>
        </Link>

        <div className="min-w-0 flex-1">
          <HomeGreeting name={displayName} locale={locale} />
        </div>

        <HomeViewSwitch locale={locale} />
      </div>

      {/* ===== الصفُّ الثالث: بطاقةُ الأرقام =====
          **والشكلُ يتبع الوضع**: بطاقةٌ واحدةٌ بفواصلَ رفيعة في المختصر —
          **سطرٌ أقلُّ ارتفاعاً لمن طلب الضغط** — وبطاقةٌ لكلِّ رقمٍ في
          البصريّ. **والمحتوى واحدٌ في الاثنين** فلا يفقد أحدُهما رقماً. */}
      {/* ===== الصفُّ الثالث: بطاقةُ الأرقام =====
          ⚖️ 🆕 **وبطاقةٌ واحدةٌ في الوضعين** (D-439، حكمُ أحمد بلقطةٍ
          للبطاقة: «خلّها بنفس هذا التصميم، **لا تقسمها**»). **كانت
          بطاقتين منفصلتين في البصريّ وواحدةً في المختصر** — **وشكلان
          لشيءٍ واحدٍ يتبدّلان بتبدّل وضع العرض يجعلان الترويسةَ نفسَها
          تُقرأ ترويستين** (القاعدة ٦). **والفاصلُ الرفيع يفرّق الخانات
          بلا أن يفصل البطاقة.** */}
      {showStats && stats.length > 0 && (() => {
        /* 🆕 **أربعُ خاناتٍ تنزل صفّين** (D-487، لقطةُ أحمد على البطاقة:
           «هذي إذا كاتب أربعة خلّها بنظام ٢ وتحتها ٢ grid»).

           **والعلّةُ مقيسةٌ في لقطته**: أربعُ خاناتٍ على عرض هاتفٍ تعني
           ~٩٠px للخانة، **والخانةُ رمزٌ ورقمٌ واسم** — **فيُقصّ الاسمُ
           إلى «To w…» و«Movi…»**، **واسمٌ مقصوصٌ لا يقول ما يعدّه**،
           فيصير الرقمُ بلا معنى. **وبعمودين يصير للخانة ~١٨٠px** فيظهر
           الاسمُ كاملاً بلا أن يصغر الخطّ (`02`: لا تصغير).

           ⚠️ **والثلاثةُ تبقى صفّاً واحداً**: ١٢٠px تكفي، **وصفٌّ ونصفٌ
           أسوأُ من صفٍّ ممتلئ.** */
        const cols = stats.length === 4 ? 2 : stats.length;
        return (
        <div
          /* ⚖️ 🆕 **والإطارُ سقط** (D-550، طلبُ أحمد بلقطةٍ معلَّمة:
             «الإطار في الكارد احذفه، أبغى أشوفها بدون إطار»).

             ⚖️ **ثمّ سقط السطحُ والزاويةُ معه** (طلبُه الثاني: «احذف
             السطح والزاوية»): **لم يبقَ للبطاقة صندوقٌ إطلاقاً** —
             **أرقامٌ على خلفيّة الصفحة مباشرةً**، وفوق الغلاف حين
             يكون هناك غلاف.

             ⚠️ **والفاصلُ بين الخانتين باقٍ**: هو ما يجعلها تُقرأ
             خانتين لا سطراً واحداً، **وهو خطٌّ بين اثنين لا إطارٌ
             حولهما** — **ولم يطلب حذفَه.**

             ⚠️ **والحشوُ الرأسيُّ باقٍ في الخانات** (`py-3.5`): **حذفُ
             الصندوق ليس حذفَ التنفّس** — ولو ضاق لالتصقت الأرقامُ
             بالتحيّة فوقها وبالقسم تحتها. */
          className="grid"
          style={{ gridTemplateColumns: `repeat(${cols}, minmax(0,1fr))` }}
        >
          {stats.map((s, i) => (
            <Link
              key={s.key}
              href={s.href ?? "/library"}
              /* **الفاصلُ يُحسب من موضع الخانة في الشبكة لا من ترتيبها**:
                 خطٌّ رأسيٌّ لكلِّ خانةٍ ليست أوّلَ عمودها، **وأفقيٌّ لكلِّ
                 خانةٍ في صفٍّ ثانٍ** — ومن أخذ `i > 0` وحدَها رسم خطّاً
                 رأسيّاً في رأس السطر الثاني. */
              className={`flex items-center justify-center gap-2 py-3.5 transition active:opacity-70 ${
                i % cols !== 0 ? "border-s border-[color:var(--divider)] " : ""
              }${i >= cols ? "border-t border-[color:var(--divider)]" : ""}`}
            >
              <StatFace stat={s} />
            </Link>
          ))}
        </div>
        );
      })()}
    </div>
    </>
  );
}

/** وجهُ الخانة — **رسمٌ واحدٌ للوضعين**، والذي يتبدّل هو الإطار حولَه */
function StatFace({ stat }: { stat: HeaderStat }) {
  /* 🆕 **سطرٌ واحدٌ لا سطران** (D-437، بلاغُ أحمد: «الرقم طالع فوق
     الكلمة، صلّحه بحيث يكون سطر واحد») — **وارتفاعُ البطاقة نزل الثلث**
     بلا أن يسقط منها حرف. */
  return (
    <>
      <Icon
        name={stat.icon}
        size={20}
        style={{ color: stat.color ?? "var(--accent)" }}
      />
      {/* 🆕 **٢٤px/٧٠٠ — «الأرقام الكبيرة» في سلّم النصّ** (D-454)،
          **والكلمةُ تحتها ١٢ ثانويّاً.** و`font-bold` لا `extrabold`:
          السلّمُ يقول ٧٠٠، **ووزنٌ ثامنُ مئةٍ لرقمٍ في بطاقةٍ يجعله
          يصرخ فوق عنوان القسم الذي يليه.** */}
      {/* 🆕 ⚖️ **٢٠ لا ٢٤** (D-459، حكمُ أحمد: «أرقام الكارد كبيرة شوي»)
          — **والعلّةُ هرميّةٌ لا مقاس**: عند ٢٤ كان **الرقمُ أكبرَ نصٍّ
          في الشاشة**، أكبرَ من «تابِع المشاهدة» (٢٢) — **فتقع العينُ على
          «١٠» قبل أن تقع على اسم القسم**، وشريطُ الأرقام اختصاراتٌ لا
          عنوانُ الصفحة. **والعنوانُ يجب أن يبقى أعلى السلّم.**
          ⚠️ **و٢٤/٧٠٠ تبقى في نظام التصميم** لسطحِ الأرقام الحقيقيّ
          (`/stats`) — **الدرجةُ لم تُلغَ، إنما لم تكن هذه موضعَها.** */}
      <span className="text-20 font-bold leading-none tabular-nums">
        {stat.value}
      </span>
      <span className="min-w-0 truncate text-12 font-medium text-muted leading-none">
        {stat.label}
      </span>
    </>
  );
}
