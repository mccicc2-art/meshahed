import Link from "next/link";
import Image from "next/image";
import { getDict, type Locale } from "@/lib/i18n";
import { Icon, type IconName } from "./Icon";
import { HEADER_ICON, headerIconControl } from "./ui/controls";
import { FollowCountButton } from "./ProfilePeeks";
import { HomeAvatarLink } from "./HomeAvatarLink";
import { HomeViewSwitch } from "./HomeViewSwitch";
import { HeaderTrailing } from "./HeaderTrailing";
import { LogoWordmark } from "./Logo";
import { AccountIdentity, badgeLabelsOf } from "./AccountIdentity";
import { isPlus } from "@/lib/plan";
/**
 * 🆕 **خانةُ بطاقة الأرقام — تسكن مع راسمها** (D-497): كانت تُستورد
 * نوعاً من `ProfileHeader` — **وتلك حُذفت بعد أن فقدت قارئَها في
 * D-438** (D-214)، **والنوعُ نُقل أوّلاً ثمّ حُذف الملفّ.**
 */
/* ===== 🆕 وصفةُ الغلاف — طبقتان، لكلٍّ خطُّ أحمد الذي وُلدت منه (D-556)
   =====

   **لقطةُ أحمد عليها منطقتان مظلَّلتان بخطَّين مسطَّرين**، ونصُّه:
   «هاتين المنطقتين خلِّ الظلام يبدأ ينولد ويتدرّج من عندها · والظلام
   الذي كان موجوداً قبلُ للسقف ممتاز رجِّعه».

   **والقياسُ من لقطته** (٤٣٠pt عرضاً، `--safe-top: 59`):
   الخطُّ الأوّل ≈ **٩٢pt** — **وهو `--safe-top + 32` بالضبط: مركزُ صفِّ
   الوردمارك والأيقونات.** والخطُّ الثاني ≈ **١٨٢pt** — **`--safe-top +
   123`: أوّلُ ما تحت سطر الترحيب وفوق بطاقة الأرقام.**

   ⚖️ 🔴 **ونقضٌ مسجَّلٌ لـD-553 بطلبه.** حذفتُ هناك الحجابَ كلَّه
   وقلتُ **«طبقتان تفعلان شيئاً واحداً إحداهما زائدة»** — **وكان الحكمُ
   خاطئاً لأنهما لا تفعلان شيئاً واحداً**: **القناعُ يُذيب الصورةَ في
   القاع، والحجابُ يحمي السقف** — **وحذفُ الحجاب ترك رأسَ الشاشة بلا
   ظلامٍ إطلاقاً**، فصار الوردماركُ والأيقوناتُ بيضاءَ على فنٍّ مجهول.
   **وهو الثمنُ الذي كتبتُه في D-553 بيدي وقلتُ «يُدفع متى رآه أحمد» —
   وقد رآه.**

   **فلكلِّ طبقةٍ عملٌ واحدٌ لا تنازعَ فيه:**
   - **`COVER_SCRIM` — السقفُ والخطُّ الأوّل.** **قيمُه الثلاثُ هي
     قيمُ أحمد نفسُها** من مواصفة D-541 (٠٫٩٦ · ٠٫٨٢ · ٠٫٤٢ · ٠)،
     **والذي تبدّل مراسيها**: كانت نِسَباً مئويّةً تنزلق مع المنطقة
     الآمنة، **وصارت `calc()` معلَّقةً بـ`--safe-top`** — **فيقع
     ٠٫٨٢ على مركز صفِّ الأيقونات في كلِّ جهاز** لا على ٤٦٪ من صندوقٍ
     يتغيّر طولُه. **ثمّ يتدرّج نازلاً إلى العدم** — وهو «يتدرّج من
     عندها» بنصّه.
   - **`COVER_FADE` — الخطُّ الثاني.** **الصورةُ تامّةٌ حتى
     `--safe-top + 123`** ثمّ تذوب إلى العدم عند قاع الصندوق —
     **وخلفيّةُ الصفحة سوداءُ فالذوبانُ هو الإظلام.** **وكان يبدأ عند
     ٨٩٪ (≈ ١٨٨pt)** — **ستّةُ بكسلاتٍ متأخّرةً عن خطّه، وبمنحدرٍ
     أقصر.**

   🔑 **ولا تنازعَ بين الطبقتين لأنهما لا تلتقيان**: الحجابُ عند ٠
   في قاع الصندوق **حيث الصورةُ قد ذابت أصلاً** — **فلا حافّةَ سوداءَ
   تُرسم فوق خلفيّةِ الصفحة، ولا شريطَ فاتحاً بينهما.**

   ⚠️ **والصندوقُ لم يُمسّ**: `--safe-top + 9.5rem` — **منتصفُ بطاقة
   الأرقام، وهو خطُّ أحمد الأحمر من D-553** ولم يشكُ منه. */

/** **الحجاب**: ظلامُ السقف، تامٌّ عند الساعة، يتدرّج من صفِّ الأيقونات
 *
 * ⚖️ 🆕 **والإنارةُ ارتفعت** (D-565، طلبُ أحمد بخطٍّ أزرقَ تحت صفِّ
 * الوردمارك: «هنا الإنارة ارفعها شوي»).
 *
 * **وخطُّه عند `--safe-top + 41` تقريباً** — **أوّلُ ما تحت صفِّ
 * الأيقونات مباشرةً.** **وكان الحجابُ هناك ٠٫٧٩** لأن انحداره يمتدّ
 * إلى `+97`. **فارتفعت العقدةُ الثالثةُ إلى `+72` وخفّت إلى ٠٫٣٤** —
 * **فيصير عند خطّه ٠٫٦ لا ٠٫٧٩**، ويستعيد الفنُّ نفسَه بعده بسرعة.
 *
 * ⚠️ **والعقدتان الأوليان لم تُمسّا** (٠٫٩٦ عند الساعة و٠٫٨٢ عند مركز
 * صفِّ الأيقونات): **هما ما يجعل الوردماركَ والجرسَ مقروءَين على أيِّ
 * فنّ** — **وهو الثمنُ الذي دُفع في D-556 حين عاد الحجاب.**
 * **والمطلوبُ رفعُ الإنارة تحتهما لا كشفُ السقف.**
 *
 * ⚖️ 🆕 **ثمّ عاد بعضُ الظلام إلى الوسط** (D-589، بلاغُ أحمد بلقطةٍ
 * محوَّطٍ فيها وسطُ الغلاف: «المنطقة المنيرة في الهيدر حطّ فيها ظلام
 * شوي») — **مراجعةٌ جزئيّةٌ لرفعةِ D-565 بيد صاحبها**: العقدةُ الثالثةُ
 * ٠٫٣٤ → **٠٫٤٦**، **وعقدةٌ رابعةٌ جديدة ٠٫٢٤ عند `+118`** — فمنطقةُ
 * صفِّ الترحيب (الاسمُ والعدّادان والمبدّل) تقف على ظلٍّ يقرؤها،
 * **والذوبانُ إلى الصفر باقٍ في القاع** فلا حافّةَ تُرسم.
 * ⚠️ **والعقدتان الأوليان كما هما للمرّة الثالثة.**
 */
/* ⚖️ 🆕 **ثمّ استوى الظلامُ كلُّه** (D-613، حكمُ أحمد بلقطةٍ خطَّط
   فيها الحزامَ من الساعة إلى ما فوق بطاقة الأرقام: «هذي المنطقة
   المخطّطة خلّي كلّه ظلامها واحد») — **سادسُ حكمٍ على هذا التدرّج
   ونقضُ نحتِه كلِّه** (D-541 قيمُه → D-553 حذفُه → D-556 عودتُه →
   D-565 إنارتُه → D-589 إظلامُ وسطِه → **واليوم استواؤه**): العقدُ
   الأربعُ المتفاوتة (٠٫٩٦/٠٫٨٢/٠٫٤٦/٠٫٢٤) صارت **قيمةً واحدةً ٠٫٨٢
   مسطّحةً على الحزام كلِّه**.

   ⚖️ 🆕 **ثمّ لم يعد تدرّجاً أصلاً** (D-616، حكمُه بلقطةٍ بعد ساعةٍ
   من D-613: «فيه تدريج سابقة، خلها كلها بالكامل تضليلها متساوي») —
   **سابعُ الأحكام وخاتمةُ النحت**: ذيلُ D-613 (٠٫٨٢ → ٠ بعد `+118`)
   كان آخرَ تدرّجٍ باقٍ، **وسقط**. الحجابُ صار **لوناً واحداً ٠٫٨٢
   بلا `linear-gradient`** يغطّي الغلافَ كلَّه — فالتظليلُ فوق الصورة
   متساوٍ من الساعة إلى آخر بكسل فيها.

   ⚠️ **والحافّةُ التي كان الذيلُ يمنعها يمنعها القناعُ الآن**: الحجابُ
   يلبس `COVER_FADE` نفسَه الذي تلبسه الصورةُ (انظر موضعَ رسمه) —
   **فيذوبان معاً في قاع الصندوق ذوباً واحداً**، لا سوادَ مسطّحاً
   ينقطع بحدٍّ فوق خلفيّة `daylight` الفاتحة، ولا ظلَّ أخفَّ على
   آخر ما يُرى من الصورة. */
/* ⚖️ 🆕 **وخُفِّف الظلامُ خطوةً** (D-660، طلبُ أحمد: «والظلام في الهوم
   خفّفه شوي») — **ثامنُ حكمٍ على هذه الطبقة، وأوّلُ ما يمسّ قيمتَها
   منذ D-613.**
   ⚖️ 🆕 **ثمّ خطوةٌ ثانيةٌ بكلمته «أخفّ»** (D-661): **٠٫٧٢ → ٠٫٦٥**.
   **٠٫٨٢ → ٠٫٧٢ → ٠٫٦٥**: خطوتان لا قفزةٌ واحدة — **«شوي» تُقرأ خطوةً**،
   **والقياسُ في عينه لا في يدي** فيُشحن ويُرى ثمّ يُخطى ثانيةً إن أراد.
   **وهذا هو الحدُّ الذي أعلنتُه قبل أن أخطوه**: ٠٫٦٥ آخرُ ما يبقى فيه
   الأبيضُ مضموناً على غلافٍ مجهول — **ودونه يصير الرهانُ على صورة
   العضو.** **فإن طلب أخفَّ بعدها فالجوابُ ليس رقماً بل طبقةً**: ظلٌّ
   موضعيٌّ تحت الوردمارك والأيقونات وحدَها بدل حجابٍ يعمّ الغلافَ كلَّه.
   ⚠️ **والحدُّ الأدنى ليس ذوقاً**: الوردماركُ والأيقوناتُ بيضاءُ فوق
   **غلافٍ يختاره كلُّ عضوٍ بنفسه** — **وقد يكون صفحةً بيضاء.**
   **فالحجابُ هو ما يجعل الأبيضَ مقروءاً على المجهول** (وهو ثمنُ D-553
   الذي دُفع مرّةً حين حُذف). **ودون ٠٫٦ يصير الرهانُ على صورة العضو.**
   ⚠️ **ولا يُنحت شكلُه**: **هو لونٌ واحدٌ مسطّحٌ بحكمه في D-616** —
   **والذي تغيّر شدّتُه لا استواؤه.** */
const COVER_SCRIM = "rgba(0,0,0,0.30)";

/**
 * 🆕 **ظلٌّ ملتصقٌ بالنصِّ بدل حجابٍ يعمّ الغلاف** (D-662، حكمُ أحمد
 * بعد أن رأى أربعَ لقطاتٍ حيّةً واختار الثالثة).
 *
 * 🔴 **ونقضٌ صريحٌ لاستواء D-616** («خلّها كلها بالكامل تظليلها
 * متساوي»): **الظلمةُ لم تعد واحدةً على الغلاف كلِّه** — **صار
 * الحجابُ خفيفاً (٠٫٣٠) والظلُّ ملتصقاً بالوردمارك والأيقونات والاسم.**
 * **وهو نقضٌ بحكمه بعد معاينة، لا اجتهادٌ منّي.**
 *
 * 🔑 **والفكرةُ: يُقرأ الأبيضُ ويُرى الفنُّ معاً** — **وإظلامُ الصورة
 * كلِّها ثمنٌ يدفعه الغلافُ لأجل ستِّ كلماتٍ فوقه.**
 *
 * ⚠️ **وهو قريبُ `coverBareControl` ولا يُوحَّد به بلا قياس**: ذاك
 * ظلٌّ **لأيقونةِ ٢٤px فوق غلافِ ملفٍّ ما زال يحمل حجابَه العلويّ**،
 * **وهذا لوردماركٍ ونصٍّ فوق غلافٍ كُشف عمداً** — **فالحِملُ مختلفٌ
 * والقيمتان مقيستان لحالتيهما.** **من أراد توحيدَهما فليقس أوّلاً.**
 *
 * ⚠️ **ولا يُرسم بلا غلاف**: بلا صورةٍ النصُّ على خلفيّة التطبيق —
 * **وظلٌّ أسودُ تحت نصٍّ داكنٍ في `daylight` وسخٌ لا وضوح.**
 */
const COVER_ART_SHADOW =
  "drop-shadow(0 1px 5px rgba(0,0,0,0.95)) drop-shadow(0 0 14px rgba(0,0,0,0.6))";

/** **القناع**: الصورةُ تامّةٌ إلى خطِّه الثاني، ثمّ تذوب إلى العدم.
 *
 * 🆕 **والخطُّ صار متغيّراً لا رقماً** (D-847): **الصندوقُ يكبر على
 * الشاشة الواسعة، وقناعٌ برقمٍ ثابتٍ كان سيبقى واقفاً عند ١٢٣**
 * **فتُقصّ الصورةُ قصّاً حادّاً في منتصف الصندوق الجديد.** **رقمان
 * يجب أن يتحرّكا معاً فليس لهما إلّا مصدرٌ واحد** — **وهما مكتوبان
 * على الصندوق نفسِه** (`--cover-h`/`--cover-solid`) **لا في
 * `globals.css`**: **الوصفةُ وتعليلُها في ملفٍّ واحد** (D-145). */
const COVER_FADE =
  "linear-gradient(180deg, #000 0," +
  " #000 calc(var(--safe-top) + var(--cover-solid))," +
  " transparent 100%)";

export interface HeaderStat {
  key: string;
  icon: IconName;
  value: string;
  label: string;
  href?: string;
  /** لون الأيقونة — ثابت لا يتبع الثيم، فالخانة تُعرف بلونها قبل كلمتها */
  color?: string;
}

/**
 * ترويسةُ الرئيسية — **الرئيسيةُ صفحةُ مكتبتك لا صفحةُ حسابك** (D-434).
 *
 * **ما سقط ولماذا:** كانت الرئيسية تفتح بترويسة الحساب كاملةً — غلافٌ
 * بارتفاع ١٦٠px واسمٌ ونبذةٌ ومتابعون ومستوى — **فأوّلُ ما يراه صاحبُ
 * الحساب هو صورةُ نفسه، وأوّلُ محتوًى حقيقيّ تحت منتصف الشاشة.**
 * **وتلك ترويسةُ ملفٍّ عامّ يقرؤه الآخرون**، ومكانُها الملفُّ العامّ
 * (`/u/<username>`) — **والرئيسيةُ تُفتح لتُستأنَف حلقة، لا لتُقرأ سيرة.**
 *
 * ⚖️ **ونقضٌ مسجَّل**: «الملفّ الشخصيّ صار جزءاً من الصفحة الرئيسية».
 * **والثمنُ مدفوعٌ في الدفعة نفسِها**: `/profile` صار يحوّل إلى ملفّك
 * العامّ لا إلى الجذر، **فما سقط رسمُه لم يسقط بابُه.**
 *
 * **وثلاثةُ صفوفٍ لا أكثر:** العلامةُ وأدواتُها · التحيّةُ ومبدّلُ العرض ·
 * بطاقةُ رقمين. **وكلُّها معاً أقصرُ من الغلاف وحدَه.**
 */
export function HomeHeader({
  displayName,
  username = null,
  avatarUrl,
  avatarPos,
  coverUrl,
  coverPos,
  unreadSignals = 0,
  unreadShares = 0,
  stats,
  showStats,
  userId,
  followers = 0,
  following = 0,
  hideFollowLists = false,
  plan = null,
  founder = false,
  plusUntil = null,
  verifiedAt = null,
  locale,
}: {
  displayName: string;
  /** 🆕 سطرُ التعريف تحت الاسم (D-618): «@ahmed» — والغيابُ يعني ألّا سطر */
  username?: string | null;
  /* 🆕 **شارةُ Loopz+** (D-633) — **اختياريّان بافتراضٍ صامت** (D-028):
     مكوّنٌ قديمٌ لا يمرّرهما لا ينكسر، ولا شارةَ تُرسم. */
  plan?: string | null;
  founder?: boolean | null;
  /* 🆕 **والتاريخُ يُمرَّر لا يُهمَل** (D-773): `isPlus` تقرؤه،
     **وغيابُه كان يُقرأ «بلا انتهاء»** — فيرى المنتهي اشتراكُه شارتَه
     حيّةً في ترويسته. */
  plusUntil?: string | null;
  /** 🆕 ختمُ التوثيق (D-773) — صفةٌ مستقلّةٌ عن الخطّة */
  verifiedAt?: string | null;
  /** ⚖️ 🆕 **يُقرأ مرّةً أخرى** (D-536): الصورةُ عادت إلى صفّ الترحيب */
  avatarUrl?: string | null;
  avatarPos?: number | null;
  /**
   * 🆕 **غلافُ ملفّك خلفَ ترويسة الرئيسية** (D-540، طلبُ أحمد: «صورة
   * الخلفية في البروفايل أبغاها تكون هنا أيضاً»).
   *
   * **ولا نداءَ جديد**: `profiles` تُقرأ للصفحة أصلاً وفيها العمودان
   * (D-470). **وغيابُه يعني ألّا شيء** — لا تدرّجَ بديلاً ولا سطحاً
   * رماديّاً (D-034/D-222): **ترويسةُ الرئيسية تعمل بلا صورةٍ منذ
   * D-434، فلا تُخترع لها خلفيّة.**
   */
  coverUrl?: string | null;
  coverPos?: number | null;
  /** 🆕 **عدّادان لا مجموع** (D-536) — لكلِّ بابٍ رقمُه */
  unreadSignals?: number;
  unreadShares?: number;
  /** خاناتُ بطاقة الأرقام — من التخصيص، اثنتان إلى أربع (D-152) */
  stats: HeaderStat[];
  showStats: boolean;
  /** ⚖️ 🆕 **والهلالُ عاد معها** (D-536/D-439) — **٠ يعني «لا هلال»** */
  /**
   * 🆕 **عدّادا المتابعة** (D-572، طلبُ أحمد) — **اختياريّةٌ في النوع**
   * (D-028): **المكوّنُ في `components` والصفحةُ في `app`، دليلان
   * فكوميتان** — **والكوميتُ الأوّل تقرؤه الصفحةُ القديمةُ التي لا
   * تمرّرها.** **وغيابُ `userId` يعني ألّا عدّاد** فلا يُرسم رقمٌ
   * بابُه معطّل (D-217).
   */
  userId?: string;
  /* ⚖️ 🆕 **وعدّادُ المتابِعين غادر في D-618 ثمّ عاد بكلمة صاحبه**
     (D-621: «ضيف المتابعون كذا») — الثنائيّةُ في سطر التعريف الآن،
     رمزَين وعدَّين بلا كلمات. */
  followers?: number;
  following?: number;
  /** قفلُ قائمتَي المتابعة (هجرة ٤٣) — الرقمُ يبقى والبابُ يُقفل */
  hideFollowLists?: boolean;
  locale: Locale;
}) {
  const t = getDict(locale);

  return (
    /* ⚖️ 🆕 **وترويسةُ الرئيسية الخاصّةُ سقطت** (D-502، طلبُ أحمد
       بلقطتين: «الرسائل ليه مكانها في كل الصفحات ما هو مطابق لمكانها في
       الهوم؟ حتى الشعار والصورة الشخصية — لازم كلها في نفس المكان عشان
       وقت الانتقال بين الصفحات يطلع شكلها ثابت»).

       **كان في التطبيق شريطان علويّان لا واحد**: هذا (وردمارك + ظرف +
       ترس) في الرئيسية على الجوّال، **و`Navbar` (رمز + عنوان + ظرف +
       صورة) في كلِّ صفحةٍ سواها** — **فالظرفُ يقفز أفقيّاً، والعلامةُ
       تتبدّل شكلاً، والأيقونةُ الأخيرة تتبدّل معنًى** في كلِّ تنقّل.
       **وهذا نقضٌ لشطرٍ من D-122** (إخفاءُ الشريط في الرئيسية على
       الجوّال): **حجّتُه أن الشريط هناك لا يحمل إلا أيقونة**، **وقد
       صار يحمل الظرفَ والصورةَ معاً** — فماتت الحجّة.

       **والباقي هنا محتوًى يجري مع الصفحة** كما ثبّت D-479. */
    <>
      {/* ===== 🆕 الغلافُ خلفَ الترويسة (D-540) =====

        **ولماذا طبقةٌ مطلقةٌ بلا غلافٍ يحملها:** صفُّ العلامة `sticky`،
        **وأيُّ حاويةٍ بارتفاعه تصير سقفاً لالتصاقه** (قاعدةُ
        `HeaderShell` بحرفها). **وهذه الطبقةُ لا أبَ موضوعاً لها**،
        فتُرسى إلى صندوق الاحتواء الابتدائيّ — **أي رأسُ المستند بعرض
        الشاشة** — **وهو بالضبط ما يريده غلافٌ يخرج عن الهوامش**، ولا
        يمسّ التصاقَ الشريط بحرف.

        **والوصفةُ وصفةُ غلاف الملفّ نفسُها** (`‎/u/[username]`): صورةٌ
        `fill` بموضعٍ رأسيٍّ يختاره صاحبُها، **ثمّ حجابٌ متدرّج**،
        **ثمّ ذوبانٌ إلى لون الخلفية في الأسفل**. **ولا نظامَ بصريٌّ
        ثانٍ لغلافٍ واحد** (القاعدة ٦).

        ⚠️ **والذوبانُ يقع قبل صفِّ الترحيب لا بعده**: تحته نصٌّ بألوان
        الثيم (وثيمُ النهار فاتح)، **ونصٌّ ثيميٌّ على صورةٍ يُقرأ في
        ثيمٍ ويختفي في آخر** (D-501). **فما يقف على الصورة هو الشريطُ
        وحدَه، وهو أبيضُ صريحٌ في الثيمين.** ===== */}
      {coverUrl && (
        <div
          aria-hidden
          /* 🔴 **ولا `z` سالبة** (مقيسٌ على المنشور قبل الإصلاح): طبقةٌ
           بـ`-z-10` تُرسم **قبل خلفيّات الكتل العاديّة** في ترتيب
           الطلاء، **وخلفيّةُ `body` صمّاء** (`rgb(5,5,5)`) — **فكانت
           تُغطّي الصورةَ تغطيةً كاملة** والشجرةُ تقول إنها موجودةٌ
           ومحمَّلة. **فالطبقةُ في مستواها الطبيعيّ، وما فوقها يُرفع
           بـ`relative`** — وهو ما تفعله الترويسةُ أصلاً بـ`z-30`. */
          /* 🆕 **الارتفاعُ يتبع المنطقةَ الآمنة لا رقماً ثابتاً** (D-543):
           الشريطُ نفسُه ارتفاعُه `--safe-top + 64px`، **فغلافٌ بارتفاعٍ
           ثابتٍ يقع تحته على الآيفون ويفيض عنه في المتصفّح.** */
          /* ⚖️ 🆕 **ودرجةٌ واحدةٌ للشاشة الواسعة** (D-847، بلاغُ أحمد:
           «البنر في المتصفّح الهوم صغير — بلّغتك اليوم لكنك ما عملتها»
           — **وهي شكوى D-836 نفسُها على صفحةٍ أخرى**).

           📏 **والمقيسُ حيّاً عند ١٩١٢px**: الصندوقُ **١٥٢** وعرضُه
           **١٩١٢** — **١٢٫٦:١**، **ومنه ٦٥ تحت الشريط العلويّ**
           **فلا يُرى منه إلا ٨٧px**. **وعلى الجوّال ٣٧٥×٢١١ —
           ١٫٨:١**، **وهي نسبةُ الخلفيّة نفسِها فتُرى الصورةُ كاملةً.**
           **فما صغُر مقاسٌ بل نسبة**، وهو حرفُ D-836.

           🔑 **ولا يُمدُّ الصندوقُ وحدَه**: **سقفُه ليس ذوقاً بل
           أوّلُ نصٍّ ثيميٍّ تحته** (D-501: نصٌّ ثيميٌّ على صورةٍ
           يُقرأ في ثيمٍ ويختفي في آخر) — **وما يقف على الصورة هو
           صفُّ الترحيب وبطاقةُ الأرقام وحدَهما.** **فالغلافُ يكبر
           بمقدار ما تكبر الترويسةُ تحته لا أكثر** — **والصورةُ
           والدائرةُ والاسمُ والبطاقةُ تكبر معاً أو لا يكبر منها
           شيء** (D-836).

           ⚠️ **والجوّالُ لم يُمسّ**: `9.5rem`/`123px` كما كانتا
           حرفاً، **والدرجةُ عند `lg` وحدَها.** */
          className="pointer-events-none absolute inset-x-0 top-0 overflow-hidden [--cover-h:9.5rem] [--cover-solid:123px] lg:[--cover-h:14rem] lg:[--cover-solid:160px]"
          /* **ينتهي عند الخطّ الأحمر** — منتصفُ بطاقة الأرقام (D-553) */
          style={{ height: "calc(var(--safe-top) + var(--cover-h))" }}
        >
          {/* 🆕 **والصورةُ تذوب بقناعٍ لا بطبقةٍ فوقها** (D-543): **حجابٌ
            ثانٍ إلى لون الخلفية كان يجتمع مع الحجاب الأوّل عند صفِّ
            الترحيب فيُظلمه مرّتين** — **والقناعُ يُنقص شفافيّةَ الصورة
            نفسِها** فيظهر ما تحتها بلا أن يُضاف سواد. **وليس مرشّحاً**
            (`filter`) ولا تغبيشاً: لا خلطَ ألوانٍ ولا تشويه. */}
          <Image
            src={coverUrl}
            alt=""
            fill
            priority
            sizes="100vw"
            className="object-cover"
            style={{
              objectPosition: `50% ${coverPos ?? 30}%`,
              maskImage: COVER_FADE,
              WebkitMaskImage: COVER_FADE,
            }}
          />
          {/* ⚖️ 🆕 **والحجابُ عاد** (D-556، نقضُ D-553 بطلبه): **يحمي
            السقفَ وصفَّ الأيقونات ولا يمسّ القاع** — **فلا يجتمع مع
            القناع على صفِّ الترحيب** كما كان يفعل حجابُ D-540.
            ⚠️ **وفوق الصورة لا تحتها**: هو ظلامُها لا خلفيّتُها. */}
          <div
            aria-hidden
            className="absolute inset-0"
            /* 🆕 قناعُ الصورة على الحجاب أيضاً (D-616): لونٌ واحدٌ فوق
             الصورة كلِّها، ويذوبان معاً فلا حافّةَ في `daylight` */
            style={{
              background: COVER_SCRIM,
              maskImage: COVER_FADE,
              WebkitMaskImage: COVER_FADE,
            }}
          />
        </div>
      )}

      {/* ===== ⚖️ 🆕 صفُّ العلامة وأدواتُها — عادَ إلى الرئيسية (D-536)
        =====

        **طلبُ أحمد بلقطتين: «رجّع التصميم إلى الهوم».** **وهو نقضٌ
        مسجَّلٌ لـD-502** التي حذفت هذا الصفَّ لتوحيد الشريط —
        **وحجّتُها كانت ثبات موضع الظرف والعلامة بين الصفحات**،
        **والذي تبدّل أنّ المالك رأى الشكلين واختار.** **والثمنُ
        يُقال**: وردماركٌ هنا ورمزٌ هناك، وترسٌ هنا وصورةٌ هناك.

        **وعلى الجوّال وحدَه** (`md:hidden`) — **والشريطُ الواسع باقٍ
        كما هو** بروابطه وبحثه (`hidesAppHeaderOnMobile` في
        `chromeRules`)، **فالقرارُ في ملفِّ القواعد لا في مكوّنين.**

        **و`chrome-top` كي يختبئ مع النزول** كبقيّة الأشرطة (D-479)،
        **وحشوةُ `--safe-top`** لأنه أوّلُ ما تحت ساعة النظام في
        التطبيق المثبَّت (D-040). ===== */}
      {/* ⚖️ 🆕 **وخلفيّةُ الشريط تتبع وجودَ الغلاف** (D-540 ثمّ D-543):
        **صمّاءُ بلا صورة** كما كانت، **ولا شيءَ إطلاقاً فوق الغلاف** —
        **والحجابُ الذي يجعل أيقوناتِه مقروءةً يرسمه الغلافُ نفسُه**
        (٩٦٪ عند الحافّة العليا)، **لا سطحٌ ثانٍ تحت الشريط.** */}
      <header
        /* ⚖️ 🆕 **ولا تغبيشَ ولا خلفيّةَ فوق الغلاف** (D-543، طلبُ أحمد
         بالنصّ: «احذف backdrop-filter وblur وأيّ طبقة تسبّب خلط أو
         تشويه ألوان الغلاف»). **وكان `backdrop-blur-xl` مع لونٍ ممزوج
         يشتري وضوحَ الأيقونات حين يعود الشريطُ بالتمرير فوق الملصقات**
         — **والثمنُ أنّ الغلافَ يُرى مغبَّشاً مشوَّهَ اللون في رأس
         الصفحة، وهو أوّلُ ما تقع عليه العين.** **والمالك رأى الاثنين
         واختار.**
         ⚠️ **وما سقط معه يُقال**: الشريطُ الآن شفّافٌ تماماً، **فإن
         عاد بالتمرير فوق صورةٍ فاتحة صارت أيقوناتُه البيضاءُ على
         فاتح.** **و`chrome-top` يُخفيه مع النزول** فالنافذةُ ضيّقة —
         **لكنها ليست معدومة.** */
        /* 🆕 **وارتفاعُه ارتفاعُ الشريط العامّ بالضبط** (D-543):
         `10 + 44 + 10 = 64` — **وهو `h-16` في `Navbar` حرفاً.**
         **فمركزُ الأيقونة `--safe-top + 32` في الشريطين**، ولا يقفز
         شيءٌ رأسيّاً في الانتقال. **وكانت `0.5rem` تعطي ٣٠ فيزيح
         بكسلين.** */
        className={`chrome-top md:hidden sticky top-0 z-30 -mx-4 px-4 -mt-6 pt-[calc(var(--safe-top)+0.625rem)] pb-2.5 ${
          coverUrl ? "" : "bg-[color:var(--background)]"
        }`}
      >
        {/* 🆕 **الظلُّ على الصفِّ لا على كلِّ رمزٍ وحدَه** (D-662):
          **قيمةٌ واحدةٌ في موضعٍ واحد** — ولو كُتبت على كلِّ أيقونةٍ
          لافترقت عند أوّل تعديل (D-145). */}
        <div
          className="flex items-center gap-1"
          style={{ filter: coverUrl ? COVER_ART_SHADOW : undefined }}
        >
          <Link
            href="/"
            prefetch={false}
            aria-label={t.brand}
            className="shrink-0 -ms-0.5"
          >
            {/* **الكلمةُ المرسومة لا الرمز** — هي علامةُ الرئيسية في
              تصميم أحمد، **والرمزُ يبقى للشريط الضيّق في بقيّة
              الصفحات** حيث يجاوره عنوانُ الصفحة. */}
            {/* **والعلامةُ على فنٍّ لا تتبع الثيم** (D-405): `on="art"`
              يمنع قلبَها في الثيم النهاريّ — **فوق الصورة هي بيضاءُ
              دائماً**، وبلا صورةٍ تتبع الثيم كما كانت. */}
            {/* 🆕 **وتجربةُ Loopz+ تبدأ من العلامة** (D-773، لوحُ الهويّة):
              صاحبُ البلس والبارتنر يرى `Loopz+`، والمجّانيُّ `Loopz`.
              **والحكمُ من `plan.ts` لا من شرطٍ مكتوبٍ هنا** (D-145). */}
            <LogoWordmark
              size={30}
              on={coverUrl ? "art" : "surface"}
              plus={isPlus({ plan, founder, plus_until: plusUntil })}
            />
          </Link>

          {/* 🆕 **والطرفُ صفٌّ واحدٌ مشترك** (D-541، بلاغُ أحمد بلقطتين:
            «مكان الرسالة والجرس لازم يطابق موقعها في كل الصفحات»):
            **المسافةُ والمقاسُ يُكتبان في `HeaderTrailing` مرّةً**،
            **وهذا الشريطُ و`Navbar` يقرآن منها** — **فلا يقفز الجرسُ
            ٥px ولا الظرفُ ٣ في الانتقال.** **والخانةُ الأخيرة وحدَها
            تختلف معنًى** (ترسٌ هنا، صورةٌ هناك) **وصندوقُها ٤٠×٤٠
            في الحالين وإلّا انكسر التطابق.** */}
          <div className="ms-auto">
            <HeaderTrailing
              unreadSignals={unreadSignals}
              unreadShares={unreadShares}
              locale={locale}
            >
              {/* ⚖️ 🆕 **وترسُ الإعدادات عاد إلى مكانه** (D-774، حكمُه:
                «الإعدادات رجّعها فوق مكان الثلاث خطوط، والثلاث خطوط
                نزّلها تحت») — **نقضٌ صريحٌ لـD-620 بيد صاحبه**، وبه
                سقطت قائمةُ الصورة التي كانت جسراً لهذا البابِ الغائب
                (انظر `HomeAvatarLink`).

                🆕 🔑 **والوصفةُ صارت مصدراً واحداً** (D-776):
                `headerIconControl` + `HEADER_ICON` في `ui/controls` —
                **يقرؤها الظرفُ والجرسُ والترسُ هنا، والغلافُ هناك.**
                **وثلاثُ نسخٍ من سلسلةِ أصنافٍ تفترق عند أوّل تعديل**
                (D-145)، **وقد افترقت فعلاً: ٤٠ هنا و٤٤ في الصورة.** */}
              <Link
                href="/profile/settings"
                prefetch={false}
                aria-label={t.settingsNavHeading}
                title={t.settingsNavHeading}
                className={headerIconControl}
              >
                <Icon name="settings" size={HEADER_ICON} />
              </Link>
            </HeaderTrailing>
          </div>
        </div>
      </header>

      {/* ===== ما بقي محتوًى عاديّاً يتحرّك مع الصفحة (طلبُ أحمد ١٩
        أغسطس): التحيّةُ والصورةُ والأرقامُ ومبدّلُ العرض تُقرأ عند
        الدخول وتغادر بالتمرير — **لا سقفَ يتكرّر في كلِّ شاشة.** ===== */}
      {/* **و`relative` ترفع ما تحت الشريط فوق الغلاف** — بلا رقمٍ: عنصرٌ
        موضوعٌ متأخّرٌ في الشجرة يعلو عنصراً موضوعاً قبله (D-540). */}
      <div
        className="relative space-y-2.5"
        /* 🆕 **والاسمُ والمعرّفُ والعدّادان تحت الظلِّ نفسِه** (D-662) —
         **هم أيضاً بيضٌ فوق فنٍّ مجهول.** */
        style={{ filter: coverUrl ? COVER_ART_SHADOW : undefined }}
      >
        {/* ===== التحيّةُ ومبدّلُ العرض ===== */}
        <div className="flex items-center gap-3">
          {/* ⚖️ 🆕 **وصورةُ الترحيب عادت ومعها الهلال** (D-536/D-439،
            لقطةُ أحمد) — **بعد أن أسقطتهما D-502.**

            **والهلالُ مجّانيٌّ هنا وحدَه**: نسبةُ المستوى تُحسب من
            أرقامٍ تقرؤها هذه الصفحةُ أصلاً (`watch_summary` والأفلام
            المشاهَدة)، **ورسمُه حول صورة الشريط العلويّ كان سيكلّف
            نداءَي عدٍّ في كلِّ مسار** — **وهذا هو نصُّ ثمنِ D-502**،
            **والموضعُ الذي لا يدفعه هو هذا.**

            **ودائرةٌ واحدةٌ بتدرّجٍ مخروطيّ** لا حلقةُ SVG: قوسٌ بلونِ
            الهوية إلى النسبة ثم رماديُّ الحدّ — **ولا مكوّنَ جديد.**
            **و`0` تعني ألّا هلال** فلا تُرسم حلقةٌ فارغةٌ تعد بشيء
            (D-222). */}
          {/* ⚖️ 🆕 **والصورةُ عادت باباً مباشراً إلى الملفّ** (D-774،
            حكمُه: «الي يضغط على الصورة يروح للبروفايل مباشرة بدون
            القائمة المنسدلة») — **نقضُ D-620**، والحجّةُ في
            `HomeAvatarLink`: القائمةُ كانت جسراً لبابِ الإعدادات
            الغائب، وقد عاد البابُ فسقط الجسر. */}
          <HomeAvatarLink
            locale={locale}
            name={displayName}
            avatarUrl={avatarUrl}
            avatarPos={avatarPos}
          />

          {/* ⚖️ 🆕 **والتحيّةُ سقطت، وبقي الاسم** (D-565، طلبُ أحمد:
            «وكذلك جود مورنينج وصباح الخير احذفها»).

            **وسطرٌ يتبدّل بالساعة يقول شيئاً لا يفعله القارئ** —
            **والاسمُ وحدَه يكفي للتعريف بمن سجّل دخولَه**، وهو مرسومٌ
            من الخادم بلا انتظار. ✅ **و`HomeGreeting` حُذفت في جولة
            التنظيف** بعد أن فقدت قارئَها الوحيد — **في رفعةٍ لاحقة لا
            مع قارئها** (D-538/D-028)، **ومفاتيحُ `greet*` الأربعةُ
            بعدها.** */}
          {/* ⚖️ 🆕 **الاسمُ وتحته سطرُ التعريف** (D-618، توزيعُ أحمد
            المرسوم): «AHMED» ثمّ «@ahmed · 5 Following» — **وما كان
            صفّاً واحداً مزدحماً** (اسمٌ + عدّادان + مبدّل، D-572/D-614)
            **صار عمودَين**: المبدّلُ صعد لصفِّ الأيقونات، وعدّادُ
            المتابِعين غادر (نقضُ الثنائيّة محصوراً — الملفُّ بعدَّيه
            كما هو)، **وعدُّ مَن تتابعهم بقي بورقته نفسِها**
            (`FollowCountButton`/D-561 — نفسُ الحارس ونفسُ السقف). */}
          <div className="min-w-0 flex-1">
            {/* 🆕 **والسطرُ كلُّه من المكوّن المركزيّ** (D-773ب، شرطُ
              القبول ٩): الاسمُ يُقصّ والشاراتُ لا — **وشارةٌ تختفي بطول
              اسمٍ ليست شارة** — **والفجوةُ ٤px تُكتب هناك مرّةً لا هنا.** */}
            <AccountIdentity
              as="p"
              name={displayName}
              profile={{ plan, founder, plus_until: plusUntil, verified_at: verifiedAt }}
              t={t}
              /* ⚖️ 🆕 **والاسمُ يكبر مع الغلاف** (D-847): **١٥ على
                الجوّال وعشرون على الواسعة** — **ودرجةٌ واحدةٌ من
                السلّم لا رقمٌ يُذاق** (لا ١٦ ولا ١٨ في السلّم،
                D-811). **و٢٢/٢٤ تبقى للملفّ العامّ**: **ذاك عنوانُ
                صفحةٍ وهذا سطرُ ترحيبٍ فوق مكتبة.** */
              className="text-15 lg:text-20 font-bold leading-tight"
            />
            {/* ⚖️ 🆕 ١٣ → ١٢ (D-619: «أحجام الخط كبيرة وغير متناسقة») —
              سطرُ تعريفٍ ثانويٌّ درجتُه درجةُ الثانويّ في السلّم */}
            {(username || userId) && (
              <p
                /* 🆕 **والثانويُّ يتبع الأوّل** (D-847): ١٢ ثمّ ١٤ */
                className={`mt-0.5 flex items-center gap-1.5 text-12 lg:text-14 leading-tight ${
                  coverUrl ? "text-white/70" : "text-muted"
                }`}
              >
                {username && (
                  <span className="min-w-0 truncate">@{username}</span>
                )}
                {username && userId && (
                  <span aria-hidden className="shrink-0 opacity-60">
                    •
                  </span>
                )}
                {/* ⚖️ 🆕 **والعدّان معاً بلا كلمات** (D-621): مَن يتابعونني
                  أوّلاً ثمّ مَن أتابعهم — بترتيب كلمته («وحدة الي
                  يتابعوني والثانية الي أتابعهم»)، والرمزان يفرّقان. */}
                {userId && (
                  <span className="inline-flex items-center gap-2">
                    <FollowCountButton
                      targetId={userId}
                      dir="followers"
                      count={followers}
                      label={t.followersLabel}
                      sheetTitle={t.followsTabFollowers}
                      locked={hideFollowLists}
                      compact
                      labels={{
                        close: t.closeLabel,
                        empty: t.followListEmpty,
                        anonymous: t.anonymousUser,
                        /* 🔴 **ولا `t` كاملاً هنا**: هذا مكوّنُ عميل،
                           **و`Dict` يحمل دوالَّ صياغةٍ لا تُسلسَل** فيتوقّف
                           القطاعُ عن الحلّ (انظر `badgeLabelsOf`). */
                        badges: badgeLabelsOf(t),
                      }}
                    />
                    <FollowCountButton
                      targetId={userId}
                      dir="following"
                      count={following}
                      label={t.followingLabel}
                      sheetTitle={t.followsTabFollowing}
                      locked={hideFollowLists}
                      compact
                      labels={{
                        close: t.closeLabel,
                        empty: t.followListEmpty,
                        anonymous: t.anonymousUser,
                        /* 🔴 **ولا `t` كاملاً هنا**: هذا مكوّنُ عميل،
                           **و`Dict` يحمل دوالَّ صياغةٍ لا تُسلسَل** فيتوقّف
                           القطاعُ عن الحلّ (انظر `badgeLabelsOf`). */
                        badges: badgeLabelsOf(t),
                      }}
                    />
                  </span>
                )}
              </p>
            )}
          </div>

          {/* ⚖️ 🆕 **والمبدّلُ نزل إلى صفِّ الترحيب** (D-774، حكمُه:
            «الثلاث خطوط نزّلها تحت») — **نقضُ شطرٍ من D-618** الذي رفعه
            إلى صفِّ الأيقونات، **والشطرُ الآخرُ باقٍ**: السطرُ ما زال
            عمودين (اسمٌ ثمّ تعريف) لا صفّاً مزدحماً — **المبدّلُ يقف
            عند الطرف بمحاذاة الصورة، لا داخلَ عمود الاسم.**

            🔑 **و`shrink-0` لأنّ جارَه `min-w-0 flex-1`**: **الاسمُ هو
            ما يُقصّ عند الضيق، لا الزرّ** — **وزرٌّ يضيق بطول اسمٍ
            يصير هدفاً لا يُصاب** (قاعدةُ ٤٤×٤٤ في D-543).
            ⚠️ **و«الطرف» هو `flex` نفسُها**: يمينٌ في LTR ويسارٌ في
            RTL بلا `ms`/`me` مكتوبةٍ بيد (D-105). */}
          <span className="shrink-0">
            <HomeViewSwitch locale={locale} />
          </span>
        </div>

        {/* ===== الصفُّ الثالث: بطاقةُ الأرقام =====
          **والشكلُ يتبع الوضع**: بطاقةٌ واحدةٌ بفواصلَ رفيعة في المختصر —
          **سطرٌ أقلُّ ارتفاعاً لمن طلب الضغط** — وبطاقةٌ لكلِّ رقمٍ في
          البصريّ. **والمحتوى واحدٌ في الاثنين** فلا يفقد أحدُهما رقماً. */}
        {/* ===== الصفُّ الثالث: بطاقةُ الأرقام =====
          ⚖️ 🆕 **وبطاقةٌ واحدةٌ في الوضعين** (D-439، حكمُ أحمد بلقطةٍ
          للبطاقة: «خلّها بنفس هذا التصميم، **لا تقسمها**»). **كانت
          بطاقتين منفصلتين في البصريّ وواحدةً في المختصر** — **وشكلان
          لشيءٍ واحدٍ يتبدّلان بتبدّل وضع العرض يجعلان الترويسةَ نفسَها
          تُقرأ ترويستين** (القاعدة ٦). **والفاصلُ الرفيع يفرّق الخانات
          بلا أن يفصل البطاقة.** */}
        {showStats &&
          stats.length > 0 &&
          (() => {
            /* 🆕 **أربعُ خاناتٍ تنزل صفّين** (D-487، لقطةُ أحمد على البطاقة:
           «هذي إذا كاتب أربعة خلّها بنظام ٢ وتحتها ٢ grid»).

           **والعلّةُ مقيسةٌ في لقطته**: أربعُ خاناتٍ على عرض هاتفٍ تعني
           ~٩٠px للخانة، **والخانةُ رمزٌ ورقمٌ واسم** — **فيُقصّ الاسمُ
           إلى «To w…» و«Movi…»**، **واسمٌ مقصوصٌ لا يقول ما يعدّه**،
           فيصير الرقمُ بلا معنى. **وبعمودين يصير للخانة ~١٨٠px** فيظهر
           الاسمُ كاملاً بلا أن يصغر الخطّ (`02`: لا تصغير).

           ⚠️ **والثلاثةُ تبقى صفّاً واحداً**: ١٢٠px تكفي، **وصفٌّ ونصفٌ
           أسوأُ من صفٍّ ممتلئ.** */
            const cols = stats.length === 4 ? 2 : stats.length;
            return (
              <div
                /* ⚖️ 🆕 **والصندوقُ عاد بحكم صاحبه** (D-618: «حط خلفية
             للكاردات») — **نقضٌ مسجَّلٌ لـD-550 التي أسقطت الإطارَ
             والسطحَ والزاويةَ بطلبه يومَها**: **والوصفةُ وصفةُ بطاقة
             أرقام الملفّ حرفاً** (D-610: `rounded-2xl border
             border-border bg-surface`) — لا وصفةَ بطاقةٍ ثانية
             (القاعدة ٦). **والفاصلُ بين الخانتين باقٍ كما كان.** */
                className="grid rounded-2xl border border-border bg-surface overflow-hidden"
                style={{
                  /* 🆕 **العمودُ بمقاس محتواه لا ثُلثاً متساوياً** (D-787،
                 بلاغُ أحمد: «الكلمة ما تطلع واضحة»).

                 🔴 **والعلّةُ أنّ `1fr` تقسم بالعدل لا بالحاجة**:
                 «56 مسلسلات» و«79 تقييماتي» يأخذان العرضَ نفسَه،
                 **فيفيض عن الأوّل ما ينقص الثاني ويُقصّ الاسم** —
                 **وقسمةٌ عادلةٌ على محتوىً غيرِ متساوٍ ظلمٌ لأطولِه.**

                 ⚖️ **و`minmax(0,auto)` تعطي كلَّ عمودٍ مقاسَ محتواه ثمّ
                 تقسم الفائضَ بينها بالتساوي** — **والحدُّ الأدنى صفرٌ
                 عمداً** فيبقى القصُّ ملاذاً أخيراً حين تضيق الشاشةُ
                 حقّاً، لا سلوكاً يوميّاً.

                 📐 **مقيسٌ على الخطّ الحيّ بعرض ٣٥٨px** (لقطةُ أحمد):
                 «Shows · Movies · My ratings» كانت تُقصّ عند ٥٤/٦٣،
                 **وصارت ٦٣/٦٣ كاملة** — وكلُّ تركيبةٍ من التسع تمرّ
                 عند ٣٥٨ و٣٧٥ و٣٩٨. **و٣٢٠px وحدَها تبقى تقصّ**
                 وهو حدُّ الصفِّ الواحد لا عطلُه. */
                  gridTemplateColumns: `repeat(${cols}, minmax(0,auto))`,
                }}
              >
                {stats.map((s, i) => (
                  <Link
                    key={s.key}
                    href={s.href ?? "/library"}
                    /* **الفاصلُ يُحسب من موضع الخانة في الشبكة لا من ترتيبها**:
                 خطٌّ رأسيٌّ لكلِّ خانةٍ ليست أوّلَ عمودها، **وأفقيٌّ لكلِّ
                 خانةٍ في صفٍّ ثانٍ** — ومن أخذ `i > 0` وحدَها رسم خطّاً
                 رأسيّاً في رأس السطر الثاني. */
                    /* **والفجوةُ والحشوةُ نزلتا درجةً** (٨→٦px): ١٢px
                 لكلِّ خانةٍ تُقرأ فرقاً في العين لا يُذكر، **وتكفي
                 لتمرير أطول تركيبةٍ إنجليزيّةٍ عند ٣٥٨px** — وهي
                 التركيبةُ الوحيدةُ التي بقيت تُقصّ بعد قسمة الفائض. */
                    /* 🆕 **والحشوةُ درجةً على الواسعة** (D-847):
                 **بطاقةٌ بارتفاع ٤٢px في عمودٍ عرضُه ١١٢٠ شريطٌ لا
                 بطاقة** — **والحشوةُ هي ما يصنع ارتفاعَها**، لا
                 ارتفاعٌ يُكتب بيد. */
                    className={`flex items-center justify-center gap-1.5 px-1.5 py-3 lg:px-3 lg:py-5 transition active:opacity-70 ${
                      i % cols !== 0
                        ? "border-s border-[color:var(--divider)] "
                        : ""
                    }${i >= cols ? "border-t border-[color:var(--divider)]" : ""}`}
                  >
                    <StatFace stat={s} />
                  </Link>
                ))}
              </div>
            );
          })()}
      </div>
    </>
  );
}

/** وجهُ الخانة — **رسمٌ واحدٌ للوضعين**، والذي يتبدّل هو الإطار حولَه */
function StatFace({ stat }: { stat: HeaderStat }) {
  /* ⚖️ 🆕 **الرقمُ فوق الكلمة والرمزُ بجوارهما** (D-618، توزيعُ أحمد
     المرسوم) — **نقضٌ محصورٌ لسطرِ D-437 الواحد بيد صاحبه**: يومَها
     كان الرقمُ فوق الكلمة **بلا رمزٍ جانبيّ** فطلب سطراً واحداً،
     **ولقطتُه اليوم ترسم الرمزَ عموداً والرقمَ والكلمةَ عموداً** —
     شكلُ خانة بطاقةٍ لها صندوقُها.
     ⚖️ 🆕 **ثمّ نزلت الدرجاتُ كلُّها بلوحته** (D-619)، **ثمّ عاد
     السطرُ الواحد بحكمه** (D-620، على اللقطة المحوَّطة: «وهذي صفّ
     واحد وصغّر الخط شوي») — **عودةُ D-437 بعينها بعد نقضها في
     D-618**: الرقمُ والكلمةُ جنباً إلى جنب، والرقمُ **١٥** والكلمةُ
     **١٢** والرمزُ **١٦**. */
  return (
    <>
      {/* 🆕 **والدرجاتُ الثلاثُ تصعد معاً على الواسعة** (D-847):
        **١٦/١٥/١٢ على الجوّال، ٢٠/٢٠/١٤ على الواسعة** — **والرمزُ
        بصنفٍ لا برقمٍ ثانٍ**: `size` سِمةٌ على الـ`svg` **والصنفُ
        يغلبها**، فيبقى مصدرُ المقاس الجوّاليِّ واحداً. */}
      <Icon
        name={stat.icon}
        size={16}
        className="lg:w-5 lg:h-5"
        style={{ color: stat.color ?? "var(--accent)" }}
      />
      <span className="text-15 lg:text-20 font-bold leading-none tabular-nums">
        {stat.value}
      </span>
      <span className="min-w-0 truncate text-12 lg:text-14 font-medium text-muted leading-none">
        {stat.label}
      </span>
    </>
  );
}
