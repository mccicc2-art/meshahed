/**
 * **رقاقةُ PLUS بجانب اسم الصفحة** (D-801 — من صورة أحمد المرفقة).
 *
 * **ولمَ ملفٌّ لها وقارئاها اثنان**: «تقريرك» و«الإحصائيات الكاملة» —
 * **وعند القارئ الثاني يُستخرج** (D-376/D-002)، **ونسختان من رقاقةٍ
 * واحدةٍ تفترقان عند أوّل تعديلِ لون** (القاعدة ٣: variantٌ ثانٍ عطل).
 *
 * ⚖️ **والاسمُ لا يُترجم**: «Loopz+» علامةٌ لا كلمة (`plusName` في
 * القاموس تقول ذلك في اللغتين) — **ورقاقةٌ تقول «بلس» بالعربيّة تخترع
 * اسماً ثانياً للمنتج.**
 */
export function PlusPill() {
  return (
    <span
      className="shrink-0 rounded-md bg-accent/15 px-1.5 py-0.5 text-12 font-bold leading-none text-accent tracking-wide"
      dir="ltr"
    >
      PLUS
    </span>
  );
}
