"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";
import { getDict, type Locale } from "@/lib/i18n";
import { useKeyboardOpen } from "@/lib/useKeyboard";
import { usePrefetchOnIntent } from "@/lib/prefetchIntent";
import { Icon, type IconName } from "./Icon";

/* ⚖️ 🆕 **وورقةُ البحث غادرت هذا الملفّ** (D-534): البحثُ صار صفحةً
   كاملةً (`/search`) بترويستها ورقائقها وأقسامها — **فالخانةُ رابطٌ
   كسائر الخانات، ولا حالةَ ولا حوارَ ولا حزمةً مؤجَّلةً هنا.**
   **والورقةُ باقيةٌ لقارئها الآخر** — التقاطُ عملٍ إلى قائمة
   (`onPick`) — **وذاك اختيارٌ لا انتقال.** */

/**
 * شريط التبويبات السفلي.
 *
 * خمس وجهات: الرئيسية، المكتبة، اكتشف، المجتمع، البحث. والبحث أُضيف
 * بقرار المالك ويُسجَّل به D-024: كان مدخلاً داخل «اكتشف» وحده، فمن أراده
 * دفع ضغطتين ومرّ على صفحةٍ لا يريدها — وهو أكثر أفعال التطبيق تكراراً
 * بعد التأشير.
 *
 * والخانات متساوية العرض بشبكةٍ من خمسة أعمدة لا بحشوٍ لكل خانة:
 * «المجتمع» أعرض من «بحث» بضعف، فالحشو المتساوي يعطي خاناتٍ متفاوتة
 * والعين تقرأ التفاوت اضطراباً. العرض واحد والنصّ يُقصّ إن طال.
 *
 * لونان لا خمسة: النشط بلون الهوية، والخامل رماديّ `--disabled`. تلوين
 * كل تبويبٍ بلونه كان يجعلها متساوية في الصياح، فلا يُعرف موضعك إلا
 * بقراءة الكلمات.
 *
 * ---
 *
 * **الشكل: شريطٌ ملتصقٌ بالحافّة لا كبسولةٌ عائمة** (طلب أحمد ١١ أغسطس:
 * «الشريط السفلي احتاجك تغير تصميمه وما يكون كذا عائم، يكون مثل تبع
 * تويتر»). وثلاثةُ أشياء تغيّرت معاً، وكلٌّ منها له سببُه:
 *
 *  ١) **الكبسولة ذهبت.** كانت لوحاً مستديراً بحدٍّ وظلٍّ ثقيل يطفو فوق
 *     المحتوى، **فتأكل من الشاشة مرّتين**: بارتفاعها، وبالتدرّج المعتم
 *     الذي كان تحتها ليفصلها عمّا خلفها. والشريطُ الملتصق يحتاج **خطّاً
 *     واحداً** ليقول أين ينتهي التطبيق ويبدأ النظام.
 *
 *  ٢) **شفافيةٌ وضباب** (طلب أحمد، ولقطته الثالثة مرجعاً): الخلفية
 *     `color-mix` مع الشفاف و`backdrop-blur` — فيُرى أن تحته محتوًى يمرّ،
 *     وهو ما يجعل الشريط جزءاً من الصفحة لا صندوقاً فوقها. **والصنف
 *     الصمّاء تبقى مكتوبةً قبله**: لو لم يفهم متصفّحٌ `color-mix` سقط
 *     السطر المضمَّن وعاد اللون الصمّاء — **ولا شريط شفّافاً بلا ضباب**،
 *     فذاك نصٌّ فوق ملصقات.
 *
 *  ٣) **النقطة الصفراء تحت الاسم ذهبت** (طلب أحمد: «ما يحتاج النقطة
 *     الصفراء اللي تحت، كفاية إذا اخترت الأيقونة يتغير لونها أصفر مثل
 *     الثيم»). **وهو محقّ:** الأيقونةُ الصفراء والاسمُ العريض يقولان
 *     «أنت هنا» مرّتين، والنقطةُ ثالثة. **وإشارةٌ تُكرَّر ثلاثاً تُقرأ
 *     زينةً لا معنى.**
 *
 * **وحلَّ محلّها معنًى مختلف تماماً — نقطةُ «هناك جديدٌ هناك»** على تبويبٍ
 * **لستَ فيه** (`NavSignalDot`). الفرق ليس في الشكل بل في الجملة: تلك
 * كانت تقول ما تعرفه، وهذه تقول ما لا تعرفه.
 */

/**
 * **وجهان لكل تبويب: مفرَّغٌ خامل ومصمتٌ نشط** (D-260، طلبُ أحمد: «هل
 * في أيقونات أفضل؟ والأيقونات تحتاج تكبر»).
 *
 * **والرموزُ لم تُستبدل** — بيتٌ وشريطُ فيلمٍ وبوصلةٌ وناسٌ وعدسة أعرافٌ
 * تُقرأ بلا تعلُّم (D-150)، **وتبديلُ رمزٍ يفهمه المستخدم ليس تحسيناً
 * بل إعادةُ تعليم.** **والذي تغيّر حالتُه:** كان النشطُ نفسَ الخطّ
 * بلونٍ أصفر وسمكٍ أعرضَ بنصف بكسل — **وذلك فرقٌ يُقاس ولا يُرى**،
 * وصار عبئُه أثقلَ يومَ سقطت الكلماتُ من تحته (D-258).
 */
const TABS: {
  href: string;
  key: "home" | "library" | "news" | "search" | "people";
  icon: IconName;
  /** الوجهُ المصمت — يُرسم حين تكون في التبويب */
  iconOn: IconName;
}[] = [
  { href: "/", key: "home", icon: "home", iconOn: "home-filled" },
  /* 🆕 **كتابٌ بشريط تعليمٍ لا شريطَ فيلم** (D-489) — انظر حجّتَه في
     `Icon.tsx`: المكتبةُ ليست أفلاماً وحدَها. */
  { href: "/library", key: "library", icon: "library", iconOn: "library-filled" },
  { href: "/news", key: "news", icon: "compass", iconOn: "compass-filled" },
  { href: "/people", key: "people", icon: "people", iconOn: "people-filled" },
  /* البحث في الطرف: فعلٌ لا وجهةَ تصفّح، والأطراف أسهل ما تصله الإبهام.
     ولأنه فعل، لا يُنقل المستخدم إلى صفحة: ضغطُه يفتح ورقةً بحقلٍ مركَّز
     فتظهر لوحة المفاتيح فوراً. الرابط يبقى مكتوباً لمن فتح `/search`
     برابطٍ مباشر أو بلا جافاسكربت. */
  { href: "/search", key: "search", icon: "search", iconOn: "search-filled" },
];

export function BottomNav({
  locale,
  signedIn = true,
}: {
  locale: Locale;
  signedIn?: boolean;
}) {
  const pathname = usePathname();
  const t = getDict(locale);
  /* تسخينُ الوجهة لحظةَ النيّة (جولة ٢٠ أغسطس، بديل موجات D-483):
     `touchstart` يسبق الضغطة بنحو ١٠٠م.ث — تكفي لبدء جلب الوجهة
     المقصودة وحدها بدل تسخين الخمس جميعاً عند الفتح */
  const prewarm = usePrefetchOnIntent();
  /* والوجهةُ المرجَّحة الوحيدة — المكتبة — تُسخَّن كاملةً من الرؤية لا
     من النيّة (`prefetch={true}` على رابطها): قيس على المنشور أن
     `router.prefetch` اليدويّ يُدمج مع مدخل الـLink الهيكليّ فلا يجلب
     شيئاً، وطريقُ الجلب الكامل الذي يعمل فعلاً هو الـLink نفسه.
     وموفّرُ البيانات وشبكات 2G تُحترمان كما في الموجات القديمة. */
  const conn = (
    typeof navigator === "undefined"
      ? undefined
      : (navigator as Navigator & {
          connection?: { saveData?: boolean; effectiveType?: string };
        }).connection
  );
  const fullOk = !conn?.saveData && !(conn?.effectiveType ?? "").includes("2g");
  /* 🔴 🆕 **والشريطُ يغيب ما دام الكيبوردُ مفتوحاً** (D-359، بلاغُ أحمد:
     «إذا ضغطت على خيار رد ليه يطلع الدوك الي تحت الى فوق الكيبورد»).

     **عناصرُ `fixed` مربوطةٌ بنافذة التخطيط، والكيبوردُ يقصّ النافذة
     المرئيّة** — فيرتفع الشريطُ فوق الكيبورد ويجلس بين إصبعك وما تكتبه.
     **وخمسُ وجهاتِ سفرٍ لن تُضغط وأنت تكتب** (D-138 من جهتها الثانية:
     أداةٌ تُرى ولا تُستعمل الآن تُزاحم ما يُستعمل) — **والمساحةُ فوق
     الكيبورد ملكُ صندوق الكتابة وأدواته وحدَهما.**

     ⚠️ **والغيابُ رسمٌ لا حذفٌ للحالة**: `hidden` يُبقي الخطّافات
     وترتيبَها كما هي (نفسُ حجّة السطر أعلاه)، **ويعود الشريطُ بإغلاق
     الكيبورد بلا رحلةٍ ولا إعادةِ رسمٍ للصفحة.** */
  const kbOpen = useKeyboardOpen();
  /* الزائر غير المسجّل لا شريط له (D-122): تبويباته الخمسة كلها خلف
     تسجيل الدخول، فكلّ ضغطةٍ فيها تردّه — وهو فوق صفحة هبوطٍ تعرّفه
     بالمنتج، لا داخل تطبيقٍ يتنقّل فيه */
  if (!signedIn) return null;
  // شاشات مركّزة: لا شريط تبويبات يزاحم زر الإجراء
  /* 🆕 **ويغيب في الإعدادات** (D-462، مواصفةُ أحمد): **الإعداداتُ رحلةٌ
     لها بدايةٌ ونهاية** — دخلتَ لتضبط شيئاً وتخرج — **وشريطُ تنقّلٍ
     بخمسة أبوابٍ أسفلها يدعوك إلى مغادرتها قبل أن تُتمّها.**
     **وبابُ الخروج واحدٌ: سهمُ الرجوع في ترويستها.** */
  if (
    pathname === "/login" ||
    pathname === "/welcome" ||
    pathname === "/profile/edit" ||
    pathname.startsWith("/profile/settings")
  )
    return null;

  const label: Record<string, string> = {
    home: t.navHome,
    library: t.navLibrary,
    news: t.navNews,
    search: t.navSearch,
    people: t.navPeople,
  };

  /* صفحات التفاصيل والسجلّ تُنسب إلى المكتبة: المستخدم في عمق التطبيق
     يحتاج مرساةً — تبويبٌ لا يضيء يقرأ وكأن الشريط تعطّل */
  /* 🆕 **و`/activity` وجهةٌ داخل المكتبة** (D-537) — **والمرساةُ تضيء
   حيث جاء القارئُ منه**؛ و`/diary` يبقى لأنه تحويلةٌ حيّة. */
const LIBRARY_PREFIXES = ["/library", "/show/", "/movie/", "/stats", "/activity", "/diary", "/lists", "/ratings"];
  // للبحث تبويبه الآن، فصفحته لم تعد تُنسب إلى «اكتشف»
  const isActive = (href: string) =>
    href === "/"
      ? pathname === "/"
      : href === "/library"
        ? LIBRARY_PREFIXES.some((p) => pathname.startsWith(p))
        : pathname.startsWith(href);

  return (
    <>
      <nav
        aria-label={t.navHome}
        /* الصنف الصمّاء أوّلاً وهي الاحتياط: السطر المضمَّن أدناه يغلبها
           حيث يُفهم، ويسقط كاملاً حيث لا يُفهم فيبقى اللون الصمّاء */
        /* **والارتفاعُ نزل** (D-259، بلاغُ أحمد بلقطةٍ معلَّمة: «الشريط
           اللي تحت ارتفاعه عالي»). **والعلّةُ كانت في الحشو السفليّ لا في
           الأيقونة**: `max(0.5rem, env(safe-area-inset-bottom))` تعني على
           الآيفون **٣٤px حشواً كاملاً تحت الأيقونات** — وهو ارتفاعُ شريط
           الإيماءة نفسِه. **والشريطُ لا يحتاج أن يقف فوقه، يحتاج ألّا
           يُدفن تحته**: نصفُه يكفي فاصلاً، **والخلفيّةُ تمتدّ تحته كما
           كانت** فلا يظهر شقٌّ.
           ⚠️ **والأرضيةُ تبقى** (`0.375rem`) للمتصفّح حيث `env` صفر —
           **وشريطٌ ملاصقٌ للحافّة بلا هامشٍ يُقرأ مقصوصاً.** */
        className={`${
          kbOpen ? "hidden" : "grid"
        } chrome-bottom md:hidden fixed bottom-0 inset-x-0 z-40 grid-cols-5 rounded-t-[22px] border-t border-[color:var(--divider)] bg-[color:var(--background)] backdrop-blur-xl pt-2.5 pb-[max(0.5rem,calc(env(safe-area-inset-bottom)*0.5))]`}
        style={{ background: "color-mix(in srgb, var(--background) 76%, transparent)" }}
      >
        {TABS.map(({ href, key, icon, iconOn }) => {
          const active = isActive(href);
          /* **أيقونةٌ بلا كلمة** (D-258، طلبُ أحمد: «اخفِ الكلمات في
             الشريط السفلي واكتبها فوق»). **والاسمُ لم يُحذف بل انتقل**
             إلى منتصف الشريط العلويّ (`NavTitle`) — انظر حجّتَه هناك.

             **والمقاسُ ٢٥ لا ٢٣**: الأيقونةُ صارت وحدَها تحمل الخانة،
             **ورمزٌ ورث فراغَ كلمةٍ يجب أن يرث شيئاً من ثقلها** — وإلا
             قُرئ الشريطُ أنحفَ لا أنظف.

             ⚠️ **والاسمُ يبقى في `aria-label`**: ما سقط رسمُه لا يسقط
             نطقُه — **وشريطُ تنقّلٍ بخمسة أزرارٍ بلا أسماء لقارئ الشاشة
             خمسةُ أزرارٍ بلا معنى** (D-177: الرمزُ عُرفٌ يُقرأ بالعين،
             والمعنى في `aria-label`). */
          const face = (
            <span className="relative flex flex-col items-center gap-1">
              {/* **٢٨ لا ٢٥** (D-260، بلاغُ أحمد: «الأيقونات تحتاج تكبر»):
                  **الخانةُ خُمسُ الشاشة والرمزُ وحده فيها** بعد سقوط
                  الكلمة — **ورمزٌ يرث خانةً كاملة يرث حجمَها.**
                  **والمصمتُ لا يحتاج سُمكاً**: `strokeWidth` للمفرَّغ
                  وحده، **وسمكٌ على شكلٍ مصمتٍ يزيده وزناً بلا معنى.** */}
              <Icon
                name={active ? iconOn : icon}
                size={24}
                strokeWidth={1.8}
                style={{ color: active ? "var(--accent)" : "var(--disabled)" }}
              />
              {/* ⚖️ 🆕 **والكلمةُ عادت تحت الرمز** (D-436، تصاميمُ أحمد
                  الجديدة تكتبها في التبويبات الخمسة). **ونقضٌ مسجَّلٌ
                  لـD-258** الذي نقلها إلى منتصف الشريط العلويّ:
                  **وعنوانُ الشريط العلويّ يقول أين أنت، والكلمةُ هنا تقول
                  إلى أين تذهب** — **وهما سؤالان لا سؤالٌ واحدٌ مكرَّر.**
                  **والرمزُ وحدَه يفترض عُرفاً محفوظاً**، وخمسةُ رموزٍ بلا
                  أسماء تُقرأ صحيحةً بعد التعوّد لا قبله (D-177).
                  **والارتفاعُ لم يزد**: الرمزُ نزل من ٢٨ إلى ٢٤ فابتلع
                  السطرُ فرقَه. */}
              <span
                className="text-[10px] font-semibold leading-none truncate max-w-full"
                style={{ color: active ? "var(--accent)" : "var(--disabled)" }}
              >
                {label[key]}
              </span>
              {/* ⚖️ 🆕 **وسقطت نقطةُ «جديد» من هنا** (D-392، حكمُ أحمد):
                  **رقمُها كان رقمَ الجرس** (`unread_signals`) — **فتشير
                  إلى تبويبٍ ليس فيه الخبر**، ولا تختفي إلّا بفتح الجرس.
                  **ورمزان لخبرٍ واحدٍ في شريطٍ واحد تكرار** (القاعدة ٦)،
                  **والجرسُ يحمل شارةً رقميّةً أصلاً** فهي أدقُّ منها. */}
            </span>
          );
          /* **والحشوُ يعوّض ارتفاعَ السطر الذاهب**: هدفُ اللمس ٤٤px
             (D-033/D-168) **لا يُشترى بالنصّ بل يُكتب** — وخانةٌ بلا
             كلمةٍ وبحشوها القديم تصير ٣٤px.
             **و`py-2` لا `py-2.5`** (D-259): ٢٨+١٦ = ٤٤px بالضبط
             (D-260 بعد أن كبرت الأيقونة) — **والهدفُ اكتمل في الخانة
             نفسِها، فلم يعد يستعير من حشو الشريط.** */
          /* 🆕 **والشريطُ أعلى ١٥٪** (D-489، طلبُ أحمد: «زِد ارتفاعها
             ١٥٪»). **والزيادةُ حشوٌ لا رمزٌ أكبر**: الرمزُ ٢٤ والكلمةُ
             ١٠ كما ضُبطا بالقياس (D-260/D-436) — **وتكبيرُهما يغيّر
             الإيقاع، وتوسيعُ الحشو يغيّر المساحةَ وحدَها.**
             **المقيسُ في المتصفّح: ٦٢px → ٧٢px** (‏+١٦٪) — ٤ من حشو
             الشريط العلويّ، و٤ من حشو الخانة، و٢ من أرضيّة السفليّ.
             ⚠️ **وذيلُ `main` كبر معه** في التخطيط، **وإلّا اختفى آخرُ
             عنصرٍ خلف الشريط.** */
          const face_cls =
            "relative flex min-w-0 items-center justify-center rounded-2xl px-1 py-2 transition active:bg-surface-2";

          /* ⚖️ 🆕 **والخمسُ وجهاتٌ اليوم** (D-534) — كان البحثُ زرّاً
             يفتح ورقةً، **فصار رابطاً كأخواته**: خانةٌ تفتح حواراً
             وخانةٌ تنتقل في شريطٍ واحد تعلّمان الإصبعَ قاعدتين
             (D-150). **والرجوعُ من الصفحة يعيدك إلى حيث كنت.** */
          return (
            <Link
              key={href}
              href={href}
              /* 🔴 **`false` صريحةٌ لا `undefined`** (تصحيحُ تسريبِ D-510):
                 `undefined` تعني `auto` — **وNext يسخّن عندها من الرؤية
                 في الإنتاج**، فكانت قشرةُ التطبيق الدائمةُ تجلب
                 `/news` و`/people` وغيرَهما لكلِّ مستخدمٍ لم يقصدها،
                 **وهو نقضُ قاعدة D-510 نفسِها** («لا يدفع مستخدمٌ كلفةَ
                 صفحةٍ لن يفتحها»). و`false` وحدَها توقف الرؤيةَ والحومان
                 معاً (توثيقُ Next 16.3). **والمكتبةُ تبقى الاستثناءَ
                 المقصود**، ومعها صمّامُ save-data/2G كما كان.
                 ⚖️ **والنيّةُ لا تتأثّر**: `usePrefetchOnIntent` أدناه
                 نداءٌ إجرائيٌّ مستقلٌّ عن هذه الراية — **قِيس بعد النشر
                 أنه ما زال يعمل** (حومانٌ على وجهةٍ غيرِ مقصودةٍ سابقاً
                 أخرج حمولةً كاملة).
                 🔴 **وتصحيحُ ادّعاءٍ سابق**: كُتب هنا أن الرايةَ تحرّر
                 النيّةَ «من ازدواجٍ مقيس» — **وهذا لم يثبت**. الطلبان
                 اللذان يظهران عند النيّة (شجرةٌ صغيرة ثم حمولةٌ كاملة)
                 **مرحلتان من `router.prefetch` نفسِه**، لا طلبُ Link
                 وطلبُ نيّةٍ متكرّران: رابطٌ عليه `prefetch={false}` بلا
                 نيّةٍ أصلاً أخرج **صفرَ طلبات**. **فالتغييرُ يوقف تسخينَ
                 الرؤية، ولا يُنسب إليه إلغاءُ ازدواج.** */
              prefetch={key === "library" && fullOk}
              aria-current={active ? "page" : undefined}
              aria-label={label[key]}
              title={label[key]}
              className={face_cls}
              /* ⚖️ 🆕 **والنيّةُ صارت للمؤشّر وحدَه** (جولة ٢٢ أغسطس،
                 بلاغُ أحمد: «رمشةٌ عند التنقّل بين التبويبات»).

                 **العلّةُ أن نافذةَ النيّة على اللمس أقصرُ من التسخين
                 نفسِه**: `touchstart` يسبق الضغطةَ بنحو ١١٠م.ث،
                 **و`router.prefetch` يمرّ بمرحلتين متسلسلتين** — شجرةٌ
                 صغيرةٌ (~٠٫٢ ك.ب) ثمّ الحمولةُ الكاملة، **والثانيةُ لا
                 تبدأ قبل أن تنتهي الأولى.** فتصل الضغطةُ والمرحلةُ
                 الأولى ما زالت في الطريق، **فتنتظرها الملاحةُ ثمّ تنتظر
                 الثانية** — 📏 **قِيس على الإنتاج**: نقرةٌ عارية = طلبٌ
                 واحد وأوّلُ رسمٍ عند ١٧٤م.ث · ونقرةٌ بنيّةِ لمس =
                 **طلبان** وأوّلُ رسمٍ عند **٤٠٨م.ث**. **فالتسخينُ على
                 اللمس ضريبةٌ لا مكسب.**

                 🔑 **ولا يكفي حذفُ `onTouchStart` وحدَه**: أحداثُ
                 المؤشّر تُطلَق للّمس أيضاً — `pointerover`/`pointerenter`
                 يسبقان `pointerdown` على أجهزة اللمس المباشر — **فبقاءُ
                 `onPointerEnter` عارياً يُبقي الطلبَ الزائد كما هو.**
                 **فالحارسُ نوعُ المؤشّر لا اسمُ الحدث**، ويبقى الفأرُ
                 والقلمُ (حومانٌ حقيقيٌّ يسبق الضغطةَ بمئات الملّي ثانية)
                 والكيبوردُ (`onFocus`) على ما كانوا. */
              onPointerEnter={(e) => {
                if (e.pointerType !== "touch") prewarm(href);
              }}
              onFocus={() => prewarm(href)}
            >
              {face}
            </Link>
          );
        })}
      </nav>

    </>
  );
}
