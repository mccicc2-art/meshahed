import { useEffect, useLayoutEffect } from "react";

/**
 * **تأثيرٌ يقع قبل الرسم — والفرقُ هنا هو الرمشةُ كلُّها** (D-295 · D-522).
 *
 * `useLayoutEffect` **يُنفَّذ بعد تحديث الـDOM وقبل أن يرسم المتصفّح
 * إطاراً**، و`useEffect` **يُنفَّذ بعد الرسم** — **فإطارٌ واحدٌ يُرى
 * بينهما.** **وكلُّ «رمشةٍ» في هذا التطبيق حتى اليوم كانت ذلك الإطار:**
 * حالةٌ بصريّةٌ تُكتب في التزام، وحالةٌ منطقيّةٌ تُصحَّح في التزامٍ تاليه.
 *
 * ⚠️ **والاسمُ المستعار لأن مكوّناتِ العميل تُرسم على الخادم أيضاً**:
 * React تحذّر من `useLayoutEffect` في SSR، **والحارسُ ثابتٌ على مستوى
 * الوحدة فلا يتبدّل الخطّافُ بين رسمتين** (وهو شرطُ الخطّافات).
 *
 * 🆕 **وصار باباً واحداً لا نسخةً في كلِّ ملفّ** (D-524): كان معرَّفاً
 * داخل `TabPager` وحدَه، **ثمّ احتاجه `TabUnderline` للعلّة نفسِها** —
 * **ونسختان من حارسٍ واحدٍ تفترقان يوماً** (القاعدة ٣).
 */
export const useBeforePaint = typeof window === "undefined" ? useEffect : useLayoutEffect;
