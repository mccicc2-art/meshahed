"use client";

/**
 * بوّابةُ Loopz+ (D-633).
 *
 * **نفسُ عمارة بوّابة الزائر** (D-627): حدثُ نافذةٍ ومضيفٌ واحدٌ في
 * التخطيط — **لا سياقَ React ولا مكوّنَ ورقةٍ ثانٍ.** يعمل من أيِّ
 * مكوّنٍ مهما عمق موضعُه.
 *
 * ⚖️ **والبوّابةُ تجربةٌ لا حراسة**: الحراسةُ الحقيقيّةُ في الخادم عند
 * الحفظ — **وواجهةٌ تقفل بلا خادمٍ يقفل زينةٌ تُفتح بأدوات المطوّر.**
 */

export const PLUS_GATE_EVENT = "loopz:plus-gate";

/** افتح ورقةَ Loopz+ — يستقبلها `PlusGateHost` المركَّب في التخطيط */
export function openPlusGate() {
  try {
    window.dispatchEvent(new Event(PLUS_GATE_EVENT));
  } catch {
    /* لا شيء — الورقةُ تحسينٌ والخادمُ يحرس على كلِّ حال */
  }
}
