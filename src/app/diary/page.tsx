import { redirect } from "next/navigation";

/**
 * ⚖️ **السجلُّ الزمنيُّ صار «النشاط»** (D-537، قرارُ أحمد): **اليومياتُ
 * كانت تعرض المشاهدةَ وحدَها**، **والنشاطُ يعرض الأربعة** — **وبابان
 * يعرضان الرحلةَ نفسَها بشكلين هو ما تمنعه القاعدة ٦.**
 *
 * **والمسارُ يبقى تحويلةً لا يُحذف**: الرئيسيةُ تشير إليه في موضعين،
 * و`revalidatePath("/diary")` مكتوبةٌ في فعلين، **وروابطُ من فتحوه
 * محفوظةٌ في تاريخ متصفّحاتهم** — **ورابطٌ ميّتٌ أسوأ من تحويلة**
 * (D-181). **و`permanentRedirect` لا `redirect`**: الوجهةُ لن تعود.
 */
export default function DiaryPage() {
  redirect("/activity");
}
