-- ============================================================
-- 168 — إسقاطُ `weekly_top_now` بعد آخرِ قارئ (D-838 · D-214)
-- ============================================================
-- **تُشغَّل بعد أن يصير `eb2907b`+ حيّاً بالشيفرة التي لا تناديها** —
-- **ودالّةٌ تُسقَط قبل أن يسقط مستدعيها انقطاعُ خدمة** (D-028 مطبَّقةً
-- على القاعدة).
--
-- **حلّت محلَّها `weekly_top_of(uid)`** (الهجرة ١٦٧): تلك ترجع أسبوعاً
-- واحداً لكلِّ الناس، وهذه سجلَّ حسابٍ واحدٍ كاملاً — **والشارةُ صارت
-- تصف حساباً لا أسبوعاً** (D-838).
--
-- ⚠️ **ولا بديلَ يُترك «للاحتياط»**: **دالّةٌ بلا قارئٍ تُقرأ عقداً
-- قائماً** (D-214)، **ومن أرادها بعد اليوم يجدها في الهجرة ١٦٦.**
-- ============================================================

drop function if exists public.weekly_top_now();

-- فحصُ صحّة:
--   select proname from pg_proc where proname like 'weekly_top%';  -- weekly_top_of وحدَها
