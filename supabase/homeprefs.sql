-- ============================================================
--  Loopz — تفضيلات الصفحة الرئيسية (التخصيص)
--  شغّله في Supabase → SQL Editor مرّةً واحدة
-- ============================================================

-- عمود JSON واحد لا عشرة أعمدة منطقية: التفضيلات ستتغيّر وتتّسع، وكل
-- خيارٍ جديد بعمودٍ جديد يعني ملفّ SQL جديداً تشغّله يدوياً في كل مرّة.
-- والقيمة الفارغة تعني «الافتراضي» — فالمستخدم الذي لم يخصّص لا يحمل
-- شيئاً.
alter table public.profiles
  add column if not exists home_prefs jsonb;
