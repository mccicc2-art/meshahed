# ملفات قاعدة البيانات — ترتيب التشغيل

كل ملف يُشغَّل في Supabase → SQL Editor مرة واحدة، **بهذا الترتيب حصراً**.
إعادة تشغيل أي ملف آمنة (idempotent)، لكن `security.sql` و `security2.sql`
هما المصدر الوحيد لسياسات القراءة ودوال العرض — لا تعيدوا إنشاءها في غيرهما.

| # | الملف | ماذا يفعل |
|---|---|---|
| 01 | `schema.sql` | الجداول الأساسية: المتابعات والمشاهدات + RLS |
| 02 | `profile.sql` | البروفايل + مخزن الصور |
| 03 | `username.sql` | عمود المعرّف @handle (توليد عشوائي) |
| 04 | `appearance.sql` | الغلاف والثيم واللغة |
| 05 | `homeprefs.sql` | تفضيلات الرئيسية |
| 06 | `show_stats.sql` | تخزين إحصاءات المسلسل مع صف المتابعة |
| 07 | `storage_limits.sql` | حدّ حجم/نوع الصور على الخادم |
| 08 | `progress.sql` | موضع التوقف في الأفلام |
| 09 | `social.sql` | التقييمات (كتابة فقط) + متابعة المستخدمين |
| 10 | `social2.sql` | إخفاء الاسم + زوّار الملف |
| 11 | `news.sql` | تفاعلات الأخبار (كتابة فقط) |
| 12 | `likes.sql` | إعجابات المراجعات (كتابة فقط) |
| 13 | `lists.sql` | القوائم الشخصية |
| 14 | `performance.sql` | فهرس «آخر ما شاهدت» |
| 15 | `leaderboard.sql` | فهارس اللوحة + الأكثر مشاهدة |
| 16 | `rewatch.sql` | إعادة المشاهدة + **watch_summary القانونية** |
| 17 | `public_profiles.sql` | إغلاق قراءة جدول الملفات |
| 18 | `security.sql` | **حزمة الأمن**: كل سياسات القراءة ودوال definer + حذف الحساب |
| 19 | `security2.sql` | فهارس الصفحات الساخنة + إخفاء المعرّف + إحصاءات الملف العام |
| 20 | `lists2.sql` · `lists3.sql` · `lists4.sql` | نوع القائمة والترتيب، السطر الشارح، بطاقات الغلاف والعدّادات |
| 21 | `image_positions.sql` | تموضع الغلاف والصورة الشخصية |
| 22 | `search_people_v2.sql` | بحث الأشخاص (مستخدمو التطبيق) |

## ✅ شُغِّلت كلها في الإنتاج — ٨ أغسطس ٢٠٢٦

| # | الملف | ماذا أضاف | حالة الواجهة |
|---|---|---|---|
| 23 | `profile_bio.sql` | عمود `bio` + إظهاره في `public_profiles` تابعاً لإخفاء الاسم | **مشحونة** |
| 24 | `review_reports.sql` | جدول البلاغات + `ratings.hidden` + مُشغِّل الإخفاء عند البلاغ العاشر | **مشحونة** |
| 25 | `referrals.sql` | رموز الدعوة والدعوات المحقَّقة + أربع دوال definer | لم تُبنَ بعد |
| 26 | `shares.sql` | `title_shares` + `share_replies` + `are_mutual` + `unread_shares` | لم تُبنَ بعد |
| 27 | `community_feed.sql` | `community_activity()` — خطّ آراء الجميع؛ عليه تقوم رقاقة «الكل» في تبويب «الأعمال» (D-187) | **RUN 12 Aug** — مُتحقَّق: `definer` · `execute` لـ`authenticated` وحدها · `ratings_updated_idx` · **والسياساتُ المفتوحة أربعٌ ولا `public.ratings` فيها** |
| 28 | `person_follows.sql` | متابعة الفنانين | **مشحونة** |
| 29 | `profile_visibility.sql` | **أُعيد كتابته ٩ أغسطس:** الحساب الخاص يخفي محتواه — `can_view_profile` + بوّابة دوال الملف الخمس + `is_private` في العرض. ⚠️ يُشغَّل **بعد** ٢٤ و٣٢ رغم رقمه | **مشحونة** |
| 30 | `security3.sql` | إغلاق قراءة `follows` و`watched_*` | لا واجهة لها |
| 31 | `blocks.sql` | جدول الحظر + `is_blocked` مطويّة داخل `are_mutual` + `block_user` + `my_blocks` | **مشحونة** |
| 32 | `follow_requests.sql` | `profiles.is_private` + طلبات المتابعة + `request_or_follow` وقبولٌ وإزالةُ متابِع | **مشحونة** |
| 33 | `dismissed_titles.sql` | جدول «غير مهتم» لاستبعاد أعمالٍ من «مقترح لك» | **مشحونة** |
| 34 | `communities.sql` | المجتمعات: غرفٌ بعضويةٍ ودردشة — دليلها مقروءٌ للجميع عمداً | **مشحونة** |
| 35 | `communities2.sql` | إصلاح تكرار RLS: فحص العضوية عبر `is_community_member` (definer) | **مشحونة** |
| 36 | `user_reports.sql` | الإبلاغ عن حساب — شقيق `review_reports` بلا إخفاءٍ تلقائيّ | **مشحونة** |
| 37 | `list_shares.sql` | مشاركة قائمةٍ لصديق في الرسائل (صفٌّ منظَّم، D-051) + `unread_shares` يشمل القوائم | **مشحونة** |
| 38 | `list_saves.sql` | حفظ قائمة غيرك مرجعاً حيّاً — «أضِفها إلى قوائمي» | **مشحونة** |
| 39 | `realtime_messages.sql` | publication الرسائل الأربعة للـ Realtime (فورية المحادثات) | **مشحونة** |
| 40 | `library_grants.sql` | «من يرى مكتبتي»: منحة فردية تُطوى في `can_view_profile` (يستبدل نسخة 29) | **مشحونة** |
| 41 | `community_photo.sql` | صورة المجتمع: `photo_url` + إرجاعها في دالتَي الدليل | **مشحونة** |
| 42 | `community_invites.sql` | دعوات المجتمعات: جدول + قبولٌ definer يمرّ بوابة الخصوصية | **مشحونة** |
| 43 | `follow_privacy.sql` | `hide_follow_lists` + العرض بذيله الجديد + دالتا أسماء المتابعة (definer) | **مشحونة** |
| 44 | `imdb_ratings.sql` | مخزن تقييمات IMDb عبر النشرات + `set_imdb_ratings` definer (D-113) | **مشحونة** |
| 45 | `activity_v2.sql` | `following_activity_v2`: خطّ النشاط بأربعة أنواع، صفٌّ واحد لكل (شخص+عمل+يوم) + أربعة فهارس زمنية (D-123) | **مشحونة** |
| 46 | `activity_likes.sql` | إعجابٌ بحدث مشاهدة: جدول بمفتاحٍ فيه يوم + `feed_activity_likes` definer (D-124) | **مشحونة** |
| 47 | `notifications.sql` | جرس الإشعارات بلا جدول: عمود `notif_seen_at` + `my_signals`/`unread_signals`/`mark_signals_seen` (D-125) | **مشحونة** |
| 48 | `social_discovery.sql` | `people_to_follow` (اقتراح متابعةٍ بتقاطع الذوق) + `title_circle` (نشاط دائرتك في صفحة العمل، مكتومٌ تحت ٣) + فهرسان (D-126/D-127) | **مشحونة** |
| 49 | `imdb_votes.sql` | `imdb_ratings.imdb_votes` + `set_imdb_ratings` تحفظه — شرطُ ترتيب IMDb البايزيّ وحاجزِ الأهليّة (D-132) | **مشحونة** |
| 50 | `imdb_chart.sql` | `imdb_pool` + `imdb_chart` + `set_imdb_pool`/`build_imdb_chart` — قائمة IMDb الحقيقية من ملفّاتها المفتوحة (D-135). ⚠️ **سياسة قراءةٍ مفتوحة رابعة** | **مشحونة** |
| 51 | `profile_prefs.sql` | `profiles.profile_prefs` + إظهاره في العرض العام + `profile_artists` definer — تخصيص البروفايل (D-129) | **مشحونة** |
| 52 | `episode_ratings.sql` | `episode_ratings` + `set_episode_rating`/`clear_episode_rating` definer — التقييم يعلّم المشاهدة داخل القاعدة (D-139) | **مشحونة** |
| 53 | `title_communities.sql` | غرف الأعمال: `kind`/`tmdb_id`/`media_type`/`archived_at`، فكُّ فرادة `owner_id` بفهرسٍ جزئيّ، `title_community`/`title_room_of`/`title_rooms`/`maintain_title_communities` + قراءةُ غرفة العمل بلا انضمام (D-140) | **مشحونة** |
| 54 | `title_art.sql` | أغلفة وبوسترات شخصية: `title_art` بـRLS للمالك + `profile_title_art` definer خلف `can_view_profile` (D-131) | **مشحونة** |
| 55 | `favorites.sql` | المفضّلات بلا جدول جديد: `kind='favorites'` + فهرسٌ فريد جزئيّ + `toggle_favorite`/`my_favorites` (D-130) | **مشحونة** |
| 56 | `account_deletion.sql` | حذف الحساب يحذف الحساب فعلاً: `delete_my_account` صارت **صفّاً واحداً** يحذف من `auth.users` فيجرّ الأربعة والأربعين مفتاحاً cascade — بدل قائمةٍ من أحد عشر جدولاً بقيت وراءها الرسائل والمجتمعات والحظر والأغلفة (D-146). **⚠️ نسخة `security.sql` §٦ حُدّثت لتطابقها** | **مشحونة** |
| 57 | `feed_seen.sql` | الجديد يعلو مرّةً ثم ينزل: `profiles.feed_seen_at` + `my_feed_seen`/`mark_feed_seen` — بلا جدولِ «مقروء» لكل صفّ (D-149). تحقُّق: `col=1 · funcs=2 · open_policies=4` | **مشحونة** |
| 58 | `feed_new_count.sql` | عدّادُ «وصل جديد وأنت جالس»: `new_feed_count()` definer فوق `following_activity_v2` — رقمٌ لا أسطر (D-151) | **معلّقة** |
| 59 | `profile_favorites.sql` | «مفضّلاتي» قسماً في البروفايل: `profile_favorites(uuid)` definer خلف `can_view_profile` — نمط `profile_title_art` نفسه، بلا جدولٍ وبلا سياسة (D-152) | **معلّقة** |
| 60 | `imdb_pool_flags.sql` | علَما البِركة: `is_doc` (يُلغي ~٨٠ نداء TMDB لكل رفّ «أفضل ٥٠»، D-165) + **رفعُ شرط الأفلام عن `is_anime` في `build_imdb_chart`** فيصير لأفلام الأنمي صنفُها (D-169/D-170) | **شُغّلت ١٢ أغسطس** |
| 61 | `follows_anime.sql` | `follows.is_anime` — علَمٌ يقبل `null` («لم يُصنَّف بعد»)، عليه يقوم تبويبُ الأنمي في المكتبة (D-182) | **RUN 12 Aug** |
| 62 | `review_replies.sql` | **الردودُ على الآراء** (D-193): جدولُ `review_replies` بعمقٍ واحد · `title_replies()` تقرأ باحترام الإخفاء والحظر · `title_talk_stats()` لعدّادَي الردود والمشاهدين · و`reply_reports` بعتبة العشرة نفسها. **لا سياسة قراءةٍ مفتوحة — تبقى أربعاً** | **شُغّلت ١٢ أغسطس** |
| 63 | `list_cover.sql` | **غلافُ القائمة** (D-208): `cover_tmdb_id` + `cover_media_type` + `cover_backdrop` على `user_lists` بقيدَي نوعٍ ومسار · و`my_lists`/`public_list` تُعيدان الغلاف — **الملصقاتُ تبقى، والغلافُ يزيد ولا يستبدل**. لا جدولَ ولا سياسة: الأعمدة ترث سياسات `user_lists` الأربع | **معلّقة** |
| 64 | `news_items.sql` | **الأخبار الحقيقية** (D-209): جدولُ `news_items` بمفتاحِ الرابط · **بلا سياسةٍ واحدة** والبابانِ definer (`news_feed` بلغة الواجهة · `set_news_items` تتحقّق من النطاق والمصدر وتفرض برودةَ خمس دقائق و`on conflict do nothing` وسقفَ ألف خبر) · و`news_host_ok`/`news_is_stale`/`news_last_at`. **لا صورَ ولا سياسةَ قراءةٍ مفتوحة — تبقى أربعاً** | **شُغّلت ١٢ أغسطس** |
| 65 | `news_posts.sql` | **أخبارُنا نحن** (D-211): `title_snapshots` (لقطةُ كل عملٍ نراقبه) + `news_posts` (**حقائقُ لا جُمَل** — الجملةُ تُركَّب من قوالب `i18n` فيُقرأ الخبرُ بلغتين بلا عمودٍ ثانٍ) · و`news_watch_slice` تختار الشريحةَ في القاعدة (أقدمُ لقطةً أوّلاً) · و`set_*`/`loopz_news`/`news_gen_stale`. **بلا سياسةٍ واحدة على الجدولين — تبقى أربعاً** | **شُغّلت ١٢ أغسطس** |
| 66 | `news_heavier.sql` | **أخبارٌ أثقل** (D-212، بلاغُ أحمد: «كلّها موعد نزول الحلقة القادمة وهو أصلاً في Upcoming»): **حذفُ نوع `episode` صفوفاً وقيداً** · وأربعةُ حقولٍ في اللقطة (`last_air_date` · `next_season_date` · `next_season_num` · `theatrical_date`) · وثلاثةُ أنواعٍ ثقيلة: `season_date` · `theatrical` · `released` | **شُغّلت ١٢ أغسطس** |
| 67 | `news_reports.sql` | **أخبارٌ أوسع وخبرٌ بنسبةٍ لمصدره** (D-213): `chart_rank` و`providers` في اللقطة · وثلاثةُ أنواع: `chart` (**بلا نداء**) · `provider` · **`report`** (الحدثُ من الصحافة والجملةُ من عندنا) · **وحارسا `report` في القاعدة**: `event` من قائمةٍ مغلقة، **والرابطُ يمرّ على `news_host_ok`** · و`news_watch_slice` تحمل الرتبة معها | **شُغّلت ١٢ أغسطس** |
| 68 | `news_items_drop.sql` | **إزالةُ الأخبار المجمَّعة** (D-214، «نعم احذفها»): حذفُ `news_items` ودوالِّه الثلاث — **بلا مستهلكٍ بعد D-211→D-213**. ⚠️ **و`news_host_ok` تبقى**: صارت حارسَ روابط `report` في `set_news_posts` | **شُغّلت ١٢ أغسطس** |
| 69 | `news_cadence.sql` | **إيقاعُ الأخبار** (D-215، سؤالُ أحمد «متى تُكتب؟»): `news_watch_slice` صارت **تختار المستحقَّ لا الأقدم** — **الحيُّ كلَّ ٦ ساعات والميتُ كلَّ ٧ أيام** (مسلسلٌ عائد/قيد إنتاج · موسمٌ قادم · فيلمٌ لم يصدر) · و`news_gen_stale` تسأل «هل يوجد مستحقّ؟» لا «هل أقدمُ لقطةٍ قديمة؟» | **معلّقة — لوحةُ Supabase لم تُفتح في الجلسة** |
| 70 | `talk_counters.sql` | **عدّادُ المفضّلة على بطاقة العمل** (D-216، طلبُ أحمد «عدد المشاهدة والتعليقات والمقيّمين والمفضّلة»): `title_talk_stats` تُرجع عموداً خامساً `favorites` — عدُّ `user_list_items` المرتبطة بقائمةٍ `kind='favorites'` (هجرة ٥٥). **و«المقيّمون» ليسوا هنا عمداً**: مقامُ نجمةِ البطاقة يُحسب في `groupByWork` من نفس الآراء المعروضة — بسطٌ ومقامٌ من قومين رقمٌ غلط | **معلّقة — لوحةُ Supabase لم تُفتح في الجلسة** |
| 71 | `reply_signals.sql` | **الردُّ يصل صاحبه** (D-218): مصدرٌ خامس في `my_signals` اسمُه `reply` — **ردٌّ على رأيك أو على ردّك**، بنوعٍ واحد لأن الجملة واحدة. **ولا جدولَ ولا كتابة** (مبدأ D-125): الإشارةُ في `review_replies` أصلاً والجرسُ يقرؤها. قيودٌ ثلاثة: **لا يُشعِر نفسَه** · المخفيُّ لا يُشعِر · والمحظورُ لا يُشعِر. ومعها فهرسان (`review_replies_to_me_idx` · `review_replies_parent_idx`) | **معلّقة — لوحةُ Supabase لم تُفتح في الجلسة** |
| 72 | `public_reads.sql` | **تثبيتُ قراءةِ غرفة الكلام بلا حساب** (D-221). ⚠️ **وهي تثبيتٌ لا فتح:** الفحصُ الحيُّ أثبت أن `anon` تملك التنفيذ أصلاً من صلاحيات Supabase الافتراضية — **والصفحةُ تعمل بدونها**. تبقى لأن **حقّاً موروثاً من إعدادٍ افتراضيّ ليس قراراً**. منحُ `execute` لـ`anon` على أربع دوالِّ قراءةٍ فقط — `title_reviews` · `title_replies` · `title_review_likes` · `community_rating`. **والحراسةُ داخلها ولا تحتاج سطراً**: `can_view_profile` بلا هويّةٍ يمرّ منها **الحسابُ غيرُ الخاصّ وحده**، و`hide_name` والمبلَّغُ عنه كما هما. **ولا كتابةَ ولا إعجابَ ولا بلاغَ لغير المسجَّل.** ⚠️ **ويوسّع «الآخرين» في وعد الخصوصية من المسجَّلين إلى الإنترنت — فنصُّ `/privacy` يجب أن يُحدَّث** | **معلّقة — لوحةُ Supabase لم تُفتح في الجلسة** |
| 73 | `news_post_replies.sql` | **الردُّ على نشرات Loopz وعدّادُه** (D-236، اختيارُ أحمد «ج»). **جدولٌ ثالثٌ لأن نشرتَنا لا صاحبَ لها في `auth.users`** — و`review_replies.review_user_id` مقيَّدٌ بها. لا يمسّ جدولاً قائماً؛ يضيف `news_post_replies` و`news_reply_reports` ودالّتَي `news_post_thread` و`news_reply_counts` (definer، بلا سياسةِ قراءةٍ مفتوحة). **ويستبدل `prune_news_posts` بنسخةٍ تتخطّى كلَّ منشورٍ عليه ردّ** — لا نحذف ما تكلّم عليه أحد. **والشيفرةُ آمنةٌ قبله**: العدّادُ يعود فارغاً فتُخفى الأرقام، **لكنّ زرَّ الإرسال يفشل حتى يُشغَّل** | **معلّقة** |
| 74 | `post_views.sql` | **مشاهداتُ المنشور** (D-237، بلاغُ أحمد: «أقصد كم واحد شاف **المنشور** وليس **الفلم**»). الخانةُ كانت تقرأ `watchers` — **رقمَ العمل** — فيظهر الرقمُ نفسُه تحت كلِّ صفٍّ يتكلّم عنه. جدولٌ واحد `post_views` بمفتاحٍ أوّليٍّ `(post_key, user_id)` — **أشخاصٌ لا مرّات** — يخدم نوعَي الصفّ بمفتاحٍ نصّيّ (`n:` للنشرة، `c:` للتعليق)، ودالّةٌ `post_view_counts` (definer، بلا سياسةِ قراءةٍ مفتوحة، تُرجع عدداً لا هويّات). **والشيفرةُ آمنةٌ قبله**: العدُّ يعود فارغاً فتُخفى الخانة، والكتابةُ تسقط صامتةً — **فلا شيءَ ينكسر، ولا رقمَ يظهر** | **معلّقة** |
| 76 | `feed_self.sql` | **الكاتبُ يرى كلامَه** (D-251): خطُّ المجتمع كان يستثني صاحبَه بسطرٍ كُتب لتبويبٍ مات. **ولا أدواتَ على مرآتك** — الصفُّ يظهر بلا ⋯ | **شُغّلت ١٥ أغسطس** |
| 77 | `loopz_account.sql` | **Loopz يصير حساباً** (D-252، **بإذن أحمد الصريح** لأنها تكتب في سكيما `auth` — D-252 يحدّ إذنَ `19` §4). صفٌّ في `auth.users` **بثلاثة أبوابٍ مغلقة** (لا كلمةَ مرور · لا بريد · ولا صفَّ في `auth.identities`)، وحارسُه **عمودٌ** `profiles.is_system` لا معرّفٌ حرفيّ. تحقُّقها: `1 sys | loopz/Loopz | identities=0 | search=0 | open_policies=4` | **شُغّلت ١٥ أغسطس** |
| 78 | `title_talk.sql` | **النقاشُ كيانٌ مستقلّ** (D-254، تصحيحُ أحمد «النقاش ليس الريفيو، يختلف»): `title_posts` (العنوان/الملصق/الغلاف على الصفّ · `parent_id` · `depth` بمُشغِّلٍ يقف عند ٣) · `title_thread` · `title_talk_rooms` · `title_post_reports`. ⚠️ **والاسمُ الأوّل `title_rooms` مشغولٌ بغرف المجتمعات — والقاعدةُ ردّت المحاولة بنفسها** (D-214). تحقُّقها: `2 tables \| fns=4 \| old_title_rooms_intact=1 \| rooms_now=0 \| open_policies=4` | **شُغّلت ١٥ أغسطس** |
| 79 | `talk_reply_signals.sql` | **الردُّ في الغرفة يصل صاحبَه** (D-259، طلبُ أحمد): **مصدرٌ سادسٌ في `my_signals` بلا جدولٍ ولا كتابة** (مبدأ D-125/D-218)، و`unread_signals()` تلتقطه بلا سطر. **ونوعٌ جديد `talk_reply` لا عَلَمٌ على `reply`** — الوجهةُ تختلف. **والمِرساةُ `parent_id` وحدَه**: الغرفةُ لا صاحبَ لها. تحقُّقها: `idx=1 \| signals=0 \| unread=0 \| open_policies=4` | **شُغّلت ١٥ أغسطس** |
| 80 | `talk_bulletins.sql` | **نشراتُ Loopz في الغرف** (D-261، طلبُ أحمد «ينشّط الغرفة»): أربعةُ أعمدةٍ على `title_posts` (`bulletin_key` · `kind` · `data` · `spoiler`) كلُّها `null` للبشر · **فهرسٌ فريدٌ جزئيّ** يمنع تكرار النشرة كلَّ دورة (D-130) · `body` يصير اختيارياً **للنشرة وحدها** بقيدٍ على الجدول · `set_talk_bulletins` (definer، ممنوحةٌ لـ`authenticated` كـ`set_news_posts`، **والهويّةُ ثابتةٌ في جسمها والعميلُ لا يرسل `user_id` ولا `body` ولا `parent_id`**) · `talk_bulletin_stale` · **و`title_thread` تُحذف وتُعاد بثلاثة أعمدةٍ ملحقةٍ بالآخر** (D-037). **ولا جدولَ ولا سياسةَ قراءةٍ مفتوحة.** تحقُّقها: `cols=4 \| uidx=1 \| chk=1 \| fns=3 \| bulletins=0 \| open_policies=4` | **✅ مُشغَّلة ومتحقَّقة (١٥ أغسطس)** |
| 81 | `people_page.sql` | **أقسامُ تبويب «الناس»** (D-263، لوحتا أحمد): `people_leaderboard` (**نافذتان في دالّة واحدة: الحالية والسابقة — فـ«الأكثر مشاركة» و«الصاعدون» سؤالان عن رقمٍ واحد**، D-198) · `people_top_review` · `people_watching`. **وعددٌ صريح لا نقاطٌ موزونة** (قرارُ أحمد): المقاييسُ الثلاثة تعود منفصلةً فيبقى الرقمُ قابلاً للمراجعة، **والوزنُ يُضاف عند العرض بلا هجرة**. **ولا جدولَ ولا عمودَ عدّاد** — كلُّ رقمٍ يُحسب من صفوفٍ قائمة، والسقفُ ٧ أيامٍ يُبقي المسحَ صغيراً. تحقُّقها: `fns=3 \| open_policies=4` | **✅ مُشغَّلة ومتحقَّقة (١٥ أغسطس)** |
| 82 | `people_page2.sql` | **«أعلى تعليق» تقبل سقفاً** (D-264، طلبُ أحمد «أظهر ٣ بدل واحد»): `people_top_review` كان سقفُها `limit 1` **مكتوباً في جسم الدالّة لا معاملاً** — فلا تستطيع الواجهةُ طلبَ ثلاثة. **تُحذف أوّلاً ثم تُنشأ بمعاملَين** (`p_days` · `p_limit`) لأن إضافة معاملٍ **توقيعٌ جديد**، و`create or replace` كانت ستُبقي النسختين **فيذهب نداءُ `{p_days}` إلى القديمة ذاتِ الصفّ الواحد** — عطلٌ صامتٌ يمرّ من الفحوص (D-037/D-214). **ولا جدولَ ولا عمودَ ولا سياسةَ خامسة.** تحقُّقها: `fns=3 \| top_review_overloads=1 \| open_policies=4` | **✅ مُشغَّلة ومتحقَّقة (١٥ أغسطس)** |
| 83 | `people_page3.sql` | **اللوحةُ تُصفَّر كلَّ سبت** (D-265، طلبُ أحمد «خلها يتصفر كل سبت» — **نقضٌ صريحٌ لجوابه في D-264**): `people_leaderboard` تُحذف وتُعاد **بمعاملٍ واحد** (`p_limit`) — **و`p_days` تسقط لأن النافذة لم يعد لها طولٌ يُختار**. **والمرساةُ سبتُ الرياض لا اثنينُ ISO ولا UTC**: `date_trunc('week', ts + 2d) - 2d` بتوقيت `Asia/Riyadh`، **وإلا وقع التصفيرُ الثالثةَ فجراً**. **⚠️ والنافذةُ السابقة تُقصّ بمقدار ما مضى من الحالية** (`t_prev + (now - t0)`) — **وإلا قُورن نصفُ أسبوعٍ بأسبوعٍ كامل فاختفى «الصاعدون» يومين من كلِّ سبعة** (D-216/D-181). **ولا جدولَ ولا عمودَ ولا سياسةَ خامسة.** تحقُّقها: `board_overloads=1 \| fns=3 \| open_policies=4` **و`week_starts` ينتهي بـ`Sat`** | **✅ مُشغَّلة ومتحقَّقة (١٥ أغسطس)** |
| 84 | `talk_spoiler.sql` | **«رسالتي فيها حرق» في النقاش** (D-271 — **وكُتب `D-268` سهواً أوّلَ مرّة**، وذاك رقمُ رموزِ الأقسام الملوّنة؛ طلبُ أحمد): عمود `has_spoiler boolean not null default false` على `title_posts`، **و`title_thread` تُحذف وتُعاد بعمودٍ حادي عشر** (D-037 — وهي المرّة الثانية بعد ٨٠). **⚠️ وعمودٌ ثانٍ لا استعمالٌ لـ`spoiler` القائمة**: تلك `jsonb` تحمل **النصَّ المحجوب** لنشرة Loopz والمتنُ الظاهر يبقى، **وهنا المتنُ نفسُه هو المحجوب** — حقلٌ يعني شيئين بحسب صاحب الصفّ عطلٌ صامت. **ولا فهرسَ ولا سياسة.** تحقُّقها: `col=1 \| thread_overloads=1 \| flagged=0 \| open_policies=4` | **✅ مُشغَّلة ومتحقَّقة (١٥ أغسطس)** |
| 85 | `people_featured.sql` | **«أعضاء مميّزون»** (D-270، طلبُ أحمد «ضِف بأوّل شي Featured Members»، **واختار محسوبين لا مختارين بيده**: «الأكثر نشاطاً على المدى الطويل»): `people_featured(p_days=90, p_limit=3)` **بنفس أعمدة `people_leaderboard` العشرة** كي يقرأها المكوّنُ نفسُه — `PeopleLeaderboard` بثلاثة أوضاع لا ثلاثةُ مكوّنات. **⚠️ ودالّةٌ ثانيةٌ لا معاملٌ في الأولى**: تلك صارت أسبوعاً تقويميّاً يبدأ السبت (٨٣) **و`p_days` أُسقطت عمداً** — **وإعادتُها لتخدم نافذةً بمعنًى آخر تُرجع الكذبةَ التي أُسقطت**. **و`prev_total` تعود صفراً** (لا «صاعدين» على تسعين يوماً) **والعمودُ يبقى ليبقى الشكلُ واحداً**. **ومن مجموعُه صفرٌ لا يُعرض** — حشوٌ تحت عنوانٍ يقول «مميّزون» يفضح القسم. **ولا جدولَ ولا عمودَ ولا سياسةَ خامسة.** تحقُّقها: `featured_overloads=1 \| fns=5 \| open_policies=4` | **✅ مُشغَّلة ومتحقَّقة (١٥ أغسطس)** |
| 86 | `people_cleanup.sql` | **إسقاطُ دالّتين بلا قارئ** (D-270): `people_watching` (حذف أحمدُ قسمَها) و`title_talk_stats` (سقط قارئُها في D-266 وبقيت شهراً). **⚠️ وهي الوحيدةُ التي تُشغَّل بعد شيفرتها لا قبلها**: كلُّ هجرةٍ تضيف تسبق شيفرتَها، **والتي تحذف تتبعُها** — **وإسقاطُها اليوم يُفرغ قسماً في الإنتاج قبل أن تصل النسخةُ التي لا تناديه** (D-028 معكوسةً). **و`people_to_follow` تبقى**: شاشةُ التهيئة تناديها (D-214: يُفحص المستهلك قبل الحذف). تحقُّقها: `watching_left=0 \| stats_left=0 \| fns=5 \| open_policies=4` | **✅ مُشغَّلة ومتحقَّقة (١٥ أغسطس، بإذن أحمد الصريح «شغلها»)** |
| 87 | `talk_rooms_episode.sql` | **آخرُ حلقةٍ نشرها Loopz على بطاقة الغرفة** (D-273، طلبُ أحمد «مكانه تحط رقم الموسم والحلقة وعنوانها ويتحدّث دوريّاً مع كل نشر للوبز»): `title_talk_rooms` **تُحذف وتُعاد بعمودٍ تاسع** `bulletin jsonb` (D-037) = `data` **أحدثِ** نشرةِ حلقةٍ في الغرفة. **ولا جدولَ ولا عمودَ جديد**: الرقمُ والعنوانُ موجودان منذ ٨٠، **والمطلوبُ إظهارُ ما نملك لا تخزينُه ثانيةً** (D-263). **و«يتحدّث دوريّاً» يقع مجّاناً** — الدالّةُ تقرأ الأحدث فأيُّ نشرٍ يزيح ما قبله بلا cron. **و`distinct on` تقصّ في القاعدة** فلا تُسلَّم الواجهةُ صفوفاً ترفضها (D-164). **ولا سياسةَ خامسة.** تحقُّقها: `room_overloads=1 \| old_title_rooms_intact=1 \| open_policies=4` | **✅ مُشغَّلة ومتحقَّقة (١٥ أغسطس)** |
| 88 | `people_top_review_one.sql` | **تعليقٌ واحدٌ لكلِّ شخص** (D-275، عطلٌ رُئي حيّاً: KHLD في بطاقتين من ثلاث): `people_top_review` تفرز الآراء بالإعجاب **ولا تعرف أصحابها**، فمن كتب رأيين محبوبين أخذ ثلثَي القسم — **واسمٌ مرّتين في ثلاثِ بطاقاتٍ يُقرأ عطلاً لا صدارة** (D-257). **والعلاجُ `distinct on (user_id)` بعد العدّ وقبل الترتيب النهائيّ**، فتُسلَّم الواجهةُ `p_limit` صفّاً كلُّها صالحة (D-164). **⚠️ والتوقيعُ لم يتغيّر فلا `drop`** — D-037 تُقرأ بشرطها: الحذفُ لتغيُّر التوقيع لا لتغيُّر الجسم. **ولا جدولَ ولا عمودَ ولا سياسةَ خامسة.** تحقُّقها: `overloads=1 \| has_dedup=1 \| open_policies=4` | **✅ مُشغَّلة ومتحقَّقة (١٥ أغسطس)** |
| 89 | `people_actions_no_likes.sql` | **الأكشنُ عملٌ لا استقبال، ومعه عدّادُ ردودِ الآراء** (D-285، طلبُ أحمد بخطٍّ أحمر تحت «2 likes»: «احذف اللايك، ما أبغاه يحسبه أكشن»). **و`likes_in` إعجاباتٌ استقبلها صاحبُ الرأي لا فعلٌ فعله** — فكان العدّادُ يخلط ما تعمله بما يُفعل بك، **ولوحةٌ اسمُها «الأكثر مشاركةً» تقيس المشاركة لا الشعبيّة**. فصار `total = posts + reviews` في `people_leaderboard` **وفي `people_featured` معاً** — **الشكلُ الواحد يُقرأ بمكوّنٍ واحد، فمعنًى يتغيّر في إحداهما دون الأخرى يعرض رقمين بمعنيين في بطاقتين متجاورتين** (D-145/D-270). **ومعها `review_reply_counts(text[])` جديدة** تُكمل ترجيحَ خطّ النشاط (D-283: كلُّ ردٍّ ساعة) — **بصيغة `commentViewKey` نفسِها فلا صيغةَ خامسة** (D-237). **⚠️ وبلا `drop` إطلاقاً**: الأعمدةُ لم تتغيّر و`create or replace` تكفي. **⬜ ودَينٌ معلَن: `likes_in` يبقى عموداً بلا قارئ** — حذفُه يوجب `drop` وهو خارج الإذن الدائم، **وسُئل أحمد فقال إنه لم يفهم السؤال، والإذنُ الذي لا يُفهَم ليس إذناً**. **ولا جدولَ ولا عمودَ ولا سياسةَ خامسة.** تحقُّقها: `lb=1 \| feat=1 \| rrc=1 \| no_likes=1 \| open_policies=4` | **✅ شُغِّلت ١٦ أغسطس** — والتحقّقُ من الكتالوج `lb=1 \| feat=1 \| rrc=1 \| new_total=1 \| still_likes=0 \| open_policies=4`. **⚠️ و`no_likes` المكتوب أعلاه لم يُستعمل كما هو**: كان يبحث عن تعليقٍ `-- ⚖️` داخل جسم الدالّة، **وتشغيلُها اليوم يقتضي تجريدَ التعليقات** (منعُ `setValue` — انظر `19`) — **فبُحث عن التعبير الحسابيّ نفسِه، وهو أقوى** (D-247) |
| 90 | `reply_likes_and_saved_lists.sql` | **إعجابٌ على كلِّ ردّ، وأكثرُ القوائم حفظاً** (D-289، طلبا أحمد في ١٥ أغسطس: «لازم فيه لايك عند كل رد» و«ضيف أكثر الليستات حفظاً وأظهر أعلى ٣ آخر ٧ أيام»). **وتأخّرا لأنّي حوّلتُ طلبيه إلى سؤالين** — وهو ما لا يُعاد. **الجدولُ `title_post_likes(post_id, user_id)` بمفتاحٍ مركَّب**، **وثلاثُ سياساتٍ كلُّها «صفوفي»** — قراءةً وإدراجاً وحذفاً — **ولا واحدةَ منها `qual = true`**، **فالسياساتُ المفتوحة تبقى أربعاً** (D-013)، **والعدُّ من `post_like_counts(uuid[])` definer** كنمط `review_likes` حرفاً (D-140). **و`mine` تأتي مع العدد** فلا نداءَ ثانٍ (D-198)، **ونداءٌ واحدٌ للغرفة كلِّها** لا لكلِّ صفّ (D-164/D-205). **ومعها `top_saved_lists(days, limit)`** — نافذةٌ متدحرجةٌ لأن القسمَ عن قوائمَ لا عن سباقٍ أسبوعيّ، **والعامّةُ وحدَها** (اسمُ قائمةٍ خاصّةٍ تسريبٌ بثوب إحصاء)، **وصاحبُها يمرّ بحارسَي `hide_name` والحظر** (D-011) **وحسابُ النظام خارجَها** (D-252)، **وثلاثةُ ملصقاتٍ للغلاف تأتي معها**. **⚠️ وبلا `drop` إطلاقاً** — ولا حتى `drop policy`: **السياساتُ تُنشأ داخل `do $$` بشرطِ الغياب**، فالملفُّ يبقى قابلاً لإعادة التشغيل والإذنُ الدائم لا يُخرَق. تحقُّقها: `tbl=1 \| pol=3 \| plc=1 \| tsl=1 \| open_policies=4` | **✅ شُغِّلت ١٦ أغسطس** |
| 91 | `saved_lists_include_loopz.sql` | **قوائمُ Loopz تدخل «ما يحفظه الناس»** (D-290، بلاغُ أحمد: «خلّه يشتغل حتى على قوائم لوبز، حالياً شغّال فقط على قوائم الأعضاء»). **والاستثناءُ كان نسخةً من حارس أقسام الأشخاص** (`is_system = false`) — **وهو صحيحٌ هناك وخطأٌ هنا**: حجّةُ D-252 أن حسابَ النظام ليس عضواً يُنافَس في لوحة، **وقائمةٌ ليست شخصاً** — والحافظون أعضاءٌ حقيقيّون في الحالتين. **فحارسٌ يُنسخ يُقرأ بحجّته لا بشكله** (D-145). **⚠️ وبلا `drop`**: الأعمدةُ التسعةُ نفسُها والجسمُ وحدَه تغيّر (D-037)، **وحارسا الحظر و`hide_name` باقيان.** تحقُّقها: `tsl=1 \| still_excludes=0 \| open_policies=4` | **✅ شُغِّلت ١٦ أغسطس** |
| 92 | `room_pins.sql` | **تثبيتُ غرفةِ نقاش** (D-301، طلبُ أحمد بلقطة: «احتاج هذي العلامة فوق بالزاوية، إذا ضغطت عليها يتثبّت»): `title_room_pins(user_id, tmdb_id, media_type)` **بمفتاحٍ مركَّبٍ ثلاثيّ لا عمودِ ترتيب** (D-130/D-263) · **وثلاثُ سياسات «صفوفي أنا»** فلا خامسةَ مفتوحة (نمطُ ٩٠ حرفاً) · **ولا دالّةَ `definer`**: الصفوفُ صفوفي وسياسةُ القراءة تكفي — **ودالّةُ definer تُكتب حين يُقرأ ما ليس لك** (D-266). **والتثبيتُ خاصٌّ بصاحبه**: علامةٌ يضغطها أيُّ أحد لو كانت عامّةً لرتّب أوّلُ من يضغطها شاشةَ الجميع (D-255). **ولا `drop`.** تحقُّقها: `tbl=1 \| pol=3 \| open_here=0 \| open_policies=4` | **⬜ تنتظر التشغيل** |

| 93 | `talk_image_only.sql` | **صورةٌ بلا نصٍّ مشاركةٌ كاملة** (D-302، بلاغُ أحمد: «الصور ما تقدر ترسل إلا بعد ما تكتب»). | **✅ شُغِّلت** |
| 94 | `post_votes.sql` | **أسهمُ التصويت على مشاركات النقاش** (D-305، بلقطةٍ من Reddit) — **الاستثناءُ الوحيدُ المسجَّل لترتيب D-294** (السهمان قبل القلب). | **✅ شُغِّلت** |
| 95 | `close_community_creation.sql` | **إغلاقُ تأسيس المجتمعات في القاعدة** (D-306، بنصّه: «احنا حاذفين الكومينتي») — **والواجهةُ وحدَها ليست بوّابة** (D-193). | **✅ شُغِّلت** |
| 96 | `talk_rooms_week.sql` | **العدُّ بشريٌّ وأسبوعٌ يُحسب** (D-311): `posts_week` بمرساة السبت‑الرياض — **فصار العنوانُ «هذا الأسبوع» صادقاً** (D-219). | **✅ شُغِّلت** |
| 97 | `news_by_title_and_cleanups.sql` | **ثلاثُ رخيصاتٍ مؤجَّلة** (D-312): ترشيحُ أخبار العمل في القاعدة · `image_path` عموداً · وحذفُ `likes_in`. | **✅ شُغِّلت** |
| 98 | `ratings_backdrop.sql` | **غلافُ «أعلى التعليقات» الحقيقيّ** (D-313): `ratings.backdrop_path` — **ولا إدراجَ رجعيَّ عمداً.** | **✅ شُغِّلت** |
| 99 | `global_room_pins.sql` | **تثبيتٌ إداريٌّ يراه الجميع** (D-314): `title_room_global_pins` — **RLS مفعَّلٌ وصفرُ سياسات**، والحارسُ `am_admin()` في جسم الدالّة. | **✅ شُغِّلت** |
| 100 | `review_spoiler.sql` | **«رسالتي فيها حرق» في الريفيو** (D-315): عَلَمٌ واحدٌ تحمله خمسُ دوالّ — **لأن شكلَ ما تُرجعه وعدٌ واحد.** | **✅ شُغِّلت** |
| 101 | `loopz_doomsday_list.sql` | **قائمةُ «طريقك إلى Avengers: Doomsday»** (D-317) — أوّلُ قائمةٍ تحريريّةٍ بحساب لوبز، **وسابقةُ D-328** حين صارت المنسّقاتُ كلُّها قوائمَ حقيقية. | **✅ شُغِّلت** |
| 102 | `for_you_lists.sql` | **«قوائمُ تناسبك»** (D-324): تقاطعُ مكتبتك مع محتوى القوائم العامّة. | **✅ شُغِّلت** |
| 103 | `list_reviews.sql` | **تقييمُ القوائم ومراجعتُها** (D-327): `list_reviews` + `list_review_reports` **بنمط `ratings`/`review_reports` حرفاً** — سياساتُ «صفوفي أنا»، وقراءةٌ عامّةٌ بدالّتَي definer، وحاجبُ العشرة بلاغات، **والسلّمُ عشرةٌ لا خمسة** (D-002). | **✅ شُغِّلت** |
| 104 | `curated_lists.sql` | **المجموعاتُ المنسّقة تصير قوائمَ حقيقية** (D-328): `user_lists.source_slug` + فهرسٌ فريدٌ جزئيّ · `upsert_curated_list` (كاتبُها `am_admin()` وصاحبُها حسابُ لوبز) · `curated_list_ids`. | **✅ شُغِّلت** |
| 105 | `list_card_stats.sql` | **أرقامُ بطاقة القائمة** (D-329، «أهم شي»): نداءٌ واحدٌ لثلاثٍ وستّين بطاقة — **والعامّةُ وحدَها، والمحجوبُ لا يدخل المتوسّط.** | **✅ شُغِّلت** |
| 106 | `list_activity.sql` | **الخيطُ الثالث: كلامُ الناس على القوائم يصل** (البند ٨). **تقييمُ القوائم بُني في ١٠٣ ولا يصل أحداً** — لا في الخطّ ولا في الجرس، **والحلقةُ مفتوحةٌ عند دورها الأوّل** (علّةُ D-218 نفسُها). **ولا سطحَ جديد**: `community_activity()` تُمدّ بذيلٍ ثلاثيّ (`list_id`/`list_name`/`list_slug`) و`my_signals()` بنوعٍ سابع (`list_review`) وعمودَي وجهة — **بابان قائمان يُوصَل إليهما مصدرٌ ثالث** كما وُصِّل `talk_reply` في ٧٩. **⚠️ و`drop function` لا مفرَّ منها** (توقيعٌ جديد) — **أذن بها أحمد بنصّه: «اكمل»**، **وهي حذفُ تعريفٍ لا حذفُ بيانات، داخل معاملةٍ واحدة**. **⚠️⚠️ والكودُ يُنشر قبلها** وإلّا رُسم صفُّ قائمةٍ برابط `/movie/null`. الحرّاس: العامّةُ وحدَها · المكتوبةُ وحدَها في الخطّ (والمجرّدةُ تُشعِر) · `hidden` · الحظرُ في الفرع الجديد. تحقُّقها: `fns=3 \| feed_cols=3 \| open_policies=4` | **⬜ تنتظر التشغيل** |
| 107 | `curated_meta.sql` | **عدُّ قائمةِ لوبز وهويّتُها** (البندان ٢ و٦). `curated_list_counts(uuid[])` — **العدُّ من القائمة لا من اسمها**: بطاقةُ «أفضل ٢٥٠ أنمي» كانت تقول ٢٥٠ وفيها ١٤٠ بأثر عتبة D-323، **ورقمٌ يكذب أسوأُ من لا رقم** (D-219). **ونداءٌ واحدٌ لاثنتين وأربعين بطاقة** لا استعلامٌ لكلِّ واحدة (D-205)، **وغيابُها يعيد رقمَ القاموس** (D-028). ومعها `curated_slug_of(uuid)` — **سلغُ القائمة للزائر بلا حساب** (يقرأ عبر `public_list` وحدَها فلا يصله `source_slug`) **فيُترجَم الاسمُ عند العرض** (دَينُ D-328). **⚠️ ولا `drop` ولا جدولَ ولا سياسة** — دالّتان جديدتان، **والعامّةُ وحدَها في الاثنتين**. تحقُّقها: `fns=2 \| open_policies=4` | **⬜ تنتظر التشغيل** |

| 108 | `featured_lists.sql` | **قائمةُ الأسبوع: تثبيتٌ تحريريٌّ لا خوارزمية** (D-349، طلبُ أحمد: «تحط بعد البيك فور يو تريندينج ليست… ليست مارفل لفلم دووم»). **والحسابيُّ كان ممكناً بلا جدولٍ أصلاً** (`list_saves.created_at`) — **ورُفض بقياس**: بقاعدةٍ فيها حفظاتٌ معدودة يعود الصفُّ فارغاً أو بقائمةٍ حفظها اثنان، **وصفٌّ يقول «الرائجة» عن قائمةٍ حفظها اثنان يكذب** (D-219/D-181). **والحقيقةُ التي يعرفها أحمد ولا تعرفها القاعدة**: فيلمٌ يُعرض بعد شهرٍ يجعل قائمتَه مهمّةً **قبل** أن يحفظها أحد. **والشكلُ شكلُ `title_room_global_pins` حرفاً** (D-314): جدولٌ بمفتاحٍ واحد · **صفرُ سياسات** (القراءةُ والكتابةُ بدالّتَي `definer`) · الكاتبُ `am_admin()` **في جسم الدالّة لا في الواجهة** (D-011/D-193) · **والعامّةُ وحدَها تُثبَّت** · **والقارئُ يُسقط ما لم يعد عامّاً** بلا تدخّل. **ولا `drop`.** تحقُّقها: `tbl=1 \| pol_here=0 \| fns=2 \| open_policies=4` | **⬜ تنتظر التشغيل** |

| 109 | `community_block_guard.sql` | **المحظورُ لا يظهر في خطّ المجتمع** (D-351) — **سدادُ دَينٍ أُعلن يومَ وُلد**: يومَ مُدَّ الخطُّ بفرع القوائم (١٠٦) حُرِس الفرعُ الجديد بـ`is_blocked` **وبقي فرعُ التقييمات بلا حارس كما وُلد في ٢٧**، فكُتب ديناً معلَناً ولم يُصلَح صامتاً في هجرةٍ عن غيره (D-155). **وحارسٌ يُطبَّق في خمسة أبوابٍ ويُنسى في السادس ليس حارساً** (D-145). **⚠️ ولا `drop`**: الأعمدةُ الخمسَ عشرةَ نفسُها والجسمُ وحدَه تغيّر بسطر (D-037). تحقُّقها: `fn=1 \| guarded=1 \| open_policies=4` | **⬜ تنتظر التشغيل** |
| 110 | `vote_like_guard.sql` | 🔴 **حارسٌ يسأل بعين النظام لا بعين صاحب الطلب** (D-355، بلاغُ أحمد: «السهم فوق وتحت في النقاش مايشتغل»). **المقيس:** `title_post_votes` **صفرُ صفوفٍ منذ ١٦ أغسطس**، وسجلُّ Vercel: `42501: new row violates row-level security policy`. **والسبب أن سياسة الإدخال (٩٤، ونظيرتُها في ٩٠) تسأل `exists (select 1 from title_posts …)` — والاستعلامُ الفرعيُّ يُنفَّذ بصلاحية صاحب الطلب**، و`title_posts` سياستُها الوحيدةُ للقراءة `auth.uid() = user_id` (كلُّ قراءةٍ عامّةٍ تمرّ بالمنظور أو بدالّة `definer` — D-010). **فالسؤال «هل ثمّة مشاركةٌ ليست لي؟» يُطرح على مجموعةٍ لا تحوي إلا ما هو لي، وجوابُه «لا» أبداً** — فسقط كلُّ تصويتٍ وكلُّ إعجابٍ على ردّ. **والعلاج `can_touch_post(uuid)` دالّةَ `definer` واحدةً للعطلين** (D-145: الشكلُ واحدٌ فالإصلاحُ واحد) **و`is distinct from` لا `<>`** (D-182). **⚠️ و`drop policy` خارج الإذن الدائم — استُؤذن فيه بعينه وأذن أحمد**، وهو حذفُ تعريفٍ لا حذفُ بيانات في معاملةٍ واحدة. **ومعها حذفُ الدخيل `tt38770727` («EARLY STREAM!» تاسعاً في أفضل ٥٠ فيلماً) بإذنه** — والحارسُ البنيويُّ في `imdbChart.ts`. تحقُّقها: `fn=1 \| fixed=2 \| intruder=0 \| open_policies=4` | **✅ شُغِّلت وتحقّقت** |
| 111 | `gif_posts.sql` | 🆕 **معرّفٌ لا رابط: الـGIF يسكن عموداً باسمه** (D-362، طلبُ أحمد: «ابغا أضيف خيار جنب الصور GIF، خيار سريع وبديل عن الصور»). **عمودٌ لا حقيبة**: الحمولةُ التي تصير معنًى دائماً تسكن عموداً باسمها — **وإعادتُها إلى `data` تكرارٌ لغلطٍ صحّحته ١٠٧/D-312**. 🔴 **ومعرّفٌ لا رابط**: حارسُ D-298 يشترط بادئةَ مخزننا لأن **رابطاً يصل من عميلٍ ونرسمه `<img>` للناس خطرٌ**، **والـGIF لا يسكن مخزننا** (وهو معنى «سريع»: بلا رفع) — **فلا يعبر حدودَنا عنوانٌ أصلاً، يعبر معرّفٌ من حروفٍ وأرقام، والرابطُ يُركَّب من قالبٍ ثابت في `media.ts`**، **والحارسُ في القاعدة كذلك** (`title_posts_gif_id_shape`). **والقيدُ وُسّع لا ضُيّق**: متنٌ **أو** صورة (عموداً أو حقيبةً — ٩٧) **أو GIF**، والنشراتُ لم تُمَسّ. **⚠️ و`drop function` لتغيّر عائد `title_thread`** (عمودٌ في الذيل — D-037/D-179) **بإذن أحمد «اكمل كل المتبقي»** بعد إذنين بالنمط نفسِه (١٠٦/١١٠)، **وهو حذفُ تعريفٍ لا بيانات في معاملةٍ واحدة**. **والمزوّدُ Giphy لا Tenor**: توثيقُ Google يقول «no longer accepting new clients as of January 2026». تحقُّقها: `col=1 \| shape=1 \| has_gif_in_result=1 \| open_policies=4` | **✅ شُغِّلت وتحقّقت** — **و`fn_col` كان مِقياساً خاطئاً**: `information_schema.columns` لا تعرف الدوالّ، **والصحيحُ `pg_get_function_result(p.oid) like '%gif_id%'`** (D-354 بروحها: يُقاس ما يُدَّعى بالأداة التي تراه). |
| 112 | `chart_vote_floor.sql` | 🆕 **عتبةُ الأهليّة تنتقل من القراءة إلى البناء** (D-365، دَينُ ٥/٢). **المقيس قبلها:** `imdb_chart` يحمل ٢٠٦ صفَّ أنمي **منها ٧٠ دون عشرين ألف صوت** (أدناها ٥٬٦١٤) — **لأن العتبة كانت تُطبَّق عند القراءة لا عند البناء** (D-323)، **فالمخزَّنُ أوسعُ من المعروض** وهو سببُ أن القائمة المولَّدة ١٤٥ لا ٢٥٠. **والعلاجُ سطرٌ واحد**: `where p.votes >= case when p.is_anime then 20000 when p.media_type='movie' then 25000 else 20000 end` داخل `build_imdb_chart`. ⚠️ **وثابتُ بايز `m` لم يُمَسّ** (٥٠٠٠/٢٥٠٠٠/٥٠٠٠): العتبةُ تقول *من يدخل السباق*، و`m` يقول *كم يُسحب المتسابقُ نحو المتوسّط* — **ورقمان بمعنيين لا يُوحَّدان لأنهما تصادفا** (D-219). 🔴 **والجدولُ لم يُعَد بناؤه في هذه الهجرة عمداً**: البناءُ أربعةُ آلاف نداء TMDB، **ويُشغَّل بقرارٍ لا بأثرٍ جانبيّ**. تحقُّقها: `floor_in_body=1 \| chart_now=705 \| open_policies=4` | **✅ شُغِّلت وتحقّقت** |
| 113 | `list_review_social.sql` | 🆕 **الإعجابُ والردُّ على مراجعة القائمة** (D-370، البند ٤ من الخمسة الباقية — طلبُ أحمد: «اكمل»). **جدولان ووصفتُهما منسوخةٌ حرفاً**: `list_review_likes` من `review_likes` (١٩) و`list_review_replies` من `review_replies` (٦٢) — حدُّ العمق في القاعدة · «اكتب باسمك» · قراءةٌ بدالّة `definer` وحدَها · **وبابُ بلاغٍ من أوّل يوم بعتبة العشرة** (D-193). **وهذا ما كان محجوزاً عليه ذيلُ صفِّ القائمة في الخطّ** منذ ١٠٦ («لا `list_review_likes` اليوم، وزرٌّ لا يكتب شيئاً أسوأُ من غيابه» — D-123). ⚠️ **وثلاثةُ فروقٍ عن أصلها**: المفتاحُ `(صاحبُ الرأي + القائمة)` لا `id` (كـ`list_review_reports` في ١٠٣) · **والحوارُ يموت بموت رأسِه** بمفتاحٍ مركّبٍ `on delete cascade` — **خلافاً لـ٦٢ عمداً**، لأن حذفَ رأي القائمة ضغطةٌ واحدةٌ في الصندوق **فردٌّ يبقى يتيماً ثم يُبعث مع أوّل رأيٍ جديد** · 🔴 **ولا سؤالَ عن `list_reviews` في أيّ سياسة** — سياستُها «صفوفي أنا»، **والاستعلامُ الفرعيُّ يُقرأ بعين صاحب الطلب** (عطلُ ١١٠/D-355 بحرفه): **الوجودُ يضمنه المفتاحُ الأجنبيّ، والعلانيةُ تُسأل من `user_lists` وحدَها** ولها سياسةُ قراءةٍ عامّةٍ حقيقيّة. **⚠️ ولا `drop function` ولا حذفَ بيانات** — دالّتان جديدتان وثلاثةُ جداول. تحقُّقها: `tbls=3 \| fns=2 \| open_policies=4` | **⬜ تنتظر التشغيل** |
| 114 | `list_social_signals.sql` | 🆕 **الجرسُ يعرف الإعجابَ والردَّ على رأي القائمة** (D-379، دَينُ D-370 المعلَن في `05`، طلبُ أحمد: «اكمل ١ و٢ و٣»). **نوعان جديدان في عمودٍ قائم لا توقيعٌ جديد** — `create or replace` وحدَها، **ولا `drop function` ولا لحظةَ يكون فيها الجرسُ بلا دالّة** (خلافاً لـ١٠٦ و١١١، وهو فرقٌ يُقال). الحرّاس: **العامّةُ وحدَها** (حارسٌ لم يحتجه فرعُ ١٠٦ لأن المُشعَرَ هناك صاحبُ القائمة وهنا صاحبُ الرأي) · `hidden` · **ولا تُشعِر نفسَك** · والحظرُ في المصفاة الأخيرة. **و«ردَّ على ردّك» ليس نوعاً ثالثاً**: الجملةُ واحدةٌ والوجهةُ واحدة، **ويُقرأ من `parent_id`** (نصُّ `reply` في ٧١ حرفاً). 🔴 **ومعها إعادةُ فرعين سقطا صامتين**: قِيست الدالّةُ الحيّة قبل كتابة سطرٍ واحدٍ فإذا هي **خمسةُ أنواعٍ لا سبعة** — **`reply` و`talk_reply` غائبان منذ ١٠٦** التي كُتب جسمُها من نسخةٍ أقدم من ٧١/٧٩، **فلا يعلم من رُدَّ عليه في عملٍ ولا في غرفةٍ أنه رُدَّ عليه** (علّةُ D-218 بُعثت من حيث دُفنت — D-380). **وهما مُعادان لا مُضافان، بصوتٍ عالٍ لا صامتين** (D-155). ⚠️⚠️ **والكودُ يُنشر قبلها**: الواجهةُ القديمة تنتهي بالنوعين الجديدين إلى `/u/<username>` لا إلى القائمة. تحقُّقها: `fn=1 \| has_like=1 \| has_reply=1 \| kinds=9 \| open_policies=4` | **⬜ تنتظر التشغيل** |
| 115 | `anime_vote_floor.sql` | ⚖️ 🆕 **عتبةُ الأنمي وحدَها تنزل إلى عشرة آلاف** (D-385، موافقةُ أحمد بنصّها «موافق» على البابِ الثاني من D-382). **نقضٌ يُسجَّل باسمه لتطبيق D-323/D-365 على الأنمي، والقاعدةُ الأمُّ باقية** (رقمٌ يكذب أسوأُ من لا رقم — D-219)، **والفيلمُ والمسلسلُ لم يُمسّا**. **والحجّة**: العشرون ألفاً رقمٌ واحدٌ كان يحرس بِركتين مختلفتين — فيلمٌ إنجليزيٌّ يبلغها في شهر، **وأشهرُ الأنمي كلِّه في IMDb ٢٠٦ أعمالٍ فوق الخمسة آلاف** (D-382) — **والعتبةُ تُقاس بالبِركة التي تقف عليها** (D-378). **والمقيس قبل القرار**: `≥20k=136 \| ≥15k=165 \| ≥10k=188 \| ≥5k=206`. **⚠️ والكودُ يُنشر قبلها** وإلّا سقطت الصفوفُ الجديدة بصمت عند القراءة (`MIN_CHART_VOTES`). **ولا `drop` ولا جدولَ ولا سياسة** — سطرٌ في جسم الدالّة. تحقُّقها: `anime_floor=1 \| open_policies=4` ثم `anime=188` بعد البناء | **⬜ تنتظر التشغيل** |
| 116 | `curated_sort_order.sql` | 🔴 **القائمةُ المرتَّبة لم تكن تخزّن رتبتَها** (D-390). **المقيس**: بعد توليد «الدبّ الذهبي» فُتحت القائمةُ فإذا أوّلُها فائزُ ١٩٧٤ لا ٢٠٢٦ — و`select sort_order …` يقول: **NULL في كلِّ الصفوف**. **و`upsert_curated_list` (١٠٤) لا تكتب العمود أصلاً**، وكلُّ الصفوف تُدرَج في جملةٍ واحدةٍ فـ`added_at` متساوٍ، **فترتيبُ العرض ترتيبٌ فيزيائيٌّ لا يَعِد به أحد**. 🔑 **ولهذا بدت القوائمُ صحيحةً شهراً**: المصادفةُ وافقت الإدراج — **وبافتا خرجت مرتَّبةً وبرلين مقلوبةً في الدفعة نفسِها**، **وما يعمل بالمصادفة يُقرأ «يعمل» حتى يكذّبه أوّلُ استثناء** (D-181). ⚠️ **والعطلُ يمسّ الاثنتين والأربعين لا الجوائزَ وحدَها**: «أفضل ٢٥٠» رتبتُها معناها، **و«بترتيب الأحداث» تكذب إن عُرضت عشوائيّةً** (D-219/D-074). **والعلاجُ `with ordinality`**: موضعُ العنصر في المصفوفة رتبتُه — **والمستدعي يرسلها مرتَّبةً أصلاً**، **فالقاعدةُ تحفظ ما أُرسل ولا تعيد ترتيبَ ما لا تعرف معناه**. **ولا `drop` ولا حذفَ بيانات** — جسمٌ تغيّر بسطر. تحقُّقها: `writes_rank=1 \| open_policies=4` ثم `unranked=0` بعد إعادة التوليد | **⬜ تنتظر التشغيل** |
| 117 | `community_spoiler_restore.sql` | 🔴 **عَلَمُ الحرق عاد إلى خطّ المجتمع** (D-395) — **وهو ثالثُ ما أسقطته الهجرة ١٠٦** بعد `reply` و`talk_reply` (D-380). **المقيس**: سؤالٌ واحدٌ على سبع دوالّ — ستٌّ تحمل `has_spoiler` **و`community_activity` وحدَها لا تحمله**. **وأعطاها إيّاه ١٠٠** (D-315) ثم كُتب جسمُها من نسخةٍ أقدم في ١٠٦ فسقط. 🔴 **والثمن**: `getCommunityFeed` تقرأ العمود فتجده غائباً، **فـ`hasSpoiler` كانت `false` دائماً** — **وكلُّ مراجعةٍ أُعلن أن فيها حرقاً عُرضت مكشوفةً في تبويب «المجتمع» منذ ١٧ أغسطس**، **والحاجبُ سليمٌ في الأسطح الخمسة الأخرى.** **⚠️ و`drop function` لا مفرَّ منها** (عمودٌ سادسَ عشرَ = توقيعٌ جديد) — **حذفُ تعريفٍ لا بيانات، في معاملةٍ واحدة، بإذن أحمد**. **والشيفرةُ لا تُنشر قبلها**: القارئُ يسأل عن العمود منذ D-315. **والجسمُ بُني على `pg_get_functiondef` الحيِّ** (قاعدةُ D-380 مطبَّقةً على نفسها) فلم يسقط حارسُ ١٠٩. تحقُّقها: `flag_back=1 \| cols=16 \| open_policies=4` | **✅ شُغِّلت** — **وحُملت في جسم ١١٩** (بُنيت ١١٩ أوّلاً من جسم ١٠٦ فأسقطتها، ثم أُعيدت من التعريف الحيّ) |
| 118 | `title_pulse.sql` | 🆕 **نبضُ العمل — قلوبٌ وتقييماتٌ في نداءٍ واحد** (D-408، طلبُ أحمد: «كم واحد معطيه قلب وكم تقييمه»). **والقلوبُ هي ما لم يكن يُقرأ**: المفضّلةُ قائمةٌ في `user_lists` بعلامة `kind='favorites'` (هجرة ٥٥)، **وسياساتُها تُظهر قوائمَ صاحبها وحدَه** — **فالعدُّ من العميل يعود واحداً دائماً** (قلبُك أنت). **دالّةٌ `definer` تُرجع عدداً لا أسماء** — ولا جدولَ ولا سياسة، **والمفتوحةُ تبقى أربعاً**. **وتُمنح لـ`anon` كأخواتها** (D-221): صفحةُ العمل تُفتح من رابطٍ مشارَك. تحقُّقها: `title_pulse=1 \| open_policies=4` | **✅ شُغِّلت** — `fn=1 \| open_policies=4` |
| 119 | `activity_self_and_list_cover.sql` | 🔴 **كلامُك عاد إلى خطّك وللقائمة وجهٌ** (D-424/D-425، بلاغُ أحمد: «خلّي الشخص يشوف نشراته في اكتيفتي» و«صورة اللستة تطلع هنا»). **والاستثناءُ الذي منع كلامَك حذفته الهجرةُ ٧٦ (D-251) ثم أعادته ١٠٦ وهي لا تقصده** — **من يعيد كتابة دالّةٍ يقرأ تاريخَها أوّلاً.** **وعمودُ `list_cover` باسمه لا `poster_path`**: الغلافُ ١٦:٩ والملصقُ ٢:٣ (D-224/D-312). ⚠️ **وأوّلُ كتابةٍ لها وقعت في العطل نفسِه**: نُسخ جسمُ ١٠٦ فسقط `has_spoiler` وحارسُ الحظر، **فأُعيدت من `pg_get_functiondef` الحيّ** (D-380 مطبَّقةً على نفسها). تحقُّقها: `cover_col=t \| flag_kept=t \| excludes_self=f \| block_guard=t \| open_policies=4` | **✅ شُغِّلت** |
| 120 | `doomsday_slug.sql` | 🆕 **هويّةٌ لقائمةٍ كُتبت بيد** (D-427، بلاغُ أحمد: «طريقك إلى… المفروض تكون بالإنجلش لأن الحساب بالإنجلش، حتى الوصف بالداخل»). **بوّابةُ D-328 تترجم من عرفت هويّته**، **وقائمةُ ١٠١ وُلدت بلا `source_slug`** فمرّت بنصّها العربيّ. **صفٌّ واحدٌ · عمودٌ واحد · وشرطُ `source_slug is null`** فإعادةُ تشغيلها لا تفعل شيئاً، **ولا اسمَ ولا وصفَ ولا عنصرَ يُمَسّ.** 🔴 ⚠️ **وهي `update` على صفٍّ قائم — خارجَ الإذن الدائم بنصّه**: **يشغّلها أحمد بيده.** تحقُّقها: `source_slug = 'doomsday-road'` | **✅ شُغِّلت** — بإذنٍ صريحٍ منه («نفّذ… الهجرة»)؛ قِيس: `source_slug = 'doomsday-road'` والاسمُ المخزَّن كما هو |
| 121 | `font_prefs.sql` | 🆕 **حجمُ الخطّ يتبع الحساب** (١٩ أغسطس، طلبُ أحمد: تحكّمان مستقلّان لواجهة النظام ومحتوى المستخدم، «مع حفظ الاختيار للحساب أو محلياً للزائر»). عمودان في `profiles` على نمط عمود الثيم: `font_ui` و`font_content`، أربعُ درجاتٍ مغلقة (`sm/md/lg/xl`) بقيدِ `check`، **ومعهما `ui_state jsonb` للتلميحات المقروءة وتقدّم الجولة** (طلبه بنصّه: «اعتمد حفظ التلميحات والجولة في حساب المستخدم» — والكاتبُ الوحيد `updateUiState`) — **قيمةٌ حرّةٌ هنا تدخل `data-*` على جذر الصفحة فالتحقق في القاعدة لا في الشيفرة وحدها**. لا سياسةَ ولا دالّةَ ولا دخولَ في `public_profiles` (حجمُ خطّك ليس شأنَ زوّارك). **الكاتبُ الوحيد `setFontPrefs`** (حقلٌ واحدٌ لا يملك كاتبَين — D-462). **والكودُ يحتملها غائبة**: قبلها يعمل التفضيل بالكوكي وحده على كل جهاز. تحقُّقها: `cols=3 | open_policies=4` | **⬜ تنتظر التشغيل** |
| 122 | `playlists.sql` | 🆕 **قوائمُ التشغيل** (٢٠ أغسطس، طلبُ أحمد: «يعمل لليست بلاي ليست وتظهر في كنتنيو واتش، كل ما يحط صح على الفلم يظهر اللي بعده»). عمودٌ واحدٌ `is_playlist boolean not null default false` على `user_lists` — صفةٌ للقائمة لا كيانٌ جديد. **لا سياسةَ جديدة**: "own lists" (for all) تغطّي التحديث. **الكاتبُ الوحيد `setListPlaylist`**، والقارئ `getMyPlaylistsBrief` يعود فارغاً بغيابها فلا شاشةَ خطأ. تحقُّقها: `col=1 | open_policies=4` | **⬜ تنتظر التشغيل** |
| 123 | `123_duplicate_watch_index.sql` | 🆕 **حذفُ الفهرس المكرَّر على `watched_episodes`** (جولة P1-A · ٢٠ أغسطس). فهرسان متطابقان بايتاً بايتاً على `(user_id, watched_at desc)` — أحدُهما من `performance.sql` والآخرُ من `activity_v2.sql` — **أُثبت تطابقُهما في الأعمدة والترتيب وopclass والقيود قبل الحذف**، وبقي `watched_episodes_user_time_idx` (المذكورُ في فحص activity_v2 الصحّي). **وسطرُ الإنشاء القديم في `performance.sql` عُلِّق** فلا تعيد إعادةُ التشغيل المكرَّرَ. rollback في رأس الملف. تحقُّقها: `remaining=1 \| open_policies=4` | **✅ شُغِّلت وتحقّقت** — `remaining=1` والباقي `watched_episodes_user_time_idx` والخطّة تستعمله؛ **واختبارُ الكتابة اكتمل**: إدراجُ ٦٠ صفّاً دفعةً عبر التطبيق ثم حذفُها (واحدٌ من الواجهة والبقيّةُ بحذفٍ محدودٍ بطابعها الزمنيّ) والبياناتُ عادت بايتاً بايتاً |
| 124 | `124_inline_hot_invoker_fns.sql` | 🆕 **تمكينُ الـinlining لـ`watch_summary` و`my_lists`** (جولة P1-A · D-516). **المقيس**: أكبرُ مستهلكِ وقتِ قاعدةٍ (٣٨٨ ثانية/١٩ يوماً) — و`SET search_path` على دالّة SQL **يمنع Postgres من تضمينها** فتنفَّذ صندوقاً أسود (نداءٌ ٥٦م.ث وجسدُه ٩٫٨م.ث بنفس RLS). **الإسقاطُ هو التغييرُ الوحيد**: الجسمان من `pg_get_functiondef` الحيّ حرفاً، كلُّ مرجعٍ مؤهَّلٌ بمخطَّطه، وSECURITY INVOKER وSTABLE والتوقيعُ كما هي — **لا DROP ولا مساسَ بسياسة**. rollback = التعريفان القديمان بسطر search_path. تحقُّقها: `cfg=NONE ×٢ \| open_policies=4` + خطّةٌ مضمّنةٌ في EXPLAIN | **✅ شُغِّلت وتحقّقت** — `cfg=NONE ×٢`، التوقيعُ والمالكُ والصلاحياتُ كما هي، **والخطّةُ صارت مضمّنة** (١٧٢ صفحةً مقروءةً بدل ٦٤٨)؛ **القياس** (١٨ تشغيلةً دافئة): `watch_summary` ‏١١٫٦٥→٨٫٤ م.ث و`my_lists` ‏٣٫٣٩→٠٫١٣ م.ث (median)، **والنتائجُ متطابقةٌ ١٠٠٪** (md5 لكلّ سياق A/B/مزوَّر/anon) |
| 125 | `125_title_aliases.sql` | 🆕 **جدولُ الأسماء البديلة — الكتابةُ الصوتيّةُ العربية** (D-544). مفتاحٌ مركَّبٌ `(media_type, tmdb_id, locale, alias_type)` هو القيدُ الفريد، وفهرسُ قراءةٍ مجمَّعة `(locale, alias_type, tmdb_id)`. **RLS مفعَّل، وسياسةُ قراءةٍ واحدةٌ `using (verified)`** — **لا `true`** كي يبقى فحصُ الصحّة أربعاً، **ولأن المواصفةَ تشترط «كتابةً صوتيّةً موثوقة».** **ولا سياسةَ كتابةٍ إطلاقاً** + `revoke insert/update/delete` من anon وauthenticated. rollback: `drop table public.title_aliases;` | **✅ شُغِّلت وتحقّقت** — `open_policies=4` (كما هي: user_follows · communities · imdb_ratings · imdb_chart)، `rls_on=true`، سياسةٌ واحدةٌ `[SELECT] verified`، `anon_can_insert=false` و`auth_can_insert=false` و`auth_can_select=true` |
| 126 | `126_content_prefs.sql` | 🆕 **تفضيلاتُ المحتوى** (D-545) — **ثلاثةُ أعمدةٍ على `profiles` لا جدولٌ جديد**: `unwanted_genres int[]` · `preferred_languages text[]` (**مرتَّبة**) · `excluded_languages text[]`، كلُّها `not null default '{}'`. **و`favorite_genres` يبقى هو «المفضَّل»** — فلا مصدرانِ للأنواع المفضّلة. **وRLS المطلوب قائمٌ أصلاً**: سياسةٌ واحدةٌ `ALL` بـ`using` و`with check` على `auth.uid() = id`. **وقيدُ `profiles_prefs_no_conflict`** يرفض أن يكون نوعٌ مفضّلاً وغيرَ مرغوبٍ معاً، أو لغةٌ مفضّلةً ومستبعدةً معاً. rollback: `drop constraint` + `drop column ×٣`. | **✅ شُغِّلت وتحقّقت** — `open_policies=4`، الأعمدةُ الثلاثةُ بقيمها الافتراضيّة، `rows_touched=0` (لا صفَّ قائماً تغيّر). **واختبارُ RLS بحسابين**: A يقرأ صفَّه ١ · صفَّ B ٠ · يكتب في B ٠ صفّاً · في صفِّه ١ · الترتيبُ محفوظٌ عند القراءة (`ar>en`) · قيدُ التعارض **رُفض كما يجب** — ثمّ نُظِّف كلُّ شيء |
| 127 | `127_profile_socials.sql` | 🆕 **روابطُ التواصل في الملفّ** (D-546) — `profiles.socials jsonb not null default '{}'` + قيدُ `jsonb_typeof = 'object'`. **`jsonb` لا أربعةُ أعمدة**: العددُ ليس أربعةً بطبيعته (تيك توك ويوتيوب غداً)، **والقراءةُ واحدةٌ في الحالين**. **ويُخزَّن المعرّفُ لا الرابط** — الرابطُ يُبنى عند العرض من نطاقٍ نكتبه نحن، **فلا يدخل نصُّ المستخدم `href` أبداً**. RLS كما هو (`ALL` بـ`using`/`with check` على `auth.uid() = id`). rollback: `drop column if exists socials`. | **✅ شُغِّلت وتحقّقت** — `open_policies=4`، العمودُ `jsonb default '{}'`، القيدُ قائم، `rows_touched=0` |
✅ **وصفوفُ ٩٣–١٠٥ أُضيفت في ١٧ أغسطس** (D-354) — كانت ملفّاتُها في
المجلّد وسردُها في الأرشيف والجدولُ يقفز من ٩٢ إلى ١٠٦. **وسطرُ كلٍّ منها
مأخوذٌ من رأس ملفّه لا من الذاكرة.**

كلها تحقَّقت بالاستعلام المكتوب في ذيل ملفّها — **لا استثناءات: كل ملفات
الجدول شُغِّلت حتى ٥٧** (٥٢–٥٧ في ١٠ أغسطس). **٥٨ و٥٩ معلّقتان** حتى تُشغَّلا،
و**الشيفرة تحتملهما غائبتين**: ٥٨ ترجع صفراً فتختفي الشارة، و٥٩ ترجع صفّاً
فارغاً فيغيب قسم المفضّلات — بلا شاشة خطأ في الحالتين. تحقُّق ٥٦: `non_cascade_fks=0` ·
`is_definer=true` · `owner=postgres` · `deletes_auth_row=true`.
تحقُّق ٥٥ على الإنتاج:
`kind_check` يضمّ `favorites` · `fav_idx=1` · `new_funcs=2` ·
**`open_policies=4`** — لا سياسة مفتوحة خامسة.

## مصيدةٌ وقعنا فيها فعلاً — `safeupdate`

Supabase تُحمّل إضافة `safeupdate` لدور `authenticated`، فأيّ
`delete`/`update` **بلا `where`** يُرفض بـ«DELETE requires a WHERE
clause» — **حتى داخل دالّة `security definer`**. وقع هذا في
`build_imdb_chart` بعد أن كانت المسوّدة قد امتلأت بأربعة آلاف نداء.
اكتب `where true` صراحةً في أي حذفٍ شامل.

للتأكد أن قاعدة الإنتاج مطابقة:
```sql
select tablename, policyname from pg_policies
where schemaname = 'public' and qual = 'true';
-- المتوقَّع **أربعٌ**: user_follows · communities (دليل المجتمعات، مقصود
-- منذ ٣٤) · imdb_ratings (٤٤) · imdb_chart (٥٠).
-- ⚠️ شغّله فعلاً ولا تكتفِ بوجوده: في ٨ أغسطس ٢٠٢٦ كشف أوّلُ تشغيلٍ له
-- ثلاث سياسات مفتوحة على المكتبة وسجلّ المشاهدة، عمرها أشهر — انظر
-- security3.sql.
```
